# Story 5.13 — Tools cérebro (FR46 + FR50 + FR57)

**Story ID:** 5.13
**Epic:** 5 — Diário + Brain Dump + Conhecimento
**Status:** Done
**Bloqueia:** Fecho do Epic 5 (13/13) — `@po *retrospective epic-5`
**Bloqueada por:** Story 5.12 (lógica client-side `proposeWebSearchCreate`/`persistProposal` em `lib/conhecimento/web-search-create.ts`) DONE em `main` (`aa385a04`). Stories 5.3/5.5 (repos + pesquisa diário). Story 5.8 (approval flow Brain Dump — `[D-5.8-CHAT-RETRO]` diferida aqui). Story 5.9 (repos conhecimento). Story 5.10 (pesquisa conhecimento). Todas Done em `main`.
**Estimativa:** 8-13h (9 tools com argsSchemas Zod + resultSchemas + execute + reverse opcional + ficheiros de registo + inicialização + ~27-35 testes Vitest + entrega de `[D-5.8-CHAT-RETRO]` + E2E do AC canónico)
**Tipo:** Backend puro — Tool Registry integration (território de risco: protocolo AI + Edge-safety + domínio do classifier)

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, vitest, build, architect-gate-entrada, architect-gate-saida]
```

> Trace: `EPIC-5.md` §5 (Story 5.13, executor `@dev`, gate `@architect`) + §8 "AI/parser/pesquisa web/tools (território de risco — protocolo AI, fetch externo, estado distribuído) → gate `@architect`". `separation-of-roles.md` (A6): executor `@dev` ≠ gate `@architect`.
>
> **Architect Gate de ENTRADA** (antes de `@dev` começar): `@architect` ratifica as decisões `[D-5.13-*]` listadas na secção "Decisões deferidas ao @architect". Obrigatório antes de `@dev *develop 5.13`. Precedente positivo das Stories 5.7/5.8/5.11/5.12: o gate de entrada evita rework no gate de saída.

---

## Nota do `@sm` River — verificação em código real (15/06/2026)

Verificação em filesystem antes de escrever o draft. Constitution Artigo IV — No Invention.

### Tool Registry — estado actual (22 tools, 0 tools do Epic 5)

`lib/agent/tools/index.ts` importa: `tasks` (5), `projects` (2), `finance` (6), `habits` (3), `goals` (3), `reminders` (3) → total 22 tools. As 9 tools do Epic 5 ainda NÃO estão registadas. Após a 5.13: `toolRegistry.all().length === 31`. [Source: `lib/agent/tools/index.ts:21-26`]

### ToolDomain — `'journal'` e `'knowledge'` já existem

`lib/agent/tools/types.ts:34-44` — `ToolDomain` union inclui `'journal'` e `'knowledge'` desde a Story 1.3. O classifier `lib/agent/classifier.ts:36-47` inclui ambos em `ALL_DOMAINS`. **Não é necessário estender o enum.** [Source: `lib/agent/tools/types.ts:34-44`; `lib/agent/classifier.ts:36-47`]

### Repos disponíveis (para referência de shapes — NÃO importar nas tools)

| Repo | Ficheiro | Funções relevantes |
|------|----------|--------------------|
| Diário | `lib/db/repos/journal-entries.ts` | `createJournalEntry`, `getJournalEntryByDate`, `listJournalEntriesByDateRange`, `searchJournalEntries` |
| Brain Dump | `lib/db/repos/brain-dumps.ts` | `createBrainDump`, `getBrainDump`, `listBrainDumps` |
| Áreas | `lib/db/repos/knowledge-areas.ts` | `createKnowledgeArea`, `listKnowledgeAreas`, `getKnowledgeArea` |
| Cadernos | `lib/db/repos/knowledge-notebooks.ts` | `createKnowledgeNotebook`, `listNotebooksByArea` |
| Notas | `lib/db/repos/knowledge-notes.ts` | `createKnowledgeNote`, `searchNotes` |
| Pesquisa diário | `lib/diario/pesquisa.ts` | `searchEntries` (helper puro) |
| Pesquisa conhecimento | `lib/conhecimento/pesquisa.ts` | `searchKnowledgeNotes` (helper puro) |

**Edge-safety obrigatória (ADR-1 / DEV-DECISION D1 da Story 2.10):** as tools correm no route handler Edge (`app/api/agent/prompt/route.ts`). Os repos importam o singleton `db` (`@/lib/db/client`). As tools NÃO importam repos directamente — usam `ctx.db.*` inline. Os helpers PUROS (`lib/diario/pesquisa.ts`, `lib/conhecimento/pesquisa.ts`) são Edge-safe (sem import de Dexie) e PODEM ser importados. [Source: `lib/agent/tools/habits.ts:18-19` como precedente]

### Lógica da 5.12 a reutilizar por `pesquisar_web_e_criar_nota`

`lib/conhecimento/web-search-create.ts` exporta `proposeWebSearchCreate` e `persistProposal`. Este módulo é **client-side puro** — importa `db` de `@/lib/db/client` e repos directamente. Uma tool no executor Edge NÃO pode importar este módulo (`[D-5.12-SCOPE-vs-5.13]` Opção C deixou o registo para a 5.13, mas não especificou o mecanismo de invocação Edge-safe). Esta é a decisão crítica `[D-5.13-WEB-SEARCH-TOOL]` deferida ao `@architect`. [Source: `lib/conhecimento/web-search-create.ts:1-8` (imports Dexie + repos)]

### `[D-5.8-CHAT-RETRO]` — contexto completo

Diferida da Story 5.8 (ratificada pela Aria como variante B). O contexto:
- Existe `addChatMessage` em `lib/db/repos/chat-messages.ts:25-29` — mas o executor não chama este repo fora do seu loop LLM.
- `ChatMessage` campos obrigatórios: `id, conversationId, role, content, timestamp` — `types/db.ts:38-46`.
- `useConversationMessages` usa `useLiveQuery` reactivo — uma escrita em `chat_messages` com `role: 'assistant'` aparece no chat.
- **O risco:** criar uma `ChatMessage` de agente dentro do `execute()` de uma tool (sem `agentRunId`) é uma "mensagem fantasma" sem contexto de run. Alternativa: o resultado da tool `brain_dump` inclui no campo `mensagem` o sumário PT-PT; o executor escreve a mensagem no seu próprio loop de resultado.
- A decisão exacta de mecanismo é `[D-5.13-CHAT-RETRO]` para o `@architect`.

### Nomes de tools — já validados ASCII

9 nomes do PRD/EPIC-5 §4: `criar_entrada_diario`, `consultar_diario`, `pesquisar_diario`, `brain_dump`, `criar_area`, `criar_caderno`, `criar_nota`, `pesquisar_conhecimento`, `pesquisar_web_e_criar_nota`. Todos validados contra `TOOL_NAME_PATTERN` (`/^[a-z][a-z0-9_]*$/`) — sem acentos, sem cedilha. `external-contract-identifiers.md` satisfeito no draft do epic. [Source: `EPIC-5.md` §4 nota; `lib/agent/tools/registry.ts:27`]

### Baseline de testes

Suite completa após 5.12 Done (PR #73, merge em `main`): **1849 testes** (conforme handoff de arranque 5.13). A 5.13 apoia-se neste baseline.

### Precedentes de registo de tools

| Story | Ficheiro | Domain | Tools | Padrão Edge-safety |
|-------|----------|--------|-------|--------------------|
| 2.10 | `lib/agent/tools/tasks.ts` + `projects.ts` | `'tasks'` | 7 | `ctx.db.*` directo |
| 3.11 | `lib/agent/tools/finance.ts` | `'finance'` | 6 | `ctx.db.*` directo |
| 4.10 | `lib/agent/tools/habits.ts` + `goals.ts` + `reminders.ts` | `'habits'` | 9 (D-DOMAIN Opção A) | `ctx.db.*` directo |

D-DOMAIN Opção A (Story 4.10): agrupou hábitos/metas/lembretes em `domain:'habits'` porque o classifier já os agrupava. O mesmo padrão pode aplicar-se a `'journal'`/`'knowledge'` — `[D-5.13-DOMAIN]` para o `@architect` verificar.

---

## Decisões deferidas ao @architect (Aria) — gate de ENTRADA obrigatório

> Padrão das Stories 5.7/5.8/5.11/5.12: decisões de risco são deferidas ao `@architect` antes da implementação. Nomeadas com `[D-5.13-*]` para rastreabilidade. O `@dev` não implementa nenhuma destas até as decisões estarem registadas com veredicto na story.

**[D-5.13-DOMAIN]** — Agrupamento de domínio das 9 tools no classifier:

As 9 tools cobrem 3 sub-módulos: Diário (FR46 — 3 tools), Brain Dump (FR50 — 1 tool), Conhecimento (FR57 — 5 tools). O `ToolDomain` enum tem `'journal'` e `'knowledge'`. O precedente da Story 4.10 (D-DOMAIN Opção A) agrupou hábitos/metas/lembretes em `domain:'habits'` porque o classifier já os agrupava.

- Opção A: 2 ficheiros de tools — `journal.ts` com `domain:'journal'` (Diário + Brain Dump, 4 tools) e `knowledge.ts` com `domain:'knowledge'` (Conhecimento, 5 tools). O `@architect` verifica se o classifier já agrupa brain-dump com journal ou separado — sem verificação de código, NÃO assumir.
- Opção B: 1 ficheiro único `epic5.ts` com domain misto (journal para diário/brain-dump, knowledge para conhecimento). Agrupa tudo num só ficheiro mas mantém os 2 domains.
- Opção C: 3 ficheiros — `journal.ts` (diário), `brain-dump.ts` (brain_dump), `knowledge.ts` (conhecimento) — separação máxima, 3 domains possíveis.

`@architect` DEVE verificar em `lib/agent/classifier.ts` e `lib/agent/prompts/classifier-system.ts` como os intents de diário/brain-dump/conhecimento são agrupados antes de decidir. NÃO assumir que o agrupamento é óbvio.

**Impacto:** afecta nomes de ficheiros, número de imports no barrel `index.ts`, e `toolRegistry.byDomain('...')` nas asserções de testes.

> **RATIFICADO [Opção A] (Architect Gate de Entrada, Aria, 15/06/2026):** 2 ficheiros — `lib/agent/tools/journal.ts` (`domain:'journal'`, 4 tools: `criar_entrada_diario`, `consultar_diario`, `pesquisar_diario`, `brain_dump`) e `lib/agent/tools/knowledge.ts` (`domain:'knowledge'`, 5 tools: `criar_area`, `criar_caderno`, `criar_nota`, `pesquisar_conhecimento`, `pesquisar_web_e_criar_nota`).
>
> **Justificação ancorada em código:** O classifier agrupa **brain dump dentro de `journal`**, não num domain separado — `classifier-system.ts:31` (`DOMAIN_DESCRIPTIONS.journal = 'diário pessoal, brain dumps, reflexões'`). `knowledge` é o seu próprio domain (`classifier-system.ts:32`). O enum `ToolDomain` tem exactamente estes 2 literais relevantes (`types.ts:38-39`) e `ALL_DOMAINS` inclui-os separadamente (`classifier.ts:43-44`). O executor faz `toolRegistry.byDomain(d)` por cada domain detectado (`executor.ts:368-391`); um prompt de brain dump classifica para `'journal'` e tem de encontrar a tool `brain_dump` sob `domain:'journal'`. **Opção C é REJEITADA** porque exigiria um 3.º domain (`brain-dump`) que o classifier não conhece — `validateClassifierOutput` (`classifier.ts:157-163`) faria fail-loud com "intent não está em availableDomains" para qualquer intent fora dos 10. **Opção B (1 ficheiro)** é funcionalmente equivalente mas inferior à separação por domain (precedente 4.10 Opção A: um ficheiro por domain; `projects.ts` partilha `domain:'tasks'` com `tasks.ts` — barrel `index.ts:21-26` prova que múltiplos ficheiros sob o mesmo domain é o padrão estabelecido). `brain_dump` vive em `journal.ts` mas com `domain:'journal'` — espelha exactamente como `goals.ts`/`reminders.ts` vivem em ficheiros próprios mas com `domain:'habits'` (`index.ts:25-26`).
>
> **Consequência para AC6/T26:** `toolRegistry.byDomain('journal').length === 4`, `toolRegistry.byDomain('knowledge').length === 5`, `4 + 5 === 9`. `all().length === 31`.

**[D-5.13-WEB-SEARCH-TOOL]** — Como a tool `pesquisar_web_e_criar_nota` invoca a lógica da 5.12 de forma Edge-safe:

A lógica de orquestração `proposeWebSearchCreate`/`persistProposal` de `lib/conhecimento/web-search-create.ts` é **client-side puro** (importa `db` singleton + repos Dexie). Uma tool Edge não pode importar este módulo.

- Opção W1: A tool `pesquisar_web_e_criar_nota` no executor faz fetch para o endpoint `/api/conhecimento/web-search` (5.11, server-side disponível), obtém os resultados, e depois chama `ctx.db.knowledge_areas.toArray()` / `ctx.db.knowledge_notebooks.where('areaId').equals(areaId).toArray()` / `ctx.db.knowledge_notes.add(...)` inline (replicando a lógica de persistência de `persistProposal` mas via `ctx.db`). A proposta/preview é encapsulada no resultado da tool com `requiresPreview: true` — o mecanismo do executor emite `preview_request` e aguarda confirmação. Assim a tool é genuinamente Edge-safe e o executor faz o preview.
- Opção W2: A tool `pesquisar_web_e_criar_nota` devolve apenas os resultados de pesquisa (via fetch para `/api/conhecimento/web-search`) como `resultSchema`, sem persistir. A persistência fica delegada à UI `/knowledge` (a tool actua como "pesquisar" e a criação é manual pelo utilizador). Âmbito muito reduzido — perde o FR57 "pesquisar web e criar nota automaticamente".
- Opção W3: Criar um endpoint server-side novo (`/api/conhecimento/web-search-create`) que orquestra pesquisa + persistência em cascata (replicando a lógica da 5.12 no server). A tool faz fetch para este endpoint. O endpoint tem `runtime = 'nodejs'` (como o 5.11). O estado é gerido pelo endpoint, não pela tool.

`@architect` ratifica: qual a opção? A decisão afecta AC1 (da tool `pesquisar_web_e_criar_nota`), o mecanismo de preview/confirmação, e a necessidade de criar novo endpoint.

**PONTO DE RISCO CRÍTICO:** a Edge-safety (`internal-state-contract-gate.md`) de `pesquisar_web_e_criar_nota` — qualquer caminho escolhido distribui estado por ≥2 camadas (executor Edge + endpoint Node/client). A análise dos 3 eixos já foi feita na 5.12 ([D-5.12-FAILURE]) — o `@architect` deve reutilizá-la e adaptá-la ao mecanismo escolhido.

> **RATIFICADO [Opção W1] (Architect Gate de Entrada, Aria, 15/06/2026):** A tool `pesquisar_web_e_criar_nota` faz `ctx.fetch('/api/conhecimento/web-search', ...)` (endpoint 5.11, reutilizado) para obter `{ results, source }`, e persiste a cascata área→caderno→nota **inline numa única transacção `ctx.db.transaction('rw', [knowledge_areas, knowledge_notebooks, knowledge_notes], ...)`** replicando a lógica de `persistProposal`. **`requiresPreview: true`** + `reversible: false`. **W3 (endpoint novo) REJEITADA** — desnecessário e contraproducente (ver justificação). **W2 REJEITADA** — perde o FR57/FR56 ("e cria nota").
>
> **Correcção de premissa arquitectural (crítica):** A story assume "executor Edge". É **FALSO** desde a Story 1.11 (ADR-9). O `runAgent` e TODAS as tools correm **no browser** (`client-executor.ts:1` `'use client'`; `:7` injecta o singleton Dexie real `db`; `:19-27` "conduz o `runAgent` no **browser**… `ctx.db` = singleton Dexie real"). O único ponto Edge restante é `/api/anthropic/proxy` (`client-executor.ts:35`). Consequências que simplificam W1:
> 1. **`ctx.db` é o Dexie real** — a tool pode fazer `ctx.db.transaction('rw', […])` exactamente como `persistProposal` faz (`web-search-create.ts:258-301`). Não há "command pattern" nem distribuição de persistência por camadas: a persistência acontece toda no browser, numa transacção atómica local. Precedente directo: todas as tools 2.10/3.11/4.10 escrevem via `ctx.db.*` (`habits.ts:186`).
> 2. **`ctx.fetch` é um `fetch` de browser same-origin** (`executor.ts:558` `fetch: globalThis.fetch`). Um POST a `/api/conhecimento/web-search` **transporta o cookie de sessão automaticamente** (same-origin credentials) — o `getSession(req)` do endpoint (`web-search/route.ts:210`) vê-o. **Não há SSRF nem propagação manual de cookie do lado da tool**: o vector SSRF (REC-SSRF-2) é interno ao endpoint 5.11 (fetch Node→Edge ao proxy Anthropic, `web-search/route.ts:87-104`), JÁ mitigado por allowlist de origin completa, e W1 **não lhe toca** (não o pioro).
> 3. **"Edge-safe" para esta story** significa: o ficheiro `knowledge.ts` NÃO faz `import { db }` de `@/lib/db/client` nem importa repos (`AC8`) — não por o runtime ser Edge, mas pela invariante contratual (db injectável, testabilidade com fake-indexeddb, paridade com habits.ts). A tool importa apenas `ctx` e tipos. NÃO importa `web-search-create.ts` (que importa `db` estaticamente).
>
> **Porque W3 é rejeitada:** criar `/api/conhecimento/web-search-create` (Node) deslocaria a persistência para o servidor — mas o Dexie é client-side (IndexedDB do browser); um endpoint Node **não tem acesso ao IndexedDB do utilizador**. W3 seria impossível de implementar sem reescrever a camada de persistência. W1 é a única opção coerente com ADR-9.
>
> **Análise `internal-state-contract-gate.md` — 3 eixos (adaptada de [D-5.12-FAILURE]):** O estado distribui-se por 2 camadas — (i) endpoint 5.11 Node (pesquisa) e (ii) transacção Dexie no browser (persistência). A coerência entre elas:
> - **(a) Classes de estado:** As classes são `{pesquisa-pendente, pesquisa-ok-com-resultados, pesquisa-falhou, persistência-confirmada, persistência-cancelada-via-preview}`. Cada camada: o endpoint 5.11 só conhece pesquisa (devolve `results` não-vazio OU `{error}`); a transacção Dexie só persiste após preview confirmado. A área/caderno seguem get-or-create idempotente (classe "nova" vs "existente") — replicar `nameMatches` localeCompare PT-PT de `web-search-create.ts:122-128` para AMBOS área E caderno (bug-de-classe A2: não só um). A nota é sempre nova.
> - **(b) Transição-já-ocorrida:** Os IDs de área/caderno **resolvem-se DENTRO da transacção** (`ctx.db.transaction`), nunca antes — lição M3 da 4.9 e `[D-5.12-FAILURE]` eixo b: a proposta carrega NOMES, não IDs, evitando referência stale a uma área eliminada entre a leitura e o write. Se a área já existir no momento da transacção, reutiliza-se; se não, cria-se — get-or-create idempotente dentro do mesmo `'rw'` scope (`web-search-create.ts:262-285`).
> - **(c) Caminhos de falha:** A falha do fetch é detectada **inspeccionando o CORPO** (`!resp.ok` OU `results` ausente/não-array OU `results` vazio OU campo `error`), NUNCA só `resp.ok` — lição M4 4.9/5.11 (`web-search-create.ts:186-196`): o endpoint pode devolver erro com HTTP 200 herdado da web search Anthropic. Falha de pesquisa → a tool lança `Error` PT-PT, **zero writes** (nenhuma transacção é aberta). Falha de persistência (Dexie indisponível, validação) → a transacção Dexie faz **rollback automático total** — zero entidades, nunca estado parcial (`[D-5.12-FAILURE]` eixo c). A tool propaga o erro ao executor, que emite `tool_error` (o utilizador NÃO recebe falsa confirmação).
>
> **Mecanismo de preview:** `requiresPreview: true` → o executor emite `preview_request` ANTES de `tool.execute()` (`executor.ts:1304-1326`); com `ClientConfirmationProvider` (in-process, `client-executor.ts:21`) a UI confirma; cancel → `tool_error` "Cancelado pelo utilizador" + zero writes (`executor.ts:1378-1409`). A persistência (transacção Dexie) corre DENTRO de `execute()`, ou seja **só após confirmação** — alinhado com `[D-5.12-PREVIEW]` P3 consolidado (1 confirmação para as N entidades). Nota: a pesquisa web (fetch ao 5.11) corre dentro de `execute()` DEPOIS da confirmação, logo o preview confirma a intenção "pesquisar X e criar em área/caderno Y/Z" sem mostrar resultados ainda — trade-off aceitável (a UI consolidada de proposta da 5.12 vive em `/knowledge`, fora do executor; no cérebro o preview é o gate genérico de confirmação de tool). Se a pesquisa falhar pós-confirmação, `tool_error` informa.

**[D-5.13-CHAT-RETRO]** — Entrega de `[D-5.8-CHAT-RETRO]`: mecanismo de mensagem retroactiva do agente após Brain Dump:

A Story 5.8 diferiu para a 5.13 a mensagem "Brain dump processado: N itens guardados" no chat principal (FE spec §1.4 [8]). Contexto da ratificação 5.8: a escrita directa de `ChatMessage` com `role: 'assistant'` fora do loop do executor é uma "mensagem fantasma" sem `agentRunId`. A 5.13 tem a tool `brain_dump` a correr no loop do executor.

- Opção R1: A tool `brain_dump` inclui no seu `resultSchema` o campo `mensagem` PT-PT com o sumário (ex: `"Brain dump processado: 7 itens guardados."`). O executor usa este campo para construir a resposta de agente no seu loop normal de resultado — sem escrita directa em `chat_messages` pela tool. O chat mostra a resposta do executor naturalmente. É a opção mais limpa e alinhada com a arquitectura do executor.
- Opção R2: O `execute()` da tool `brain_dump` chama `ctx.db.chat_messages.add({ id: crypto.randomUUID(), conversationId: 'main', role: 'assistant', content: mensagem, timestamp: Date.now() })` directamente. Cria a mensagem com `agentRunId: undefined` (campo opcional em `ChatMessage`). O chat aparece reactivamente via `useLiveQuery`. É tecnicamente viável mas é o "anti-padrão" que a 5.8 quis evitar.

`@architect` ratifica: qual a opção? Se Opção R1, os AC de `brain_dump` devem especificar o campo `mensagem` no `resultSchema` com o formato PT-PT. Se Opção R2, o `@architect` deve confirmar que `agentRunId: undefined` é aceitável e que a escrita directa em `chat_messages` pelo `execute()` não quebra invariantes do chat.

> **RATIFICADO [Opção R1] (Architect Gate de Entrada, Aria, 15/06/2026):** A tool `brain_dump` devolve no seu `resultSchema` o campo `mensagem: z.string()` PT-PT (ex: `"Brain dump registado: «N» itens detectados. Vai a Brain Dump para aprovar."`). O executor injecta o resultado da tool como `tool_result` no loop (`executor.ts:1462-1466` via `stringifyToolResult`), e o Sonnet **gera a resposta de chat naturalmente** no seu próprio turno. **R2 (escrita directa em `ctx.db.chat_messages.add`) REJEITADA.**
>
> **Justificação ancorada em código:** O executor é **stateless quanto a persistência de chat** — NÃO escreve em `chat_messages` nem em `agent_runs`. `executor.ts:75-83`: "o executor NÃO escreve em IndexedDB. Persistência Dexie é responsabilidade do client consumer da SSE". A persistência de chat-log mantém-se no hook `useAgentStream` (`client-executor.ts:31` "A persistência de chat-log (agent_runs/chat_messages) mantém-se no hook"). Uma escrita directa em `ctx.db.chat_messages.add({ ... role:'assistant', agentRunId: undefined })` dentro de `execute()` criaria uma `ChatMessage` **fora do `runId` corrente** (a tool não tem o `conversationId` da UI nem o vínculo ao `AgentRun` que o hook está a construir) — é precisamente a "mensagem fantasma" que a ratificação 5.8 (variante B) quis evitar, e viola a fronteira de responsabilidade do executor. R1 entrega a mesma UX (a confirmação aparece no chat) pelo caminho arquitectural correcto: a tool informa via `mensagem`, o Sonnet responde, o hook persiste a resposta do agente com o `agentRunId` certo. **Consequência para AC2/T11:** o `resultSchema` de `brain_dump` DEVE incluir `mensagem: z.string()`; T11 (mock-fidelidade) assere que o resultado da tool flui para `tool_result` (não que a tool escreve em `chat_messages`).

**[D-5.13-BRAIN-DUMP-SCOPE]** — Âmbito da tool `brain_dump`: activação ou processo completo?

A Story 5.7 (parser AI) + 5.8 (approval flow) implementaram o Brain Dump como fluxo client-side: o utilizador abre o `BrainDumpLauncher`, o AI estrutura, e o utilizador aprova item-a-item. A tool `brain_dump` (FR50) activa este fluxo via cérebro multi-intent.

- Opção B1: A tool `brain_dump` **regista o texto do utilizador** num `BrainDump` com `status: 'pending'` (ou dispara o parser AI inline via `ctx.fetch`) e devolve um resultado que o executor usa para navegar o utilizador à página de Brain Dump para aprovação. A persistência final de tarefas/projectos/notas fica no approval flow da UI (5.8) — a tool não persiste entidades finais.
- Opção B2: A tool `brain_dump` faz **o processo completo** inline (chama o parser AI, devolve os 4 buckets propostos, usa `requiresPreview: true` para o utilizador aprovar via mecanismo do executor, e persiste as entidades aprovadas via `ctx.db`). Réplica completa do fluxo 5.7+5.8 dentro do executor.

`@architect` ratifica: qual o âmbito da tool? A Opção B1 é mais conservadora e reutiliza o fluxo existente. A Opção B2 é mais ambiciosa e duplica lógica. A decisão afecta AC3 (tool `brain_dump`) e o `resultSchema`.

> **RATIFICADO [Opção B1] (Architect Gate de Entrada, Aria, 15/06/2026):** A tool `brain_dump` **regista o texto** num `BrainDump` via `ctx.db.brain_dumps.add({ id: crypto.randomUUID(), createdAt: Date.now(), bodyMarkdown: args.texto, status: 'pending' })` e devolve `{ brainDumpId, mensagem }` para o utilizador ir aprovar à UI Brain Dump (5.8). **A tool NÃO processa o parser AI inline nem persiste tarefas/projectos/notas finais.** `requiresPreview: false`, `reversible: false`. **B2 (processo completo inline) REJEITADA.**
>
> **Transição de estado FIXADA — `status: 'pending'` (canónico):** O enum `BrainDump.status` é `'pending' | 'parsed' | 'partially_approved' | 'fully_approved'` (`types/db.ts:266`). O comentário canónico (`types/db.ts:256-257`) define-o como uma **máquina de estados que atravessa sessões: parse → aprovação item-a-item**. A tool `brain_dump` só faz o **registo inicial** (FR47 input original), logo o estado correcto é `'pending'` — o brain dump foi capturado mas ainda não passou pelo parser AI (5.7) nem pela aprovação (5.8). O estado `'parsed'` só seria correcto SE a tool corresse o parser inline (B2). Como B1 não corre o parser, `'pending'` é o valor canónico e não há ambiguidade (a preocupação do PO fica resolvida: B1 → `'pending'`; se fosse B2 → `'parsed'` após o parser, nunca `'pending'`). O `parsedOutput` fica `undefined` (a story 5.7 preenche-o quando o utilizador abrir o Launcher).
>
> **Justificação ancorada em código (porque B2 é rejeitada):** B2 duplicaria todo o fluxo 5.7 (parser AI) + 5.8 (approval) dentro de `execute()`, replicando `lib/brain-dump/*` que importa Dexie e repos client-side — risco de divergência de lógica (dois caminhos de parsing/persistência a manter sincronizados) e violação de DRY. O fluxo de aprovação item-a-item da 5.8 é uma UI dedicada com decisões INBOX/TAG por item (memória [[project_nexus_v2_story_5_8_gate_entrada]]: 7 stores numa transacção única, IDs UUID determinísticos) — não é replicável de forma fiel num `requiresPreview` genérico do executor sem reescrever a UI de aprovação. B1 reutiliza o fluxo existente intacto: a tool é apenas o ponto de entrada via cérebro multi-intent (FR50), e o approval da 5.8 mantém-se a fonte única de verdade da persistência final. **Consequência para AC2/T9:** `resultSchema` de `brain_dump` = `z.object({ brainDumpId: z.string(), mensagem: z.string() })`; T9 assere `BrainDump` criado com `status: 'pending'`, `bodyMarkdown` correcto, `parsedOutput` ausente.

---

## User Story

Como utilizador do Nexus (o Eurico), quando digo ao cérebro "regista no diário de hoje que tive uma reunião produtiva", "o que escrevi no diário semana passada?", "faz brain dump ao texto…", "cria área IA com caderno LLMs", "cria nota sobre React Hooks no caderno Frontend", "pesquisa Artemis 2 e cria área Espaço com caderno Artemis 2", quero que o chat do Nexus execute acções reais de diário/brain-dump/conhecimento directamente a partir de linguagem natural, sem abrir formulários UI, cumprindo FR46, FR50 e FR57.

---

## Contexto

| Pilar | Detalhe |
|-------|---------|
| **PRD** | FR46: "Tools cérebro: `criar_entrada_diario`, `consultar_diario`, `pesquisar_diario`." FR50: "Tool cérebro: `brain_dump`." FR57: "Tools cérebro: `criar_area`, `criar_caderno`, `criar_nota`, `pesquisar_conhecimento`, `pesquisar_web_e_criar_nota`." — `PRD-NEXUS-V2.md` §6.8, §6.9, §6.10. |
| **Arquitectura** | ADR-1 Tool Registry pattern (não reabre). `architecture-v2.md` §7.2 (`ToolDefinition`/`ExecutionContext`) + §7.4 (inventário de tools por epic). |
| **Precedente** | `lib/agent/tools/finance.ts` (Story 3.11), `lib/agent/tools/habits.ts`+`goals.ts`+`reminders.ts` (Story 4.10) — shape exacto a replicar para diário/conhecimento. |
| **Domínio** | `'journal'` e `'knowledge'` — ambos em `ToolDomain` e `ALL_DOMAINS`. `[D-5.13-DOMAIN]` para o `@architect` confirmar o agrupamento exacto. |
| **Repos (via `ctx.db`)** | `ctx.db.journal_entries`, `ctx.db.brain_dumps`, `ctx.db.knowledge_areas`, `ctx.db.knowledge_notebooks`, `ctx.db.knowledge_notes` — todos disponíveis. |
| **Edge-safety** | Módulos sem `import { db }` de `@/lib/db/client`. Helpers puros Edge-safe (`lib/diario/pesquisa.ts`, `lib/conhecimento/pesquisa.ts`) PODEM ser importados. |
| **Lições Epic 1/3/4** | `mock-protocol-fidelity.md` — mocks de `toolRegistry`/executor espelham o contrato real. Tests com `fake-indexeddb` (precedente Stories 2.10/3.11/4.10). |
| **`[D-5.8-CHAT-RETRO]`** | Diferida da Story 5.8 — a tool `brain_dump` entrega a mensagem retroactiva no chat. `[D-5.13-CHAT-RETRO]` para o `@architect` confirmar o mecanismo. |
| **A6 separation-of-roles** | `@dev` executa, `@architect` faz gate. Backend puro sem UI nova → `@architect` é o quality gate correcto. |
| **PT-PT** | `language-standards.md` — todas as `description` das tools em PT-PT, erros PT-PT, textos nos resultados PT-PT. |

### Fronteiras com outras stories

- **5.12 (Done):** a tool `pesquisar_web_e_criar_nota` orquestra a lógica da 5.12 de forma Edge-safe. `[D-5.13-WEB-SEARCH-TOOL]` para o `@architect` ratificar o mecanismo exacto.
- **5.8 (Done):** a tool `brain_dump` entrega `[D-5.8-CHAT-RETRO]`. O approval flow UI da 5.8 mantém-se intacto.
- **5.3/5.5 (Done):** repos de diário (`journal-entries.ts`) e pesquisa (`lib/diario/pesquisa.ts`) são a base das tools de diário.
- **5.9/5.10 (Done):** repos de conhecimento e pesquisa são a base das tools de conhecimento.

---

## Reconciliação PRD ↔ Arquitectura

> `EPIC-5.md` §7: a arquitectura prevalece; nenhum ADR é reaberto; divergências escalam a `@architect`.

| # | PRD / arch / contexto | Código real | Resolução para a 5.13 |
|---|------------------------|-------------|----------------------|
| R1 | FR46/FR50/FR57 — 9 tools no Tool Registry | `lib/agent/tools/index.ts` tem 22 tools (Epics 2/3/4); Epic 5 não tem tools | A 5.13 regista as 9 tools. Após o import: `toolRegistry.all().length === 31`. |
| R2 | `ToolDomain` já inclui `'journal'` e `'knowledge'` | `lib/agent/tools/types.ts:34-44` — confirmado | Sem extensão do enum necessária. `[D-5.13-DOMAIN]` decide agrupamento. |
| R3 | `external-contract-identifiers.md` | Nomes validados no EPIC-5 §4 (`TOOL_NAME_PATTERN`) | AC5 cobre nomes ASCII. Sem reconciliação necessária. |
| R4 | `[D-5.12-SCOPE-vs-5.13]` Opção C — registo de `pesquisar_web_e_criar_nota` explicitamente para a 5.13 | `lib/agent/tools/index.ts` não inclui nenhuma tool de conhecimento | Confirmado: a 5.13 adiciona as 9 tools sem conflito com a 5.12 (que não tocou o Tool Registry). |
| R5 | Edge-safety (ADR-1 §7.3) | `habits.ts:18-19`: "NÃO importa `@/lib/db/client` NEM `@/lib/db/repos/*`. Persistência via `ctx.db`." | Padrão replicado para as 9 tools. Único risco: `pesquisar_web_e_criar_nota` — `[D-5.13-WEB-SEARCH-TOOL]`. |
| R6 | `mock-protocol-fidelity.md` (A1 Epic 1) | Mocks de `toolRegistry`/executor em `finance.test.ts` e `habits.test.ts` espelham protocolo real | ≥1 teste anti-tautológico por tool de risco (brain_dump, pesquisar_web_e_criar_nota). |
| R7 | `[D-5.8-CHAT-RETRO]` — diferida da 5.8 | Ratificação 5.8 §D-5.8-CHAT-RETRO: variante B (loop do executor), não escrita directa em `chat_messages` | `[D-5.13-CHAT-RETRO]` para o `@architect` confirmar o mecanismo na 5.13. |
| R8 | A3 Epic 4 — mapa de verificabilidade por AC | `pesquisar_web_e_criar_nota` e `pesquisar_diario`/`pesquisar_conhecimento` dependem de fetch externo / DB real | AC11 mapeia verificabilidade por tool e por ambiente. |
| R9 | D-DOMAIN Opção A da 4.10 | `habits.ts` usa `domain:'habits'` para 3 sub-áreas | Precedente: verificar se o classifier agrupa diário/brain-dump/conhecimento. `[D-5.13-DOMAIN]`. |

---

## Acceptance Criteria

> Trace: FR46, FR50, FR57 (`PRD-NEXUS-V2.md` §6.8/§6.9/§6.10), `EPIC-5.md` §5 (Story 5.13), §8 (lições A1/A3/A4 Epics 1/3/4, A6 Epic 1), `mock-protocol-fidelity.md`, `external-contract-identifiers.md`, `separation-of-roles.md`, NFR17/NFR18.
>
> AC marcados com `[A]` dependem de decisões `[D-5.13-*]` a ratificar pelo `@architect` no gate de entrada. Se o `@architect` ajustar a abordagem, os AC correspondentes são actualizados antes de `@dev *develop 5.13`.
>
> `external-contract-identifiers.md`: nomes ASCII validados no EPIC-5 §4 — sem reconciliação necessária nos AC.
> `separation-of-roles.md`: executor `@dev` ≠ gate `@architect` — confirmado.

- **AC1 (tools de diário — FR46):** `[A — decisão [D-5.13-DOMAIN]]`

  Existem 3 tools de diário registadas via `toolRegistry.register(defineTool<TArgs, TResult>({...}))`, com `domain` conforme `[D-5.13-DOMAIN]`:

  **`criar_entrada_diario`** (FR46):
  - `name: 'criar_entrada_diario'`, `requiresPreview: false`, `reversible: true`
  - `argsSchema`: `{ data: string (YYYY-MM-DD, default hoje), bodyMarkdown: z.string().min(1), mood: z.number().int().min(1).max(5) }`
  - `resultSchema`: `z.object({ id: z.string(), data: z.string(), mensagem: z.string() })`
  - `execute(args, ctx)`: verifica se já existe entrada para a data via `ctx.db.journal_entries.where('date').equals(args.data).first()` — se existir, erro PT-PT "Já existe uma entrada de diário para {data}. Usa 'actualizar diário' ou especifica outra data."; se não, cria via `ctx.db.journal_entries.add({ id: crypto.randomUUID(), date: args.data, mood: args.mood, bodyMarkdown: args.bodyMarkdown })`. Retorna `{ id, data: args.data, mensagem: 'Entrada de diário criada para {data} com mood {mood}/5.' }`.
  - `reverse(_args, result, ctx)`: `ctx.db.journal_entries.delete(result.id)`.
  _Trace: FR46; PRD §6.8; `JournalEntry` em `types/db.ts:223-229`; `journal-entries.ts:28-33` (contrato de write)._

  **`consultar_diario`** (FR46):
  - `name: 'consultar_diario'`, `requiresPreview: false`, `reversible: false`
  - `argsSchema`: `{ dataInicio: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).nullable().default(null), dataFim: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).nullable().default(null) }` — intervalo aberto unilateral suportado (ver tabela abaixo); só se ambos forem `null` retorna as últimas 7 entradas.
  - `resultSchema`: `z.object({ entradas: z.array(z.object({ id: z.string(), date: z.string(), mood: z.number(), excerpt: z.string() })), total: z.number(), mensagem: z.string() })`
  - `execute(args, ctx)` (Finding 1 CR Iter 1 — semântica de intervalo aberto, Aria 16/06/2026): usa `ctx.db.journal_entries.where('date').between(lo, hi, true, true).sortBy('date')` sempre que **pelo menos um** de `dataInicio`/`dataFim` for não-`null`, com `lo = dataInicio ?? SENTINEL_MIN` ('0000-01-01') e `hi = dataFim ?? SENTINEL_MAX` ('9999-12-31') para o lado ausente; só quando AMBOS são `null` cai em `ctx.db.journal_entries.orderBy('date').reverse().limit(7).toArray()`. Os 4 casos: range fechado `[dataInicio, dataFim]` / desde `dataInicio` até hoje (só `dataInicio`) / do início até `dataFim` (só `dataFim`) / últimas 7 (ambos `null`). Mapeia para `excerpt = bodyMarkdown.slice(0, 150)`.
  - `reverse`: omitido (read-only).
  _Trace: FR46; PRD §6.8; `listJournalEntriesByDateRange` (`journal-entries.ts:54-58`) como referência de índice._

  **`pesquisar_diario`** (FR46 / FR45):
  - `name: 'pesquisar_diario'`, `requiresPreview: false`, `reversible: false`
  - `argsSchema`: `{ query: z.string().min(1) }`
  - `resultSchema`: `z.object({ resultados: z.array(z.object({ id: z.string(), date: z.string(), mood: z.number(), excerpt: z.string() })), total: z.number(), mensagem: z.string() })`
  - `execute(args, ctx)`: carrega todas as entradas via `ctx.db.journal_entries.toArray()` e delega a `searchEntries(entradas, args.query)` (helper puro Edge-safe importado de `@/lib/diario/pesquisa`). Mapeia resultados para `excerpt = bodyMarkdown.slice(0, 150)`. Retorna `{ resultados, total, mensagem: 'Encontrei N entr{a/adas} no diário com "{query}".' }`.
  - `reverse`: omitido (read-only).
  _Trace: FR46/FR45; `searchJournalEntries` (`journal-entries.ts:71-74`) como precedente de delegação ao helper puro._

- **AC2 (tool de brain dump — FR50):** `[RECONCILIADO — [D-5.13-BRAIN-DUMP-SCOPE]=B1 + [D-5.13-CHAT-RETRO]=R1, Aria 15/06/2026]`

  Existe 1 tool de brain dump registada, em `lib/agent/tools/journal.ts` com `domain:'journal'` (`[D-5.13-DOMAIN]`=A):

  **`brain_dump`** (FR50):
  - `name: 'brain_dump'`, `requiresPreview: false`, `reversible: false` (`[D-5.13-BRAIN-DUMP-SCOPE]`=B1)
  - `argsSchema`: `{ texto: z.string().min(1).describe('Texto livre do brain dump (ideias, pensamentos, tarefas não estruturadas)') }`
  - `resultSchema`: `z.object({ brainDumpId: z.string(), mensagem: z.string() })` (`[D-5.13-BRAIN-DUMP-SCOPE]`=B1 + `[D-5.13-CHAT-RETRO]`=R1 — campo `mensagem` PT-PT).
  - `execute(args, ctx)` (B1): cria via `ctx.db.brain_dumps.add({ id: crypto.randomUUID(), createdAt: Date.now(), bodyMarkdown: args.texto, status: 'pending' })` (NÃO corre parser AI nem persiste entidades finais). Retorna `{ brainDumpId: id, mensagem: 'Brain dump registado. Vai a Brain Dump para rever e aprovar os itens detectados.' }`. `parsedOutput` fica `undefined` (a 5.7/5.8 preenchem-no no Launcher).
  - **`[D-5.13-CHAT-RETRO]`=R1:** o `resultSchema` inclui `mensagem` PT-PT. O executor injecta o resultado como `tool_result` (`executor.ts:1462-1466`) e o Sonnet responde no chat no seu loop. **NÃO há escrita directa em `chat_messages` pela tool** (R2 rejeitada — ver `[D-5.13-CHAT-RETRO]`).
  - `reverse`: omitido (`reversible: false`).
  _Trace: FR50; PRD §6.9; `BrainDump` em `types/db.ts:261-267` (`status` enum `:266`, máquina de estados `:256-257`); `[D-5.8-CHAT-RETRO]` ratificada na Story 5.8; ratificação Aria 15/06/2026._

- **AC3 (tools de conhecimento — FR57, parte 1: CRUD):** `[A — decisão [D-5.13-DOMAIN]]`

  Existem 3 tools de CRUD de conhecimento registadas:

  **`criar_area`** (FR57):
  - `name: 'criar_area'`, `requiresPreview: false`, `reversible: true`
  - `argsSchema`: `{ nome: z.string().min(1).max(200), cor: z.string().regex(/^#[0-9a-fA-F]{6}$/).default('#8892A4'), icone: z.string().default('📁') }`
  - `resultSchema`: `z.object({ id: z.string(), nome: z.string(), mensagem: z.string() })`
  - `execute(args, ctx)`: verifica se já existe área com o nome exacto via `ctx.db.knowledge_areas.toArray()` (fuzzy match case-insensitive PT-PT via `localeCompare` — padrão D-5.12-PERSIST); se existir, erro PT-PT "Área '{nome}' já existe. Usa um nome diferente ou consulta as áreas existentes."; se não, cria via `ctx.db.knowledge_areas.add({ id: crypto.randomUUID(), name: args.nome.trim(), color: args.cor, icon: args.icone })`. Valida `KnowledgeAreaSchema` (id UUID, name não vazio, color, icon — `lib/db/schemas.ts:409-414`). Retorna `{ id, nome: args.nome, mensagem: 'Área "{nome}" criada.' }`.
  - `reverse(_args, result, ctx)` (Finding 3 CR Iter 1, Aria 16/06/2026): conta cadernos por `areaId`; se houver filhos, **recusa com Error PT-PT** que remete para a UI de gestão (a cascata destrutiva de 2 níveis é exclusiva de `deleteKnowledgeArea` da 5.9, accionada deliberadamente pela UI com confirmação — um reverse que cascateasse apagaria dados sem consentimento). Só procede sobre área folha: `ctx.db.knowledge_areas.delete(result.id)`. **NÃO aplica a cascata `deleteKnowledgeArea`.**
  _Trace: FR57; `KnowledgeArea` `types/db.ts:231-236`; `createKnowledgeArea` (`knowledge-areas.ts:21-27`)._

  **`criar_caderno`** (FR57):
  - `name: 'criar_caderno'`, `requiresPreview: false`, `reversible: true`
  - `argsSchema`: `{ nomeArea: z.string().min(1), nomeCaderno: z.string().min(1).max(200) }`
  - `resultSchema`: `z.object({ id: z.string(), areaId: z.string(), nomeCaderno: z.string(), mensagem: z.string() })`
  - `execute(args, ctx)`: resolve área por nome (fuzzy match — padrão `resolveHabitoByNome` de `habits.ts`); se não encontrar, erro PT-PT com lista de áreas disponíveis. Verifica se já existe caderno com `nomeCaderno` nessa área; se existir, erro PT-PT. Se não, cria via `ctx.db.knowledge_notebooks.add({ id: crypto.randomUUID(), areaId: area.id, name: args.nomeCaderno.trim() })`. Retorna `{ id, areaId: area.id, nomeCaderno, mensagem: 'Caderno "{nomeCaderno}" criado na área "{nomeArea}".' }`.
  - `reverse(_args, result, ctx)` (Finding 3 CR Iter 1, Aria 16/06/2026): conta notas por `notebookId`; se houver filhas, **recusa com Error PT-PT** (mesma semântica de `criar_area` — eliminação com conteúdo é exclusiva da UI de gestão da 5.9). Só procede sobre caderno folha: `ctx.db.knowledge_notebooks.delete(result.id)`.
  _Trace: FR57; `KnowledgeNotebook` `types/db.ts:238-242`; `createKnowledgeNotebook` (`knowledge-notebooks.ts`) como referência._

  **`criar_nota`** (FR57):
  - `name: 'criar_nota'`, `requiresPreview: false`, `reversible: true`
  - `argsSchema`: `{ nomeArea: z.string().min(1), nomeCaderno: z.string().min(1), titulo: z.string().min(1).max(500), conteudo: z.string().min(1), tags: z.array(z.string()).default([]) }`
  - `resultSchema`: `z.object({ id: z.string(), notebookId: z.string(), titulo: z.string(), mensagem: z.string() })`
  - `execute(args, ctx)`: resolve área → caderno (fuzzy match encadeado); se não encontrar área ou caderno, erro PT-PT com sugestão (ex: "Usa 'criar_caderno' primeiro"). Cria nota via `ctx.db.knowledge_notes.add({ id: crypto.randomUUID(), notebookId: caderno.id, title: args.titulo.trim(), bodyMarkdown: args.conteudo, tags: args.tags, updatedAt: Date.now() })`. Valida que o caderno existe antes do write (padrão `createKnowledgeNote` `knowledge-notes.ts:29-34` — a lança se o caderno não existe, mas a tool deve apanhar o erro e devolvê-lo como erro PT-PT). Retorna `{ id, notebookId: caderno.id, titulo: args.titulo, mensagem: 'Nota "{titulo}" criada no caderno "{nomeCaderno}" (área "{nomeArea}").' }`.
  - `reverse(_args, result, ctx)`: `ctx.db.knowledge_notes.delete(result.id)`.
  _Trace: FR57; `KnowledgeNote` `types/db.ts:244-252`; `createKnowledgeNote` (`knowledge-notes.ts:26-37`)._

- **AC4 (tools de conhecimento — FR57, parte 2: pesquisa e web):** `[A — decisões [D-5.13-DOMAIN] + [D-5.13-WEB-SEARCH-TOOL]]`

  Existem 2 tools de pesquisa/web de conhecimento registadas:

  **`pesquisar_conhecimento`** (FR57 / FR53):
  - `name: 'pesquisar_conhecimento'`, `requiresPreview: false`, `reversible: false`
  - `argsSchema`: `{ query: z.string().min(1), nomeArea: z.string().nullable().default(null) }`
  - `resultSchema`: `z.object({ resultados: z.array(z.object({ id: z.string(), titulo: z.string(), nomeArea: z.string(), nomeCaderno: z.string(), excerpt: z.string(), updatedAt: z.number() })), total: z.number(), mensagem: z.string() })`
  - `execute(args, ctx)`: carrega todas as notas via `ctx.db.knowledge_notes.toArray()`, delega a `searchKnowledgeNotes(notas, args.query)` (helper puro importado de `@/lib/conhecimento/pesquisa`). Se `nomeArea` fornecido, filtra pré-pesquisa por área (resolve área pelo nome, obtém cadernos, filtra notas por `notebookId`). Para enriquecer resultados com `nomeArea`/`nomeCaderno`, carrega `knowledge_areas` e `knowledge_notebooks` de `ctx.db` e faz lookup por id. Retorna `{ resultados, total, mensagem: 'Encontrei N nota{s/a} de conhecimento com "{query}".' }`.
  - `reverse`: omitido (read-only).
  _Trace: FR57/FR53; `searchKnowledgeNotes` (`conhecimento/pesquisa.ts`) como referência do helper; `lib/db/repos/knowledge-notes.ts:56-59` (`searchNotes`)._

  **`pesquisar_web_e_criar_nota`** (FR57 / FR56): `[RECONCILIADO — [D-5.13-WEB-SEARCH-TOOL]=W1, Aria 15/06/2026]`
  - `name: 'pesquisar_web_e_criar_nota'`, `requiresPreview: true` (W1), `reversible: false` (a transacção é atómica; não há reverse parcial — o undo de uma cascata get-or-create reutilizada apagaria entidades pré-existentes; manter `false`).
  - `argsSchema`: `{ query: z.string().min(1), nomeArea: z.string().min(1), nomeCaderno: z.string().min(1), tituloNota: z.string().nullable().default(null) }`
  - `resultSchema`: `z.object({ noteId: z.string(), notebookId: z.string(), sourceUrl: z.string(), mensagem: z.string() })` (W1 — orquestração inline via `ctx.db`).
  - `execute(args, ctx)` (W1): (1) `ctx.fetch('/api/conhecimento/web-search', { method:'POST', body: JSON.stringify({ query }) })` — cookie de sessão same-origin automático (browser); (2) **inspecciona o CORPO** (`!resp.ok` OU `results` ausente/não-array OU vazio OU `error`) → lança `Error` PT-PT, zero writes (lição M4 4.9/5.11, espelha `web-search-create.ts:186-196`); (3) persiste cascata área→caderno→nota numa **única `ctx.db.transaction('rw', [ctx.db.knowledge_areas, ctx.db.knowledge_notebooks, ctx.db.knowledge_notes], …)`** — get-or-create idempotente por nome (`nameMatches` localeCompare PT-PT, espelha `web-search-create.ts:122-128`) para área E caderno (bug-de-classe A2), nota sempre nova com `sourceUrl` + corpo `excerto + Fonte: url`; IDs via `crypto.randomUUID()` resolvidos DENTRO da transacção (sem ids stale). Retorna `{ noteId, notebookId, sourceUrl, mensagem: 'Pesquisei "{query}" e criei a nota "{titulo}" no caderno "{nomeCaderno}" (área "{nomeArea}").' }`. Falha de persistência → rollback total automático (zero entidades).
  - **NÃO importa** `lib/conhecimento/web-search-create.ts` (importa `db` estaticamente — `AC8`). Replica a lógica via `ctx.db`/`ctx.fetch`. **NÃO toca** REC-SSRF-2 (interno ao endpoint 5.11).
  - **Análise `internal-state-contract-gate.md` (3 eixos):** documentada na ratificação `[D-5.13-WEB-SEARCH-TOOL]` acima — (a) classes nova/existente get-or-create; (b) IDs resolvidos na transacção (sem stale, lição M3); (c) fetch inspecciona corpo, rollback total na persistência. Reverificável no Gate de Saída (T8.3).
  - **A3 Epic 4 — verificabilidade:** depende de fetch externo (Anthropic web search ou DDG via 5.11) → verificação manual em preview/produção (AC11). CI cobre via MSW mock do endpoint (T23-T25).
  _Trace: FR57/FR56; `[D-5.12-SCOPE-vs-5.13]` Opção C; `lib/conhecimento/web-search-create.ts` como referência da lógica (NÃO importar); endpoint 5.11 `app/api/conhecimento/web-search/route.ts`; ADR-9 (executor client-side); ratificação Aria 15/06/2026._

- **AC5 (nomes ASCII e descriptions PT-PT):**
  - Todos os 9 nomes de tools (`criar_entrada_diario`, `consultar_diario`, `pesquisar_diario`, `brain_dump`, `criar_area`, `criar_caderno`, `criar_nota`, `pesquisar_conhecimento`, `pesquisar_web_e_criar_nota`) satisfazem `TOOL_NAME_PATTERN` (`/^[a-z][a-z0-9_]*$/`). `external-contract-identifiers.md` satisfeito.
  - Todas as 9 tools têm `description` em PT-PT (exemplos de utterances que o cérebro deve reconhecer):
    - `criar_entrada_diario`: `'Cria uma entrada de diário para o dia especificado (ou hoje). Use para "regista no diário", "escreve no diário de hoje", "entrada de diário: …".'`
    - `consultar_diario`: `'Consulta entradas de diário por intervalo de datas. Use para "o que escrevi semana passada", "mostra o diário de Maio", "ver entradas de diário".'`
    - `pesquisar_diario`: `'Pesquisa full-text nas entradas de diário. Use para "pesquisa no diário sobre…", "encontra entradas com…", "o que escrevi sobre X?".'`
    - `brain_dump`: `'Regista um brain dump (texto livre de ideias não estruturadas). Use para "faz brain dump de…", "vomita ideias sobre…", "preciso organizar estes pensamentos: …".'`
    - `criar_area`: `'Cria uma nova área de conhecimento. Use para "cria área…", "nova área de conhecimento sobre…", "adiciona área X".'`
    - `criar_caderno`: `'Cria um caderno dentro de uma área de conhecimento. Use para "cria caderno X na área Y", "novo caderno para…", "adiciona caderno sobre…".'`
    - `criar_nota`: `'Cria uma nota dentro de um caderno de conhecimento. Use para "cria nota sobre X no caderno Y", "regista esta informação em…", "adiciona nota: …".'`
    - `pesquisar_conhecimento`: `'Pesquisa full-text nas notas de conhecimento. Use para "pesquisa nas notas sobre…", "encontra notas de…", "o que sei sobre X?".'`
    - `pesquisar_web_e_criar_nota`: `'Pesquisa na web e cria automaticamente uma nota com o resultado numa área/caderno. Use para "pesquisa X e cria área Y com caderno Z", "procura informação sobre… e guarda".'`
  _Trace: `external-contract-identifiers.md` (A4 Epic 3); `language-standards.md`; `EPIC-5.md` §5 nota._

- **AC6 (Tool Registry — integridade após import):**
  - O barrel `lib/agent/tools/index.ts` é extendido com os imports das tools do Epic 5 (1 ou mais imports, conforme `[D-5.13-DOMAIN]`).
  - Após `import '@/lib/agent/tools'`, `toolRegistry.all().length === 31` (22 existentes + 9 novas).
  - `toolRegistry.byDomain('journal').length === N` e `toolRegistry.byDomain('knowledge').length === M` onde `N + M === 9` (conforme `[D-5.13-DOMAIN]`).
  - `toolRegistry.get('criar_entrada_diario')` retorna a definição correcta.
  - `toolRegistry.toAnthropicTools(toolRegistry.byDomain('journal').concat(toolRegistry.byDomain('knowledge')))` retorna array de 9 `AnthropicToolShape` sem lançar excepção (prova que todos os `argsSchemas` são `z.ZodObject` válidos).
  _Trace: `lib/agent/tools/registry.ts:27` (`TOOL_NAME_PATTERN`); Story 3.11 AC3/AC6 como precedente._

- **AC7 (AC canónico do Epic 5 — demonstração E2E via classifier + executor):**
  - Prompt `"regista no diário de hoje que tive reunião produtiva com mood 4"` →
    - Classifier detecta `intents: ['journal']` (ou conforme `[D-5.13-DOMAIN]`).
    - Executor chama `criar_entrada_diario` com `{ data: <hoje>, bodyMarkdown: 'tive reunião produtiva', mood: 4 }`.
    - Entrada criada em `ctx.db.journal_entries` com `date: <hoje>`, `mood: 4`, `bodyMarkdown: 'tive reunião produtiva'`.
    - Resposta PT-PT confirma criação.
  - Prompt `"cria área IA com caderno LLMs"` →
    - Classifier detecta `intents: ['knowledge']`.
    - Executor chama `criar_area` → cria área "IA"; depois chama `criar_caderno` → cria caderno "LLMs" na área "IA".
    - Resposta PT-PT confirma ambas as criações (em 2 tool calls ou 1 com argsSchema combinado, conforme `@architect`).
  - **Evidência:** Teste Vitest de integração com `fake-indexeddb` + mock do executor (mock do classifier devolve `intents: ['journal']`/`['knowledge']`; mock do executor devolve `tool_use: criar_entrada_diario`/`criar_area` com args correctos). `mock-protocol-fidelity.md` (A1 Epic 1) — mock reflecte protocolo real.
  _Trace: `EPIC-5.md` §5 (Story 5.13); PRD §10 AC1-AC4 do epic (verificação de que o cérebro usa as tools do domínio certo)._

- **AC8 (Edge-safety):**
  - Os ficheiros de tools do Epic 5 (`lib/agent/tools/journal.ts` e/ou `lib/agent/tools/knowledge.ts`, conforme `[D-5.13-DOMAIN]`) NÃO importam `@/lib/db/client` NEM `@/lib/db/repos/*`.
  - `npm run build` — exit 0. O build Edge deve ser limpo de imports Dexie nos ficheiros de tools.
  - Helpers puros Edge-safe (`@/lib/diario/pesquisa` e `@/lib/conhecimento/pesquisa`) PODEM ser importados — sem Dexie.
  - Excepção deferida ao `@architect`: `pesquisar_web_e_criar_nota` — a abordagem de `[D-5.13-WEB-SEARCH-TOOL]` pode implicar importar helpers puros adicionais ou fazer fetch para endpoint; o `@architect` confirma Edge-safety.
  _Trace: ADR-1 (Edge/Node split); `habits.ts:18-19` (DEV-DECISION D1 precedente)._

- **AC9 (quality gates locais):**
  - `npm run lint` — exit 0.
  - `npm run typecheck` — exit 0 (`ToolDefinition` tipagem correcta, `ctx.db` tipos alinhados com `NexusDB`, domains em `ToolDomain`, `argsSchema: z.ZodObject<z.ZodRawShape>`).
  - `npm run test:unit` — todos os existentes (1849 pós-5.12) + novos (≥27) passam. Full suite verde.
  - `npm run build` — exit 0.
  _Trace: Story 3.11 AC8 como precedente._

- **AC10 (testes Vitest):**
  Testes em `tests/unit/agent/tools/journal.test.ts` (ou conforme `[D-5.13-DOMAIN]`) e `tests/unit/agent/tools/knowledge.test.ts`:

  **Tools de diário:**
  - T1 — `criar_entrada_diario` sucesso: entrada criada com `date: hoje`, `mood: 4`, `bodyMarkdown` correcto. Assertar campos persistidos exactos.
  - T2 — `criar_entrada_diario` duplicado: entrada já existe para a data → erro PT-PT, nenhuma entrada criada.
  - T3 — `criar_entrada_diario` mood fora de range: `mood: 6` → Zod lança (`.min(1).max(5)`).
  - T4 — `criar_entrada_diario` reverse: entrada criada → reverse → `ctx.db.journal_entries.get(result.id)` retorna `undefined`.
  - T5 — `consultar_diario` intervalo: 3 entradas no range, 1 fora → retorna as 3.
  - T6 — `consultar_diario` sem args: últimas 7 entradas (ou menos se DB vazia).
  - T7 — `pesquisar_diario` match: 2 entradas contêm "reunião", 1 não → retorna as 2.
  - T8 — `pesquisar_diario` sem match: query sem resultados → `total: 0`, mensagem PT-PT.

  **Tool brain dump:** `[A — ver [D-5.13-BRAIN-DUMP-SCOPE] + [D-5.13-CHAT-RETRO]]`
  - T9 — `brain_dump` sucesso Opção B1: `BrainDump` criado com `status:'pending'`, id retornado.
  - T10 — `brain_dump` texto vazio: Zod lança (`.min(1)`).
  - T11 — `brain_dump` mock-fidelidade: mock do executor reflecte protocolo real do tool call.

  **Tools de conhecimento (CRUD):**
  - T12 — `criar_area` sucesso: área criada com `name`, `color`, `icon`. Assertar campos exactos.
  - T13 — `criar_area` duplicado: área com nome já existe → erro PT-PT, nenhuma área criada.
  - T14 — `criar_area` reverse: área criada → reverse → eliminada.
  - T15 — `criar_caderno` sucesso: área existe → caderno criado com `areaId` correcto.
  - T16 — `criar_caderno` área não encontrada: `nomeArea: 'InexistenteXYZ'` → erro PT-PT.
  - T17 — `criar_caderno` reverse: caderno criado → reverse → eliminado.
  - T18 — `criar_nota` sucesso: área + caderno existem → nota criada com `notebookId`, `title`, `bodyMarkdown`, `tags: []`, `updatedAt: number`.
  - T19 — `criar_nota` caderno não encontrado: resolve área mas caderno não existe → erro PT-PT.
  - T20 — `criar_nota` reverse: nota criada → reverse → eliminada.
  - T21 — `pesquisar_conhecimento` match: 2 notas contêm "React", 1 não → retorna as 2 com `nomeArea`/`nomeCaderno` enriquecidos.
  - T22 — `pesquisar_conhecimento` filtro por área: só pesquisa na área especificada.

  **Tool `pesquisar_web_e_criar_nota`:** `[A — ver [D-5.13-WEB-SEARCH-TOOL]]`
  - T23 — `pesquisar_web_e_criar_nota` sucesso: mock do endpoint `/api/conhecimento/web-search` retorna `{ results: [...], source: 'duckduckgo' }` → nota criada com `sourceUrl`, `bodyMarkdown` com excerto + fonte. Mock reflecte o shape real da 5.11 (mock-fidelidade).
  - T24 — `pesquisar_web_e_criar_nota` fetch falha: endpoint retorna 503 → erro PT-PT, nenhuma nota criada.
  - T25 — mock-fidelidade anti-tautológica: se `results` ausente na resposta do mock → nota NÃO criada (falha se shape diverge).

  **Tool Registry:**
  - T26 — `toolRegistry.all().length === 31` após import.
  - T27 — `toolRegistry.toAnthropicTools(...)` para as 9 tools não lança.

  > **Contagem mínima:** 27 cenários (T1-T27). Cenários T9/T11/T23/T24/T25 são `[A]` — actualizados após ratificação `[D-5.13-*]`.
  _Trace: Story 3.11 AC7 como precedente de contagem e estrutura; `mock-protocol-fidelity.md` (T11/T23/T25)._

- **AC11 (mapa de verificabilidade por AC — A3 Epic 4):**
  - AC1 (`criar_entrada_diario`): CI (fake-indexeddb, T1-T4).
  - AC1 (`consultar_diario`): CI (fake-indexeddb, T5-T6).
  - AC1 (`pesquisar_diario`): CI (helper puro — T7-T8).
  - AC2 (`brain_dump`): CI (T9-T11) + gate `@architect` (análise `[D-5.13-BRAIN-DUMP-SCOPE]`).
  - AC3 (`criar_area`/`criar_caderno`/`criar_nota`): CI (fake-indexeddb, T12-T20).
  - AC4 (`pesquisar_conhecimento`): CI (helper puro + fake-indexeddb, T21-T22).
  - AC4 (`pesquisar_web_e_criar_nota`): CI (mock endpoint 5.11, T23-T25) + **preview manual** (verificação com API key real, Anthropic/DDG, nota aparece em `/knowledge`) + **produção** (Eurico).
  - AC7 (E2E canónico): CI (mock classifier + executor, protocolo real).
  - AC8 (Edge-safety): CI (build).
  _Trace: A3 Epic 4; EPIC-5.md §8 A3._

---

## Tasks / Subtasks

> Padrão herdado da Story 3.11 — sequential, com referência aos AC. `[A]` = bloqueada por decisão `@architect`.

- [x] **T0 — Architect Gate de ENTRADA** (gate `@architect` — antes de T1) — **PASS-COM-CONDIÇÕES (Aria, 15/06/2026)**
  - [x] T0.1 — Ratificar `[D-5.13-DOMAIN]`: verificado `classifier-system.ts:31-32` (brain dump ∈ `journal`) + `classifier.ts:43-44` + `types.ts:38-39` + barrel `index.ts:21-26`. **Opção A** — 2 ficheiros (`journal.ts` 4 tools `domain:'journal'`, `knowledge.ts` 5 tools `domain:'knowledge'`). Registado.
  - [x] T0.2 — Ratificar `[D-5.13-WEB-SEARCH-TOOL]`: **Opção W1** — fetch ao endpoint 5.11 via `ctx.fetch` (cookie same-origin) + persistência inline `ctx.db.transaction('rw', […])`. Premissa "executor Edge" corrigida (ADR-9: executor client-side, `client-executor.ts`). Análise 3 eixos `internal-state-contract-gate.md` documentada. W3 impossível (Node sem acesso a IndexedDB do browser). Registado.
  - [x] T0.3 — Ratificar `[D-5.13-CHAT-RETRO]`: **Opção R1** — `mensagem` no `resultSchema`, Sonnet responde no loop; R2 (escrita directa `chat_messages`) rejeitada (`executor.ts:75-83` stateless; `client-executor.ts:31` hook persiste). Registado.
  - [x] T0.4 — Ratificar `[D-5.13-BRAIN-DUMP-SCOPE]`: **Opção B1** — só regista `BrainDump` com `status:'pending'` (canónico, `types/db.ts:266`); B2 rejeitada (duplicaria 5.7+5.8). Registado.
  - [x] T0.5 — AC2 e AC4 (`pesquisar_web_e_criar_nota`) reconciliados pela Aria no gate (removido `[A]`, fixados schemas/estado/preview). Pronto para `@dev *develop 5.13`.

- [x] **T1 — Tools de diário** (`lib/agent/tools/journal.ts`) (AC1)
  - [x] T1.1 — Implementar `criar_entrada_diario`: argsSchema Zod, resultSchema, execute com check de duplicado + `ctx.db.journal_entries.add`, reverse.
  - [x] T1.2 — Implementar `consultar_diario`: argsSchema, execute com query por range ou últimas 7, mapeamento para excerpts.
  - [x] T1.3 — Implementar `pesquisar_diario`: argsSchema, execute com `ctx.db.journal_entries.toArray()` + `searchEntries(...)` (helper Edge-safe importado).
  - [x] T1.4 — Verificar varredura de bug-de-classe (A2 Epic 4): check de duplicado de `criar_entrada_diario` usa o MESMO índice `date` que `consultar_diario` — não só um deles.

- [x] **T2 — Tool brain dump** (`[D-5.13-DOMAIN]`=A em `journal.ts` + `[D-5.13-BRAIN-DUMP-SCOPE]`=B1) (AC2)
  - [x] T2.1 — Implementar `brain_dump` conforme Opção B1: regista `BrainDump` com `status:'pending'`, `parsedOutput` undefined; NÃO corre parser AI nem persiste entidades finais.
  - [x] T2.2 — Implementar entrega `[D-5.13-CHAT-RETRO]`=R1: campo `mensagem` PT-PT no `resultSchema`; sem escrita directa em `chat_messages`.

- [x] **T3 — Tools de conhecimento CRUD** (`lib/agent/tools/knowledge.ts`) (AC3)
  - [x] T3.1 — Implementar `criar_area`: argsSchema, execute com check de duplicado por nome (case-insensitive PT-PT `localeCompare` — `nameMatches`), `ctx.db.knowledge_areas.add`, reverse.
  - [x] T3.2 — Implementar `criar_caderno`: argsSchema, `resolveAreaByNome` inline, check de duplicado caderno na área, `ctx.db.knowledge_notebooks.add`, reverse.
  - [x] T3.3 — Implementar `criar_nota`: argsSchema, resolve área → caderno, `ctx.db.knowledge_notes.add` (caderno inexistente → erro PT-PT), reverse.
  - [x] T3.4 — Verificar varredura de bug-de-classe: `criar_area` E `criar_caderno` usam o MESMO `nameMatches` idempotente de check de duplicado — não só um deles.

- [x] **T4 — Tools de conhecimento pesquisa e web** (`[D-5.13-WEB-SEARCH-TOOL]`=W1) (AC4)
  - [x] T4.1 — Implementar `pesquisar_conhecimento`: argsSchema, `ctx.db.knowledge_notes.toArray()` + `searchKnowledgeNotes(...)` (helper Edge-safe), enriquecimento `nomeArea`/`nomeCaderno` via Map lookup, filtro pré-pesquisa por área.
  - [x] T4.2 — Implementar `pesquisar_web_e_criar_nota` (W1): `ctx.fetch('/api/conhecimento/web-search')` + persistência inline em transacção `'rw'` atómica única (C1/C2/C3). Edge-safe (sem `web-search-create` import). Análise 3 eixos honrada.

- [x] **T5 — Barrel e registo no Tool Registry** (AC6)
  - [x] T5.1 — Extender `lib/agent/tools/index.ts` com `import './journal'` + `import './knowledge'`.
  - [x] T5.2 — Actualizar o JSDoc do `index.ts`: `toolRegistry.all().length === 31`, `byDomain('journal')===4`, `byDomain('knowledge')===5`.
  - [x] T5.3 — Verificar `toolRegistry.byDomain('journal').concat(toolRegistry.byDomain('knowledge')).length === 9` (T27).

- [x] **T6 — Testes Vitest** (AC10)
  - [x] T6.1 — Testes das tools de diário (T1-T8): `tests/unit/agent/tools/journal.test.ts`. `fake-indexeddb`. `searchEntries` real (Edge-safe).
  - [x] T6.2 — Testes da tool brain dump (T9-T11): integrados em `journal.test.ts` (T9/T9b/T10/T11 — T11 anti-tautológico via `resultSchema.parse` + `chat_messages.count()===0`).
  - [x] T6.3 — Testes das tools de conhecimento CRUD (T12-T20): `tests/unit/agent/tools/knowledge.test.ts`.
  - [x] T6.4 — Testes das tools de pesquisa (T21-T22): integrados em `knowledge.test.ts`.
  - [x] T6.5 — Testes da tool `pesquisar_web_e_criar_nota` (T23-T25): `ctx.fetch` fake com shape real da 5.11 (`{ results, source }`). Anti-tautológicos T25 (results ausente) + T25b (results vazio).
  - [x] T6.6 — Testes do Tool Registry (T26-T27) em `knowledge.test.ts`; contagens 22→31 actualizadas em `projects.test.ts`/`finance.test.ts`.
  - [x] T6.7 — Suite completa: `npm run test:unit` — 1888 verde (baseline 1849 + 39 novos).

- [x] **T7 — Quality gates** (AC8/AC9)
  - [x] T7.1 — `npm run lint` — exit 0 (1 warning pré-existente fora scope: logout route NextResponse).
  - [x] T7.2 — `npm run typecheck` — exit 0.
  - [x] T7.3 — `npm run test:unit` — full suite verde (1888/1888).
  - [x] T7.4 — `npm run build` — exit 0 (Edge-safe — zero imports Dexie em ficheiros de tools; /knowledge 13,3kB).
  - [x] T7.5 — CodeRabbit pre-commit `review --agent -t uncommitted --dir` — 0 findings. (Gate de saída `--base main` é do `@architect`, T8.)

- [x] **T8 — Architect Gate de SAÍDA** (gate `@architect`) — **PASS (Aria, 16/06/2026)**
  - [x] T8.1 — AC1-AC11 verificados contra código real (`journal.ts`, `knowledge.ts`, `index.ts`, testes). Ver secção "Architect Gate de Saída (T8)" abaixo.
  - [x] T8.2 — Edge-safety confirmada: grep prova zero `import` de `@/lib/db/client`/repos/`web-search-create.ts` em ambos os ficheiros (só comentários). `npm run build` exit 0.
  - [x] T8.3 — Análise `internal-state-contract-gate.md` (3 eixos) reverificada contra o código real de `pesquisar_web_e_criar_nota`. Documentada abaixo.
  - [x] T8.4 — `mock-protocol-fidelity.md`: `WebSearchResult` real = `{title,url,excerpt}` (`web-search-ddg.ts:27-31`) === mock `okBody` (T23) e `WebSearchResultShape` (`knowledge.ts:101-105`). T25/T25b anti-tautológicos provam inspecção de corpo. T11 prova `chat_messages.count()===0`.
  - [x] T8.5 — `[D-5.13-CHAT-RETRO]`=R1 entregue: `brain_dump` devolve `mensagem` PT-PT no `resultSchema`; T11 prova zero escrita directa em `chat_messages`. Sonnet responde no loop (`executor.ts` stateless).

---

## Architect Gate de Saída (T8) — Aria, 16/06/2026

**Veredicto: PASS.** Verificação contra código real no commit `384dbd15` (branch `feat/nexus-v2-5.13-tools-cerebro`). Executor `@dev` ≠ gate `@architect` (separation-of-roles A6; fiz o gate de entrada T0, não o código).

### Verificação AC1-AC11 (contra código real)

| AC | Verificação | Resultado |
|----|-------------|-----------|
| AC1 | `criar_entrada_diario` (`journal.ts`, check duplicado índice `date`, reverse delete), `consultar_diario` (range fechado / intervalo aberto unilateral via sentinelas / últimas 7 — Finding 1 CR Iter 1), `pesquisar_diario` (`searchEntries` puro) | PASS |
| AC2 | `brain_dump` (`journal.ts:249-280`): `status:'pending'`, `parsedOutput` undefined (B1), `mensagem` PT-PT no `resultSchema` (R1), `requiresPreview:false`, `reversible:false` | PASS |
| AC3 | `criar_area`/`criar_caderno`/`criar_nota` (`knowledge.ts`): get-or-create por `nameMatches` localeCompare PT-PT, resolução área→caderno encadeada; reverse seguro — `criar_area`/`criar_caderno` recusam com Error PT-PT se houver filhos (Finding 3 CR Iter 1, alinhado com cascata 5.9), `criar_nota` reverse delete (folha) | PASS |
| AC4 | `pesquisar_conhecimento` (`:370-427`, helper puro + filtro área), `pesquisar_web_e_criar_nota` (`:434-538`, W1) | PASS |
| AC5 | 9 nomes ASCII válidos contra `TOOL_NAME_PATTERN`; 9 `description` PT-PT | PASS |
| AC6 | `index.ts:34-35` importa `journal`+`knowledge`; T26 `all().length===31`, `byDomain('journal')===4`, `byDomain('knowledge')===5` verde; T27 `toAnthropicTools` 9 tools sem throw | PASS |
| AC7 | E2E canónico coberto por testes de domínio + CRUD com `fake-indexeddb` (`ctx.db` real, classifier domains alinhados) | PASS |
| AC8 | Edge-safety: grep confirma zero `import` proibido; build exit 0 | PASS |
| AC9 | typecheck exit 0; test:unit 1896/1896 (1888 + 8 testes CR Iter 1); build exit 0 | PASS |
| AC10 | journal.test.ts (T1-T11, 16 casos) + knowledge.test.ts (T12-T27, 18 casos) — anti-tautológicos T25/T25b/T11 | PASS |
| AC11 | Mapa de verificabilidade respeitado: CI cobre tudo excepto fetch real (`pesquisar_web_e_criar_nota` → preview/produção manual) | PASS |

### C1-C5 (condições do gate de entrada, contra código real)

- **C1 (transacção atómica única):** `knowledge.ts:482-528` — uma única `ctx.db.transaction('rw', [knowledge_areas, knowledge_notebooks, knowledge_notes], …)`. Zero writes soltos fora da transacção. Falha → rollback total (T24 prova 0 áreas/cadernos/notas; T25/T25b provam 0 notas).
- **C2 (inspecção de corpo):** `knowledge.ts:458-468` — `data === null || !Array.isArray(data.results)` E `results.length === 0` antes de qualquer write; `!resp.ok` incluído. Lança `Error` PT-PT, zero writes. T25 (results ausente HTTP 200) + T25b (results vazio HTTP 200) provam-no (lição M4 4.9/5.11).
- **C3 (get-or-create idempotente):** `nameMatches` localeCompare `'pt-PT' sensitivity:'accent'` (`:75-79`) para área (`:488`) E caderno (`:504`); IDs `crypto.randomUUID()` resolvidos DENTRO da transacção (`:491`, `:507`, `:517`) — sem stale (lição M3). T23b prova reutilização case-insensitive ("espaço"/"artemis 2" → 1 área, 1 caderno, 1 nota).
- **C4 (sem imports proibidos):** grep em ambos os ficheiros — `@/lib/db/client`, `@/lib/db/repos`, `web-search-create` aparecem SÓ em comentários/JSDoc, zero declarações `import`. Imports reais: `zod`, `registry`, `types`, helpers puros `searchEntries`/`searchKnowledgeNotes`, tipos `@/types/db`.
- **C5 (brain_dump sem chat_messages):** `journal.ts:259-278` devolve `{brainDumpId, mensagem}` PT-PT; zero referência a `chat_messages`. T11 assere `db.chat_messages.count()===0`.

### Análise `internal-state-contract-gate.md` — 3 eixos (`pesquisar_web_e_criar_nota`, código real)

- **(a) Classes de estado:** `{pesquisa-pendente, pesquisa-ok-resultados, pesquisa-falhou, área/caderno nova, área/caderno existente, nota-criada, rollback}`. Get-or-create distingue "nova" (`undefined` → `add`) de "existente" (`find` por `nameMatches`) para área E caderno — bug-de-classe A2 coberto (não só um). Nota sempre nova. `knowledge.ts:486-526`.
- **(b) Transição-já-ocorrida:** os IDs resolvem-se DENTRO de `ctx.db.transaction` (`:482-528`), nunca antes — a proposta carrega NOMES (`args.nomeArea`/`args.nomeCaderno`), não IDs. Se a área/caderno já existir no momento da transacção reutiliza-se; senão cria-se no mesmo `'rw'` scope. Sem referência stale a entidade eliminada entre leitura e write (lição M3 4.9, `[D-5.12-FAILURE]` eixo b).
- **(c) Caminhos de falha:** fetch falhado detectado por corpo (`:458-468`), não só `resp.ok` — lança antes de abrir transacção (zero writes). Falha de persistência (Dexie) → rollback automático total da transacção (zero entidades, nunca parcial). Erro propaga ao executor → `tool_error` (utilizador não recebe falsa confirmação). T24/T25/T25b são a prova executável.

### Gates (re-corridos por mim dentro de `imersao-tools/nexus/v2/`)

| Gate | Comando | Resultado |
|------|---------|-----------|
| Typecheck | `npm run typecheck` | exit 0 |
| Testes | `npm run test:unit` | **1888/1888 passed** (163 ficheiros) — baseline 1849 + 39 novos |
| Build | `npm run build` | exit 0 (Edge-safe; `/knowledge` 13,3 kB) |
| CodeRabbit | `coderabbit review --agent -t committed --base main` (WSL) | 3 findings (1 minor + 2 major) — todos **não-bloqueantes** (ver abaixo) |

### CodeRabbit `--base main` — triagem (lição 5.11)

O CLI respondeu (sem rate-limit). 3 findings, todos não-bloqueantes:

1. **minor `journal.ts:64`** — `toISOString()` default em UTC. **Não bloqueia:** convenção pré-existente em todo o stack de diário (repo + `consultar_diario`); utilizador em Portugal (UTC+0/+1), edge case de offset negativo não ocorre. Débito menor cosmético.
2. **major `journal.ts:43-45`** — double cast `as unknown as ToolDefinition`. **Não bloqueia:** padrão idêntico nos 8 ficheiros de tools pré-existentes (`tasks`/`projects`/`finance`/`habits`/`goals`/`reminders` — verificado por grep). É a fronteira de variância genérica entre `ToolDefinition<TArgs,TResult>` e o store invariante do registry. Refactorizar a assinatura do registry tocaria 8 ficheiros fora do scope desta story. Aprovado nas Stories 2.10/3.11/4.10.
3. **major `journal.ts:224-226`** — `pesquisar_diario` faz `toArray()` antes de pesquisar. **Não bloqueia:** by-design — espelha exactamente `searchJournalEntries` do repo (`journal-entries.ts:71-74`, load-all + helper puro) e `searchKnowledgeNotes`. App single-user (`userId:'eurico'`, C1) com dataset pessoal limitado. Mesma escolha ratificada para os helpers puros Edge-safe.

**Conclusão:** os 3 findings serão também cobertos pelo CR App no head SHA do PR (merge-authority cond.2). Nenhum exige acção do `@dev` antes do PR.

### Débito menor não-bloqueante (registado, não corrige nesta story)

- `toISOString()` UTC no default de `criar_entrada_diario` (CR minor #1) — alinhar com timezone local se algum dia o utilizador mudar de fuso. Cosmético para PT.
- O padrão `toArray()`+helper puro nas tools de pesquisa (CR major #3) é aceitável para single-user; se o Nexus escalar a multi-utilizador ou datasets grandes, reavaliar com índice/paginação (cross-cutting, todas as tools de pesquisa do projecto).

---

## Dev Notes

### Contexto de implementação — o que reutilizar, não inventar

**Padrão de registo (copiar de `habits.ts` para todas as 9 tools):**
```typescript
// Edge-safe — sem import de @/lib/db/client nem @/lib/db/repos/*
import { z } from 'zod';
import { toolRegistry, defineTool } from '@/lib/agent/tools/registry';
import type { ExecutionContext, ToolDefinition } from '@/lib/agent/tools/types';
// Helpers puros PODEM ser importados (sem Dexie):
import { searchEntries } from '@/lib/diario/pesquisa';         // para pesquisar_diario
import { searchKnowledgeNotes } from '@/lib/conhecimento/pesquisa'; // para pesquisar_conhecimento
```

**Padrão de helper fuzzy match (copiar de `habits.ts:47-51`):**
```typescript
function fuzzyMatches(candidate: string, input: string): boolean {
  const a = candidate.toLowerCase();
  const b = input.toLowerCase();
  return a.includes(b) || b.includes(a);
}
```

**Padrão de check de duplicado de área/caderno (reutilizar de `[D-5.12-PERSIST]`):**
```typescript
// Case-insensitive PT-PT via localeCompare — padrão de web-search-create.ts:122-128
function nameMatches(a: string, b: string): boolean {
  return a.trim().localeCompare(b.trim(), 'pt-PT', { sensitivity: 'accent' }) === 0;
}
```

**Não reinventar:** `applyDirection` para tools financeiras (irrelevante aqui), `RRule` (irrelevante), `currencyInput` (irrelevante). O padrão de `fake-indexeddb` nos testes vem de `finance.test.ts` / `habits.test.ts` — copiar a infra de setup.

### Decisões deferidas não-resolvidas (marcadas `[D-5.13-*]`)

| Decisão | Impacto em código | Quem resolve |
|---------|-------------------|--------------|
| `[D-5.13-DOMAIN]` | Nomes de ficheiros, imports barrel, `domain:` em cada tool, asserções T26 | `@architect` Gate de Entrada |
| `[D-5.13-WEB-SEARCH-TOOL]` | Implementação de `pesquisar_web_e_criar_nota`, T23-T25, Edge-safety | `@architect` Gate de Entrada |
| `[D-5.13-CHAT-RETRO]` | `resultSchema` de `brain_dump`, T11, escrita em `chat_messages` ou não | `@architect` Gate de Entrada |
| `[D-5.13-BRAIN-DUMP-SCOPE]` | `execute()` de `brain_dump`, `requiresPreview`, T9 | `@architect` Gate de Entrada |

### Referências de código canónico (verificadas no filesystem)

| Artefacto | Caminho | Nota |
|-----------|---------|------|
| Tool Registry | `lib/agent/tools/registry.ts` | `TOOL_NAME_PATTERN`, `defineTool`, `toolRegistry` |
| ToolDefinition/ExecutionContext | `lib/agent/tools/types.ts:100-130` | `ctx.db: NexusDB` — usar directamente |
| Barrel existente | `lib/agent/tools/index.ts` | Estender com novos imports |
| Precedente habits | `lib/agent/tools/habits.ts` | Padrão Edge-safety + fuzzy match |
| `JournalEntry` | `types/db.ts:223-229` | `id, date, mood, bodyMarkdown, structuredAI?` |
| `BrainDump` | `types/db.ts:261-267` | `id, createdAt, bodyMarkdown, parsedOutput?, status` |
| `KnowledgeArea` | `types/db.ts:231-236` | `id, name, color, icon` — 4 campos obrigatórios |
| `KnowledgeNotebook` | `types/db.ts:238-242` | `id, areaId, name` |
| `KnowledgeNote` | `types/db.ts:244-252` | `id, notebookId, title, bodyMarkdown, tags, sourceUrl?, updatedAt` |
| Helper pesquisa diário | `lib/diario/pesquisa.ts` | `searchEntries(entries, query)` — Edge-safe |
| Helper pesquisa conhecimento | `lib/conhecimento/pesquisa.ts` | `searchKnowledgeNotes(notes, query)` — Edge-safe |
| Lógica 5.12 (referência, NÃO importar) | `lib/conhecimento/web-search-create.ts` | Client-side puro — ver `[D-5.13-WEB-SEARCH-TOOL]` |
| Repos diário (ref. shapes, NÃO importar) | `lib/db/repos/journal-entries.ts` | `createJournalEntry`, `getJournalEntryByDate` |
| Repos brain dump (ref., NÃO importar) | `lib/db/repos/brain-dumps.ts` | `createBrainDump`, `getBrainDump` |
| Repos áreas/cadernos/notas (ref., NÃO importar) | `lib/db/repos/knowledge-*.ts` | Shapes de write/read |

### Lições aplicadas preventivamente

| Lição | Aplicação |
|-------|-----------|
| A1 Epic 1 (`mock-protocol-fidelity.md`) | T11/T23/T25 — mocks com shape real (não inventado) |
| A1 Epic 4 (`internal-state-contract-gate.md`) | `pesquisar_web_e_criar_nota` — análise 3 eixos obrigatória no gate de entrada |
| A2 Epic 4 (bug-de-classe) | T1.4/T3.4 — check de duplicado para área E caderno, não só um |
| A3 Epic 3 (`react-component-test-criteria.md`) | N/A (story backend puro sem UI) |
| A4 Epic 3 (`external-contract-identifiers.md`) | Nomes validados no draft do epic — sem reconciliação de AC |
| A6 Epic 1 (`separation-of-roles.md`) | Executor `@dev` ≠ gate `@architect` |
| A3 Epic 4 (mapa verificabilidade) | AC11 — mapa por tool e por ambiente |
| Lição 5.11 (CR --base main) | T7.5 — gate de saída corre `--base main`, não só `-t uncommitted` |
| D-DOMAIN Opção A (4.10) | Verificar agrupamento no classifier antes de decidir — `[D-5.13-DOMAIN]` |

---

## File Locations

> Caminhos esperados, conforme `[D-5.13-DOMAIN]` ratificada pelo `@architect`. Opção A (2 ficheiros) é a hipótese de trabalho provisória; o `@architect` pode alterar.

### Ficheiros novos (a criar)

| Ficheiro | Descrição | AC |
|----------|-----------|----|
| `imersao-tools/nexus/v2/lib/agent/tools/journal.ts` | 4 tools: `criar_entrada_diario`, `consultar_diario`, `pesquisar_diario`, `brain_dump` (provisório — `[D-5.13-DOMAIN]`) | AC1/AC2 |
| `imersao-tools/nexus/v2/lib/agent/tools/knowledge.ts` | 5 tools: `criar_area`, `criar_caderno`, `criar_nota`, `pesquisar_conhecimento`, `pesquisar_web_e_criar_nota` (provisório) | AC3/AC4 |
| `imersao-tools/nexus/v2/tests/unit/agent/tools/journal.test.ts` | Testes Vitest tools de diário + brain dump (T1-T11) | AC10 |
| `imersao-tools/nexus/v2/tests/unit/agent/tools/knowledge.test.ts` | Testes Vitest tools de conhecimento (T12-T27) | AC10 |

### Ficheiros a modificar

| Ficheiro | Modificação | AC |
|----------|-------------|----|
| `imersao-tools/nexus/v2/lib/agent/tools/index.ts` | Adicionar imports das tools Epic 5 (1-3 linhas) | AC6 |

### Ficheiros verificados (NÃO modificar)

| Ficheiro | Motivo |
|----------|--------|
| `imersao-tools/nexus/v2/lib/agent/tools/types.ts` | `ToolDomain` já tem `'journal'`/`'knowledge'` — não estender |
| `imersao-tools/nexus/v2/lib/agent/classifier.ts` | `ALL_DOMAINS` inclui ambos — não alterar |
| `imersao-tools/nexus/v2/lib/agent/tools/registry.ts` | Infraestrutura da Story 1.3 — não alterar |
| `imersao-tools/nexus/v2/lib/conhecimento/web-search-create.ts` | Client-side puro da 5.12 — não alterar nem importar nas tools |
| `imersao-tools/nexus/v2/lib/db/repos/*.ts` | Todos os repos — não importar nas tools (Edge-safety) |

---

## Testing

### Estratégia de testes

**Infraestrutura:** `fake-indexeddb` (padrão `finance.test.ts`, `habits.test.ts`). `vi.mock` para `@/lib/db/client` com `fake-indexeddb`. MSW para mock do endpoint `/api/conhecimento/web-search` nos testes da `pesquisar_web_e_criar_nota`.

**Cobertura mínima por tool:**
- Tools write (criar): sucesso, duplicado/erro PT-PT, reverse.
- Tools read (consultar/pesquisar): sucesso, sem resultados.
- Tool `pesquisar_web_e_criar_nota`: sucesso (mock endpoint), falha fetch, mock-fidelidade anti-tautológica.
- Tool `brain_dump`: sucesso, texto vazio, mock-fidelidade.

**Suite completa:** `npm run test:unit` — 1849 (baseline pós-5.12) + ≥ 27 novos = ≥ 1876 testes verde.

### Verificação manual (preview + produção)

| Cenário | Ambiente |
|---------|----------|
| `pesquisar_web_e_criar_nota`: fluxo completo "pesquisa Artemis 2 e cria área Espaço com caderno Artemis 2" — nota aparece em `/knowledge` | Preview (`ANTHROPIC_API_KEY` real) |
| `criar_entrada_diario`: diário de hoje aparece na vista de heatmap | Preview |
| `brain_dump`: brain dump processado + toast/mensagem PT-PT | Preview |
| AC4 do epic ("Pesquisa web cria nota com resumo + fonte URL") — validação final | Produção (Eurico) |

---

## CodeRabbit Integration

> `coderabbit_integration.enabled: true` (confirmado em `core-config.yaml`).

### Story Type Analysis

```yaml
Primary Type: Integration (Tool Registry + AI tools + fetch externo)
Secondary Type(s): Architecture (Edge-safety, domínio do classifier)
Complexity: High (9 tools, 4 decisões deferidas ao @architect, fetch externo, D-5.8-CHAT-RETRO)
```

### Specialized Agent Assignment

```yaml
Primary Agents:
  - "@dev" (implementação das 9 tools + testes)
  - "@architect" (gate de entrada + saída — decisões D-5.13-*)

Supporting Agents:
  - "@devops" (push + PR + merge quando gates verdes)
```

### Quality Gate Tasks

- [ ] Architect Gate de ENTRADA (@architect): ratificação das 4 decisões `[D-5.13-*]` antes de `@dev *develop 5.13`
- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted` antes de marcar story complete
- [ ] Pre-PR Architect Gate de SAÍDA (@architect): verificar AC1-AC11 + Edge-safety + `internal-state-contract-gate.md` para `pesquisar_web_e_criar_nota` + `[D-5.13-CHAT-RETRO]` entregue
- [ ] Pre-PR CodeRabbit (@dev): `coderabbit --prompt-only --base main` (diff completo vs main — lição 5.11 `[[feedback_cr_base_main_no_gate_saida]]`)

### CodeRabbit Focus Areas

```yaml
Primary Focus:
  - Edge-safety: zero imports de @/lib/db/client ou @/lib/db/repos/* nos ficheiros de tools
  - Tool name compliance: 9 nomes satisfazem TOOL_NAME_PATTERN (/^[a-z][a-z0-9_]*$/)
  - argsSchema: todos são z.ZodObject<z.ZodRawShape> (não z.ZodType genérico)

Secondary Focus:
  - mock-protocol-fidelity: T11/T23/T25 são anti-tautológicos
  - internal-state-contract-gate: pesquisar_web_e_criar_nota (fetch externo + persistência)
  - D-5.13-CHAT-RETRO: brain_dump entrega mensagem retroactiva conforme decisão @architect
  - descriptions PT-PT: 9 descriptions não vazias com exemplos de utterances
```

### Self-Healing Configuration

```yaml
Primary Agent: "@dev" (light mode)
Max Iterations: 2
Timeout: 15 min
Severity Filter: CRITICAL only

Severity Behavior:
  CRITICAL: auto_fix (Edge-safety violada, tool name inválido)
  HIGH: document_only (mock não-fiel, description vazia)
  MEDIUM: ignore
  LOW: ignore
```

---

## Dev Agent Record

### Agent Model Used

Dex (`@dev`) — Claude Opus 4.8 (1M context). Modo YOLO (autónomo).

### Implementação (15/06/2026)

Implementação das 9 tools do Epic 5 no Tool Registry, contra as 4 decisões `[D-5.13-*]` ratificadas pela Aria no Architect Gate de Entrada (Opção A / W1 / R1 / B1) e as condições C1-C5. Branch `feat/nexus-v2-5.13-tools-cerebro` a partir de `main`. Não pushed (fase `@devops`).

Padrão replicado de `habits.ts` (Edge-safety via `ctx.db.*`, helper `registar`, `nameMatches` localeCompare PT-PT de `web-search-create.ts`). Helpers puros importados: `searchEntries` (5.5) e `searchKnowledgeNotes` (5.10). Nenhum import de `@/lib/db/client`, repos, ou `web-search-create.ts` (AC8/C4 verificado por grep — só aparecem em comentários JSDoc).

`pesquisar_web_e_criar_nota` (W1): `ctx.fetch('/api/conhecimento/web-search')` (cookie same-origin automático — ADR-9 executor client-side) com inspecção do CORPO da resposta (C2: `!resp.ok` OU `results` ausente/não-array/vazio OU `error`) antes de qualquer write; persistência da cascata área→caderno→nota numa única `ctx.db.transaction('rw', [3 stores], …)` atómica (C1) com get-or-create idempotente por nome para área E caderno (C3) e IDs resolvidos DENTRO da transacção (lição M3). Réplica fiel de `persistProposal` via `ctx.db`.

### Decisões autónomas

- `[AUTO-DECISION]` pesquisar_conhecimento enriquecimento → Map(id→entidade) em memória de `knowledge_areas`/`knowledge_notebooks` carregados via `Promise.all` (reason: volume moderado, evita N+1 `get` por nota; precedente 5.10 breadcrumbByNotebook).
- `[AUTO-DECISION]` cor/ícone default de área → constantes locais DEFAULT_AREA_COLOR='#8892A4' + WEB_AREA_ICON='🌐' (reason: AC3 especifica '#8892A4'; web-search-create.ts usa DEFAULT_TAG_COLOR de @/lib/tags/colors — Edge-safe — mas C4 exige minimizar imports; valor inline espelha o canónico sem acoplar).
- `[AUTO-DECISION]` contagem registry hard-coded 22→31 em projects.test.ts:T22 e finance.test.ts:T19 (reason: o barrel global passa a registar 31 tools; estes testes fazem import '@/lib/agent/tools' e assertam o total — actualização forçada análoga ao precedente version-bump-full-suite; sem isto regridem).

### Testes adicionados

- `journal.test.ts` — 15 testes (T1-T11 + domínio + defaults + DB vazia). T11 anti-tautológico: valida que o resultado passa o `resultSchema` real e que `chat_messages.count()===0` (R2 rejeitada).
- `knowledge.test.ts` — 24 testes (T12-T27 + domínio + idempotência + filtro área). T23b idempotência get-or-create case-insensitive PT-PT (C3). T25/T25b anti-tautológicos: `results` ausente (HTTP 200) e `results` vazio → erro + zero notas (C2).

### Completion Notes

- Todos os 11 AC implementados e verificados contra código real. C1-C5 cumpridas.
- Sem version bump Dexie (AC8 — só `version(5)` read/write das tabelas existentes).
- `vitest.config.ts` INTOCADO: `lib/agent/tools/**` já na allowlist de coverage (5.10).
- 4 gates FRESCOS verdes + CR pre-commit 0 findings (Iter 1). Nenhuma FLAG `@architect` — nenhuma decisão ratificada contradita.
- Próximo: Architect Gate de SAÍDA (T8) — `@architect` corre `coderabbit review --agent -t committed --base main` (diff completo, lição 5.11) + verifica AC1-AC11 + `internal-state-contract-gate.md` para `pesquisar_web_e_criar_nota` + `[D-5.13-CHAT-RETRO]`.

### Architect Adjudicação CR Iter 1 (6 findings)

> **Aria (`@architect`), 16/06/2026.** A CodeRabbit App postou 6 findings (CR Iter 1) no PR #74 (head SHA `15f02df4`). 3 tocam o contrato das tools e exigem ratificação antes de `@dev` aplicar os fixes. Adjudicação contra o código real (`journal.ts`, `knowledge.ts`, `journal.test.ts`) e a semântica de cascata canónica da Story 5.9 (`deleteKnowledgeArea`). `@dev` implementa; nada marcado done aqui.

#### Finding 1 — `journal.ts:69-72` `consultar_diario`: filtro de data unilateral (Major, CONTRATO)

**Veredicto:** ACEITE com correcção de contrato. O `argsSchema` define `dataInicio`/`dataFim` independentes (ambos `nullable`), mas o `execute` (`:180`) só usa o range quando AMBOS estão presentes; com apenas um, cai silenciosamente em "últimas 7 entradas", descartando a intenção explícita do utilizador. Isto é perda de intenção, não um default benigno.

**Contrato correcto (intervalo aberto, NÃO rejeitar):** o caso unilateral é intenção legítima e frequente ("o que escrevi desde Maio", "entradas até 10 de Junho"). Rejeitar com Error seria hostil ao utilizador e ao classifier. A semântica passa a ser:

| `dataInicio` | `dataFim` | Comportamento |
|--------------|-----------|---------------|
| presente | presente | range fechado `[dataInicio, dataFim]` (já implementado) |
| presente | `null` | **desde `dataInicio` até hoje** (intervalo aberto à direita) |
| `null` | presente | **desde o início até `dataFim`** (intervalo aberto à esquerda) |
| `null` | `null` | últimas 7 entradas (fallback — já implementado) |

**Fix-spec exacto (`@dev`):** em `journal.ts`, no `execute` de `consultar_diario` (`:178-191`), substituir a condição binária por: usar o ramo `.between(...)` sempre que **pelo menos um** de `dataInicio`/`dataFim` não for `null`, com limites de sentinela ISO para o lado ausente:

```ts
const SENTINEL_MIN = '0000-01-01';
const SENTINEL_MAX = '9999-12-31';
// ...
if (args.dataInicio !== null || args.dataFim !== null) {
  const lo = args.dataInicio ?? SENTINEL_MIN;
  const hi = args.dataFim ?? SENTINEL_MAX;
  entries = await ctx.db.journal_entries
    .where('date')
    .between(lo, hi, true, true)
    .sortBy('date');
} else {
  entries = await ctx.db.journal_entries.orderBy('date').reverse().limit(7).toArray();
}
```

As sentinelas são seguras: `date` é sempre `YYYY-MM-DD` (validado por `ISO_DATE_RE`), comparação lexicográfica = comparação cronológica, e `0000-01-01`/`9999-12-31` ficam fora de qualquer data real plausível sem truncar resultados. Declarar as duas como `const` ao nível do módulo (junto de `ISO_DATE_RE`).

**Teste a adicionar (`journal.test.ts`):** T5b — só `dataInicio` (3 entradas, 1 anterior) → retorna as 3 ≥ `dataInicio`; T5c — só `dataFim` → retorna as ≤ `dataFim`. Garante que o ramo unilateral não cai nas "últimas 7".

**Reconciliação de AC:** AC1 (`consultar_diario`) é ampliado — a sua semântica passa a cobrir explicitamente os 4 casos da tabela acima (antes só "range completo" e "ambos null"). Actualizar a descrição de AC1 em §AC e a linha de Trace (`:489`) para referir "range fechado, intervalo aberto unilateral, ou últimas 7". Sem mudança de assinatura do `argsSchema` (continua `nullable().default(null)` em ambos) — só a lógica de `execute` e os testes.

#### Finding 2 — `knowledge.ts:129-158` `.min(1)` antes de `.trim()` (Major, validação)

**Veredicto:** ACEITE. `z.string().min(1)` aceita `"   "` (só espaços), que depois persiste como `.trim()` vazio nos campos onde o código já faz `.trim()` no `execute` (`name`, `title`). Resultado: área/caderno/nota/título visualmente vazios na BD. A validação tem de rejeitar whitespace-only **na fronteira do schema**.

**Fix-spec exacto (`@dev`):** aplicar `.trim().min(1)` (a forma curta e correcta em Zod — `.trim()` é transform antes de `.min`) a TODOS os campos de texto obrigatórios. Confirmação dos campos afectados:

| Schema | Campo | Linha | Mudança |
|--------|-------|-------|---------|
| `CriarAreaArgs` | `nome` | `:130` | `z.string().trim().min(1).max(200)` |
| `CriarCadernoArgs` | `nomeArea` | `:136` | `z.string().trim().min(1)` |
| `CriarCadernoArgs` | `nomeCaderno` | `:137` | `z.string().trim().min(1).max(200)` |
| `CriarNotaArgs` | `nomeArea` | `:141` | `z.string().trim().min(1)` |
| `CriarNotaArgs` | `nomeCaderno` | `:142` | `z.string().trim().min(1)` |
| `CriarNotaArgs` | `titulo` | `:143` | `z.string().trim().min(1).max(500)` |
| `CriarNotaArgs` | `conteudo` | `:144` | `z.string().trim().min(1)` |
| `PesquisarConhecimentoArgs` | `query` | `:149` | `z.string().trim().min(1)` |
| `PesquisarWebECriarNotaArgs` | `query` | `:155` | `z.string().trim().min(1)` |
| `PesquisarWebECriarNotaArgs` | `nomeArea` | `:156` | `z.string().trim().min(1)` |
| `PesquisarWebECriarNotaArgs` | `nomeCaderno` | `:157` | `z.string().trim().min(1)` |

Nota: como `.trim()` passa a normalizar no schema, os `.trim()` redundantes no `execute` (`:256`, `:301`, `:347`, `:471`, `:492`, `:509`) podem permanecer (são idempotentes e defensivos) — NÃO é obrigatório removê-los; manter para robustez. O `cor`/`icone` de `CriarAreaArgs` NÃO mudam (têm regex/default próprios). Aplicar o MESMO tratamento aos campos de texto obrigatórios de `journal.ts` por simetria: `CriarEntradaDiarioArgs.bodyMarkdown` (`:65` → `.trim().min(1)`), `PesquisarDiarioArgs.query` (`:75`), `BrainDumpArgs.texto` (`:79-82`). Mantém o `.describe()` do `BrainDumpArgs` na cadeia.

**Teste:** acrescentar a um teste existente por schema um caso `"   "` que lance (`.toThrow()`), espelhando T10 (texto vazio).

#### Finding 3 — `knowledge.ts:263-267` `reverse` de `criar_area`/`criar_caderno` orfana filhos (Major, CONTRATO)

**Veredicto:** ACEITE com correcção de contrato — **recusar o reverse se houver filhos** (Opção "só seguro imediatamente após criação"). NÃO cascade-delete.

**Justificação (alinhamento com 5.9):** o `reverse` da máquina de undo do executor existe para desfazer a operação atómica que a tool acabou de fazer — criar UMA área/caderno vazio. Não é uma operação de gestão. A cascata destrutiva de 2 níveis é responsabilidade exclusiva de `deleteKnowledgeArea` (5.9, `knowledge-areas.ts:70-91`), accionada deliberadamente pela UI de gestão com confirmação do utilizador. Um `reverse` que silenciosamente cascateasse apagaria cadernos/notas que o utilizador possa ter criado entretanto — destruição de dados sem consentimento, pior que o orfanamento. A semântica segura é: o reverse só procede se a área/caderno continuar folha (sem filhos); caso contrário recusa com Error PT-PT que remete para a UI de gestão.

**Fix-spec exacto (`@dev`):**

`criar_area` reverse (`:263-267`):
```ts
reverse: async (_args, result, ctx) => {
  const filhos = await ctx.db.knowledge_notebooks
    .where('areaId')
    .equals(result.id)
    .count();
  if (filhos > 0) {
    throw new Error(
      `Não posso desfazer: a área "${result.nome}" já tem ${filhos} caderno(s). Usa a gestão de conhecimento para a eliminar com os seus conteúdos.`,
    );
  }
  await ctx.db.knowledge_areas.delete(result.id);
},
```

`criar_caderno` reverse (`:311-313`):
```ts
reverse: async (_args, result, ctx) => {
  const filhos = await ctx.db.knowledge_notes
    .where('notebookId')
    .equals(result.id)
    .count();
  if (filhos > 0) {
    throw new Error(
      `Não posso desfazer: o caderno "${result.nomeCaderno}" já tem ${filhos} nota(s). Usa a gestão de conhecimento para o eliminar com os seus conteúdos.`,
    );
  }
  await ctx.db.knowledge_notebooks.delete(result.id);
},
```

`criar_nota` reverse (`:360-362`): SEM mudança — nota é folha, `delete(result.id)` é correcto e completo.

**Teste:** T14b — `criar_area` → adicionar caderno via `criar_caderno` → `reverse` lança Error (área NÃO eliminada, caderno intacto). T17b — análogo para `criar_caderno` com nota filha. Manter T14/T17 (reverse imediato sem filhos → eliminado).

**Reconciliação de AC:** AC3 (linha `:491`) não fixava o comportamento de reverse com filhos. A spec da story (`:273`) dizia "avisa: AVISO …" mas o código nunca emitiu aviso — divergência. Substituir essa linha `:273` por: "reverse recusa com Error PT-PT se a área já tiver cadernos (alinhado com a semântica de cascata da 5.9, que é exclusiva da UI de gestão); reverse só procede sobre área folha". Idem para a linha de `criar_caderno`. Reconciliar a descrição de AC3 para incluir "reverse seguro (recusa se houver filhos)".

#### Finding 4 — `knowledge.ts:458-473` `pesquisar_web_e_criar_nota` não valida itens da resposta web (Major, validação — gap da C2)

**Veredicto:** ACEITE — **filtrar itens inválidos (skip), e só abortar se NÃO sobrar nenhum item válido.** Coerente com C2 (zero writes em falha real) sem ser frágil a um único item malformado vindo de um provider externo.

**Justificação:** a resposta web é uma fronteira de confiança externa (endpoint 5.11 → provider Anthropic/DuckDuckGo). Um item com `title` vazio ou `url` malformada não deve nem fazer crash (`buildNoteBody`/`new URL` implícito) nem persistir uma nota lixo. Mas abortar a operação inteira por causa de UM item mau, quando há outros válidos, seria desperdiçar uma pesquisa boa. A regra robusta: validar cada item com Zod, descartar os inválidos, escolher o primeiro VÁLIDO como `top`. Se a filtragem esvaziar a lista, aí sim Error PT-PT + zero writes (mesmo path que `results.length === 0`).

**Fix-spec exacto (`@dev`):**

1. Definir um schema Zod por item ao nível do módulo (junto de `WebSearchResultShape`):
```ts
const WebSearchResultSchema = z.object({
  title: z.string().trim().min(1),
  url: z.string().trim().url(),
  excerpt: z.string().default(''),
});
```
2. No `execute`, após a guarda C2 actual (`:459-468`), substituir `const top = data.results[0]` por uma filtragem validada:
```ts
const validos = data.results.flatMap((r) => {
  const parsed = WebSearchResultSchema.safeParse(r);
  return parsed.success ? [parsed.data] : [];
});
if (validos.length === 0) {
  throw new Error(
    `Os resultados de «${args.query}» não têm título/URL válidos — não há conteúdo para criar a nota.`,
  );
}
const top = validos[0];
```
3. `noteTitle`/`buildNoteBody`/`sourceUrl` passam a usar este `top` validado (já trimmado, `url` garantidamente válida). `buildNoteBody` mantém-se.

Resultado: itens malformados nunca chegam à transacção; a transacção (C1) só corre com dados validados; um único item bom basta para criar a nota; zero writes se nenhum for válido (coerente com C2 e com o ramo `length === 0`).

**Teste:** T25c — `results` com 1 item `title:''` + 1 item válido → cria a nota a partir do válido. T25d — `results` com TODOS os itens inválidos (`url` malformada) → Error + `knowledge_notes.count()===0` (zero writes, espelha T25/T25b).

#### Finding 5 — `journal.test.ts:49-52` `beforeEach` não limpa `chat_messages` (Major, teste)

**Veredicto:** RATIFICADO. O T11 (`:238`) assenta em `db.chat_messages.count() === 0`, mas o `beforeEach` (`:49-52`) só limpa `journal_entries` e `brain_dumps`. Se qualquer outro teste da suite escrever em `chat_messages` antes (ordem de execução não-determinística do Vitest), o T11 falha por contaminação — flakiness order-dependent real.

**Fix-spec exacto (`@dev`):** adicionar a limpeza de `chat_messages` ao `beforeEach` (`journal.test.ts:49-52`):
```ts
beforeEach(async () => {
  await db.journal_entries.clear();
  await db.brain_dumps.clear();
  await db.chat_messages.clear();
});
```

Sem reconciliação de AC (fix puramente de isolamento de teste). Sem impacto em produção.

#### Finding 6 — `5.13.story.md:754` markdownlint MD038 (Minor, cosmético)

**Veredicto:** RATIFICADO. Espaço dentro de code span na linha 754 (back-tick a abrir code span seguido de espaço). Corrigido nesta mesma edição da story (ver abaixo) — o code span passa a envolver só o identificador sem espaços interiores.

#### Sumário de reconciliação de AC

| AC | Estado | Mudança |
|----|--------|---------|
| AC1 (`consultar_diario`) | RECONCILIADO | semântica ampliada: range fechado / intervalo aberto unilateral / últimas 7 (finding 1) |
| AC3 (`criar_area`/reverse) | RECONCILIADO | reverse recusa com Error se houver filhos, alinhado com cascata 5.9 (finding 3) |
| AC2, AC4-AC11 | INALTERADOS | findings 2/4/5/6 são validação/teste/cosmético, sem mudança de contrato de AC |

`@dev` aplica os 6 fixes + os 7 testes novos (T5b, T5c, casos whitespace, T14b, T17b, T25c, T25d), volta a correr typecheck/lint/vitest, e reconcilia as descrições de AC1/AC3 + as linhas `:239`/`:273`/`:281`/`:489`/`:491` da spec. NÃO marcar done até gate de re-saída.

#### Dev Agent Record — aplicação dos 6 findings (Dex `@dev`, 16/06/2026)

**Ficheiros tocados nesta iteração (CR Iter 1 fixes):**

| Ficheiro | Findings | Mudança |
|----------|----------|---------|
| `lib/agent/tools/journal.ts` | F1 + F2 | `SENTINEL_MIN`/`SENTINEL_MAX` ao nível do módulo; `consultar_diario.execute` usa `.between(lo, hi, true, true)` se pelo menos um limite presente (intervalo aberto unilateral); `.trim().min(1)` em `bodyMarkdown`/`PesquisarDiarioArgs.query`/`BrainDumpArgs.texto` (`.describe()` preservado) |
| `lib/agent/tools/knowledge.ts` | F2 + F3 + F4 | `.trim().min(1)` nos 11 campos de texto obrigatórios (`cor`/`icone` intactos); reverse `criar_area`/`criar_caderno` conta filhos e recusa com Error PT-PT se >0; `WebSearchResultSchema` (title `.trim().min(1)`, url `.trim().url()`, excerpt `.default('')`) + filtragem `flatMap`/`safeParse` por item (aborta só se `validos.length===0`) |
| `tests/unit/agent/tools/journal.test.ts` | F5 + testes | `beforeEach` limpa `chat_messages`; +T5b (só dataInicio), +T5c (só dataFim), +T10b (whitespace em 3 schemas journal) |
| `tests/unit/agent/tools/knowledge.test.ts` | testes | +T13b (whitespace nome área), +T14b (reverse área com caderno → Error), +T17b (reverse caderno com nota → Error), +T25c (item malformado filtrado), +T25d (todos inválidos → Error + zero writes) |

**Gates frescos (16/06/2026, dentro de `imersao-tools/nexus/v2/`):** lint 0 erros (1 warning pré-existente fora do scope — `logout/route.ts` `NextResponse`), typecheck exit 0, `test:unit` **1896 passed** (163 ficheiros; baseline 1888 + 8), build exit 0 (24 rotas, `/knowledge` 13,3 kB). Nenhuma FLAG `@architect` — todos os fix-specs da adjudicação foram aplicáveis ao código real. AC1/AC3 reconciliados. Status mantém-se `Ready for Review` para o gate de re-saída do `@architect`.

### File List

**Novos (produção):**
- `imersao-tools/nexus/v2/lib/agent/tools/journal.ts` — 4 tools `domain:'journal'`.
- `imersao-tools/nexus/v2/lib/agent/tools/knowledge.ts` — 5 tools `domain:'knowledge'`.

**Novos (testes):**
- `imersao-tools/nexus/v2/tests/unit/agent/tools/journal.test.ts` — 15 testes.
- `imersao-tools/nexus/v2/tests/unit/agent/tools/knowledge.test.ts` — 24 testes.

**Modificados:**
- `imersao-tools/nexus/v2/lib/agent/tools/index.ts` — +2 imports (`./journal`, `./knowledge`) + JSDoc 22→31.
- `imersao-tools/nexus/v2/tests/unit/agent/tools/projects.test.ts` — T22 contagem 22→31 + lista de 31 nomes.
- `imersao-tools/nexus/v2/tests/unit/agent/tools/finance.test.ts` — T19 contagem 22→31.

### Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 15/06/2026 | v1.1 | Implementação das 9 tools (T1-T7), 39 testes novos, 4 gates verdes + CR 0 findings. Status → Ready for Review. | Dex (`@dev`) |
| 16/06/2026 | v1.2 | Aplica 6 findings CR Iter 1 (adjudicação Aria). F1: `consultar_diario` intervalo aberto unilateral via sentinelas ISO. F2: `.trim().min(1)` nos 14 campos de texto obrigatórios (11 knowledge + 3 journal). F3: reverse `criar_area`/`criar_caderno` recusa com Error se houver filhos (alinhado cascata 5.9). F4: `WebSearchResultSchema` valida itens da resposta web por item (skip inválidos, aborta se nenhum válido). F5: `beforeEach` limpa `chat_messages`. F6: MD038 (já feito pela Aria). +8 testes (T5b/T5c/T10b/T13b/T14b/T17b/T25c/T25d). 4 gates verdes (lint 0err/typecheck 0/vitest 1896/build OK). AC1/AC3 reconciliados. Status mantém-se Ready for Review. | Dex (`@dev`) |

---

## PO Closure (Pax) — 16/06/2026

> `*close-story 5.13` executado por Pax (`@po`) após o merge do PR #74 em `main` (squash `79626969`, "feat(nexus-v2): 9 tools cérebro no Tool Registry [Story 5.13] (#74)"). `separation-of-roles.md`: o PO fecha a story; não implementa nem assina o quality gate (executor `@dev`, gate `@architect`). Transição `Ready for Review → Done`. **ÚLTIMA story do Epic 5 — o seu fecho fecha o Epic 5 (13/13).**

### Resumo do fecho

A Story 5.13 (Tools cérebro — FR46 + FR50 + FR57) está **DONE** em `main`. Implementação MERGED via PR #74 (`79626969`). As 9 tools de diário/brain-dump/conhecimento (`criar_entrada_diario`, `consultar_diario`, `pesquisar_diario`, `brain_dump`, `criar_area`, `criar_caderno`, `criar_nota`, `pesquisar_conhecimento`, `pesquisar_web_e_criar_nota`) estão registadas no Tool Registry (`toolRegistry.all().length === 31`; `byDomain('journal') === 4`; `byDomain('knowledge') === 5`). Cumpre as 4 decisões `[D-5.13-*]` ratificadas (Opção A / W1 / R1 / B1) e as condições C1-C5.

Ciclo completo: `@sm` draft → Architect Gate de ENTRADA (Aria, PASS-COM-CONDIÇÕES, 4 `[D-5.13-*]` ratificadas: DOMAIN=A, WEB-SEARCH-TOOL=W1, CHAT-RETRO=R1, BRAIN-DUMP-SCOPE=B1, premissa "executor Edge" corrigida via ADR-9) → PO Validation (Pax, GO 9/10) → `@dev` implementação (`v1.1`, 9 tools + 39 testes, vitest 1888) → Architect Gate de SAÍDA (Aria, PASS, AC1-AC11 + C1-C5 + 3 eixos `internal-state-contract-gate.md` verificados contra código real) → PR #74 → CR Iter 1 (6 findings; Aria adjudica — 3 de contrato exigiram reconciliação de AC) → `@dev` aplica os 6 fixes + 8 testes novos (`v1.2`, vitest 1896) → CR Iter 2 limpa → auto-merge `@devops` (`79626969`). Hard-stop §8 respeitado (≤2 iterações CR). **0 waivers.**

### Definition of Done — padrão 5.x (projecto sem `story-dod-checklist.md`)

O projecto Nexus não tem `story-dod-checklist.md` dedicado (confirmado em filesystem) — aplica-se o DoD padrão das stories 5.x (igual ao das 5.8/5.9/5.10/5.11/5.12). Resultado: **18/18 PASS**.

| # | Item DoD | Resultado |
|---|----------|-----------|
| 1 | Todos os AC implementados (AC1-AC11) | PASS — AC1-AC11 verificados contra código real no Architect Gate de Saída (T8); AC1/AC3 reconciliados na adjudicação CR Iter 1 (intervalo aberto unilateral de `consultar_diario`; reverse seguro de `criar_area`/`criar_caderno` que recusa com filhos, alinhado à cascata 5.9). AC11 mapeia `pesquisar_web_e_criar_nota` (fetch real) para verificação preview/produção. |
| 2 | Todas as tasks/subtasks `[x]` | PASS — T0-T8 completas, incluindo todas as subtasks. |
| 3 | Implementação MERGED em `main` | PASS — PR #74 squash `79626969`. |
| 4 | lint 0 erros novos | PASS — 1 warning pré-existente `logout/route.ts` fora-scope (desde 5.4). |
| 5 | typecheck exit 0 | PASS — `tsc --noEmit` exit 0. |
| 6 | vitest suite COMPLETA PASS | PASS — **1896 testes** / 163 ficheiros (baseline 1849 + 39 v1.1 + 8 v1.2 CR Iter 1). |
| 7 | `next build` exit 0 | PASS — Edge-safe (zero imports Dexie nos ficheiros de tools); 24 rotas; `/knowledge` 13,3 kB. |
| 8 | Architect Gate de ENTRADA | PASS-COM-CONDIÇÕES — Aria, 4 `[D-5.13-*]` ratificadas (A/W1/R1/B1), C1-C5. |
| 9 | Architect Gate de SAÍDA | PASS — Aria, AC1-AC11 + C1-C5 + 3 eixos verificados contra código real (`384dbd15`); CR `--base main` triado (3 findings não-bloqueantes, depois cobertos pelo CR App). |
| 10 | CodeRabbit sem CRITICAL no head SHA | PASS — CR Iter 1 (6 findings, adjudicados + corrigidos por `@dev`), CR Iter 2 limpa; 0 CRITICAL, 0 actionable no head SHA do merge. |
| 11 | `separation-of-roles.md` | PASS — executor `@dev` ≠ gate `@architect`; Aria fez gates entrada+saída + adjudicação CR sem implementar (os fixes foram do `@dev`). |
| 12 | `internal-state-contract-gate.md` (3 eixos) | PASS — análise dos 3 eixos de `pesquisar_web_e_criar_nota` documentada no gate de entrada (W1) e reverificada no gate de saída contra código real; Finding 4 CR Iter 1 (validação por item da resposta web) reforça o eixo (c). |
| 13 | `mock-protocol-fidelity.md` | PASS — `ctx.fetch` fake espelha o shape real da 5.11 (`{ results, source }`, `WebSearchResult{title,url,excerpt}`); T25/T25b/T25d anti-tautológicos (inspecção de corpo); T11 prova `chat_messages.count()===0` (R1, não R2). |
| 14 | `react-component-test-criteria.md` (≥3 estados) | N/A — backend puro, sem componente novo de UI (a 5.13 só regista tools; a UI de proposta vive na 5.12 `/knowledge`). |
| 15 | Sem version bump Dexie indevido (AC8) | PASS — `version(5)` intacta; `types/db.ts`/`schemas.ts`/`client.ts` não tocados; `vitest.config.ts` intocado (`lib/agent/tools/**` já na allowlist desde 5.10). |
| 16 | Falha nunca silenciosa | PASS — `pesquisar_web_e_criar_nota` inspecciona o corpo do fetch (C2) e faz rollback total atómico (C1); zero writes em falha real; erro propaga ao executor como `tool_error`. |
| 17 | `Not-tested:` correctamente ausente | PASS — nenhum path bloqueador tocado (`not-tested-trailer-rules.md`); sem trailer. |
| 18 | File List actualizada | PASS — 2 produção NOVOS (`journal.ts`, `knowledge.ts`) + 2 testes NOVOS + 3 modificados (`index.ts`, `projects.test.ts`, `finance.test.ts`); confirmado por `git diff`. |

### Decisões `[D-5.13-*]` ratificadas (não-reabríveis)

| Decisão | Veredicto ratificado |
|---------|----------------------|
| `[D-5.13-DOMAIN]` | **Opção A** — 2 ficheiros: `journal.ts` (`domain:'journal'`, 4 tools incl. `brain_dump`, porque o classifier agrupa brain dump dentro de `journal`) + `knowledge.ts` (`domain:'knowledge'`, 5 tools). Opção C (3.º domain) rejeitada (`validateClassifierOutput` faria fail-loud). |
| `[D-5.13-WEB-SEARCH-TOOL]` | **Opção W1** — `pesquisar_web_e_criar_nota` faz `ctx.fetch('/api/conhecimento/web-search')` (cookie same-origin via ADR-9 executor client-side) + persiste cascata área→caderno→nota numa única transacção `'rw'` atómica inline. W3 (endpoint Node novo) impossível (Node sem acesso ao IndexedDB do browser). W2 (sem persistir) rejeitada (perde FR56/FR57). |
| `[D-5.13-CHAT-RETRO]` | **Opção R1** — `brain_dump` devolve `mensagem` PT-PT no `resultSchema`; o executor injecta como `tool_result` e o Sonnet responde no loop. R2 (escrita directa em `chat_messages`) rejeitada (executor stateless; persistência de chat é do hook `useAgentStream` com o `agentRunId` certo — evita a "mensagem fantasma" da 5.8). Entrega `[D-5.8-CHAT-RETRO]`. |
| `[D-5.13-BRAIN-DUMP-SCOPE]` | **Opção B1** — `brain_dump` só regista o `BrainDump` com `status:'pending'` (canónico, `types/db.ts:266`), `parsedOutput` undefined; NÃO corre parser AI nem persiste entidades finais. O approval flow da 5.8 mantém-se a fonte única de verdade. B2 (processo completo inline) rejeitada (duplicaria 5.7+5.8). |

### Reconciliações de AC na adjudicação CR Iter 1 (Aria, 16/06/2026)

| AC | Mudança |
|----|---------|
| AC1 (`consultar_diario`) | Semântica ampliada para 4 casos: range fechado / desde `dataInicio` / até `dataFim` / últimas 7 (intervalo aberto unilateral via sentinelas ISO `0000-01-01`/`9999-12-31`). Sem mudança de assinatura do `argsSchema`. |
| AC3 (`criar_area`/`criar_caderno` reverse) | Reverse recusa com Error PT-PT se houver filhos (alinhado à cascata destrutiva exclusiva da UI de gestão da 5.9, `deleteKnowledgeArea`); só procede sobre entidade folha. |

Os restantes 4 findings CR Iter 1 (whitespace-only `.trim().min(1)`, validação por item da resposta web, isolamento de `chat_messages` no `beforeEach`, MD038) foram fixes de validação/teste/cosmético — sem mudança de contrato de AC.

### Débitos não-bloqueantes (registados — NÃO resolvidos nesta story)

Dois débitos menores cosméticos registados pela Aria no gate de saída, ambos **Baixa (advisory)**, não-bloqueantes e fora do scope da 5.13:

| ID | Descrição | Severidade |
|----|-----------|------------|
| OBS-5.13-TZ | `toISOString()` UTC no default de data de `criar_entrada_diario` — alinhar com timezone local se o utilizador mudar de fuso. Cosmético para PT (UTC+0/+1). | Baixa (advisory) |
| OBS-5.13-SCAN | Padrão `toArray()`+helper puro nas tools de pesquisa (`pesquisar_diario`/`pesquisar_conhecimento`) — by-design para single-user; reavaliar com índice/paginação só se o Nexus escalar a multi-utilizador/datasets grandes (cross-cutting, todas as tools de pesquisa). | Baixa (advisory) |

Herdados (não-dívida da 5.13, registados pela 5.12, mantêm-se para housekeeping pós-Epic 5): REC-SSRF-2, FLAG env Vercel, CONCERN 5.11 #1-3, OBS-5.10-A2 — a 5.13 reutiliza o endpoint 5.11 same-origin sem o tocar nem o agravar.

### Estado do epic

Epic 5: **12/13 → 13/13 DONE — COMPLETO.** Sub-módulo Conhecimento 4/5 → **5/5** (5.9 + 5.10 + 5.11 + 5.12 + 5.13). Os três sub-módulos fechados: Diário (5.3-5.5), Brain Dump (5.6-5.8), Conhecimento (5.9-5.13), mais a fundação (5.1 schema + 5.2 editor). **Waiver rate Epic 5: 0/13 (0%)** — iguala o melhor padrão de sempre (Epic 2 e Epic 4).

### Próximo passo

`@po *retrospective epic-5` (retrospectiva do Epic 5, à imagem das dos Epics 1-4). O Epic 5 não bloqueia nenhum epic seguinte (PRD §9 coluna "Bloqueia" = vazio); o sucessor natural é o Epic 6 (decisão do Eurico / `@pm`).

---

## Handoff

next_agent: "@po"
next_command: "*validate-story-draft 5.13"
condition: Status = Draft, AC não-vazios, tasks em checkboxes, decisões `[D-5.13-*]` documentadas
alternatives:
  - agent: "@architect", command: "Architect Gate de Entrada — ratificar [D-5.13-*]", condition: após PO GO
  - agent: "@dev", command: "*develop 5.13", condition: após Architect Gate de Entrada PASS
