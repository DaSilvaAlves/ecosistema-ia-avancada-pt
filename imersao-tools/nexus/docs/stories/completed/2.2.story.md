# Story 2.2 — Migration localStorage v1 → IndexedDB v2 (refactor via repos)

**Story ID:** 2.2
**Epic:** 2 — Tarefas v2 + Projectos
**Status:** Done (CLOSED 15/05/2026 por `@po` Pax — aguarda `@devops *push`)
**Branch sugerida:** `feature/2.2-migration-v1-v2`
**Bloqueia:** 2.3, 2.4, 2.5 (vistas dependem de dados migrados correctamente)
**Bloqueada por:** Story 2.1 (Done — repos `createTask` disponível; aguarda push/merge em `main`)
**Estimativa:** 2-3h
**Tipo:** Migration (refactor) — sem novo schema, sem nova UI

---

## Executor Assignment

```
executor: "@dev"
quality_gate: "@data-engineer"
quality_gate_tools: [lint, typecheck, vitest, coverage]
```

> Trace: `EPIC-2.md` §5 (tabela de stories — Story 2.2, executor `@dev`, quality gate `@data-engineer`). Respeita `separation-of-roles.md` (A6) — `executor != quality_gate`. `@dev` (Dex) refactora o código de migration; `@data-engineer` (Dara) assina o quality gate (domínio de dados, conhece os repos que a migration vai usar).

---

## Nota do `@sm` River sobre interpretação

A Story 2.2 no `EPIC-2.md` §5 é descrita como "Migrar dados de tarefas do `localStorage` v1 (`nexus_tasks`) para o schema Dexie v2". O skeleton dessa migration **já existe** desde a Story 0.3 em `lib/db/migrations/v1-to-v2.ts`.

O que a 2.2 entrega é o **refactor** desse skeleton para que passe a delegar nos repos tipados entregues pela Story 2.1, em vez de escrever directamente em `db.tasks.bulkAdd()`. Isto garante:

1. **Validação Zod por tarefa** — cada `V1Task` mapeada é validada por `TaskSchema` antes de persistir. Tarefas inválidas são contadas e reportadas (sem bloquear a migration das válidas).
2. **Mensagens de erro PT-PT consistentes** — via `repos/tasks.ts` (AC6 da 2.1).
3. **Single source of truth** — o repo `tasks.ts` é o único caminho de escrita para a tabela `tasks`; a migration não é excepção.

O restante da migration (flag de idempotência, leitura de `localStorage`, mapeamento `V1Task` → `Task`, rollback strategy) **permanece intacto** — já estava correcto.

---

## User Story

Como `@dev` (Dex), quero refactorar `lib/db/migrations/v1-to-v2.ts` para que a escrita de tarefas migradas seja feita via `createTask()` de `lib/db/repos/tasks.ts` (em vez de `db.tasks.bulkAdd()` directo), para que a migration respeite a validação Zod e as mensagens PT-PT estabelecidas pela Story 2.1, mantendo a idempotência e o rollback strategy já existentes.

---

## Contexto

`architecture-v2.md` §4.4 especifica o contrato da migration: idempotente via flag `nexus_v1_migrated_to_v2`, lê `localStorage.getItem('nexus_tasks')`, mapeia `V1Task` → `Task`, e **mantém `localStorage` v1 intacto** para rollback de emergência (Epic 8 Story 8.10 limpa). O pseudocódigo da §4.4 usa `db.tasks.bulkAdd(tasksV2)` — mas esse pseudocódigo foi escrito **antes** da Story 2.1 criar `repos/tasks.ts`. A arquitectura não proíbe o uso de repos na migration — é a direcção correcta para single source of truth.

`EPIC-2.md` §7 reconciliação: "Story 2.2 lê o `localStorage` v1 e escreve no Dexie v2." A forma de escrever é via repos.

`EPIC-2.md` §8 (lição A2 — Not-Tested Evidence Gate): a migration é candidata a commits que tocam código de aplicação sem paths bloqueadores — mas se algum commit tocar `package.json` scripts ou config, activar a secção.

**Código actual do skeleton (Story 0.3):**
- `lib/db/migrations/v1-to-v2.ts` — tem `db.tasks.bulkAdd(tasksV2)` numa transacção. Usa `db` directamente.
- `lib/db/migrations/index.ts` — reexporta `migrateV1ToV2` e `MIGRATION_FLAG_KEY`.
- A interface `V1Task` e o mapeamento para `Task` estão correctos.
- `MigrationResult` já tem os campos `migrated`, `status`, `error`.

**O que muda com o refactor:**
- `db.tasks.bulkAdd(tasksV2)` → loop com `createTask(task)` por tarefa, com tratamento de erro individual.
- A transacção Dexie é removida (ou convertida em try/catch por tarefa) — `createTask` não é transaccional na 2.1, mas isso é aceitável dado que a migration tem flag de idempotência. [AUTO-DECISION: sem transacção wrapping o loop — razão: `createTask` valida via Zod individualmente; falhas parciais são reportadas em `MigrationResult`; a migration pode re-tentar tarefas falhadas numa futura re-execução se o flag não for marcado. Alternativa transaccional foi descartada porque o `bulkAdd` dentro de transacção seria contornar a validação Zod dos repos.]
- `MigrationResult` é estendido com `skipped: number` — conta tarefas inválidas que falharam a validação Zod (sem bloquear as válidas).

---

## Reconciliação PRD ↔ Arquitectura

> `EPIC-2.md` §7 instrui: a arquitectura prevalece sobre a descrição do PRD; nenhum ADR é reaberto; divergências face à arquitectura escalam a `@architect` antes de implementar.

| # | `PRD §10` / `EPIC-2.md §5` diziam | `architecture-v2.md` / Story 2.1 decidiram | Resolução para Story 2.2 |
|---|-----------------------------------|---------------------------------------------|--------------------------|
| R1 | "Migrar dados de tarefas do `localStorage` v1" — implica escrita directa no Dexie | Story 2.1 entregou `repos/tasks.ts` com `createTask()` como único ponto de escrita canónico | Story 2.2 **usa `createTask()`** em vez de `db.tasks.bulkAdd()`. Arquitectura prevalece. |
| R2 | PRD §4.4 pseudocódigo usa `db.tasks.bulkAdd(tasksV2)` | Pseudocódigo é anterior à Story 2.1; `repos/tasks.ts` é o contrato de escrita actual | O pseudocódigo §4.4 é orientação, não obrigação. Refactor para repos é a implementação correcta conforme `EPIC-2.md` §2 (single source of truth). |
| R3 | `MigrationResult` já existe com `migrated`, `status`, `error` | Story 2.1 não tocou em `MigrationResult` | Story 2.2 adiciona campo `skipped: number` ao interface existente para reportar tarefas inválidas. Extensão, não reescrita. |

---

## Acceptance Criteria

- **AC1:** `lib/db/migrations/v1-to-v2.ts` refactora o bloco de escrita: o `db.tasks.bulkAdd(tasksV2)` é substituído por um loop que chama `createTask(task)` de `@/lib/db/repos/tasks` para cada tarefa mapeada. A função `migrateV1ToV2` mantém a assinatura `() => Promise<MigrationResult>`.
- **AC2:** A interface `MigrationResult` (em `v1-to-v2.ts`) é estendida com `skipped: number` — conta tarefas que falharam a validação Zod mas **não bloqueia** a migration das restantes. Tarefas inválidas são ignoradas (logged via `console.warn` com mensagem PT-PT e o id da tarefa).
- **AC3:** O campo `migrated` em `MigrationResult` reflecte apenas as tarefas **persistidas com sucesso** via `createTask()`. `skipped` reflecte as tarefas que falharam Zod. `migrated + skipped === v1Tasks.length` quando `status !== 'failed'` global.
- **AC4:** A idempotência é preservada: flag `nexus_v1_migrated_to_v2` é marcado `'true'` em `localStorage` **apenas depois** do loop completo (com ou sem tarefas skipped), nunca antes. Comportamento de `already-done` e `no-data` permanecem inalterados.
- **AC5:** O `localStorage` v1 (`nexus_tasks`) é mantido intacto após a migration — não é apagado nem modificado. Trace: `architecture-v2.md §4.4` ("localStorage v1 mantém-se para rollback emergência"). Anti-padrão explícito: não limpar o `localStorage` nesta story.
- **AC6:** A escrita de dados v1 corromptos/inválidos (task sem `id`, sem `text`, `priority` fora do enum) NÃO bloqueia a migration das tarefas válidas. A tarefa inválida é contada em `skipped`, um `console.warn` com mensagem PT-PT é emitido (ex: `Tarefa ignorada na migration (id: "${t.id}"): ${zodError.message}`), e o loop continua.
- **AC7:** `lib/db/migrations/index.ts` reexporta o tipo `MigrationResult` actualizado (com o campo `skipped`). Nenhuma outra alteração a `index.ts`.
- **AC8:** O mapeamento `V1Task` → `Task` (interface `V1Task`, transformação de campos) permanece **inalterado** face ao skeleton da Story 0.3. Não adicionar campos novos ao mapeamento nem alterar lógica existente.
- **AC9:** Existe `tests/unit/db/migrations/v1-to-v2.test.ts` com cobertura de:
  - Migration com N tarefas v1 válidas → todas persistidas via `createTask`, `migrated === N`, `skipped === 0`, flag marcado.
  - Migration idempotente: segundo `migrateV1ToV2()` devolve `{ status: 'already-done', migrated: 0, skipped: 0 }`.
  - Migration sem dados: `localStorage` sem `nexus_tasks` → `{ status: 'no-data', migrated: 0, skipped: 0 }`, flag marcado.
  - Migration com 1 tarefa inválida (sem `text`) e 2 válidas → `migrated === 2`, `skipped === 1`, flag marcado, `console.warn` chamado 1 vez com mensagem PT-PT.
  - SSR guard: chamar `migrateV1ToV2()` com `window === undefined` simulado → `{ status: 'failed', error: 'No window (SSR)' }`.
  - Verificar que `localStorage` v1 (`nexus_tasks`) não foi apagado após migration bem-sucedida (AC5).
- **AC10:** Quality gates locais passam em `imersao-tools/nexus/v2/`: `npm run lint`, `npm run typecheck`, `npm run test:unit` (todos os existentes + novos), `npm run build`.
- **AC11:** Coverage de `lib/db/migrations/v1-to-v2.ts` é **>= 80% lines** medido via `npm run test:coverage`. Não alterar o threshold global em `vitest.config.ts`.

---

## CodeRabbit Integration

| Item | Configuração |
|------|--------------|
| Story Type | **Migration** (refactor de escrita) + secundário **Database** (repos Dexie) |
| Complexity | Low-Medium — 1 ficheiro de produção refactorado + 1 ficheiro de teste novo. Surface area pequena (~2-3 ficheiros). |
| Specialized Agents | `@dev` (executor — refactor), `@data-engineer` (quality gate — valida que o uso de repos está correcto), `@qa` (consulta se a cobertura do AC9 for questionável) |
| Quality Gate Tasks | Pre-Commit (`@dev`): `coderabbit --prompt-only -t uncommitted` antes de Ready for Review · Pre-PR (`@devops`): `coderabbit --prompt-only --base main` antes do PR |
| Self-Healing Configuration | mode: `light`, max_iterations: **2** (hard-stop — `EPIC-2.md` §8), severity_filter: `[CRITICAL, HIGH]`, behavior: `CRITICAL: auto_fix`, `HIGH: auto_fix (iter<2) else document_as_debt` |
| Focus Areas | **Migration correctness**: idempotência preservada, flag marcado só após loop completo, `localStorage` v1 intacto. **Repo usage**: `createTask()` chamado correctamente, não `db.tasks.*` directo. **Error handling**: tarefas inválidas contadas em `skipped`, não silenciadas. **Type safety**: `MigrationResult.skipped` adicionado ao tipo, zero `any`. |
| Risco | Baixo — refactor cirúrgico de 1 ficheiro. Mitigação: AC9 (6 cenários de teste) + AC10 (gates locais) |
| **Local CLI skip** | CodeRabbit corre via **integração GitHub no PR** (server-side, automático). Convenção Nexus v2 — ver `PO-VALIDATION-STORY-2.1.md §7`. |

---

## Tasks / Subtasks

- [x] T1. Verificação de estado inicial (AC1, AC8)
  - [x] T1.1 Ler `lib/db/migrations/v1-to-v2.ts` na íntegra — confirmar que o mapeamento `V1Task → Task` e o SSR guard estão correctos
  - [x] T1.2 Ler `lib/db/repos/tasks.ts` — confirmar assinatura de `createTask(input: Task): Promise<Task>`
  - [x] T1.3 Confirmar que `lib/db/schemas.ts` tem `TaskSchema` importável (AC1 vai usá-lo via `createTask`)
- [x] T2. Estender `MigrationResult` (AC2, AC3, AC7)
  - [x] T2.1 Adicionar `skipped: number` ao interface `MigrationResult` em `v1-to-v2.ts`
  - [x] T2.2 Atualizar `lib/db/migrations/index.ts` — reexportar `MigrationResult` actualizado (sem outra alteração — `type` reexport forwarda automaticamente o campo novo)
- [x] T3. Refactorar bloco de escrita (AC1, AC4, AC5, AC6)
  - [x] T3.1 Importar `createTask` de `@/lib/db/repos/tasks`
  - [x] T3.2 Substituir `db.transaction('rw', db.tasks, async () => { await db.tasks.bulkAdd(tasksV2) })` por loop `for...of` com `createTask(task)` por tarefa
  - [x] T3.3 Envolver cada `createTask(task)` em `try/catch`: capturar `ZodError` e erros genéricos, incrementar `skipped`, emitir `console.warn` com mensagem PT-PT (`Tarefa ignorada na migration (id: "${task.id}"): ${error.message}`)
  - [x] T3.4 Remover import de `db` de `@/lib/db/client` — confirmado que `db` deixou de ser usado após o refactor; import eliminado
  - [x] T3.5 Confirmar que o flag `localStorage.setItem(MIGRATION_FLAG_KEY, 'true')` ainda é marcado **depois** do loop (não antes) — L114
  - [x] T3.6 Confirmar que `localStorage.getItem('nexus_tasks')` não é apagado ou modificado — nunca é chamado `removeItem`
  - [x] T3.7 Actualizar o `return` de `success` para incluir `skipped` no resultado
- [x] T4. Testes Vitest (AC9, AC11)
  - [x] T4.1 Criar `tests/unit/db/migrations/v1-to-v2.test.ts`
  - [x] T4.2 Implementar 6 cenários: migration completa, idempotente, sem dados, com tarefa inválida, SSR guard, `localStorage` v1 intacto
  - [x] T4.3 Usar `fake-indexeddb` (já em `tests/setup.ts`) e mock de `localStorage` via `vi.stubGlobal` ou similar
  - [x] T4.4 Verificar que `createTask` é chamado o número correcto de vezes (spy via `vi.spyOn`)
- [x] T5. Quality gates locais (AC10, AC11)
  - [x] T5.1 `npm run lint` — PASS (1 warning pré-existente fora do scope: `NextResponse` unused em `app/api/auth/logout/route.ts`)
  - [x] T5.2 `npm run typecheck` — exit 0
  - [x] T5.3 `npm run test:unit` — 398 PASS (392 pré-existentes + 6 novos da 2.2)
  - [x] T5.4 `npm run build` — PASS
  - [x] T5.5 `npm run test:coverage` — `lib/db/migrations/v1-to-v2.ts` = 96.22% lines (alvo: ≥80%)
- [x] T6. Story file maintenance — File List + Change Log + Dev Agent Record
- [ ] T7. Delegar push a `@devops *push` — fora do âmbito do executor

---

## Dev Notes

### Ficheiros relevantes (ler antes de implementar)

| Ficheiro | Porquê |
|----------|--------|
| `imersao-tools/nexus/v2/lib/db/migrations/v1-to-v2.ts` | Ficheiro principal a refactorar |
| `imersao-tools/nexus/v2/lib/db/migrations/index.ts` | Reexportação — actualizar tipo `MigrationResult` |
| `imersao-tools/nexus/v2/lib/db/repos/tasks.ts` | `createTask()` — assinatura e validação Zod |
| `imersao-tools/nexus/v2/lib/db/schemas.ts` | `TaskSchema` — entender o que `createTask` vai validar |
| `imersao-tools/nexus/v2/tests/unit/db/repos/tasks.test.ts` | Padrão de teste com `fake-indexeddb` para replicar |

### Contrato de `createTask` (Story 2.1 AC6)

```typescript
// lib/db/repos/tasks.ts
export async function createTask(input: Task): Promise<Task> {
  TaskSchema.parse(input); // lança ZodError com mensagem PT-PT se inválido
  await db.tasks.add(input);
  return input;
}
```

O `TaskSchema.parse(input)` lança `ZodError` se o input for inválido. A migration deve capturar este erro e continuar com a próxima tarefa (AC6).

### Mapeamento V1Task → Task (já correcto no skeleton)

```typescript
// NÃO alterar este mapeamento (AC8)
const tasksV2: Task[] = v1Tasks.map((t) => ({
  id: t.id,
  title: t.text,          // V1Task.text → Task.title
  description: '',
  priority: t.priority,
  status: t.status ?? (t.done ? 'done' : 'todo'),
  dueDate: t.dueDate ?? null,
  projectId: null,
  tags: [],
  context: t.context ?? null,
  lastWorkedAt: t.lastWorkedAt ?? null,
  recurrenceId: null,
  parentTaskId: null,
  createdAt: t.createdAt,
  updatedAt: Date.now(),
}));
```

**Atenção:** `V1Task.priority` tem os mesmos valores que `Task.priority` (`'high'|'medium'|'low'`) — sem remapeamento necessário. `V1Task.id` pode não ser UUID — `TaskSchema` valida `z.string().uuid()`. Tarefas v1 com `id` não-UUID serão contadas em `skipped` (AC6).

### Padrão do loop de escrita (AC1, AC3, AC6)

```typescript
// Substituir o bloco try/bulkAdd por isto:
let migrated = 0;
let skipped = 0;
for (const task of tasksV2) {
  try {
    await createTask(task);
    migrated++;
  } catch (error) {
    skipped++;
    console.warn(
      `Tarefa ignorada na migration (id: "${task.id}"): ${
        error instanceof Error ? error.message : 'erro desconhecido'
      }`
    );
  }
}
```

### Import de `db` pode ser removido

Se o refactor remover o `db.tasks.bulkAdd` e não usar mais `db` directamente, o import `import { db } from '@/lib/db/client'` pode ser eliminado de `v1-to-v2.ts`. Verificar antes de remover (T3.4).

### Idempotência — ordem do flag (AC4)

```typescript
// CORRECTO: flag SÓ depois do loop
for (const task of tasksV2) { /* ... */ }
window.localStorage.setItem(MIGRATION_FLAG_KEY, 'true');
return { migrated, skipped, status: 'success' };

// ERRADO: flag antes do loop — nunca fazer
window.localStorage.setItem(MIGRATION_FLAG_KEY, 'true');
for (const task of tasksV2) { /* ... */ }
```

### LocalStorage v1 — NÃO apagar (AC5)

Nunca chamar `localStorage.removeItem('nexus_tasks')`. Trace: `architecture-v2.md §4.4`. O cleanup é responsabilidade da Story 8.10.

### Testing — mock de localStorage no Vitest

No ambiente `jsdom` (já configurado em `tests/setup.ts`), `localStorage` está disponível mas pode precisar de ser limpo entre testes. Padrão:

```typescript
beforeEach(() => {
  localStorage.clear();
  // Re-inicializar fake-indexeddb entre testes — ver tasks.test.ts como referência
});
```

Para o cenário SSR guard (AC9, cenário 5), usar `vi.stubGlobal('window', undefined)` e restaurar com `vi.unstubAllGlobals()` no `afterEach`.

Para verificar que `createTask` foi chamado N vezes (T4.4), importar e usar `vi.spyOn`:

```typescript
import * as tasksRepo from '@/lib/db/repos/tasks';
const createTaskSpy = vi.spyOn(tasksRepo, 'createTask');
```

### Padrão de test herdado

Ver `tests/unit/db/repos/tasks.test.ts` (Story 2.1) para o padrão de `beforeEach` + `fake-indexeddb`. A estrutura do ficheiro de migration test deve seguir o mesmo padrão.

### Não-inventar

- NÃO adicionar migration de notas v1 (`nexus_notes`) — não está no scope desta story (ver architecture-v2.md §4.4 que menciona notas mas apenas na vision; a tabela `knowledge_notes` existe mas não faz parte do scope da 2.2)
- NÃO tocar em `vitest.config.ts` (AC11 proíbe)
- NÃO criar UI para mostrar progresso da migration — é a Story 0.10/app layout (já existe)

---

### Testing

- **Framework:** Vitest 1.x + `fake-indexeddb` + jsdom (já configurados em `tests/setup.ts`, Story 0.9)
- **Localização:** `tests/unit/db/migrations/v1-to-v2.test.ts` (novo directório `migrations/` dentro de `unit/db/`)
- **Padrão:** seguir `tests/unit/db/repos/tasks.test.ts` (Story 2.1) — `beforeEach` limpa localStorage + fake DB, asserções directas sobre o resultado e sobre `createTask` spy
- **Coverage alvo:** >= 80% lines em `lib/db/migrations/v1-to-v2.ts` (AC11)
- **6 cenários obrigatórios** (AC9): ver lista em AC9 acima
- **SSR guard**: `vi.stubGlobal('window', undefined)` com `vi.unstubAllGlobals()` no afterEach

---

## Open Questions for PO

Nenhuma. Todas as decisões de implementação são deriváveis directamente de:
- `architecture-v2.md §4.4` (contrato da migration)
- `lib/db/migrations/v1-to-v2.ts` (código actual)
- `lib/db/repos/tasks.ts` (API de escrita da Story 2.1)
- `EPIC-2.md §5` e §7 (scope e reconciliação)

---

## Risk Assessment

| # | Risco | Probabilidade | Severidade | Mitigação |
|---|-------|---------------|------------|-----------|
| AR1 | `V1Task.id` não-UUID — tarefas v1 antigas sem UUID válido falham `TaskSchema` e são contadas em `skipped` | Média (Nexus v1 pode ter usado ids não-UUID) | Média | AC6 + AC9 cobrem este cenário. `skipped` é reportado; o utilizador não perde os dados originais (localStorage v1 intacto, AC5). |
| AR2 | Remoção inadvertida do import `db` quando ainda é necessário | Baixa | Alta | T3.4 exige verificação explícita antes de remover o import. |
| AR3 | Flag marcado antes do loop (regressão de idempotência) | Muito baixa | Alta | AC4 + AC9 cenário idempotente cobrem. Dev Notes documenta ordem explicitamente. |

---

## Not-Tested Evidence Gate

**N/A.**

Esta story toca `lib/db/migrations/v1-to-v2.ts`, `lib/db/migrations/index.ts`, `tests/unit/db/migrations/` — nenhum caminho bloqueador (CI config, test-runner config, build config, segurança). Logo, nenhum commit desta story deve usar o trailer `Not-tested:` num path bloqueador.

> **Activação:** se durante a implementação algum commit precisar de tocar `vitest.config.ts`, `tsconfig*.json`, `package.json` (campo `scripts`) ou `.github/workflows/**`, esta secção passa a **obrigatória** com evidência local prévia. Ver `.claude/rules/not-tested-trailer-rules.md`.

---

## Anti-padrões (NÃO fazer)

- NÃO usar `db.tasks.bulkAdd()` ou `db.tasks.add()` directamente — usar `createTask()` de `repos/tasks.ts`
- NÃO marcar o flag de idempotência antes do loop de escrita
- NÃO apagar ou modificar `localStorage.getItem('nexus_tasks')` — manter intacto para rollback
- NÃO bloquear a migration quando uma tarefa é inválida — contar em `skipped` e continuar
- NÃO alterar o mapeamento `V1Task → Task` (AC8)
- NÃO adicionar migration de notas v1 — fora de scope
- NÃO criar UI de progresso da migration — fora de scope
- NÃO tocar em `vitest.config.ts`
- NÃO usar `any` — `unknown` com type guards quando necessário
- NÃO implementar o motor de recorrência — é a Story 2.7
- NÃO criar vistas (lista, Kanban, calendário) — são as Stories 2.3-2.5

---

## Referências

- `imersao-tools/nexus/docs/EPIC-2.md` §5 (tabela de stories — Story 2.2), §7 (reconciliação PRD↔arquitectura), §8 (lições retrospectiva Epic 1)
- `imersao-tools/nexus/docs/PRD-NEXUS-V2.md` §10 Epic 2 Story 2.2
- `imersao-tools/nexus/docs/architecture-v2.md` §4.4 (migration localStorage v1 → IndexedDB v2, pseudocódigo e contrato), §16 ("Story 2.2 migration localStorage→IndexedDB corre uma vez (idempotente, §4.4)")
- `imersao-tools/nexus/v2/lib/db/migrations/v1-to-v2.ts` — skeleton a refactorar (Story 0.3)
- `imersao-tools/nexus/v2/lib/db/repos/tasks.ts` — `createTask()` da Story 2.1
- `imersao-tools/nexus/v2/lib/db/schemas.ts` — `TaskSchema` da Story 2.1
- `imersao-tools/nexus/docs/stories/completed/2.1.story.md` — padrão de repo + Zod + testing
- `.claude/rules/not-tested-trailer-rules.md` — classificação de paths bloqueadores
- `.claude/rules/separation-of-roles.md` — `executor != quality_gate` (A6)

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 15/05/2026 | 0.1 | Story criada via `*draft 2.2`. Interpretação "refactor via repos" (skeleton 0.3 existe, escrita passa para `createTask()`), 3 reconciliações PRD↔arquitectura, 11 ACs, 7 tasks. Zero perguntas abertas para Pax — tudo resolvível pelo código e arquitectura existentes. Status: Draft | `@sm` River |
| 15/05/2026 | 0.2 | PO validation completa — 10-point checklist score 10/10. Veredicto: **APPROVED**. Decisão autónoma de River sobre não-transacção e exclusão de migration de notas v1 ratificadas. Pontos de atenção do handoff (pair executor/gate, extensão `MigrationResult.skipped`, ausência de transacção wrapping) confirmados como decisões correctas com fundamentação em código + arquitectura + EPIC-2. Status: Draft → Approved (implementação aguarda merge PR #18). | `@po` Pax |
| 15/05/2026 | 0.3 | Implementação completa por Dex. Refactor cirúrgico de `v1-to-v2.ts`: `db.tasks.bulkAdd()` substituído por loop `for...of` com `createTask()` por tarefa, try/catch individual com `console.warn` PT-PT, `skipped` counter incrementado em falhas Zod. Mapeamento V1Task→Task inalterado (AC8). `MigrationResult` estendido com `skipped: number`. Import de `db` removido (deixou de ser necessário). Test file novo com 6 cenários (398 testes totais PASS). Test pre-existente em `client.test.ts` actualizado para usar UUID válido (alinhamento com validação Zod do `createTask`). Quality gates locais: lint PASS (1 warning pré-existente fora-scope), typecheck 0, test:unit 398 PASS, build PASS, coverage `v1-to-v2.ts` = 96.22% lines. Status: Approved → Ready for Review. | `@dev` Dex |
| 15/05/2026 | 0.4 | Quality gate Dara — **PASS**. 11/11 ACs verificados directamente em código (commit `dd6dc0d8`). 6 pontos focais do handoff + 6 pontos adicionais (separação de papéis, trailers, file list, regressão, etc.) verificados explicitamente. Gates locais reproduzidos por Dara: typecheck 0, lint OK (1 warning pré-existente fora-scope), test:unit 398/398 PASS (33 files). Linhas 72-74 não-cobertas confirmadas via `git blame` como débito pré-existente Story 0.3 (commit `c362b171`) — aceito (coverage 96.22% > 80% alvo). Recomendação não-bloqueadora: registar teste de JSON malformado em backlog Epic 2. Trailers do commit conformes; `Not-tested:` correctamente omitido. Status: Ready for Review → Done. 0/2 qa-loop-fix consumidas. | `@data-engineer` Dara |
| 15/05/2026 | 0.5 | PO closure executada — DoD 15/15 PASS com evidência directa. Status `Done` confirmado, secção `## PO Closure` preenchida com checklist, ratificação das 4 [AUTO-DECISION] honradas, lições registadas para retrospectiva Epic 2. Story movida `stories/` → `stories/completed/` via `git mv` (padrão Story 2.1). `EPIC-2.md` actualizado (counter 2/10 Done, 2.3 marcada `Ready for draft`, §10 próximo passo). Handoff de entrada qa-PASS arquivado. Handoff de saída criado para `@devops *push` da branch `feature/2.2-migration-refactor`. Status final: Done (CLOSED). | `@po` Pax |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — `claude-opus-4-7[1m]` (Dex via AIOX dev agent persona).

### Debug Log References

Nenhuma. Implementação YOLO single-pass sem necessidade de debug loop. Gates locais passaram à primeira:
- typecheck exit 0
- lint PASS (1 warning pré-existente NextResponse unused fora-scope)
- test:unit 398/398 PASS (392 pré + 6 novos)
- test:coverage `lib/db/migrations/v1-to-v2.ts` = 96.22% lines
- build PASS (12 rotas, middleware 34.5 kB)

### Completion Notes List

- **T1** — Verificação inicial: confirmado mapeamento V1Task→Task no skeleton (AC8 honrado), `createTask(input: Task): Promise<Task>` em repos/tasks.ts (Story 2.1 AC6), `TaskSchema` em schemas.ts com validação UUID + PT-PT.
- **T2** — `MigrationResult` estendido com `skipped: number` em `v1-to-v2.ts`. `index.ts` não tocou — `export type { MigrationResult }` forwarda automaticamente o campo novo.
- **T3** — Refactor cirúrgico em `v1-to-v2.ts`: substituí o bloco `db.transaction('rw', db.tasks, async () => { await db.tasks.bulkAdd(tasksV2) })` por loop `for (const task of tasksV2)` com `try { await createTask(task); migrated++ } catch { skipped++; console.warn(...) }`. Import de `db` eliminado (deixou de ser necessário). Flag `localStorage.setItem(MIGRATION_FLAG_KEY, 'true')` mantém-se **depois** do loop (AC4). Mapeamento V1Task→Task **inalterado** (AC8).
- **T4** — Test file novo `tests/unit/db/migrations/v1-to-v2.test.ts` com 6 cenários (AC9):
  1. Happy path: 3 v1 válidas → 3 createTask spy calls, migrated=3, skipped=0, flag marcado, 3 tasks persistidas
  2. Idempotência: flag pré-marcado → spy não chamado, already-done
  3. No-data: localStorage sem nexus_tasks → no-data, flag marcado
  4. Invalid task: id `'not-a-uuid'` → skipped=1, migrated=2, console.warn 1x com mensagem PT-PT contendo o id
  5. SSR guard: `vi.stubGlobal('window', undefined)` → failed com error `'No window (SSR)'`
  6. localStorage v1 intacto: comparação byte-a-byte (`JSON.stringify` original === `localStorage.getItem` after) após success
- **T5** — Quality gates locais todos PASS. Test pre-existente em `tests/unit/db/client.test.ts` (Story 0.3) actualizado: hard-coded `id: 't1'` substituído por `crypto.randomUUID()` para representar canonicamente o happy path sob o novo regime de validação Zod (alinhamento com AC1+AC3, sem regressão de comportamento — só sintonização da expectativa do teste face à validação introduzida pela Story 2.1).
- **T6** — Story file actualizada (este Dev Agent Record + tasks marcadas + status + change log v0.3).

### File List

**Ficheiros modificados (3):**
- `imersao-tools/nexus/v2/lib/db/migrations/v1-to-v2.ts` — refactor do bloco de escrita (bulkAdd → loop createTask) + extensão de `MigrationResult` com `skipped: number` + remoção do import `db` (já não usado)
- `imersao-tools/nexus/v2/tests/unit/db/client.test.ts` — actualizar test pré-existente (Story 0.3) para usar `crypto.randomUUID()` em vez de `'t1'` hard-coded + adicionar asserção `skipped === 0` no happy path
- `imersao-tools/nexus/docs/stories/2.2.story.md` — Dev Agent Record, tasks marcadas, status `Approved → Ready for Review`, change log v0.3

**Ficheiros novos (1):**
- `imersao-tools/nexus/v2/tests/unit/db/migrations/v1-to-v2.test.ts` — 6 cenários AC9 + helper `makeV1Task` (159 linhas)

**Ficheiros NÃO modificados (intencionalmente):**
- `imersao-tools/nexus/v2/lib/db/migrations/index.ts` — `export type { MigrationResult }` forwarda automaticamente o campo `skipped`. Sem alteração necessária (AC7 cumprido por tipo-passthrough).

---

## QA Results

**Data:** 15/05/2026
**Reviewer:** Dara (`@data-engineer`) — domain quality gate (inversão A6 vs Story 2.1)
**Veredicto:** **PASS**
**qa-loop-fix iterações consumidas:** 0/2

> Trace separação de papéis: `separation-of-roles.md` (A6) — Dex (`@dev`) executou; Dara (`@data-engineer`) avalia. Dara não tocou em nenhum ficheiro de produção da 2.2 (commit `dd6dc0d8` é integralmente de Dex). Sem conflito. Story 2.1 (que Dara executou) já está merged em `main` via squash `86ddb6a6` — sem sobreposição com 2.2.

### Reprodutibilidade dos gates locais (Dex → Dara)

| Gate | Esperado (Dex) | Real (Dara) | Match |
|------|----------------|-------------|-------|
| `npm run typecheck` | exit 0 | exit 0 | PASS |
| `npm run lint` | PASS (1 warn pré-existente fora-scope) | PASS (1 warn `NextResponse` unused em `app/api/auth/logout/route.ts`) | PASS |
| `npm run test:unit` | 398/398 | 33 files, **398/398 tests PASS** | PASS |

Outros gates (build, coverage) — aceito evidência de Dex (handoff §Resumo executivo) dado que typecheck+lint+test:unit batem byte-a-byte.

### Verificação dos 11 ACs

| AC | Resultado | Evidência directa |
|----|-----------|-------------------|
| **AC1** | PASS | `v1-to-v2.ts:1` importa `createTask` de `@/lib/db/repos/tasks`. Linhas 98-112: loop `for (const task of tasksV2)` com `await createTask(task)`. Assinatura `migrateV1ToV2(): Promise<MigrationResult>` preservada (linha 60). Zero referências a `db.tasks.bulkAdd` ou `db.tasks.add` no ficheiro (`grep` clean). |
| **AC2** | PASS | `MigrationResult` (linhas 42-47) tem `skipped: number` declarado. Catch (linhas 104-111) incrementa `skipped++` e emite `console.warn` em PT-PT com id da tarefa e mensagem do erro — loop não interrompe. |
| **AC3** | PASS | `migrated` incrementa apenas após `await createTask(task)` resolver sem lançar (linhas 101-103). `skipped` incrementa apenas no catch. Soma `migrated + skipped === tasksV2.length` quando status=success — verificado em test cenário 4 (`expect(migrated).toBe(2) + expect(skipped).toBe(1)` com 3 v1 inputs). |
| **AC4** | PASS | `localStorage.setItem(MIGRATION_FLAG_KEY, 'true')` (linha 114) está **após** o `for...of` completo, **antes** do `return`. Caminhos `already-done` (65-67) e `no-data` (76-79) inalterados. Caminho `failed` (62) intencionalmente não marca flag (SSR guard). Test cenário 2 prova idempotência. |
| **AC5** | PASS | Zero ocorrências de `localStorage.removeItem` ou modificação de `nexus_tasks` no código (`grep -n removeItem v1-to-v2.ts` = 0 matches). Test cenário 6 valida byte-a-byte: `expect(localStorage.getItem(V1_TASKS_KEY)).toBe(serialized)` após sucesso. |
| **AC6** | PASS | Try/catch individual por tarefa (101-112) cobre `ZodError` **e** qualquer outro erro (`error instanceof Error`). Mensagem PT-PT correcta: `Tarefa ignorada na migration (id: "${task.id}"): ${error.message}`. Test cenário 4 confirma com `id: 'not-a-uuid'` que falha `TaskSchema.parse` (regex UUID em `schemas.ts:30`). |
| **AC7** | PASS | `migrations/index.ts:8` faz `export type { MigrationResult } from './v1-to-v2'` — TypeScript propaga automaticamente o novo campo `skipped`. Sem alteração necessária ao `index.ts`. Verificado: ficheiro identical a `main`. |
| **AC8** | PASS | `git diff main..HEAD -- v1-to-v2.ts`: mapeamento V1Task→Task (linhas 81-96) é **byte-a-byte idêntico** à Story 0.3. Substituição é exclusivamente do bloco transactional → loop. Zero campos adicionados/removidos/alterados. |
| **AC9** | PASS | `tests/unit/db/migrations/v1-to-v2.test.ts` (173 linhas) tem os 6 cenários enumerados (1: happy 3 válidas, 2: idempotência, 3: no-data, 4: invalid id `'not-a-uuid'`, 5: SSR guard, 6: localStorage intacto). Todos com asserções concretas e verificações em `db.tasks.toArray()` para confirmar persistência real. |
| **AC10** | PASS | Reproduzidos por Dara: typecheck 0, lint OK (warning fora-scope), test:unit 398/398. Build aceito por evidência de Dex (mesma sessão, sem alterações desde). |
| **AC11** | PASS | Coverage `v1-to-v2.ts` = 96.22% lines (Dex; aceito — typecheck+lint+test:unit batem). Acima do alvo ≥80%. `vitest.config.ts` intacto (proibido modificar). Linhas 72-74 não-cobertas analisadas no ponto adicional 9 abaixo. |

### Pontos focais 1-6 do handoff (verificação explícita)

| # | Item | Verificação |
|---|------|------------|
| 1 | Uso correcto de `createTask` (assinatura, await/throw propaga) | PASS — `createTask(input: Task): Promise<Task>` em `repos/tasks.ts:25-29`. Loop usa `await createTask(task)` e o catch apanha `ZodError` (TaskSchema.parse lança) e qualquer Error. Nenhuma chamada engole erros silenciosamente. |
| 2 | Mapeamento V1Task→Task preservado | PASS — `git diff main..HEAD` mostra **apenas** substituição do bloco `db.transaction(...)`→loop. Linhas 81-96 inalteradas vs skeleton 0.3. Zero campos novos. |
| 3 | `skipped` cobre todos os edge cases (não só ZodError) | PASS — catch é genérico `(error)` (linha 104), não tipado para ZodError. Trata `error instanceof Error` correctamente, fallback `'erro desconhecido'` para non-Error throws. Constraint violation Dexie, transaction rollback, ou qualquer outra falha de `createTask` cai no mesmo branch. |
| 4 | Test honest (mock-protocol-fidelity) | PASS — `fake-indexeddb` (`tests/setup.ts`) é o mock canónico de IndexedDB, fiel ao protocolo. `vi.spyOn(tasksRepo, 'createTask')` (linha 66 do test) é spy de contagem, NÃO substitui implementação — `createTask` real continua a correr e a persistir em Dexie (`db.tasks.toArray()` linhas 79-82 confirmam persistência real). Test cenário 4 prova que `id: 'not-a-uuid'` falha realmente o `TaskSchema.parse` (não é tautologia ajustada). |
| 5 | Flag set-once após sucesso | PASS — linha 114 (após loop), não antes. Caminho `no-data` (77) marca o flag (correcto: não há nada a migrar). Caminho `failed` SSR (62) intencionalmente NÃO marca (correcto: re-tentativa quando window estiver disponível). |
| 6 | AR1 mitigation real (id não-UUID → skipped) | PASS — `TaskSchema` (linha 30 de `schemas.ts`): `z.string().uuid('id deve ser UUID válido')`. V1Task com id `'t1'` (formato Story 0.3) faria fail real. Test cenário 4 prova com `id: 'not-a-uuid'`. Console.warn é chamado 1x apenas com a mensagem correcta contendo o id. |

### Pontos adicionais 7-12 (verificação Dara)

| # | Item | Verificação |
|---|------|------------|
| 7 | 4 [AUTO-DECISION] ratificadas pela PO | PASS — (a) Par Dex+Dara confirmado por inversão A6. (b) `MigrationResult.skipped: number` é extensão pura — `index.ts` reexporta `type` sem alteração, propaga automaticamente; nenhum consumidor actual de `MigrationResult` quebra (verificado via grep — apenas reexport). (c) Sem `db.transaction()`: `grep -n 'db.transaction' v1-to-v2.ts` = 0 matches. Anti-padrão evitado. (d) Exclusão notas: `grep -n 'nexus_notes' v1-to-v2.ts` = 0 matches. Notas correctamente fora de scope. |
| 8 | Test alteration em `client.test.ts` | PASS — diff (3 linhas alteradas): `id: 't1'` → `crypto.randomUUID()` + comentário PT-PT explicativo + asserção nova `expect(result.skipped).toBe(0)`. NÃO esconde regressão: a string `'t1'` antes passava porque `db.tasks.bulkAdd` não validava; agora `createTask` valida. A alteração **sintoniza** o teste para o regime novo, sem mudar o comportamento testado (happy path single-task). O teste continua a representar o caminho de migration bem-sucedida. |
| 9 | Coverage gap linhas 72-74 (`catch { v1Tasks = []; }`) | PASS (aceito como débito Story 0.3) — `git blame` confirma autoria `c362b1711` (Eurico, 04/05/2026 — Epic 0 Story 0.3). Linhas inalteradas por Dex. Adjacente alterada em linha 78 (`return { migrated: 0, skipped: 0, status: 'no-data' }`) mas esta é uma alteração ortogonal de retorno, não de comportamento do `catch`. **Decisão: aceitar como débito pré-existente.** Sugiro registar em Epic 2 backlog para 1 teste do cenário "JSON malformado em `nexus_tasks`" — não-bloqueador para PASS desta story (coverage 96.22% > 80% alvo). |
| 10 | Trailers do commit | PASS — `git log -1 dd6dc0d8` contém: `Constraint:`, `Rejected:`, `Confidence: high`, `Scope-risk: narrow`, `Directive:`. Todos correctos. `Not-tested:` correctamente **omitido** — commit não toca CI config, test-runner config, build config, segurança (verificado: ficheiros alterados são apenas `lib/db/migrations/v1-to-v2.ts` + tests + docs/story). Regra `not-tested-trailer-rules.md` clean. |
| 11 | File List autoritativa | PASS — `git show --stat dd6dc0d8` mostra: `lib/db/migrations/v1-to-v2.ts` (mod), `tests/unit/db/client.test.ts` (mod), `tests/unit/db/migrations/v1-to-v2.test.ts` (novo). Coincide exactamente com File List em Dev Agent Record (3 modificados + 1 novo, contando a story file). `migrations/index.ts` correctamente NÃO alterado (AC7 por type-passthrough). |
| 12 | Regressão dos 392 tests pré-existentes | PASS — Dara reproduziu `npm run test:unit` em `imersao-tools/nexus/v2/`: **33 test files, 398/398 tests passed**. Zero falhas. A "alteração de alinhamento" em `client.test.ts` não mascara regressão — o teste continua a validar o happy path single-task agora sob o regime Zod correcto. |

### Decisão sobre linhas 72-74 (`catch { v1Tasks = []; }`)

**Aceito como débito Story 0.3.** Fundamentação:
- `git blame` confirma autoria pré-existente (commit `c362b171`, Epic 0)
- Dex não introduziu nem moveu o `catch`
- Coverage agregada 96.22% > 80% alvo AC11
- O caminho protege contra `JSON.parse` falhar em dados corruptos — comportamento defensivo justificado
- Cobertura completa exigiria simular `localStorage.getItem('nexus_tasks')` a devolver string malformada (não-JSON). Cenário razoável de adicionar como teste, mas claramente fora de scope da 2.2 (refactor)

**Recomendação não-bloqueadora:** abrir item em Epic 2 backlog para "teste de JSON malformado em `nexus_tasks` → migration retorna `no-data` graciosamente". Pode ser absorvido em Story 8.10 (cleanup) ou criado como story de débito técnico.

### Top 3 achados

1. **Refactor cirúrgico exemplar** — `git diff main..HEAD -- v1-to-v2.ts` mostra substituição mínima (`db.transaction(...)` block → loop) com mapeamento V1Task→Task **intacto** (AC8 honrado byte-a-byte). Zero scope creep.
2. **Test cenário 4 é honesto** — usa real `createTask` (não mock substitutivo) para provar que `id: 'not-a-uuid'` falha o `TaskSchema.parse` no caminho real, validando AR1 mitigation. Cumpre `mock-protocol-fidelity.md` à letra.
3. **`MigrationResult.skipped` extensão type-passthrough perfeita** — `export type { MigrationResult }` em `index.ts` propaga automaticamente o novo campo sem alteração de ficheiro reexportador. Decisão arquitecturalmente correcta (sem breaking change a consumidores). Único reparo é a recomendação não-bloqueadora sobre cobertura de linhas 72-74 (débito 0.3).

### Veredicto final

**PASS** — todos os 11 ACs verificados directamente em código, todos os pontos focais 1-6 do handoff e os adicionais 7-12 verificados. Gates locais reproduzidos. Story pronta para closure pela PO (Pax).

**Próxima acção:** `@po *close-story 2.2`

---

## PO Validation

**Data:** 15/05/2026
**PO:** Pax
**Score:** 10/10
**Veredicto:** APPROVED
**Constraint contextual:** implementação só pode arrancar após merge do PR #18 (Story 2.1) em `main`.

### 10-Point Checklist

| # | Check | Verdict | Evidência |
|---|-------|---------|-----------|
| 1 | Goal traceability | PASS | Goal cita `EPIC-2.md` §5 linha 57 (Story 2.2 — "Migrar dados de tarefas do `localStorage` v1 (`nexus_tasks`) para o schema Dexie v2", FR9, executor `@dev`, quality gate `@data-engineer`). Cita também `architecture-v2.md` §4.4 e §7. |
| 2 | Dependencies clarity | PASS | Dependência única à Story 2.1 documentada com API concreta: `createTask(input: Task): Promise<Task>` de `lib/db/repos/tasks.ts`. Verificado em disco (lido às linhas 25-29 de `repos/tasks.ts`). |
| 3 | ACs testability | PASS | 11 ACs todos binários (PASS/FAIL inequívoco). AC9 lista 6 cenários de teste concretos. AC11 fixa threshold de coverage (>= 80% lines) com referência ao `vitest.config.ts`. |
| 4 | ACs trace | PASS | AC1-AC4 traçam a `architecture-v2.md §4.4` (idempotência, flag, localStorage intacto). AC1 traça a `EPIC-2.md §5` (escrita via repos). AC8 traça ao skeleton da Story 0.3 (`lib/db/migrations/v1-to-v2.ts` linhas 64-79). AC10-AC11 traçam às convenções do `tests/setup.ts` (Story 0.9). |
| 5 | Tasks operacionais | PASS | T1-T7 ordenadas (verificação → refactor de tipo → refactor de código → testes → gates → maintenance → delegação). T3 tem 7 subtarefas com instruções cirúrgicas (substituir bulkAdd por loop, manter ordem do flag, manter localStorage v1 intacto). Sem ambiguidade. |
| 6 | Risk assessment | PASS | AR1-AR3 com mitigações concretas: AR1 (UUID não-UUID) mitigado por AC6+AC9; AR2 (remoção inadvertida de `db` import) mitigado por T3.4 verificação explícita; AR3 (regressão de idempotência) mitigado por AC4+AC9 cenário idempotente. Cada AR cita ACs ou tasks específicas. |
| 7 | DoD implícito | PASS | DoD derivável dos ACs: AC10 (gates locais lint/typecheck/test:unit/build) + AC11 (coverage >= 80%) + AC9 (6 cenários) + File List actualizada (T6) + delegação push (T7). Sem invenção. |
| 8 | Architecture alignment | PASS | `architecture-v2.md §4.4` citado correctamente (verificado às linhas 275-319). Reconciliação R1-R3 explica honestamente porque o pseudocódigo `bulkAdd` é superado pela Story 2.1 (sem reabrir ADR — segue `EPIC-2.md §7` "Nenhum ADR é reaberto"). |
| 9 | Constitution Article IV (No Invention) | PASS | Anti-padrões secção lista explicitamente o que NÃO fazer (notas v1, UI de progresso, transacção, alterar mapeamento, motor de recorrência). Não inventa features fora do epic. A exclusão da migration de notas v1 (que `architecture-v2.md §4.4` linhas 290+307-310 prevê) está fundamentada: EPIC-2 §5 é "Tarefas + Projectos" — notas pertencem a outro epic. Confirmado por mim. |
| 10 | PT-PT compliance | PASS | Mensagens de erro em PT-PT (AC6: `Tarefa ignorada na migration (id: "${task.id}"): ${error.message}`). Comentários e descrições todos em PT-PT. Repos da 2.1 já estabelecem este padrão (verificado em `tasks.ts` linha 13: "input inválido lança ZodError com mensagens PT-PT"). |

**Score: 10/10 PASS → APPROVED.**

### Ratificação de decisões autónomas

River declarou explicitamente apenas 1 `[AUTO-DECISION]` na secção Contexto (não-transacção). No entanto, o handoff de entrada lista 3 pontos de atenção que correspondem a decisões autónomas implícitas. Trato cada uma:

| # | Decisão de River | Veredicto | Justificação |
|---|------------------|-----------|--------------|
| 1 | **Executor `@dev` + Quality gate `@data-engineer`** (linhas 17-22) | **RATIFICADA** | Respeita `separation-of-roles.md` A6. EPIC-2 §5 linha 57 prevê exactamente este par. Dara conhece os repos da 2.1, está bem posicionada para validar uso correcto da API. |
| 2 | **Extender `MigrationResult` com `skipped: number`** (AC2/AC3/AC7) | **RATIFICADA** | Necessário para reportar tarefas inválidas sem bloquear válidas (AC6). Verifiquei que `MigrationResult` é actualmente reexportado apenas em `migrations/index.ts` (não consumido por código existente) — extensão segura. Stories 2.3+ ainda não existem, sem risco de quebra. |
| 3 | **Sem transacção Dexie wrapping o loop** (`[AUTO-DECISION]` linha 62) | **RATIFICADA** | Fundamentação sólida: a transacção `bulkAdd` contornaria a validação Zod individual de `createTask`. Idempotência continua garantida pelo flag (AC4). Falhas parciais reportadas em `skipped` (AC3/AC6). Re-tentativa possível se o flag não for marcado. Trade-off correcto para single source of truth (EPIC-2 §2). |
| 4 | **Excluir migration de notas v1 do scope** (Anti-padrões + Dev Notes "Não-inventar") | **RATIFICADA** | `architecture-v2.md §4.4` linhas 289+307-310 prevê migrar `nexus_notes` numa transacção conjunta com tasks. River exclui isto invocando `EPIC-2.md §5` (Epic 2 é "Tarefas v2 + Projectos"). Confirmei: notas pertencem a outro epic (knowledge). Decisão arquitecturalmente válida — quando o epic de knowledge for executado, criará a sua própria story de migration de notas. |

**Conclusão:** todas as 4 decisões autónomas estão ratificadas. Zero items revertidos para Open Question.

### Verificações de campo (não-checklist)

| Item | Resultado |
|------|-----------|
| `lib/db/migrations/v1-to-v2.ts` existe | SIM — verificado, 96 linhas, conforme descrito no draft |
| `lib/db/repos/tasks.ts` existe com `createTask` | SIM — verificado, assinatura `createTask(input: Task): Promise<Task>` confirmada (linhas 25-29) |
| `lib/db/schemas.ts` existe com `TaskSchema` | SIM — verificado |
| `tests/unit/db/repos/` existe (padrão de teste) | SIM — verificado |
| `tests/setup.ts` existe (fake-indexeddb + jsdom) | SIM — verificado |

### Items requeridos para fix

Nenhum. Story aprovada sem fixes.

### Constraint de execução

A 2.2 não pode arrancar até a Story 2.1 (PR #18, branch `feature/2.1-schema-tarefas-projectos`) estar merged em `main`. O `Status: Approved` autoriza Dara a começar **após** esse merge. Esta validação não bloqueia o merge — é trabalho paralelo.

---

## PO Closure

**Data:** 15/05/2026
**PO:** Pax
**Veredicto:** **CLOSED**
**Commit local:** `dd6dc0d8` (ainda não pushed)
**Branch:** `feature/2.2-migration-refactor`

> Trace separação de papéis (`separation-of-roles.md` A6): Pax validou o draft inicial (Change Log v0.2) mas **não escreveu código**. Dex executou (commit `dd6dc0d8`), Dara fez quality gate (Change Log v0.4). Pax assina agora o closure formal — sem conflito de papéis, função natural do PO no Story Development Cycle Phase 5.

### Definition of Done — 15/15 PASS

| # | Item DoD | Evidência | Verdict |
|---|----------|-----------|---------|
| 1 | Status story = `Done` | Linha 5 do header + Change Log v0.4 Dara | PASS |
| 2 | QA Results completa e PASS | Linhas 399-477 (11/11 ACs + 6 focais + 6 adicionais + decisão coverage + top 3 achados + veredicto) | PASS |
| 3 | File List bate com commit | `git show --stat dd6dc0d8`: 3 mod (`v1-to-v2.ts`, `client.test.ts`, `2.2.story.md`) + 1 novo (`v1-to-v2.test.ts`) = exactamente o que Dev Agent Record declara | PASS |
| 4 | Tasks T1-T6 `[x]`, T7 `[ ]` (delegação correcta) | Linhas 118-145; T7 push delegado a `@devops` por design | PASS |
| 5 | 11/11 ACs honrados com evidência directa | Tabela QA Results linhas 421-432; cada AC tem prova em código (linhas exactas) | PASS |
| 6 | Loop `createTask` substitui `bulkAdd` (AC1) | `v1-to-v2.ts:98-112` loop `for (const task of tasksV2)` com `await createTask(task)`; zero referências a `db.tasks.bulkAdd` | PASS |
| 7 | Zod validation per-item preservada (AC6) | Try/catch genérico (`error instanceof Error`) cobre `ZodError` + outros; mensagem PT-PT `Tarefa ignorada na migration (id: "${task.id}"): ${error.message}` | PASS |
| 8 | `MigrationResult.skipped` extensão type-passthrough (AC2/AC7) | `MigrationResult` linhas 42-47 com campo `skipped: number`; `migrations/index.ts:8` reexporta o tipo sem alteração — propaga automaticamente | PASS |
| 9 | 6 cenários AC9 testados | `tests/unit/db/migrations/v1-to-v2.test.ts` 173 linhas: happy 3 válidas / idempotência / no-data / id `'not-a-uuid'` / SSR guard / `localStorage` v1 intacto byte-a-byte | PASS |
| 10 | Coverage ≥80% em `v1-to-v2.ts` (AC11) | 96.22% lines confirmado (Dex declarou, Dara aceitou via typecheck+lint+test:unit match byte-a-byte). Linhas 72-74 não-cobertas = débito Story 0.3 (`git blame c362b171`, autoria Eurico 04/05/2026) — aceito como pré-existente | PASS |
| 11 | typecheck / lint / test:unit / build PASS | Dara reproduziu: typecheck exit 0, lint OK (1 warn `NextResponse` unused pré-existente fora-scope), test:unit 398/398 (33 files), build aceito por evidência Dex | PASS |
| 12 | Skeleton Story 0.3 preservado (idempotência + mapeamento + rollback) | AC4 flag set-once linha 114 após loop; AC5 `localStorage` v1 intacto (zero `removeItem`); AC8 mapeamento V1Task→Task `git diff main..HEAD` byte-a-byte idêntico linhas 81-96 | PASS |
| 13 | Sem regressão nos 392 tests pré-existentes | 398/398 PASS reproduzido por Dara (392 pré + 6 novos); alteração `client.test.ts` (`'t1'` → `crypto.randomUUID()`) sintoniza expectativa face a Zod, sem mascarar regressão | PASS |
| 14 | Trailers commit conformes | `git log -1 dd6dc0d8`: `Constraint:` + `Rejected:` + `Confidence: high` + `Scope-risk: narrow` presentes; `Not-tested:` correctamente omitido (commit não toca CI/test-runner/build config nem segurança — clean perante `not-tested-trailer-rules.md`) | PASS |
| 15 | Mensagens PT-PT consistentes | `Tarefa ignorada na migration (id: "${t.id}"): ${error.message}` (AC6) + Change Logs + Dev Agent Record + Comments + QA Results todos em PT-PT | PASS |

### Ratificação final das 4 [AUTO-DECISION]

| # | Decisão (Change Log v0.2) | Honrada pela implementação? | Evidência |
|---|----------------------------|------------------------------|-----------|
| 1 | Executor `@dev` + Quality gate `@data-engineer` (par A6) | SIM | Dex commitou `dd6dc0d8` (integralmente). Dara fez gate sem tocar ficheiros de produção da 2.2. Sem conflito A6. |
| 2 | Estender `MigrationResult` com `skipped: number` | SIM | Campo declarado linhas 42-47 + propagado via `export type` em `migrations/index.ts` sem alteração ao reexportador. Zero breaking change. |
| 3 | Sem transacção Dexie wrapping o loop | SIM | `grep -n 'db.transaction' v1-to-v2.ts` = 0 matches. Loop puro com try/catch individual — falhas parciais reportadas em `skipped`, idempotência preservada pelo flag. |
| 4 | Excluir migration de notas v1 (`nexus_notes`) do scope | SIM | `grep -n 'nexus_notes' v1-to-v2.ts` = 0 matches. Notas pertencem a Epic de knowledge — não a Epic 2 (Tarefas + Projectos). |

**Todas as 4 decisões autónomas honradas pela implementação. Zero desvios.**

### Decisão sobre cobertura 72-74 (`catch { v1Tasks = []; }`)

Confirmo a decisão de Dara: **aceito como débito Story 0.3** (autoria Eurico via `git blame c362b171`, 04/05/2026). Não introduzido por Dex. Coverage agregada 96.22% > 80% alvo AC11. Caminho defensivo justificado (protege contra `JSON.parse` falhar em dados corruptos).

**Decisão de backlog (não-bloqueador):** o item "teste de JSON malformado em `nexus_tasks` → migration retorna `no-data` graciosamente" fica **registado para a retrospectiva Epic 2** (não bloqueia esta closure). Pode ser absorvido em Story 8.10 (cleanup do Epic 8) ou criado como story de débito técnico quando o Epic 2 fechar.

### Lições registadas para a retrospectiva Epic 2

1. **Type-passthrough via `export type`** — quando uma extensão de interface é puramente aditiva, o reexport por type forwarda automaticamente sem alterar consumidores. Padrão a replicar quando aplicável (poupa scope sem perder type safety).
2. **`skipped` counter pattern** — separar `migrated` de `skipped` em operações de batch permite reportar falhas parciais sem bloquear o todo. Padrão útil para futuras migrations multi-entidade.
3. **Test cenário com erro real (não mock substitutivo)** — usar `createTask` real com input que falha `TaskSchema` (`id: 'not-a-uuid'`) prova AR1 mitigation a sério, sem tautologia. Cumpre `mock-protocol-fidelity.md` à letra (`feedback_mock_must_reflect_real_protocol.md`).
4. **Refactor cirúrgico vs reescrita** — Dex preservou byte-a-byte o mapeamento V1Task→Task (AC8) e o SSR guard. Substituição exclusivamente do bloco `db.transaction(...)` → loop. Zero scope creep. Padrão exemplar.
5. **Débito de Epic anterior** — coverage gap em linhas 72-74 era pré-existente da Story 0.3. `git blame` foi a ferramenta correcta para confirmar autoria e responsabilizar fora-de-scope. Replicar este reflexo em futuras stories.

### Veredicto final

**CLOSED.** Story 2.2 pronta para `@devops *push` da branch `feature/2.2-migration-refactor` (commit `dd6dc0d8`).

**Próxima acção:** `@devops *push` (PR para `main`) → aguardar CodeRabbit + merge → `@sm *draft 2.3` (Vista lista).

---

## Próximo passo natural

Story 2.2 criada em Draft. Sequência:
1. `@po *validate-story-draft 2.2` — validar draft (aguarda push/merge da 2.1 em `main`)
2. `@dev *develop 2.2` — após Approved
3. `@data-engineer *qa-gate 2.2`
4. `@devops *push`
