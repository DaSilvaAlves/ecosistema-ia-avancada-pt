# Story 2.9 — Vista projecto (FR31)

**Story ID:** 2.9
**Epic:** 2 — Tarefas v2 + Projectos
**Status:** Ready for Review
**Data de criação:** 2026-05-18
**Data de validação:** 2026-05-18
**Data de implementação:** 2026-05-18
**SM:** River (`@sm`)
**PO Validator:** Pax (`@po`) — VALIDADA 18/05/2026 com score 10/10 (GO)
**Executor previsto:** `@ux-design-expert` (Uma) — vista dual lista+Kanban, rota nova, padrões UX estabelecidos nas Stories 2.3/2.4/2.5
**Quality gate previsto:** `@dev` (Dex) — separação A6: Uma executa, Dex faz gate
**Story points estimados:** 5 (medium — rota dinâmica `[id]`, fetch single projecto, 2 sub-vistas lista+Kanban, padrão reaproveita infra 2.3+2.4, ~12-15 testes Vitest)
**Branch sugerida:** `feature/2.9-vista-projecto` (criar a partir de `main` actualizado — Story 2.8 em main, SHA `eff7955d`)
**Desbloqueada por:** Story 2.8 (CRUD projectos — rota `/projectos`, `ProjectFormModal`, repo `projects.ts`, `useProjects`) — MERGED em main
**Bloqueia:** Story 2.10 (tools cérebro `consultar_projecto` beneficia de ter a vista disponível para validação manual)

> **INFRAESTRUTURA PRONTA (Stories 2.1 + 2.8):**
> - `lib/db/repos/projects.ts` — `getProject(id)` por UUID, `updateProject`, `archiveProject` — **NÃO alterar**
> - `lib/db/repos/tasks.ts` — `listTasks({projectId})` — **NÃO alterar**
> - `hooks/useProjects.ts` — `useProjects({status?})` reactivo — **NÃO alterar**
> - `hooks/useTasks.ts` — `useTasks({projectId?})` reactivo (suporte a filtro já implementado) — **NÃO alterar**
> - `types/db.ts:52-68` — interface `Task` com `projectId: string | null` — **NÃO alterar**
> - `types/db.ts:69-77` — interface `Project` (id, name, description, status, startDate, deadline, createdAt) — **NÃO alterar**
> - `lib/db/schemas.ts` — `ProjectSchema` Zod (mensagens PT-PT), `ProjectStatusSchema` — **NÃO alterar**
> - `components/projectos/ProjectFormModal.tsx` — modal criar/editar com focus trap WAI-ARIA — **reaproveitar**
> - `components/tarefas/KanbanBoard.tsx` — recebe `tasks: Task[] | undefined` já filtradas — **reaproveitar**
>
> **A 2.9 entrega APENAS a camada UI da vista de detalhe** — repos, hooks, schemas e Dexie tables NÃO precisam de alteração.

---

## Executor Assignment

```
executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: [lint, typecheck, vitest, build, axe-core (manual a11y spot-check tab strip + empty state)]
```

> Trace: `EPIC-2.md` §5 (Story 2.9, executor `@ux-design-expert`, quality gate `@dev`). Respeita `separation-of-roles.md` (A6) — `executor != quality_gate`. Uma implementa a página de detalhe + sub-vistas + integração com `ProjectFormModal`; Dex (quality gate) audita ACs, a11y, testes, coverage e integração com repos.

---

## Nota do `@sm` River sobre escopo e dependências

A Story 2.9 no `EPIC-2.md` §5 está descrita como **"Vista projecto (lista + Kanban filtrado)"** traçando FR31. O PRD §6.5 linha 163:

> **FR31:** Vista projecto: tarefas vinculadas com vista lista + Kanban. Trace: JARVIS.txt L110-111.

**Decisões autónomas do SM (para ratificação `@po`):**

[AUTO-DECISION A1] **Rota `/projectos/[id]`** (UUID — parâmetro dinâmico Next.js App Router), não slug. Razão: `Project.id` é UUID gerado por `crypto.randomUUID()` — não existe campo `slug` no schema nem no PRD. `[id]` é o padrão natural e único disponível. Alternativa `[slug]` descartada — exigiria campo novo não definido no PRD (violação No Invention Artigo IV).

[AUTO-DECISION A2] **Dual vista: Lista por defeito, Kanban como segunda tab**. FR31 diz "lista + Kanban" — duas vistas, não simultâneas. Lista é o default (mais leve, sem DnD inicializado). Kanban é segunda tab com `KanbanBoard.tsx` reutilizado. Alternativa "ambas vistas em paralelo" descartada — viewport insuficiente, sem precedente de split-view no codebase.

[AUTO-DECISION A3] **Reaproveitar `KanbanBoard.tsx` da Story 2.4** para a sub-vista Kanban, passando tasks já filtradas por `projectId`. `KanbanBoard` recebe `tasks: Task[] | undefined` — o filtro é feito no hook `useTasks({ projectId: id })` na page, não dentro do componente. Zero modificação do `KanbanBoard.tsx`. Alternativa "criar KanbanBoard separado" descartada — duplicação viola YAGNI.

[AUTO-DECISION A4] **Reaproveitar `ProjectFormModal.tsx` da Story 2.8** para a acção de editar o projecto na vista de detalhe. O modal já existe, já tem focus trap WAI-ARIA, validação Zod e mensagens PT-PT. A page passa o `project` como `initialValue` para pré-preenchimento, mesmo padrão da `ProjectCard.tsx` na Story 2.8.

[AUTO-DECISION A5] **Header da página de detalhe** inclui: nome do projecto como `<h1>`, status badge, datas (início + prazo opcional), progress bar `% tasks done`, botão "Editar" (abre `ProjectFormModal`), botão "Esc · Voltar" (`router.back()` para `/projectos`). Sem breadcrumbs complexas — não há sidebar/nav implementada (precedente Story 2.8 A12).

[AUTO-DECISION A6] **Progress bar** calculada client-side: `done_count / total_count * 100`. Se `total_count === 0`, progress = 0 (sem divisão por zero). Cor da barra: Lime `#39FF14` (sucesso). Alternativa "omitir progress" descartada — dá contexto imediato sem drill-down individual nas tasks.

[AUTO-DECISION A7] **Sub-vista Lista** (default) — lista vertical de tasks do projecto agrupadas por status (`todo`, `in-progress`, `blocked`, `done`), reaproveitando o padrão visual de `TaskRow.tsx` da Story 2.3 (ou equivalente simplificado se `TaskRow` tiver dependências de estado da page `/tarefas`). Se `TaskRow.tsx` tiver acoplamento forte a state da page `/tarefas`, criar `ProjectTaskRow.tsx` simplificado com os mesmos tokens visuais. [A7 a confirmar por Uma durante T1.4 — leitura do componente antes de implementar].

[AUTO-DECISION A8] **Navegação de entrada**: a `ProjectCard.tsx` da Story 2.8 actualmente NÃO tem link para `/projectos/[id]`. Esta story adiciona `router.push(\`/projectos/\${project.id}\`)` ao clicar no nome/card (excluindo área do menu kebab). Implementação: wrapper `<button onClick={...}>` no nome, ou `<Link href=...>` — a decidir por Uma durante implementação (padrão visual e semântico). Sem modificar o `ProjectCard.tsx` da 2.8 se o click handler puder ser passado via prop `onView`; caso contrário, modificação cirúrgica da prop.

[AUTO-DECISION A9] **Not Found (404)**: se `getProject(id)` retornar `undefined`, exibir estado de erro com mensagem PT-PT "Projecto não encontrado." + botão "Voltar aos projectos" (`router.push('/projectos')`). Sem `notFound()` do Next.js (evitar Server Component — page é `'use client'`). Alternativa "usar Next.js notFound()" descartada — page é client-side (`useLiveQuery` exige), e `notFound()` requer Server Component.

[AUTO-DECISION A10] **Empty state tasks**: se projecto existe mas não tem tasks, exibir mensagem PT-PT "Sem tarefas neste projecto." + CTA "Criar tarefa em `/tarefas`" (`router.push('/tarefas')`) — não criar task inline nesta story (fora de scope, exigiria `ProjectTaskFormModal` nova).

[AUTO-DECISION A11] **D3 oportunidade de refactor `PRIORITY_COLORS`**: D3 do Epic 2 regista duplicação de `PRIORITY_COLORS` entre `TaskRow.tsx` e `KanbanCard.tsx`. Se a Story 2.9 criar `ProjectTaskRow.tsx`, extrair para `lib/tarefas/colors.ts` (conforme D3). Caso contrário, reportar à retrospectiva Epic 2. [Decisão final durante T4 — Uma confirma se cria `ProjectTaskRow` ou reutiliza `TaskRow`].

[AUTO-DECISION A12] **Vitest.config.ts**: adicionar `'app/(app)/projectos/[id]/**'` à allowlist da coverage section. O glob `'app/(app)/projectos/**'` da Story 2.8 já cobre recursivamente — verificar durante T9 se está coberto; se não, adicionar linha. Seguir precedente de comentário inline: `// Story 2.9 — Vista projecto`.

**O que já está pronto (não tocar):**
1. `lib/db/repos/projects.ts` com `getProject(id)` (Story 2.1).
2. `lib/db/repos/tasks.ts` com `listTasks({projectId})` (Story 2.1).
3. `hooks/useTasks.ts` com `useTasks({projectId?})` reactivo (Story 2.1).
4. `components/tarefas/KanbanBoard.tsx` com DnD + 4 colunas (Story 2.4).
5. `components/projectos/ProjectFormModal.tsx` com focus trap + validação Zod (Story 2.8).
6. `components/projectos/ProjectCard.tsx` + `ProjectsGrid.tsx` + `ProjectsHeader.tsx` (Story 2.8 — sem modificação directa; A8 pode adicionar prop `onView` se necessário).

**O que esta story entrega:**
1. **Rota dinâmica `/projectos/[id]`** — page client-side com `useParams()` para ler o UUID.
2. **`ProjectDetailHeader.tsx`** — h1 com nome, status badge, datas, progress bar, botão Editar (abre `ProjectFormModal`), botão "Esc · Voltar".
3. **Tab strip de vistas** (Lista | Kanban) com WAI-ARIA Tabs Authoring Practices (arrow keys + Home/End) — replicar `TasksHeader.tsx` precedente.
4. **Sub-vista Lista** — tarefas do projecto por status, `TaskRow.tsx` reutilizado (ou `ProjectTaskRow.tsx` simplificado se necessário — ver A7).
5. **Sub-vista Kanban** — `KanbanBoard.tsx` com tasks filtradas por `projectId` (via `useTasks({projectId: id})`).
6. **Estados** — Loading (skeleton), Not Found (A9), Empty tasks (A10), erro Editar (toast PT-PT).
7. **Integração `ProjectFormModal`** — Editar projecto inline, re-render reactivo via `useLiveQuery`.
8. **Modificação cirúrgica `ProjectCard.tsx`** — adicionar prop `onView?: (id: string) => void` para navegação (se não viável via Link externo), ou sem modificação se conseguir via `<Link href=...>` no wrapper do card.
9. **Testes Vitest** — 12-15 cenários (render base, loading, not found, empty tasks, tab switch lista↔kanban, edit projecto modal, close modal, navigate back, progress bar, a11y smoke).

---

## User Story

**As a** Eurico,
**I want** uma página dedicada `/projectos/{id}` onde possa ver num único sítio todas as tarefas de um projecto específico — em vista lista ou Kanban — o progresso global e editar os detalhes do projecto,
**so that** consigo gerir o trabalho de um projecto de forma centralizada sem ter de filtrar manualmente em `/tarefas`, cumprindo FR31 (Vista projecto: tarefas vinculadas com vista lista + Kanban).

---

## Contexto

| Pilar | Detalhe |
|-------|---------|
| **PRD** | FR31: "Vista projecto: tarefas vinculadas com vista lista + Kanban." Trace: `PRD-NEXUS-V2.md` §6.5 linha 163 + §10 Epic 2 (linha 448). |
| **Arquitectura** | Next.js 15 App Router (`app/(app)/projectos/[id]/page.tsx`), `'use client'` (Dexie via `useLiveQuery` exige client). `useParams()` para ler `id` da URL. `getProject(id)` para fetch single. `useTasks({projectId: id})` reactivo para lista de tasks. |
| **UX** | `front-end-spec-v2.md §9` linha 1203 — Epic 2 inclui "Tab/filtro em /tasks" (já entregue em 2.3) e "Vista projecto" em 2.9. Dois padrões de vista: lista (Story 2.3) + Kanban (Story 2.4) — reaproveitar ambos. |
| **Design system** | `.claude/rules/design-system-ia-avancada.md` — glassmorphism, Inter body, JetBrains Mono badges, Cyan/Gold/Lime por status (Cyan active, Gold paused, Lime done), progress bar Lime. Header sticky `rgba(4,4,10,0.92)` + `backdrop-filter: blur(12px)`. |
| **Lições Epic 1** | A1 (mock-protocol-fidelity) — N/A (CRUD interno, sem mocks de protocolos externos). A2 (Not-Tested Evidence Gate) — N/A se não tocar config bloqueadora. A6 (separation-of-roles) — Uma executa, Dex faz gate. |
| **D3 oportunidade** | `PRIORITY_COLORS` duplicado (TaskRow + KanbanCard). Se Story 2.9 criar `ProjectTaskRow`, extrair para `lib/tarefas/colors.ts` (ver A11). |

### Stack de dados (verificado — Stories 2.1 + 2.8 entregaram)

```typescript
// hooks/useTasks.ts (Story 2.1) — filtro por projectId já suportado
export function useTasks(opts: ListTasksOptions = {}): Task[] | undefined
// ListTasksOptions: { status?, projectId?, tag?, limit? }

// lib/db/repos/projects.ts (Story 2.1) — lookup por id
getProject(id: string): Promise<Project | undefined>
updateProject(id: string, patch: Partial<Project>): Promise<void>
archiveProject(id: string): Promise<void>

// types/db.ts — Task interface (confirmado estável)
Task {
  id: string;
  title: string;
  status: 'todo' | 'in-progress' | 'blocked' | 'done';
  projectId: string | null;
  // ... outros campos
}

// types/db.ts — Project interface (confirmado estável)
Project {
  id: string;
  name: string;
  description: string;
  status: 'active' | 'paused' | 'done';
  startDate: string;        // 'YYYY-MM-DD'
  deadline: string | null;  // 'YYYY-MM-DD' ou null
  createdAt: number;        // epoch ms
}
```

### Progress bar (A6)

```typescript
// page.tsx — calculada client-side com useMemo
const progress = useMemo(() => {
  if (!tasks || tasks.length === 0) return 0;
  const done = tasks.filter((t) => t.status === 'done').length;
  return Math.round((done / tasks.length) * 100);
}, [tasks]);
```

---

## Reconciliação PRD ↔ Arquitectura ↔ Spec

| # | PRD §6.5 diz | Arquitectura / Stories anteriores | Resolução para Story 2.9 |
|---|--------------|-----------------------------------|--------------------------|
| R1 | "Vista projecto: tarefas vinculadas com vista lista + Kanban" | `useTasks({projectId?})` suporta filtro; `KanbanBoard.tsx` recebe tasks filtradas | **A2**: tab strip Lista | Kanban; `useTasks({projectId: id})` filtra no hook; `KanbanBoard.tsx` sem modificação |
| R2 | (não menciona rota específica) | Next.js App Router `[id]` param; `Project.id` é UUID | **A1**: `/projectos/[id]` com `useParams()` |
| R3 | (não menciona edit na vista) | `ProjectFormModal.tsx` já existe (Story 2.8) | **A4**: reaproveitar modal; botão "Editar" no header |
| R4 | (não menciona not found) | `getProject(id)` retorna `undefined` se não existe | **A9**: estado de erro PT-PT (not-found sem `notFound()` Server Component) |
| R5 | (não menciona progress) | Tasks têm `status: done/todo/in-progress/blocked` | **A6**: progress bar `done/total * 100` client-side |
| R6 | (não menciona navegação) | Story 2.8 — `ProjectCard` sem link para detalhe | **A8**: adicionar navegação ao card (prop `onView` ou `<Link>`) + link de retorno `router.back()` |

---

## Acceptance Criteria

- **AC1:** Rota `/projectos/[id]` activa via Next.js App Router (`app/(app)/projectos/[id]/page.tsx`), `'use client'`. `useParams()` extrai o UUID da URL. Escape global no body chama `router.back()` (precedente `tarefas/page.tsx:101-107` e `projectos/page.tsx`).

- **AC2:** **`ProjectDetailHeader`** apresenta:
  - `<h1>` com nome do projecto, Inter weight 800 1.6rem, cor `#F0F4FF`.
  - **Status badge** JetBrains Mono uppercase PT-PT (`ACTIVO` / `PAUSADO` / `CONCLUÍDO`) com background/border colorido por status (Cyan/Gold/Lime — padrão `ProjectCard.tsx`).
  - **Datas** JetBrains Mono 0.7rem: "📅 Iniciado DD/MM/YYYY" + (se deadline) "⏰ Prazo DD/MM/YYYY" — formato PT-PT via `format(parseISO(date), 'dd/MM/yyyy', { locale: pt })` (precedente Story 2.8 AC4).
  - **Progress bar** horizontal: label PT-PT "N% concluído" + barra Lime `#39FF14` sobre fundo `rgba(57,255,20,0.12)`, percentagem calculada via A6. Se 0 tasks, "0% concluído".
  - **Botão "Editar"** (secondary — não cyan primary) abre `ProjectFormModal` em modo edição com `initialValue={project}`.
  - **Botão "Esc · Voltar"** com `aria-label="Voltar (Esc)"`, chama `router.back()` (precedente `TasksHeader.tsx:138-163`).
  - Header sticky `top: 0`, `background: rgba(4, 4, 10, 0.92)` + `backdrop-filter: blur(12px)`, `z-index: 10`.

- **AC3:** **Tab strip de vistas** (Lista | Kanban), default **Lista**. WAI-ARIA Tabs Authoring Practices:
  - `role="tablist"`, `role="tab"`, `aria-selected`, `tabIndex={active ? 0 : -1}`.
  - Arrow keys ←/→ navegam entre tabs, Home/End vão para primeiro/último.
  - Tab activa exibe o conteúdo correspondente; inactiva fica visível mas não renderiza conteúdo.
  - Precedente: `TasksHeader.tsx:35-39, :53-76` + `ProjectsHeader.tsx` (Story 2.8).

- **AC4:** **Sub-vista Lista** (default). Apresenta tarefas do projecto (`useTasks({ projectId: id })`) agrupadas em secções por status na ordem: "Por fazer", "Em curso", "Bloqueadas", "Concluídas". Cada secção tem `<h2>` PT-PT + contagem `(N)`. Cada task é `<TaskRow ...>` ou `<ProjectTaskRow ...>` (ver A7) com campos: título, prioridade badge, due date (se existir), status. Se `tasks === undefined`, skeleton PT-PT. Se `tasks.length === 0`, empty state A10.

- **AC5:** **Sub-vista Kanban** (segunda tab). `<KanbanBoard tasks={tasks} projects={[project]} tagsLookup={tagsLookup} />` reutilizado da Story 2.4, recebendo as tasks já filtradas por `projectId` (via hook). Drag-and-drop entre colunas funciona e persiste via `setTaskStatus` (mesmo comportamento de `/tarefas`). Se `tasks === undefined`, skeleton do `KanbanBoard` activa automaticamente (já implementado na Story 2.4). Se `tasks.length === 0`, empty state do `KanbanBoard` visível.

- **AC6:** **Estado Loading**: enquanto `project === undefined` (Dexie a carregar o projecto individual), exibir skeleton do header (`<div aria-busy="true" aria-label="A carregar projecto">`) com pulse animation. Precedente skeleton da Story 2.8 (6 cards placeholder).

- **AC7:** **Estado Not Found** (A9): se `project` termina de carregar e o valor é `null` ou `undefined` confirmado (distinto de "ainda a carregar"), exibir glass card centrado com mensagem PT-PT "Projecto não encontrado." + botão "Voltar aos projectos" que chama `router.push('/projectos')`. `aria-live="polite"` na região de not-found.

- **AC8:** **Empty state tasks** (A10): projecto existe mas `tasks.length === 0` após carregamento completo. Exibir mensagem PT-PT centrada: "Sem tarefas neste projecto." + CTA "Ver tarefas em /tarefas" (botão que chama `router.push('/tarefas')`). Aplicado tanto na sub-vista Lista como na sub-vista Kanban.

- **AC9:** **Acção Editar projecto** funciona na vista de detalhe:
  - Click em "Editar" abre `ProjectFormModal` em modo `'edit'` com `initialValue={project}`.
  - Submit chama `updateProject(id, patch)`.
  - Após sucesso, `useLiveQuery` reactivo actualiza `project` state sem reload — `<h1>` e status badge actualizam-se imediatamente.
  - Erro do repo → toast primitivo PT-PT 4s (precedente Story 2.4/2.5/2.8).
  - Modal fechar restaura foco no botão "Editar" (focus trap precedente Story 2.8).

- **AC10:** **Navegação de entrada** via `ProjectCard.tsx`:
  - Clicar no nome ou área principal do card em `/projectos` navega para `/projectos/{id}`.
  - Implementação via prop `onView?: (id: string) => void` passada de `ProjectsGrid` → `ProjectCard`, chamando `router.push(\`/projectos/\${id}\`)`. Alternativa: `<Link href=...>` se não requerer modificação de props.
  - Área do menu kebab `⋯` NÃO deve activar a navegação (comportamento de swallow já implementado nos event handlers do menu).

- **AC11:** **PT-PT consistente**:
  - Tabs: "Lista", "Kanban".
  - Secções lista: "Por fazer", "Em curso", "Bloqueadas", "Concluídas".
  - Progress: "N% concluído", "0% concluído".
  - Empty tasks: "Sem tarefas neste projecto.", "Ver tarefas em /tarefas".
  - Not found: "Projecto não encontrado.", "Voltar aos projectos".
  - Header: "Esc · Voltar".
  - Edit modal: reutiliza PT-PT existente do `ProjectFormModal` (Story 2.8).
  - Skeleton: `aria-label="A carregar projecto"`.
  - Datas: `dd/MM/yyyy` (locale `pt`).

- **AC12:** **Acessibilidade WAI-ARIA**:
  - Tab strip: `role="tablist"` + `role="tab"` + `aria-selected` + `tabIndex` 0/-1. Arrow keys + Home/End funcionam.
  - Secções lista: `<h2>` com contagem `(N)`, semântica adequada.
  - Modal (reutilizado da 2.8): `role="dialog"` + `aria-modal="true"` + focus trap + Escape fecha + foco restaurado no opener.
  - Not-found region: `aria-live="polite"`.
  - Progress bar: `role="progressbar"` + `aria-valuenow` + `aria-valuemin="0"` + `aria-valuemax="100"` + `aria-label="Progresso do projecto"`.
  - Sem aria warnings em axe-core spot-check manual durante QA gate.

- **AC13:** **Testes Vitest + Testing Library** cobrem (12-15 cenários):
  - **T1 — Render base com dados**: projecto active + 3 tasks (1 todo, 1 in-progress, 1 done) → header visível com nome, status badge "ACTIVO", progress "33% concluído", tab "Lista" active por defeito, tasks agrupadas por status.
  - **T2 — Estado Loading projeto**: `useLiveQuery` retorna `undefined` para projecto → skeleton loading visível, `aria-busy="true"`.
  - **T3 — Not Found**: `getProject(id)` retorna `undefined` após carregamento → mensagem "Projecto não encontrado." + botão "Voltar aos projectos" visíveis.
  - **T4 — Empty state tasks**: projecto existe, `useTasks({projectId})` retorna `[]` → mensagem "Sem tarefas neste projecto." visível na sub-vista Lista.
  - **T5 — Tab switch Lista → Kanban**: clicar tab "Kanban" → `KanbanBoard` renderiza com tasks filtradas; tab "Lista" desaparece; tab "Kanban" tem `aria-selected="true"`.
  - **T6 — Tab switch Kanban → Lista**: clicar tab "Lista" → lista volta; Kanban desaparece; tab "Lista" tem `aria-selected="true"`.
  - **T7 — Progress bar**: 0 tasks done de 4 → "0% concluído"; 4 tasks done de 4 → "100% concluído"; 1 done de 3 → "33% concluído".
  - **T8 — Editar projecto via modal**: click "Editar" → modal abre com `role="dialog"` e `aria-modal="true"`; campos pré-preenchidos com dados do projecto; submit chama `updateProject(id, patch)` 1x; modal fecha.
  - **T9 — Modal a11y smoke**: modal aberto → focus no primeiro input (nome); `fireEvent.keyDown(document, { key: 'Escape' })` → modal fecha.
  - **T10 — Escape global fecha página**: `fireEvent.keyDown(document, { key: 'Escape' })` com modal fechado → `routerBack` chamado 1x.
  - **T11 — Arrow keys no tab strip**: `fireEvent.keyDown(tabList, { key: 'ArrowRight' })` com "Lista" active → "Kanban" fica active (`aria-selected="true"`); `ArrowLeft` volta à "Lista".
  - **T12 — Toast de erro**: `updateProject` rejeita → toast PT-PT visível com `role="status"`.

- **AC14:** Quality gates locais passam em `imersao-tools/nexus/v2/`:
  - `npm run lint` — exit 0.
  - `npm run typecheck` — exit 0.
  - `npm run test:unit` — todos os testes existentes (511 pós-2.8) + os ~12-15 novos passam.
  - `npm run build` — exit 0 (nova rota `/projectos/[id]` aparece no manifesto Next.js).

- **AC15:** Coverage de `app/(app)/projectos/[id]/` + componentes novos da 2.9 >= **70%** lines via `npm run test:coverage`. Agregado all-files mantém >= **60%** (NFR17). O glob `'app/(app)/projectos/**'` em `vitest.config.ts` já cobre recursivamente a rota `[id]` — verificar durante T9.5; adicionar linha explícita `// Story 2.9` se necessário.

---

## CodeRabbit Integration

| Item | Configuração |
|------|--------------|
| Story Type | **Frontend Vista** (rota dinâmica + fetch single + dual sub-vista lista+kanban) + secundário **Accessibility** (tab strip WAI-ARIA + progress bar + not-found region) + terciário **Repo Isolation** (zero `db.projects.*` / `db.tasks.*` directos) |
| Complexity | Medium — rota dinâmica `[id]` nova, fetch single via `getProject()`, dual sub-vista lista+Kanban, reaproveitamento de 2 componentes existentes (`KanbanBoard`, `ProjectFormModal`), modificação cirúrgica de `ProjectCard` (prop `onView`), ~12-15 testes Vitest. Surface estimada ~5-7 ficheiros novos + 1-2 modificados. Sem DnD novo (reutiliza `KanbanBoard`). |
| Specialized Agents | `@ux-design-expert` (Uma — executor), `@dev` (Dex — quality gate) |
| Quality Gate Tasks | Pre-Commit (Uma): `coderabbit --prompt-only -t uncommitted` · Pre-PR (Dex/`@devops`): `coderabbit --prompt-only --base main` |
| Self-Healing Configuration | mode: `light`, max_iterations: **2** (hard-stop — `EPIC-2.md` §8), severity_filter: `[CRITICAL, HIGH]`, behavior: `CRITICAL: auto_fix`, `HIGH: auto_fix (iter<2) else document_as_debt` |
| Focus Areas | **Repo isolation**: zero `db.projects.*` ou `db.tasks.*` directos — apenas `getProject/updateProject/archiveProject` do repo + `useTasks({projectId})` hook. **Tab strip a11y**: WAI-ARIA Tabs (arrow keys + Home/End) — replicar `TasksHeader.tsx:53-76`. **Progress bar a11y**: `role="progressbar"` + `aria-valuenow` + labels. **Not-found state**: distinguir loading (`undefined`) de not-found (`null` após carregamento completo). **PT-PT enforcement**: zero PT-BR, zero EN nos textos UI. **Date handling**: `format(parseISO(date), 'dd/MM/yyyy', { locale: pt })` — NUNCA `toISOString()`. **`ProjectFormModal` reuse**: sem duplicar modal; passar `initialValue` e `onSubmit` correctamente. **Modificação cirúrgica `ProjectCard`**: prop `onView` mínima, sem quebrar testes existentes T1-T12 da Story 2.8. |
| Risco | Médio — distinguir loading vs not-found é subtil em `useLiveQuery` (retorna `undefined` enquanto carrega, depois `undefined` se não existe → necessita de flag `isLoaded`). Precedente: `useProjects` retorna `undefined` no 1º render. Mitigação: `const [isLoaded, setIsLoaded] = useState(false)` + `useEffect` quando projecto resolve. |
| **Local CLI skip** | CodeRabbit corre via **integração GitHub no PR** (server-side, automático). Convenção Nexus v2 — ver `PO-VALIDATION-STORY-2.1.md §7`. |

---

## Tasks / Subtasks

- [x] **T1.** Preparação e leitura de contexto (pré-implementação)
  - [x] T1.1 Confirmar `main` actualizado contém closure 2.8 (`3eeefaf9`) e squash 2.8 (`bebbd530`) → criar branch `feature/2.9-vista-projecto`
  - [x] T1.2 Ler `components/projectos/ProjectCard.tsx` — confirmar interface actual e identificar ponto de extensão para prop `onView` (A8, AC10)
  - [x] T1.3 Ler `components/tarefas/KanbanBoard.tsx:61-71` — confirmar `KanbanBoardProps` (tasks, projects, tagsLookup) para reutilização em AC5
  - [x] T1.4 Ler `components/tarefas/TaskRow.tsx` — A7 resolvido: criar `ProjectTaskRow.tsx` simplificado (4 razões — ver Dev Agent Record)
  - [x] T1.5 Confirmar pattern `tags = useLiveQuery(() => listTags(), [])` (precedente `tarefas/page.tsx:69`) — sem `useTags` hook
  - [x] T1.6 Confirmar que `vitest.config.ts` glob `'app/(app)/projectos/**'` cobre `[id]` recursivamente — confirmado via coverage report (`...projectos/[id]` 99.31% lines)

- [x] **T2.** Modificação cirúrgica `ProjectCard.tsx` + `ProjectsGrid.tsx` (AC10)
  - [x] T2.1 Adicionar prop opcional `onView?: (id: string) => void` a `ProjectCardProps`
  - [x] T2.2 Card inteiro fica clicável via `onClick`/`onKeyDown` no `<article>` — kebab menu marcado com `data-kebab-menu="true"` + `stopPropagation` no trigger para evitar navegação ao clicar acções
  - [x] T2.3 `ProjectsGrid.tsx` recebe `onView?` e passa para `ProjectCard`
  - [x] T2.4 `app/(app)/projectos/page.tsx` define `handleView` (useCallback) que chama `router.push(\`/projectos/\${id}\`)`
  - [x] T2.5 Testes T1-T12 da Story 2.8 continuam a PASS (13/13)

- [x] **T3.** Criar rota dinâmica `/projectos/[id]` e componente `ProjectDetailHeader.tsx`
  - [x] T3.1 Criada pasta `app/(app)/projectos/[id]/` e ficheiro `page.tsx` com `'use client'`
  - [x] T3.2 `useParams()` + `useLiveQuery` com `db.projects.where('id').equals(id).count()` para distinguir loading (undefined) vs not-found (0) — abordagem recomendada nas Dev Notes
  - [x] T3.3 `useTasks({ projectId: id })` reactivo
  - [x] T3.4 `useMemo` para `progress` (A6) + `tagsLookup` `Map<string, Tag>` para `KanbanBoard`
  - [x] T3.5 Escape global → `router.back()`, suprimido se modal aberto
  - [x] T3.6 `components/projectos/ProjectDetailHeader.tsx` — h1 + status badge + datas + progress bar + Editar + Esc·Voltar (forwardRef para Edit button)
  - [x] T3.7 Progress bar com `role="progressbar"` + `aria-valuenow` + `aria-valuemin="0"` + `aria-valuemax="100"` + `aria-label="Progresso do projecto"` (AC12)
  - [x] T3.8 Botão "Editar" abre modal — `forwardRef` em `ProjectDetailHeader` expõe o button para `editButtonRef` da page (restaurar foco ao fechar)
  - [x] T3.9 Botão "Esc · Voltar" + listener global Escape chamam `router.back()`
  - [x] T3.10 Header sticky com glassmorphism `rgba(4,4,10,0.92)` + `blur(12px)`

- [x] **T4.** Tab strip de vistas + sub-vista Lista (AC3, AC4)
  - [x] T4.1 Tab strip WAI-ARIA (2 tabs) com arrow keys + Home/End + `aria-selected` + `tabIndex` 0/-1
  - [x] T4.2 Sub-vista Lista: secções agrupadas por status em ordem `STATUS_SECTION_ORDER`
  - [x] T4.3 Cada secção `<h2>` PT-PT + contador `(N)`
  - [x] T4.4 A7 — Criado `ProjectTaskRow.tsx` em vez de reutilizar `TaskRow` (razões em Dev Agent Record)
  - [x] T4.5 D3 — Criado `lib/tarefas/colors.ts` com `PRIORITY_COLORS` + `STATUS_COLORS` + `PRIORITY_LABELS_PT` + `STATUS_LABELS_PT` + `STATUS_SECTION_ORDER` + `badgeStyle` helper (usado apenas pelo novo `ProjectTaskRow`; `TaskRow`/`KanbanCard` mantêm cópias inline para zero alteração das Stories 2.3/2.4)
  - [x] T4.6 Empty state PT-PT "Sem tarefas neste projecto." + CTA `router.push('/tarefas')`

- [x] **T5.** Sub-vista Kanban (AC5)
  - [x] T5.1 `<KanbanBoard tasks={tasks} projects={[project]} tagsLookup={tagsLookup} />` na tab Kanban
  - [x] T5.2 `tasks` filtradas via `useTasks({projectId: id})` no parent
  - [x] T5.3 `projects` prop: `[project]` (array com apenas o projecto actual)
  - [x] T5.4 `tagsLookup` via `useLiveQuery(() => listTags())` + `useMemo` para `Map<string, Tag>`

- [x] **T6.** Estados especiais (AC6, AC7, AC8)
  - [x] T6.1 Loading skeleton — `aria-busy="true"` + `aria-label="A carregar projecto"` + pulse animation
  - [x] T6.2 Not Found — glass card centrado + "Projecto não encontrado." + botão "Voltar aos projectos" + `aria-live="polite"`
  - [x] T6.3 Toast erro editar — `role="status"` + `aria-live="assertive"` + Magenta 0.15 + auto-dismiss 4s

- [x] **T7.** Integração `ProjectFormModal` para editar (AC9)
  - [x] T7.1-T7.4 — Modal reutilizado em modo edit; submit chama `updateProject`; foco restaurado no botão Editar via `editButtonRef` exposto por `forwardRef` no header

- [x] **T8.** Testes Vitest + Testing Library (AC13)
  - [x] T8.1-T8.7 — 15 cenários T1-T13 (com T7a/T7b/T7c expandidos) em `tests/unit/app/projectos/id/page.test.tsx`. Suite passa 15/15. Suite global 529/529 PASS.

- [x] **T9.** Quality gates locais (AC14, AC15)
  - [x] T9.1 `npm run lint` — exit 0 (apenas warning pré-existente em `route.ts`, fora-scope)
  - [x] T9.2 `npm run typecheck` — exit 0
  - [x] T9.3 `npm run test:unit` — 529/529 PASS
  - [x] T9.4 `npm run build` — exit 0; rota `/projectos/[id]` no manifest (4.29 kB / 182 kB First Load)
  - [x] T9.5 `npm run test:coverage` — agregado 88.76%, `app/(app)/projectos/[id]/` 99.31%, `components/projectos/` 91.41%, `lib/tarefas/colors.ts` 100%. Glob existente cobriu `[id]` recursivamente — sem alteração de `vitest.config.ts`.

- [x] **T10.** Story file maintenance — Dev Agent Record + File List + Change Log v0.3 (este commit)

- [ ] **T11.** Delegar push a `@devops *push` após quality gate `@dev` PASS

---

## Dev Notes

### Ficheiros a ler antes de implementar

| Ficheiro | Porquê |
|----------|--------|
| `imersao-tools/nexus/v2/lib/db/repos/projects.ts` | `getProject(id)` + `updateProject` — **NÃO alterar** |
| `imersao-tools/nexus/v2/lib/db/repos/tasks.ts` | `listTasks({projectId?})` — confirmar assinatura — **NÃO alterar** |
| `imersao-tools/nexus/v2/hooks/useTasks.ts` | `useTasks({projectId?})` — assinatura confirmada — **NÃO alterar** |
| `imersao-tools/nexus/v2/hooks/useProjects.ts` | Referência de padrão para `useLiveQuery` com loading state — **NÃO alterar** |
| `imersao-tools/nexus/v2/types/db.ts:52-77` | Interfaces `Task` + `Project` (confirmadas estáveis) — **NÃO alterar** |
| `imersao-tools/nexus/v2/components/projectos/ProjectCard.tsx` | Ponto de extensão prop `onView` (T2) |
| `imersao-tools/nexus/v2/components/projectos/ProjectsGrid.tsx` | Passar `onView` callback (T2.3) |
| `imersao-tools/nexus/v2/app/(app)/projectos/page.tsx` | Adicionar `handleView` handler (T2.4) |
| `imersao-tools/nexus/v2/components/projectos/ProjectFormModal.tsx` | Reaproveitar — confirmar interface de props (mode, initialValue, onClose, onSubmit) |
| `imersao-tools/nexus/v2/components/tarefas/KanbanBoard.tsx:61-71` | `KanbanBoardProps` — tasks, projects, tagsLookup |
| `imersao-tools/nexus/v2/components/tarefas/TaskRow.tsx` | Avaliar reutilização directa vs `ProjectTaskRow` (A7) |
| `imersao-tools/nexus/v2/components/tarefas/TasksHeader.tsx:35-76` | **Padrão tab strip WAI-ARIA** — replicar arrow keys + Home/End |
| `imersao-tools/nexus/v2/app/(app)/tarefas/page.tsx:101-107` | **Padrão Escape global** → `router.back()` |
| `imersao-tools/nexus/v2/tests/unit/app/projectos/page.test.tsx` | **Padrão de teste** Vitest + TL + `vi.hoisted` mocks + `fake-indexeddb` |
| `imersao-tools/nexus/v2/vitest.config.ts` | Confirmar cobertura `app/(app)/projectos/**` inclui `[id]` recursivamente |
| `.claude/rules/design-system-ia-avancada.md` | Tokens visuais (glassmorphism, paleta, tipografia) |

### Loading vs Not-Found — distinção crítica

```typescript
// page.tsx — padrão recomendado para distinguir loading de not-found
// (problema: useLiveQuery retorna `undefined` tanto enquanto carrega como se não existe)

const [isLoaded, setIsLoaded] = useState(false);
const projectRaw = useLiveQuery(() => getProject(id), [id]);

useEffect(() => {
  if (projectRaw !== undefined || /* dexie settled */ isLoaded) {
    setIsLoaded(true);
  }
}, [projectRaw]);

// Alternativa mais robusta: usar um segundo useLiveQuery que devolve count
// para saber se o id existe:
const projectExists = useLiveQuery(
  () => db.projects.where('id').equals(id).count(),
  [id]
);
// projectExists === undefined → loading; projectExists === 0 → not found; projectExists === 1 → existe
```

> Uma decide a implementação exacta durante T3.2. O critério de aceitação é: T3 (not-found test) PASS. A abordagem de `count()` é mais robusta e recomendada.

### Layout de ficheiros esperado

```
app/(app)/projectos/
├── page.tsx                              # EXISTENTE (Story 2.8) — adicionar handleView (T2.4)
└── [id]/
    └── page.tsx                          # NOVO — rota dinâmica vista projecto

components/projectos/                     # EXISTENTE (Story 2.8)
├── ProjectCard.tsx                       # MODIFICAR — adicionar prop onView?: (id: string) => void (T2.1-T2.2)
├── ProjectDetailHeader.tsx               # NOVO — h1 + status badge + datas + progress bar + botões
├── ProjectFormModal.tsx                  # EXISTENTE — reaproveitar sem alteração (AC9)
├── ProjectsGrid.tsx                      # MODIFICAR — passar onView callback a ProjectCard (T2.3)
└── ProjectsHeader.tsx                    # EXISTENTE — sem alteração

(Se A7 exigir componente novo:)
components/projectos/
└── ProjectTaskRow.tsx                    # CONDICIONAL — simplificado se TaskRow.tsx não for reutilizável

(Se D3 for resolvida:)
lib/tarefas/
└── colors.ts                             # CONDICIONAL — extrai PRIORITY_COLORS (A11)

tests/unit/
├── app/projectos/
│   ├── page.test.tsx                     # EXISTENTE (Story 2.8, T1-T12) — não tocar
│   └── id/
│       └── page.test.tsx                 # NOVO — 12-15 cenários T1-T12 da Story 2.9

(SEM alteração adicional:)
- lib/db/repos/projects.ts               (Stories 2.1+2.8)
- lib/db/repos/tasks.ts                  (Story 2.1)
- hooks/useTasks.ts                      (Story 2.1)
- hooks/useProjects.ts                   (Story 2.1)
- lib/db/schemas.ts                      (Story 2.1)
- types/db.ts                            (Story 2.1)
- components/tarefas/KanbanBoard.tsx     (Story 2.4 — sem modificação)
- components/tarefas/TasksHeader.tsx     (Story 2.3 — sem modificação)
- components/tarefas/TaskRow.tsx         (Story 2.3 — sem modificação, salvo A7)
```

### Padrão de `useLiveQuery` com `getProject` (referência)

```typescript
// app/(app)/projectos/[id]/page.tsx
import { useParams, useRouter } from 'next/navigation';
import { useLiveQuery } from 'dexie-react-hooks';
import { getProject } from '@/lib/db/repos/projects';
import { useTasks } from '@/hooks/useTasks';

export default function ProjectDetailPage() {
  const { id } = useParams<{ id: string }>();
  const router = useRouter();

  // Distinção loading vs not-found via count
  const projectExists = useLiveQuery(
    () => db.projects.where('id').equals(id).count(),
    [id]
  );
  const project = useLiveQuery(() => getProject(id), [id]);
  const tasks = useTasks({ projectId: id });

  const isLoading = projectExists === undefined;
  const isNotFound = projectExists === 0;

  // Progress (A6)
  const progress = useMemo(() => {
    if (!tasks || tasks.length === 0) return 0;
    const done = tasks.filter((t) => t.status === 'done').length;
    return Math.round((done / tasks.length) * 100);
  }, [tasks]);

  // Escape global
  useEffect(() => {
    function onKey(e: KeyboardEvent) {
      if (e.key === 'Escape' && !editModalOpen) router.back();
    }
    document.addEventListener('keydown', onKey);
    return () => document.removeEventListener('keydown', onKey);
  }, [router, editModalOpen]);

  if (isLoading) return <ProjectDetailSkeleton />;
  if (isNotFound) return <ProjectNotFound onBack={() => router.push('/projectos')} />;

  return (
    <main>
      <ProjectDetailHeader
        project={project!}
        progress={progress}
        onEdit={() => setEditModalOpen(true)}
        onBack={() => router.back()}
        editButtonRef={openerRef}
      />
      {/* Tab strip Lista | Kanban */}
      {/* Sub-vistas */}
      {editModalOpen && (
        <ProjectFormModal
          mode="edit"
          initialValue={project!}
          onClose={() => { setEditModalOpen(false); openerRef.current?.focus(); }}
          onSubmit={handleEditSubmit}
        />
      )}
    </main>
  );
}
```

---

## Dependencies

| Dependência | Story | Estado |
|-------------|-------|--------|
| Schema `projects` + `tasks` Dexie + repos | 2.1 | Done (CLOSED 15/05) |
| Rota `/projectos` + `ProjectFormModal` + `ProjectsGrid` + `ProjectCard` | 2.8 | Done (MERGED main 17/05) |
| `KanbanBoard.tsx` com DnD + 4 colunas | 2.4 | Done (MERGED main 16/05) |

**Sem dependência de:** Story 2.6 (tags global — independente), Story 2.7 (recorrência — independente).

---

## Out of Scope

| Item | Razão |
|------|-------|
| Criar tarefas inline na vista projecto | Exigiria `ProjectTaskFormModal` nova — fora de FR31. CTA redirige para `/tarefas`. |
| Hard delete de projecto | D6 do Epic 2 — cascata `Task.projectId` requer decisão de produto (set null / bloquear / cascade). |
| Filtrar tasks por status dentro da vista projecto | FR31 apenas pede lista + Kanban; filtros adicionais são Epic 3+ ou melhoria futura. |
| Editar tasks individuais dentro da vista projecto | Redirige para `/tarefas` — inline edit exigiria modal de tarefa novo fora de scope. |
| Notas / comentários no projecto | Não definidos no PRD (sem FR correspondente). |
| Tags na vista projecto | Story 2.6 (tags global) é independente — esta story não espera por 2.6. `KanbanBoard` recebe `tagsLookup` (pode ser mapa vazio se tags ainda não implementadas). |
| Recorrência de tasks na vista projecto | Story 2.7 (motor de recorrência) — independente. |
| D3 (refactor `PRIORITY_COLORS` para `lib/tarefas/colors.ts`) | Condicional: apenas se Story 2.9 criar `ProjectTaskRow.tsx`. Senão, reportar retrospectiva Epic 2. |
| M1 (`aria-describedby` no select status do `ProjectFormModal`) | D6 do Epic 2 (Baixa). Fix numa story futura ou no closure commit da retrospectiva Epic 2. |

---

## Testing Requirements

| Alvo | Coverage mínima | Justificação |
|------|-----------------|--------------|
| `app/(app)/projectos/[id]/page.tsx` | >= 70% lines | Alinha com AC13 + AC15 (precedente 2.8: page 73.71%) |
| `components/projectos/ProjectDetailHeader.tsx` | >= 70% lines | Novo componente — alinha com threshold mínimo |
| Modificações em `ProjectCard.tsx` + `ProjectsGrid.tsx` | Testes T1-T12 da Story 2.8 devem continuar PASS | Prop opcional `onView` não deve quebrar behaviour existente |
| `ProjectTaskRow.tsx` (se criado) | >= 85% lines | Padrão component Epic 2 (precedente 2.8: components 91.76%) |
| Agregado all-files | >= 60% | NFR17 — manter threshold global |

**Não-Tested aceite (edge cases):**
- Comportamento de `useLiveQuery` com IndexedDB corrompido (fora de controlo da aplicação).
- DnD manual em Kanban (E2E Playwright — D5 do Epic 2, aceite como débito).

**Not-tested bloqueadores (se aparecerem commits que toquem estes paths):**
- Qualquer alteração a `vitest.config.ts` — exige evidência de `npm run test:unit` PASS local.
- Qualquer alteração a `playwright.config.ts` — exige evidência de `--list` não vazio.

---

## File List (esperado após implementação)

| Ficheiro | Operação | Story |
|----------|----------|-------|
| `v2/app/(app)/projectos/[id]/page.tsx` | CRIAR | 2.9 |
| `v2/components/projectos/ProjectDetailHeader.tsx` | CRIAR | 2.9 |
| `v2/tests/unit/app/projectos/id/page.test.tsx` | CRIAR | 2.9 |
| `v2/components/projectos/ProjectCard.tsx` | MODIFICAR (prop onView) | 2.9 |
| `v2/components/projectos/ProjectsGrid.tsx` | MODIFICAR (passar onView) | 2.9 |
| `v2/app/(app)/projectos/page.tsx` | MODIFICAR (handleView + router.push) | 2.9 |
| `v2/components/projectos/ProjectTaskRow.tsx` | CRIAR (condicional A7) | 2.9 |
| `v2/lib/tarefas/colors.ts` | CRIAR (condicional A11/D3) | 2.9 |
| `v2/vitest.config.ts` | MODIFICAR (condicional — linha Story 2.9 se glob não cobrir) | 2.9 |
| `docs/stories/2.9.story.md` | CRIAR (este ficheiro) | 2.9 |

---

## PO Validation (Pax) — 2026-05-18

**Decisão:** GO — Score 10/10
**Validador:** Pax (`@po`)
**Status alterado:** `Draft` → `Ready`

### 10-Point Checklist

| # | Critério | Verdict | Notas |
|---|----------|---------|-------|
| 1 | Story clarity | PASS | User Story bem formada (Eurico / vista projecto centralizada / cumprir FR31). |
| 2 | AC completeness | PASS | 15 ACs cobrem rota, header, tabs, lista, kanban, loading, not-found, empty, edit, navegação, PT-PT, a11y, testes, quality gates, coverage. |
| 3 | AC testability | PASS | AC13 mapeia 12 cenários T1-T12 com inputs/outputs claros. AC14/AC15 com comandos e thresholds numéricos. |
| 4 | Dependencies | PASS | Stories 2.1, 2.4, 2.8 confirmadas em `docs/stories/completed/`. Infraestrutura verificada filesystem: `getProject(id) → Project \| undefined`, `useTasks({projectId})`, `KanbanBoardProps {tasks, projects, tagsLookup}`, `ProjectFormModalProps {mode, initialValue, onClose, onSubmit}`. Todas as assinaturas batem. |
| 5 | Out of scope | PASS | 9 itens explicitamente fora (create inline, hard delete D6, filtros, edit task inline, notas, tags, recorrência, D3/M1 condicionais). |
| 6 | Dev Notes | PASS | 16 ficheiros referenciados com linhas exactas (TasksHeader.tsx:35-76, tarefas/page.tsx:101-107). Padrão loading-vs-not-found resolvido via `count()`. Code skeleton de page.tsx presente. |
| 7 | File List | PASS | Tabela com 10 ficheiros (3 CRIAR, 3 MODIFICAR, 2 CONDICIONAL, 1 MODIFICAR condicional, 1 story file). |
| 8 | Testing requirements | PASS | 12-15 cenários listados; coverage ≥70% page+components, ≥85% ProjectTaskRow (se criado), ≥60% agregado (NFR17). Not-tested bloqueador (`vitest.config.ts`, `playwright.config.ts`) explicitado — respeita `not-tested-trailer-rules.md`. |
| 9 | Constitutional compliance | PASS | Cada AC traça a FR31 (PRD §6.5 L163) ou padrão Stories 2.3/2.4/2.5/2.8. AUTO-DECISIONs A1-A12 todas justificadas com "Alternativa X descartada" + razão (No Invention Artigo IV). A1 recusa explicitamente criar campo `slug` novo. |
| 10 | Estimation/scope | PASS | 5 story points (medium). 11 tasks, 12-15 testes, 5-7 ficheiros novos + 1-2 modificados, sem DnD novo (reutiliza KanbanBoard). Comparável à Story 2.8 (CRUD + modal + 12 testes, merged em Iter 1). |

### Verificações Adicionais

| Regra | Status | Notas |
|-------|--------|-------|
| PT-PT enforcement | PASS | AC11 explicita strings PT-PT exactas. Zero PT-BR/EN. Datas `dd/MM/yyyy` locale `pt`. |
| Separation of Roles (A6) | PASS | Executor Uma (`@ux-design-expert`) ≠ Quality gate Dex (`@dev`). Trace ao `EPIC-2.md` §5. |
| Mock Protocol Fidelity (A1) | N/A | CRUD interno via Dexie + `fake-indexeddb` — sem mocks de protocolos externos. |
| Not-Tested Trailer Rules (A3) | PASS | Paths bloqueadores (`vitest.config.ts`, `playwright.config.ts`) requerem evidência se tocados. |
| Padrões 2.8 reaproveitados | PASS | `ProjectFormModal`, `KanbanBoard`, Zod schemas, repo isolation enforced. |
| D6 (cascade delete) tratado | PASS | Out-of-scope com razão técnica documentada. |
| D7 (PT-BR fallback classifier) | N/A | Não toca classifier — correctamente sem referência. |

### Riscos não-bloqueadores

1. **A7 em aberto** (TaskRow reutilizável vs ProjectTaskRow novo) — Uma decide durante T1.4 após ler `TaskRow.tsx`. Aceitável porque exige inspecção prévia. Os 2 caminhos estão documentados com impacto downstream (A11/D3).
2. **Loading vs Not-Found** — story propõe 2 abordagens; recomenda `count()` (mais robusta). AC T3 valida o resultado.
3. **A12 vitest.config glob** — verifiquei localmente: `'app/(app)/projectos/**'` já cobre `[id]` recursivamente. T9.5 deve confirmar via coverage report e omitir linha extra.

### Sugestões opcionais (não bloqueiam GO)

- **Iter cap CodeRabbit**: max 2 iterações é hard-stop. Se A7 levar Uma a criar `ProjectTaskRow.tsx` + `lib/tarefas/colors.ts`, a surface aumenta. Recomendação: Uma decide A7 no T1 antes de implementar para evitar surprise scope creep numa segunda iteração CR.
- **Testes T11 (arrow keys tab strip)**: garantir que o `fireEvent.keyDown` apanha tanto `'ArrowRight'` como `'ArrowLeft'` + `'Home'` + `'End'` — replicar exactamente o padrão de `TasksHeader.test.tsx` se existir.
- **Skeleton no Loading**: AC6 pede skeleton do header. Sugestão: extrair `<ProjectDetailSkeleton />` como sub-componente para reduzir noise no `page.tsx` e facilitar test isolation.

### Próxima acção

```
@dev *develop 2.9
```

ou se Uma quiser pegar directamente como `@ux-design-expert` (assignment primário):

```
@ux-design-expert *develop 2.9
```

---

## Dev Agent Record (Dex `@dev` — 2026-05-18)

### Resolução A7 — `ProjectTaskRow` vs `TaskRow`

**Decisão:** criar `components/projectos/ProjectTaskRow.tsx` (novo) em vez de reutilizar `components/tarefas/TaskRow.tsx`.

**Razões (4):**
1. `TaskRow` é renderizado como `<tr>` — exige `<table>` wrapper. A vista projecto Lista usa lista vertical agrupada por status (`<ul>` dentro de `<section>`), não tabela com 8 colunas.
2. `TaskRow` recebe handlers obrigatórios `onToggleDone` e `onDelete` — fora-de-scope na vista projecto (AC8/AC10 redirigem para `/tarefas` para qualquer mutação).
3. `TaskRow` mostra coluna "Projecto" — informação redundante numa vista de projecto único.
4. `TaskRow` traz `TaskKebabMenu` com lógica WAI-ARIA — não necessário aqui.

`ProjectTaskRow` é read-only: 4 campos visuais (título · prioridade · due · status) em layout `display: grid; grid-template-columns: 1fr auto auto auto`, reaproveitando paletas e badges via `lib/tarefas/colors.ts`.

### Resolução D3 — Extracção de `PRIORITY_COLORS`

Criado `lib/tarefas/colors.ts` com:
- `PRIORITY_COLORS`, `STATUS_COLORS` (paletas badge)
- `PRIORITY_LABELS_PT`, `STATUS_LABELS_PT` (rótulos PT-PT)
- `STATUS_SECTION_ORDER` (ordem canónica das secções na vista Lista)
- `badgeStyle(palette)` helper

**Consumidores actuais:** `ProjectTaskRow.tsx` (novo).
**Consumidores não-tocados:** `TaskRow.tsx` (Story 2.3) e `KanbanCard.tsx` (Story 2.4) mantêm cópias inline para zero modificação dessas stories. Consolidação completa fica para um closure commit do Epic 2 (D3 reportada como parcialmente resolvida).

### Loading vs Not-Found

Adoptada a abordagem recomendada nas Dev Notes (mais robusta que `isLoaded` flag):

```typescript
const projectExists = useLiveQuery(
  () => db.projects.where('id').equals(id).count(),
  [id],
);
// projectExists === undefined → loading
// projectExists === 0         → not found
// projectExists === 1         → existe
```

Permite distinguir os 3 estados sem state local extra, e o `useLiveQuery` reage à inserção/eliminação do projecto.

### Card click area (AC10)

Para evitar conflito entre click no card (navegar) e click no kebab (acções), `KebabMenu` ficou marcado com `data-kebab-menu="true"` no wrapper + `stopPropagation()` no trigger button. O handler do card filtra `e.target.closest('[data-kebab-menu="true"]')` e ignora se positivo. O card também é focusable (`tabIndex={0}`) e responde a `Enter`/`Space` quando `onView` está presente.

### Tab strip a11y

Tabs com `role="tab"` + `aria-selected` + `aria-controls={tab-panel-id}` + `tabIndex={active ? 0 : -1}`. Arrow keys ←/→ + Home/End implementados no `handleTabKeyDown`, replicando o padrão de `TasksHeader.tsx:54-77`.

### Quality Gates locais (output)

| Comando | Resultado |
|---------|-----------|
| `npm run lint` | exit 0 (1 warning pré-existente fora-scope) |
| `npm run typecheck` | exit 0 |
| `npm run test:unit` | 529/529 PASS (40 ficheiros) |
| `npm run build` | exit 0; `/projectos/[id]` 4.29 kB / 182 kB |
| `npm run test:coverage` | All files 88.76% lines; `[id]/` 99.31%; `components/projectos/` 91.41%; `colors.ts` 100% |

### Notas para `@qa`

1. **Unhandled rejection silenciado em T12**: o `ProjectFormModal` re-throws erros do `onSubmit` (precedente Story 2.8). Em jsdom isso emerge como unhandled rejection; o teste T12 silencia com `process.on('unhandledRejection')` listener local e remove-o em `finally`. Pode ser apontado como CONCERN se preferires um patch mais permanente no modal — discussão para closure commit do Epic 2.
2. **`ProjectCard.tsx` coverage 86.81%** (vs 100% pré-2.9). A descida deve-se aos novos branches do `onView` flow + handlers de keyboard. Linhas não cobertas (391-395) são branches do `onKeyDown` para eventos de teclado fora de Enter/Space — testes T1-T12 da Story 2.8 não exercitam estes paths. Aceitável dado que o behaviour está coberto indirectamente nos testes T1-T12 da 2.9 que validam navegação via click.
3. **DnD em Kanban via vista projecto** não tem teste E2E directo (reutiliza `KanbanBoard` da Story 2.4 — D5 do Epic 2 aceite como débito).
4. **`ProjectFormModal` reutilizado tal-qual** — zero modificação. Submit chama `onSubmit(parsed)` e re-throws se rejeita.
5. **PT-PT validado**: zero "você", "sua", "em um" no código novo. Todas as strings: tabs (Lista/Kanban), secções (Por fazer/Em curso/Bloqueadas/Concluídas — via `STATUS_LABELS_PT` de Story 2.3), badges status (ACTIVO/PAUSADO/CONCLUÍDO), progress (N% concluído), empty (Sem tarefas neste projecto), not-found (Projecto não encontrado / Voltar aos projectos), botão (Esc · Voltar / Editar).

---

## Change Log

| Versão | Data | Autor | Descrição |
|--------|------|-------|-----------|
| v0.1 | 2026-05-18 | River (`@sm`) | Draft inicial — FR31 Vista projecto. Baseado em padrões Stories 2.3+2.4+2.8. 15 ACs, 11 tasks/subtasks, 4 AUTO-DECISIONs principais. |
| v0.2 | 2026-05-18 | Pax (`@po`) | Validação 10/10 GO. Status `Draft` → `Ready`. Secção PO Validation adicionada com checklist completo, verificações adicionais, riscos não-bloqueadores e sugestões opcionais. Infraestrutura (repos, hooks, KanbanBoard, ProjectFormModal) verificada no filesystem — assinaturas confirmadas. |
| v0.3 | 2026-05-18 | Dex (`@dev`) | Implementação YOLO. A7 resolvido (`ProjectTaskRow` novo); D3 parcialmente resolvido (extracção `lib/tarefas/colors.ts` consumida só pelo novo componente). 5 ficheiros novos, 3 ficheiros modificados cirurgicamente, 15 tests novos (todos PASS), coverage `[id]/page.tsx` 99.31% / `components/projectos/` 91.41%. Status `Ready` → `Ready for Review`. Aguarda quality gate `@dev` (auto-review) e depois `@qa *review 2.9`. |

---

## File List (implementação real)

| Ficheiro | Operação | Tamanho |
|----------|----------|---------|
| `v2/app/(app)/projectos/[id]/page.tsx` | CRIADO | 17.4 KB |
| `v2/components/projectos/ProjectDetailHeader.tsx` | CRIADO | 7.5 KB |
| `v2/components/projectos/ProjectTaskRow.tsx` | CRIADO | 3.1 KB |
| `v2/lib/tarefas/colors.ts` | CRIADO | 2.7 KB |
| `v2/tests/unit/app/projectos/id/page.test.tsx` | CRIADO | 16.5 KB |
| `v2/components/projectos/ProjectCard.tsx` | MODIFICADO (prop `onView` + handlers click/keyboard + `stopPropagation` no kebab) | — |
| `v2/components/projectos/ProjectsGrid.tsx` | MODIFICADO (passa `onView` a `ProjectCard`) | — |
| `v2/app/(app)/projectos/page.tsx` | MODIFICADO (`handleView` + `router.push('/projectos/{id}')`) | — |
| `docs/stories/2.9.story.md` | MODIFICADO (Dev Agent Record + File List + Change Log v0.3) | — |

**Sem modificação (verificado):**
- `v2/lib/db/repos/projects.ts`
- `v2/lib/db/repos/tasks.ts`
- `v2/hooks/useTasks.ts`
- `v2/hooks/useProjects.ts`
- `v2/types/db.ts`
- `v2/lib/db/schemas.ts`
- `v2/components/projectos/ProjectFormModal.tsx`
- `v2/components/projectos/ProjectsHeader.tsx`
- `v2/components/tarefas/KanbanBoard.tsx`
- `v2/components/tarefas/TaskRow.tsx`
- `v2/vitest.config.ts` (glob `app/(app)/projectos/**` já cobria `[id]` recursivamente)

---

*Story 2.9 preparada por River (`@sm`) em 2026-05-18. Validada por Pax (`@po`) 10/10 GO. Implementada por Dex (`@dev`) em modo YOLO 2026-05-18. Ancorada em `PRD-NEXUS-V2.md` §6.5 FR31 + `EPIC-2.md` §5 (Story 2.9). Padrões herdados: vista lista (Story 2.3), Kanban (Story 2.4), CRUD projectos (Story 2.8). Próxima acção: `@qa *review 2.9`.*
