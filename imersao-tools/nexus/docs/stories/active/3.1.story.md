# Story 3.1 — Schema finanças (Data Access Layer Dexie v3)

**Story ID:** 3.1
**Epic:** 3 — Finanças Completas
**Status:** Done
**Bloqueia:** 3.2 (categorias default), 3.3 (CRUD transações variáveis), 3.4 (CRUD recorrências), 3.5 (CRUD cartões), 3.6 (compras parceladas), 3.7 (vista mensal), 3.8 (vista cartões), 3.9 (vista património), 3.10 (geração diária), 3.11 (tools cérebro finanças)
**Bloqueada por:** Epic 1 (Cérebro Multi-Intent) — DONE, 10/10 em `main` @ `5514b310`. Schema Dexie `version(2)` — Story 2.1, Done.
**Estimativa:** 3-4h
**Tipo:** Database — Schema increment + Data Access Layer

---

## Executor Assignment

```
executor: "@data-engineer"
quality_gate: "@architect"
quality_gate_tools: [coderabbit, lint, typecheck, vitest]
```

> Trace: `EPIC-3.md` §5 (tabela de stories — Story 3.1). Respeita `separation-of-roles.md` (A6) — `executor != quality_gate`. `@data-engineer` (Dara) executa o incremento de schema + repos (domínio de dados); `@architect` (Aria) assina o quality gate (schema de domínio financeiro com implicações arquitecturais — paridade com Story 2.1 onde gate também foi agente arquitectural).

---

## Nota do `@sm` River — interpretação e resolução do [GAP-3.1]

### Contexto do [GAP-3.1]

`EPIC-3.md` §7 sinalizou explicitamente: "a Story 2.1 já criou uma tabela `recurrences` genérica — verificar em código se é reutilizável para finanças por `ownerType`, ou se precisa de extensão". Esta story resolve o gap com evidência directa do código.

### Evidência verificada (resolução do [GAP-3.1])

**Ficheiro:** `imersao-tools/nexus/v2/lib/db/client.ts` (linhas 76-79)
**Ficheiro:** `imersao-tools/nexus/v2/types/db.ts` (linhas 79-86)

```typescript
// client.ts:76-79 — Story 2.1 adicionou:
this.version(2).stores({
  recurrences: 'id, ownerType, ownerId, [ownerType+ownerId]',
  tags: 'id, name',
});

// types/db.ts:79-86 — interface Recurrence:
export interface Recurrence {
  id: string;
  rule: string; // RRULE string
  startDate: string;
  endDate: string | null;
  ownerType: 'task' | 'transaction' | 'habit' | 'reminder';
  ownerId: string;
}
```

**Conclusão do [GAP-3.1]: A tabela `recurrences` da Story 2.1 é REUTILIZÁVEL para finanças recorrentes (FR17) sem qualquer extensão de schema.**

O campo `ownerType` já contém o literal `'transaction'` (ver `types/db.ts:84`). A tabela foi desenhada explicitamente para ser genérica entre Epics 2/3/4 (`architecture-v2.md §6.2 L512-519`, `§16 L1128`; decisão registada na Story 2.1 Reconciliação R2 e Dev Notes). O motor de recorrência da Story 2.7 (`runRecurrenceEngine`) é igualmente genérico por `ownerType`.

**A Story 3.1 NÃO cria uma tabela `recurrences` separada para finanças.** Reutiliza a tabela `recurrences` existente com `ownerType: 'transaction'`. Não há duplicação nem extensão necessária.

### O que falta criar (version 3)

A `client.ts:28` já antecipa: "Epic 3 incrementa para version 3 (accounts, cards, installments, categories)". As interfaces `Account`, `Card`, `Transaction`, `Installment`, `Category` já existem em `types/db.ts:98-143` (criadas pela Story 0.3 para completar os tipos do domínio). O que falta é:

1. `this.version(3).stores({...})` com as 4 tabelas novas: `accounts`, `cards`, `installments`, `categories`
2. Notar que `transactions` JÁ EXISTE em `version(1)` (`client.ts:54`) com índices `id, accountId, cardId, category, date, recurrenceId, [accountId+date]` — não recriar
3. Repositórios tipados e schemas Zod para os 5 domínios financeiros (`accounts`, `cards`, `transactions`, `installments`, `categories`)
4. Hooks reactivos para os domínios principais

**[AUTO-DECISION] A1 — "version(3)" é o número correcto:** `version(1)` criou as tabelas base (Story 0.3/1.1); `version(2)` adicionou `recurrences` + `tags` (Story 2.1). Logo a próxima migração Dexie é `version(3)`. Decisão firme baseada no código actual — não requer escalação a `@architect`. (Razão: `client.ts:28` confirma explicitamente "Epic 3 incrementa para version 3".)

---

## User Story

Como `@data-engineer` (Dara), quero incrementar o schema Dexie para `version(3)` com as tabelas `accounts`, `cards`, `installments` e `categories`, confirmar que `transactions` já existe em `version(1)` com os índices correctos, e entregar schemas Zod + repositories tipados + hooks reactivos para os 5 domínios financeiros (`accounts`, `cards`, `transactions`, `installments`, `categories`), para que as Stories 3.2-3.11 do Epic 3 possam persistir/ler dados financeiros sem duplicar lógica de acesso a Dexie nem cair em invariantes inconsistentes.

---

## Contexto

`PRD-NEXUS-V2.md` §6.3 (FR16-FR19) e `EPIC-3.md` §4 definem o domínio Finanças. A Story 3.1 é a equivalente de schema para o Epic 3, seguindo o padrão já estabelecido pela Story 2.1 (Epic 2) e Story 1.1 (Epic 1): incremento Dexie aditivo + data access layer via repositórios tipados com validação Zod.

`architecture-v2.md` ADR-2 decidiu **Dexie 4 IndexedDB desde o dia 1**. A `client.ts:28` é explícita: "Epic 3 incrementa para version 3 (accounts, cards, installments, categories)". Cada Epic adiciona `this.version(N+1).stores({...})` sem reescrever o anterior — Dexie preserva os dados existentes no upgrade.

As interfaces `Account`, `Card`, `Transaction`, `Installment`, `Category` já existem em `types/db.ts:98-143` (criadas pela Story 0.3 para completar os tipos do schema futuro). Esta story cria a camada de acesso que as Stories 3.2-3.11 vão consumir.

O helper de formatação monetária PT-PT (`formatCurrency`) é criado aqui para reutilização — trace ao risco R3 do `EPIC-3.md` §5 e ao AC5 do Epic 3 ("valores em formato PT-PT `€1.234,56`"). Todas as vistas (3.3/3.7/3.8/3.9) reutilizam este helper.

---

## Reconciliação PRD ↔ Arquitectura

> `EPIC-3.md` §7 instrui: a arquitectura prevalece sobre a descrição do PRD; nenhum ADR é reaberto; divergências face à arquitectura escalam a `@architect` antes de implementar.

| # | `PRD §10` / `EPIC-3.md §5` diziam | Código real | Resolução para Story 3.1 |
|---|-----------------------------------|-------------|--------------------------|
| R1 | Story 3.1 lista `recurrences` como tabela a criar no schema de finanças | `recurrences` já existe em `version(2)` com `ownerType: 'task' \| 'transaction' \| 'habit' \| 'reminder'` (`types/db.ts:84`, `client.ts:77`) | Story 3.1 **NÃO recria** `recurrences`. Reutiliza a tabela existente com `ownerType: 'transaction'` para finanças recorrentes. Ver resolução [GAP-3.1] acima. |
| R2 | Story 3.1 lista `transactions` como tabela a criar | `transactions` já existe em `version(1)` (`client.ts:54`) com índices `id, accountId, cardId, category, date, recurrenceId, [accountId+date]` | Story 3.1 **NÃO recria** `transactions`. Confirma que os índices de `version(1)` cobrem as queries das Stories 3.3/3.7/3.8. Se faltarem índices, abre sub-task explícita. |
| R3 | PRD §10 Epic 3 refere "schema `accounts`, `cards`, `transactions`, `recurrences`, `installments`, `categories`" | Só 4 das 6 tabelas precisam ser criadas — `transactions` e `recurrences` já existem | Story 3.1 cria `version(3)` com apenas 4 tabelas novas: `accounts`, `cards`, `installments`, `categories`. Reutiliza as 2 existentes. |
| R4 | Formato monetário PT-PT (AC5 Epic 3) não tem helper mencionado no PRD | Risco R3 do `EPIC-3.md` aponta para helper único `lib/financas/formatCurrency.ts` | [AUTO-DECISION] A2: Story 3.1 cria o helper `formatCurrency` em `lib/financas/formatCurrency.ts` — camada de dados, non-UI. Todas as stories de UI (3.3/3.7/3.8/3.9) importam daqui. Não é invenção: é a resolução do risco R3 do EPIC-3, que é explícito. |

---

## Acceptance Criteria

> Trace: FR16 (transações variáveis), FR17 (finanças recorrentes — reutiliza `recurrences` existente), FR18 (contas bancárias + cartões), FR19 (compras parceladas), NFR17 (cobertura >=60%), NFR15 (ESLint+TS strict), NFR18 (CodeRabbit CRITICAL bloqueia).

- **AC1:** `lib/db/client.ts` incrementa para `this.version(3).stores({ accounts: '...', cards: '...', installments: '...', categories: '...' })` **sem reescrever** os blocos `version(1)` e `version(2)`. As tabelas anteriores ficam intactas — Dexie preserva os dados existentes no upgrade. Apenas as 4 tabelas novas são adicionadas neste incremento.
  _Trace: R1/R2/R3 acima; `client.ts:28` explicita "Epic 3 incrementa para version 3"._

- **AC2:** A classe `NexusDB` declara as 4 novas propriedades: `accounts!: Table<Account, string>`, `cards!: Table<Card, string>`, `installments!: Table<Installment, string>`, `categories!: Table<Category, string>` — importando as interfaces de `@/types/db`, que já existem em `types/db.ts:98-143`. **NÃO** declara novamente `transactions` (já existe em `version(1)`).
  _Trace: FR16 (transactions), FR18 (accounts, cards), FR19 (installments)._

- **AC3:** Índices `version(3)`:
  - `accounts: 'id, type, createdAt'`
  - `cards: 'id, accountId, closingDay, dueDay'`
  - `installments: 'id, cardId, startDate, [cardId+startDate]'`
  - `categories: 'name, isDefault'` — chave primária `name` (string, único por definição)

  [AUTO-DECISION] A3: chave primária de `categories` é `name` (não `id`) porque `Transaction.category` referencia o nome directamente (ver `types/db.ts:118-119` campo `category: string`). Usar nome como PK simplifica a lookup e é consistente com o contrato da `Transaction`. Decisão a confirmar por `@po`/`@architect` no gate.
  _Trace: FR16 (category em Transaction), FR18 (accountId/cardId em Transaction), FR19 (installmentId em Transaction)._

- **AC4:** O comentário em `client.ts:21-29` é actualizado para incluir a descrição do incremento `version(3)` (accounts, cards, installments, categories — Epic 3), com referência às tabelas reutilizadas (`transactions` de `version(1)`, `recurrences` de `version(2)`) e à resolução do [GAP-3.1].
  _Trace: Lição 3 da Story 2.1 (comentário com trace canónico baked-in)._

- **AC5:** Existe `lib/db/schemas.ts` **estendido** com schemas Zod `AccountSchema`, `CardSchema`, `TransactionSchema`, `InstallmentSchema`, `CategorySchema` que espelham fielmente as interfaces de `types/db.ts:98-143`. Campos obrigatórios com mensagem PT-PT. Nenhum `any`. `TransactionSchema` valida que `amount` é inteiro (cêntimos), sem casas decimais.
  _Trace: FR16 (Transaction — amount em cêntimos), FR18 (Account, Card), FR19 (Installment)._

- **AC6:** Existe `lib/db/repos/accounts.ts` que exporta: `createAccount(input)`, `getAccount(id)`, `listAccounts()`, `updateAccount(id, patch)`, `updateBalance(id, delta)` (aplica delta atómico em cêntimos), `deleteAccount(id)`. `updateBalance` lança `Error` PT-PT se conta não existir.
  _Trace: FR18 (contas bancárias com saldo — balance em cêntimos)._

- **AC7:** Existe `lib/db/repos/cards.ts` que exporta: `createCard(input)`, `getCard(id)`, `listCards()`, `listCardsByAccount(accountId)`, `updateCard(id, patch)`, `deleteCard(id)`.
  _Trace: FR18 (cartões de crédito com closingDay e dueDay)._

- **AC8:** Existe `lib/db/repos/transactions.ts` que exporta: `createTransaction(input)`, `getTransaction(id)`, `listTransactions(opts?)`, `updateTransaction(id, patch)`, `deleteTransaction(id)`. `listTransactions` suporta filtros por `accountId`, `cardId`, `category`, `date` (range ISO), `recurrenceId`, `installmentId` via índices de `version(1)`. Default: ordenado desc por `date`. Montantes **sempre em cêntimos** (inteiros).
  _Trace: FR16 (transações variáveis — valor, categoria, data, conta/cartão), FR17 (recurrenceId), FR19 (installmentId)._

- **AC9:** Existe `lib/db/repos/installments.ts` que exporta: `createInstallment(input)`, `getInstallment(id)`, `listInstallmentsByCard(cardId)`, `updateInstallment(id, patch)`, `deleteInstallment(id)`. `listInstallmentsByCard` usa o índice composto `[cardId+startDate]`.
  _Trace: FR19 (compras parceladas vinculadas a cartão)._

- **AC10:** Existe `lib/db/repos/categories.ts` que exporta: `createCategory(input)`, `getCategory(name)`, `listCategories()`, `listDefaultCategories()`, `deleteCategory(name)`. `createCategory` **rejeita duplicados por nome normalizado** (case-insensitive, padrão Story 2.1 Q2). Mensagem PT-PT `'Já existe uma categoria com o nome "{name}"'`.
  _Trace: FR22 (10 categorias default PT — Mercearia, Restauração, Combustível, Saúde, Habitação, Educação, Lazer, Subscrições, Serviços, Outros)._

- **AC11:** Existe `lib/financas/formatCurrency.ts` que exporta: `formatCurrency(cents: number): string` — formata inteiro em cêntimos para string `€1.234,56` (separador de milhar ponto, separador decimal vírgula, símbolo `€` prefixado). Zero-safe: `formatCurrency(0) === '€0,00'`. Negativo-safe: `formatCurrency(-100) === '-€1,00'`.
  _Trace: FR16 (formato PT-PT `€1.234,56`), AC5 Epic 3 (transversal a todas as vistas), EPIC-3.md risco R3._

- **AC12:** Hooks reactivos: `hooks/useAccounts.ts` exporta `useAccounts()` (wrapper `useLiveQuery` sobre `listAccounts`); `hooks/useTransactions.ts` exporta `useTransactions(opts?)` (wrapper `useLiveQuery` sobre `listTransactions`, retorna `Transaction[] | undefined`). Seguem o padrão de `hooks/useTasks.ts` (Story 2.1).
  _Trace: FR16 (leitura reactiva de transações), FR18 (leitura reactiva de contas)._

- **AC13:** Tests Vitest em `tests/unit/db/repos/` cobrem CRUD roundtrip de cada repo (`accounts`, `cards`, `transactions`, `installments`, `categories`), filtros principais de `listTransactions` (por accountId, cardId, category, range de dates), `listInstallmentsByCard` via índice composto, `createCategory` a rejeitar duplicado case-insensitive, e `updateBalance` com delta em cêntimos.
  _Trace: NFR17 (cobertura >=60% em packages core)._

- **AC14:** Tests Vitest em `tests/unit/db/schemas.test.ts` cobrem casos negativos do domínio financeiro: `Transaction` com `amount` decimal rejeitada; `Account` sem `name` rejeitada; `Card` sem `accountId` rejeitado; `Category` sem `name` rejeitada; `Installment` com `installments <= 0` rejeitada; `Account.type` fora do enum rejeitado.
  _Trace: NFR15 (TypeScript strict + validação Zod)._

- **AC15:** Teste de upgrade de schema `version(2) → version(3)`: abrir a DB com `NexusDBV2Only` (réplica de `version(1)` + `version(2)` sem o `version(3)`), inserir dados em `recurrences` e `transactions`, confirmar que após upgrade para `version(3)` (com `NexusDB` completa) os dados de `version(1)`/`version(2)` persistem e as 4 tabelas novas estão disponíveis e vazias.
  _Trace: Lição 1 da Story 2.1 ("NexusDBV1Only é gold standard para schema increments") — replicar para v3. EPIC-3.md §8 A2._

- **AC16:** `tests/unit/financas/formatCurrency.test.ts` cobre: valor zero, valor positivo (ex: 123456 → `€1.234,56`), valor negativo (ex: -100 → `-€1,00`), valor sem casas decimais inteiras (ex: 100 → `€1,00`), valor com casas decimais não-zero (ex: 99 → `€0,99`), valor grande (ex: 1000000 → `€10.000,00`).
  _Trace: AC5 Epic 3 (formato PT-PT), FR16._

- **AC17:** Quality gates locais passam em `imersao-tools/nexus/v2/`: `npm run lint`, `npm run typecheck`, `npm run test:unit` (todos os existentes + novos), `npm run build`.
  _Trace: NFR15, NFR18._

- **AC18:** Coverage de `lib/db/repos/accounts.ts`, `cards.ts`, `transactions.ts`, `installments.ts`, `categories.ts`, `lib/db/schemas.ts` (domínio financeiro) e `lib/financas/formatCurrency.ts` é **>= 80% lines** medido via `npm run test:coverage`. **NÃO** alterar o threshold global em `vitest.config.ts` (caminho bloqueador — ver Not-Tested Evidence Gate; alterar global é Story F.1, fora de scope).
  _Trace: NFR17 (>=60% global; esta story visa >=80% nos ficheiros novos, acima do mínimo NFR)._

---

## CodeRabbit Integration

| Item | Configuração |
|------|--------------|
| Story Type | **Database** (incremento schema Dexie `version(3)`) + secundário **Architecture** (data access layer padrão) |
| Complexity | Medium — 4 tabelas novas + 5 schemas Zod + 5 repos + 2 hooks + 1 helper formatCurrency + tests. Surface area média (~15 ficheiros), padrão Story 2.1 maduro replicado |
| Specialized Agents | `@data-engineer` (executor — schema + repos), `@architect` (quality gate + decisão AC3 / [AUTO-DECISION] A3) |
| Quality Gate Tasks | Pre-Commit (`@data-engineer`): self-review antes de Ready for Review · Pre-PR (`@devops`): `coderabbit --prompt-only --base main` antes do PR |
| Self-Healing Configuration | mode: `light`, max_iterations: **2** (hard-stop — `EPIC-3.md` §8), severity_filter: `[CRITICAL, HIGH]`, behavior: `CRITICAL: auto_fix`, `HIGH: auto_fix (iter<2) else document_as_debt` |
| Focus Areas | **Database**: integridade do incremento `version(3)` (não reescrever `version(1)`/`version(2)`), índices Dexie correctos, schema compliance com `types/db.ts`. **Financeiro**: montantes em cêntimos (inteiros, nunca float), formatCurrency fidelidade PT-PT. **Type safety**: validação Zod, zero `any`. **Test coverage**: casos negativos + teste de upgrade de schema (AC15) |
| Risco | Médio — fundação que as Stories 3.2-3.11 consomem; um índice errado ou `version(3)` mal-formado propaga-se. Mitigação: AC15 (teste de upgrade honest mock) + cobertura forte (AC13, AC14) |
| **Local CLI skip** | CodeRabbit corre via **integração GitHub no PR** (server-side, automático). Convenção Nexus v2 — ver `PO-VALIDATION-STORY-2.1.md §7` |

---

## Tasks / Subtasks

- [x] 1. Validação inicial de reconciliação (AC1, R1-R3, [GAP-3.1])
  - [x] 1.1 Confirmar com `@po` a interpretação "Data Access Layer" e resolução [GAP-3.1] (tabela `recurrences` reutilizável, não duplicar) — RESOLVIDO pela `@po` no PO-VALIDATION §2; `recurrences` reutilizada com `ownerType: 'transaction'`, não recriada
  - [x] 1.2 Confirmar/escalar [AUTO-DECISION] A3 (chave primária `categories.name`) com `@po`/`@architect` antes de escrever `version(3)` — A3 ratificada pela `@po` (PO-VALIDATION §3) como decisão firme; implementada `categories: 'name, isDefault'`
  - [x] 1.3 Verificar se os índices de `version(1)` em `transactions` cobrem as queries previstas das Stories 3.3/3.7/3.8 — `[cardId+date]` em falta para a vista cartões (Story 3.8). [AUTO-DECISION] A4: adicionado `[cardId+date]` re-declarando `transactions` em `version(3)` (operação aditiva de índice, preserva dados)

- [x] 2. Incremento de schema Dexie (AC1, AC2, AC3, AC4)
  - [x] 2.1 Declarar as 4 novas propriedades na classe `NexusDB`: `accounts!`, `cards!`, `installments!`, `categories!`
  - [x] 2.2 Adicionar `this.version(3).stores({...})` com as 4 tabelas novas + re-declaração de `transactions` (índice `[cardId+date]`) — sem tocar nos blocos `version(1)` e `version(2)`
  - [x] 2.3 Actualizar o comentário em `client.ts` com trace canónico version(3) + resolução [GAP-3.1] + [AUTO-DECISION] A4

- [x] 3. Schemas Zod — domínio financeiro (AC5)
  - [x] 3.1 Estender `lib/db/schemas.ts` com `AccountSchema`, `CardSchema`, `TransactionSchema`, `InstallmentSchema`, `CategorySchema`
  - [x] 3.2 Mensagens PT-PT nos campos obrigatórios; `TransactionSchema.amount` validado como inteiro

- [x] 4. Repositories tipados (AC6, AC7, AC8, AC9, AC10)
  - [x] 4.1 `lib/db/repos/accounts.ts` — CRUD + `updateBalance(id, delta)` atómico em cêntimos (transacção `'rw'`)
  - [x] 4.2 `lib/db/repos/cards.ts` — CRUD + `listCardsByAccount(accountId)`
  - [x] 4.3 `lib/db/repos/transactions.ts` — CRUD + `listTransactions` com filtros (accountId/cardId/category/date-range/recurrenceId/installmentId)
  - [x] 4.4 `lib/db/repos/installments.ts` — CRUD + `listInstallmentsByCard` via índice composto `[cardId+startDate]`
  - [x] 4.5 `lib/db/repos/categories.ts` — CRUD + rejeição de nome duplicado case-insensitive (padrão Story 2.1 Q2)

- [x] 5. Helper formatCurrency (AC11)
  - [x] 5.1 Criar `lib/financas/formatCurrency.ts` com `formatCurrency(cents: number): string`
  - [x] 5.2 Garantir zero-safe, negativo-safe, e separadores PT-PT correctos (ponto milhar via `groupThousands`, vírgula decimal)

- [x] 6. Hooks reactivos (AC12)
  - [x] 6.1 `hooks/useAccounts.ts` — `useAccounts()` wrapper `useLiveQuery`
  - [x] 6.2 `hooks/useTransactions.ts` — `useTransactions(opts?)` wrapper `useLiveQuery`

- [x] 7. Testes Vitest (AC13, AC14, AC15, AC16)
  - [x] 7.1 `tests/unit/db/repos/accounts.test.ts` — CRUD + `updateBalance` delta + rejeição de conta inexistente (13 testes)
  - [x] 7.2 `tests/unit/db/repos/cards.test.ts` — CRUD + `listCardsByAccount` + múltiplas contas (12 testes)
  - [x] 7.3 `tests/unit/db/repos/transactions.test.ts` — CRUD + cada filtro de `listTransactions` + ordenação desc por date (17 testes)
  - [x] 7.4 `tests/unit/db/repos/installments.test.ts` — CRUD + `listInstallmentsByCard` via índice composto + isolação por cartão (11 testes)
  - [x] 7.5 `tests/unit/db/repos/categories.test.ts` — CRUD + rejeição duplicado (3 variantes case-insensitive) + `listDefaultCategories` (12 testes)
  - [x] 7.6 `tests/unit/db/schemas.test.ts` — estendido com casos negativos do domínio financeiro (AC14): amount decimal, type fora do enum, cardId ausente, installments<=0
  - [x] 7.7 `tests/unit/db/schema-upgrade-v3.test.ts` — novo ficheiro: `NexusDBV2Only` honest mock (réplica de `version(1)`+`version(2)`) → inserir dados → abrir `NexusDB` completa → confirmar persistência + 4 tabelas novas vazias + 19 tabelas totais + índice `[cardId+date]` funcional (AC15)
  - [x] 7.8 `tests/unit/financas/formatCurrency.test.ts` — 9 casos cobrindo zero, positivo, negativo, sem/com casas decimais, valor grande, múltiplos separadores (AC16)

- [x] 8. Quality gates locais (AC17, AC18)
  - [x] 8.1 `npm run lint` — PASS (0 erros; 1 warning preexistente em `route.ts` fora de scope)
  - [x] 8.2 `npm run typecheck` — exit 0
  - [x] 8.3 `npm run test:unit` — 728/728 PASS (59 ficheiros)
  - [x] 8.4 `npm run build` — PASS
  - [x] 8.5 `npm run test:coverage` — 100% lines em todos os 7 ficheiros do domínio financeiro (alvo >=80%)

- [x] 9. Story file maintenance — File List + Change Log + Dev Agent Record
- [ ] 10. Delegar push a `@devops *push` — fora do âmbito do executor

---

## Dev Notes

- **NÃO reescrever `version(1)` nem `version(2)`:** o padrão Dexie é aditivo. `this.version(1).stores({...})` e `this.version(2).stores({...})` mantêm-se literais; `this.version(3).stores({...})` adiciona apenas as tabelas novas. Reescrever `version(1)` ou `version(2)` corromperia o upgrade path dos dados do Eurico já em produção. [Source: `client.ts:21-29` + `architecture-v2.md §4.2`]

- **Tabela `transactions` JÁ EXISTE — não recriar:** `transactions` está em `version(1).stores({...})` (`client.ts:54`) com índices `id, accountId, cardId, category, date, recurrenceId, [accountId+date]`. Verificar se `[cardId+date]` é necessário para a Story 3.8 (vista cartões fatura corrente). Se sim, adicionar em `version(3)` via `.upgrade()` ou sub-task explícita — não duplicar tabela.

- **Tabela `recurrences` JÁ EXISTE — [GAP-3.1] RESOLVIDO:** `recurrences` está em `version(2)` com `ownerType: 'task' | 'transaction' | 'habit' | 'reminder'` (`types/db.ts:84`). Para finanças recorrentes (FR17), usar `ownerType: 'transaction'`. A Story 3.4 (CRUD recorrências financeiras) consome `repos/recurrences.ts` da Story 2.1 com `ownerType: 'transaction'` — NÃO criar novo repo de recorrências financeiras. [Source: `types/db.ts:79-86`, `client.ts:77`, Story 2.1 R2]

- **Montantes SEMPRE em cêntimos (inteiros):** `Account.balance`, `Transaction.amount`, `Card.limit`, `Installment.totalAmount` são todos inteiros em cêntimos. Nunca float. `TransactionSchema` valida `z.number().int()`. `formatCurrency` converte para display PT-PT. [Source: `types/db.ts:102` `// cêntimos`, `types/db.ts:117` `// cêntimos`]

- **`Transaction.amount` negativo = saída, positivo = entrada:** semântica já definida em `types/db.ts:117`. `formatCurrency` deve lidar com negativos (saídas). Asserção em `TransactionSchema`: sem restrição de sinal (ambos são válidos). [Source: `types/db.ts:117`]

- **Chave primária `categories.name`:** [AUTO-DECISION] A3 — `Transaction.category` é `string` (o nome da categoria directamente, `types/db.ts:118`). Usar `name` como PK evita um join desnecessário. Trade-off: rename de categoria não é trivial (precisa de migração das `transactions`). Aceitável no MVP (10 categorias default estáticas + user-defined). `@architect` pode rever no gate.

- **`Category.isDefault`:** campo `isDefault: boolean` (`types/db.ts:143`) permite distinguir as 10 categorias default PT das categorias criadas pelo utilizador. A Story 3.2 semeia as 10 defaults via `db.categories.bulkAdd` — NÃO é responsabilidade desta story (evitar duplicação se a DB já contiver dados).

- **Padrão de repo herdado da Story 2.1:** repos em `lib/db/repos/{tabela}.ts` (kebab-case singular), validação Zod no input antes de `db.*.add()`, throw `ZodError` com mensagem PT-PT. Ver `lib/db/repos/accounts.ts` (Epic 1/Story 1.1 padrão). [Source: Story 2.1 Dev Notes]

- **`NexusDBV2Only` para AC15:** réplica literal da NexusDB com apenas `version(1)` + `version(2)` (sem `version(3)`) — honest mock, conforme `mock-protocol-fidelity.md`. Inserir dados em `transactions` e `recurrences` (tabelas existentes), fechar, reabrir com `NexusDB` completa. Confirmar que: dados `version(1)`+`version(2)` persistem; tabelas `accounts`/`cards`/`installments`/`categories` existem e estão vazias; **total de tabelas em `version(3)`: 19** (15 em `version(2)` — 13 de `version(1)` + 2 de `version(2)` — mais 4 novas de `version(3)`). O `schema-upgrade.test.ts` da Story 2.1 (`tests/unit/db/schema-upgrade.test.ts:174-179`) confirma as 15 tabelas em `version(2)`; a Story 3.1 adiciona 4 → 19. [Source: Story 2.1 Completion Notes #6 + `schema-upgrade.test.ts:174-179`, `mock-protocol-fidelity.md`]

- **`formatCurrency` — implementação de referência:**
  ```typescript
  export function formatCurrency(cents: number): string {
    const negative = cents < 0;
    const abs = Math.abs(cents);
    const euros = Math.floor(abs / 100);
    const centavos = abs % 100;
    const formatted = euros.toLocaleString('pt-PT') + ',' + String(centavos).padStart(2, '0');
    return (negative ? '-' : '') + '€' + formatted;
  }
  ```
  Alternativa mais robusta via `Intl.NumberFormat('pt-PT', { style: 'currency', currency: 'EUR' })` — avaliar compatibilidade browsers (NFR23: Chrome 110+). Se `Intl.NumberFormat` produzir o formato exacto `€1.234,56`, preferir por correcção automática.

- **Import convention:** Constitution Article VI — sempre `@/...` absoluto. Excepção: imports entre ficheiros dentro de `lib/db/repos/` podem ser relativos entre si (padrão Story 2.1).

### Testing

- **Framework:** Vitest 1.x + `fake-indexeddb` (configurado em `tests/setup.ts`, Story 0.9).
- **Localização:** `tests/unit/db/repos/*.test.ts` (um por repo), estender `tests/unit/db/schemas.test.ts`, novo `tests/unit/db/schema-upgrade-v3.test.ts`, `tests/unit/financas/formatCurrency.test.ts`.
- **Padrão:** seguir `tests/unit/db/repos/tasks.test.ts` (Story 2.1) — `beforeEach` limpa a DB, roundtrip create→get, asserções sobre filtros via índice.
- **AC15 (teste de upgrade) é crítico:** `NexusDBV2Only` réplica literal do estado anterior — honest mock. Inserir em `transactions` + `recurrences`, forçar upgrade, confirmar persistência + tabelas novas. [Source: Story 2.1 QA Results AC13]
- **Coverage:** alvo local >= 80% lines nos ficheiros novos (AC18). NFR17 pede >= 60% global em packages core — esta story contribui mas não altera threshold global.

---

## Not-Tested Evidence Gate

**N/A para o arranque.**

Esta story toca `lib/db/client.ts`, `lib/db/schemas.ts`, `lib/db/repos/**`, `lib/financas/**`, `hooks/**`, `tests/**` — nenhum caminho bloqueador (CI config, test-runner config, build config, segurança). AC18 proíbe explicitamente tocar em `vitest.config.ts` (esse seria bloqueador).

> **Activação:** se durante a implementação algum commit precisar de tocar `vitest.config.ts`, `tsconfig*.json`, `package.json` (campo `scripts`) ou `.github/workflows/**`, esta secção passa a **obrigatória** com evidência local prévia. Ver `.claude/rules/not-tested-trailer-rules.md`.

---

## Anti-padrões (NÃO fazer)

- NÃO reescrever os blocos `this.version(1).stores({...})` ou `this.version(2).stores({...})` em `client.ts` — incremento é aditivo (`version(3)`)
- NÃO recriar a tabela `transactions` — já existe em `version(1)` com índices correctos
- NÃO recriar a tabela `recurrences` — já existe em `version(2)` com `ownerType: 'transaction'` disponível ([GAP-3.1] RESOLVIDO)
- NÃO criar `transaction_recurrences` ou qualquer tabela de recorrências específica de finanças
- NÃO recriar as interfaces `Account`, `Card`, `Transaction`, `Installment`, `Category` — já existem em `types/db.ts:98-143`
- NÃO usar floats para montantes — sempre inteiros em cêntimos
- NÃO semear as 10 categorias default PT — é a Story 3.2
- NÃO implementar o motor de geração de transações recorrentes — é a Story 3.10
- NÃO implementar CRUD UI de transações, contas, ou cartões — são as Stories 3.3/3.4/3.5
- NÃO registar tools no Tool Registry — é a Story 3.11
- NÃO criar vistas mensais, de cartões, ou de património — são as Stories 3.7/3.8/3.9
- NÃO tocar em `vitest.config.ts` (threshold global é Story F.1)
- NÃO usar `any` — `unknown` com type guards quando necessário

---

## [AUTO-DECISIONS] documentadas

| # | Pergunta | Decisão | Razão |
|---|----------|---------|-------|
| A1 | Qual o número correcto da versão Dexie? | `version(3)` | `client.ts:28` é explícito. `version(1)` = Story 0.3/1.1; `version(2)` = Story 2.1. Próxima é 3. |
| A2 | Helper `formatCurrency` — deve ser criado aqui? | Sim, em `lib/financas/formatCurrency.ts` | Resolve risco R3 EPIC-3 explicitamente. Todas as stories de UI (3.3/3.7/3.8/3.9) precisarão dele. Melhor criar na story de schema do domínio do que em cada story de UI. |
| A3 | Chave primária de `categories` — `name` ou `id`? | `name` como PK | `Transaction.category` é `string` referenciando o nome directamente (`types/db.ts:118`). Usar `name` como PK evita join desnecessário. `@po`/`@architect` deve confirmar no gate. |
| A4 | Índice `[cardId+date]` em falta em `transactions` para a vista cartões (Story 3.8) — adicionar agora? | Sim — re-declarar `transactions` em `version(3)` adicionando `[cardId+date]` | Decisão tomada pelo `@data-engineer` durante a Task 1.3, que instrui explicitamente "se faltar `[cardId+date]` para a vista cartões (Story 3.8), abrir sub-task". A vista cartões (fatura corrente) filtra transações por `cartão + range de datas`; sem o índice composto a query seria full-scan. Re-declarar uma tabela existente num `version()` posterior é o mecanismo Dexie aditivo de alteração de índices — não recria a tabela nem perde dados. Confirmado por teste (`schema-upgrade-v3.test.ts` — índice `[cardId+date]` funcional após upgrade). `@architect` confirma no gate. |

---

## Referências

- `imersao-tools/nexus/docs/EPIC-3.md` §2 (contexto), §4 (FRs), §5 (tabela de stories — Story 3.1), §7 (reconciliação PRD↔arquitectura, [GAP-3.1]), §8 (lições retrospectivas Epic 1 e Epic 2)
- `imersao-tools/nexus/docs/PRD-NEXUS-V2.md` §6.3 (FR16-FR23), §10 Epic 3 Story 3.1
- `imersao-tools/nexus/v2/lib/db/client.ts` — `version(1)` e `version(2)` actuais (linhas 51-79) + comentário a actualizar (linhas 21-29) + antecipação "Epic 3 incrementa para version 3" (linha 28)
- `imersao-tools/nexus/v2/types/db.ts` linhas 98-143 — interfaces `Account`, `Card`, `Transaction`, `Installment`, `Category` (já existem, Epic 3)
- `imersao-tools/nexus/v2/types/db.ts` linhas 79-86 — interface `Recurrence` com `ownerType: 'task' | 'transaction' | 'habit' | 'reminder'` (resolução [GAP-3.1])
- `imersao-tools/nexus/docs/stories/completed/2.1.story.md` — padrão Data Access Layer (repos + Zod + hooks) que esta story replica para o domínio Finanças
- `imersao-tools/nexus/docs/stories/completed/1.1.story.md` — padrão original (Epic 1 referência de formato)
- `.claude/rules/not-tested-trailer-rules.md` — classificação de paths bloqueadores
- `.claude/rules/separation-of-roles.md` — `executor != quality_gate` (A6)
- `.claude/rules/mock-protocol-fidelity.md` — AC15 honest mock `NexusDBV2Only`

---

## Dev Agent Record

**Agente executor:** Dara (`@data-engineer`)
**Data de implementação:** 21/05/2026
**Comando:** `*develop 3.1`
**Branch:** `feature/3.1-schema-financas` (criada a partir de `main`)
**Modo:** YOLO (iteração autónoma)

### Decisões de implementação

| # | Decisão | Justificação |
|---|---------|--------------|
| D1 | `[AUTO-DECISION] A4` — adicionar `[cardId+date]` a `transactions` em `version(3)` | A Task 1.3 instrui explicitamente a verificar os índices e abrir sub-task se faltar `[cardId+date]`. A vista cartões da Story 3.8 (fatura corrente) filtra por cartão + range de datas. Re-declarar `transactions` no `version(3)` adiciona o índice de forma aditiva sem recriar a tabela nem perder dados. Provado pelo teste `schema-upgrade-v3.test.ts`. |
| D2 | `formatCurrency` — formatação manual de milhares via `groupThousands`, sem `Intl`/`toLocaleString` | Em runtime Node, `Number.toLocaleString('pt-PT')` usa espaço (non-breaking space U+202F) como separador de milhar, não ponto — divergindo do formato-alvo `€1.234,56` exigido pelo AC11/AC5 Epic 3. A implementação de referência da story (Dev Notes L258-269) tinha este defeito latente. Substituída por agrupamento manual determinístico, independente do ICU/locale do runtime. |
| D3 | `listDefaultCategories` — filtro `isDefault` em memória, não via `.where('isDefault')` | O IndexedDB não indexa booleanos de forma fiável (não são keys válidas). O índice `categories: 'name, isDefault'` (literal do AC3) é mantido como declarado, mas a query usa leitura completa + filtro — padrão idêntico a `listTasks` (Story 2.1), adequado à cardinalidade (~10 default + user-defined). |
| D4 | `updateBalance` envolvido numa transacção Dexie `'rw'` | O AC6 pede delta atómico. Ler e escrever o saldo na mesma transacção garante ACID local contra escritas concorrentes — padrão herdado de `deleteTag` cascata (Story 2.6). |

### Manutenção de teste preexistente

`tests/unit/db/schema-upgrade.test.ts` (Story 2.1) fazia asserções literais `verno === 2` e "15 tabelas". Como o AC1 incrementa `NexusDB` para `version(3)` com 19 tabelas, 2 asserções desse ficheiro ficaram desactualizadas. Foram actualizadas para `verno === 3` / "19 tabelas" — o teste continua a provar que o upgrade desde `version(1)` preserva os dados (lição #1 da Story 2.1: o `schema-upgrade.test.ts` é mantido a cada incremento). Não é scope-creep: é manutenção necessária causada directamente pelo AC1.

### Quality gates locais (AC17, AC18)

| Gate | Comando | Resultado |
|------|---------|-----------|
| ESLint | `npm run lint` | PASS — 0 erros (1 warning preexistente em `app/api/auth/logout/route.ts`, fora de scope) |
| TypeScript | `npm run typecheck` | PASS — exit 0 |
| Testes unitários | `npm run test:unit` | PASS — 728/728 testes, 59 ficheiros |
| Build | `npm run build` | PASS |
| Coverage financeiro | `npm run test:coverage` (ficheiros financeiros) | PASS — **100% lines** em `accounts.ts`, `cards.ts`, `transactions.ts`, `installments.ts`, `categories.ts`, `schemas.ts`, `formatCurrency.ts` (alvo AC18 >=80%); branch 98,76% |

### Completion Notes

- 18/18 ACs honrados. Schema `version(3)` aditivo (4 tabelas novas + re-declaração de `transactions` para o índice `[cardId+date]`). `version(1)` e `version(2)` intactos.
- [GAP-3.1] respeitado — `recurrences` reutilizada, não recriada. Nenhuma tabela de recorrências de finanças criada.
- 5 schemas Zod, 5 repos, 1 helper, 2 hooks, 8 ficheiros de teste (116 testes novos do domínio financeiro).
- Zero `any`, zero invenção. Interfaces `Account/Card/Transaction/Installment/Category` não foram tocadas (já existiam em `types/db.ts:98-142`).
- Not-Tested Evidence Gate: N/A — nenhum commit toca `vitest.config.ts`, `tsconfig*.json`, `package.json` (scripts) ou `.github/workflows/**`.

### File List

**Ficheiros novos (15) — 8 de código + 7 de teste:**
- `imersao-tools/nexus/v2/lib/db/repos/accounts.ts`
- `imersao-tools/nexus/v2/lib/db/repos/cards.ts`
- `imersao-tools/nexus/v2/lib/db/repos/transactions.ts`
- `imersao-tools/nexus/v2/lib/db/repos/installments.ts`
- `imersao-tools/nexus/v2/lib/db/repos/categories.ts`
- `imersao-tools/nexus/v2/lib/financas/formatCurrency.ts`
- `imersao-tools/nexus/v2/hooks/useAccounts.ts`
- `imersao-tools/nexus/v2/hooks/useTransactions.ts`
- `imersao-tools/nexus/v2/tests/unit/db/repos/accounts.test.ts`
- `imersao-tools/nexus/v2/tests/unit/db/repos/cards.test.ts`
- `imersao-tools/nexus/v2/tests/unit/db/repos/transactions.test.ts`
- `imersao-tools/nexus/v2/tests/unit/db/repos/installments.test.ts`
- `imersao-tools/nexus/v2/tests/unit/db/repos/categories.test.ts`
- `imersao-tools/nexus/v2/tests/unit/db/schema-upgrade-v3.test.ts`
- `imersao-tools/nexus/v2/tests/unit/financas/formatCurrency.test.ts`

**Ficheiros modificados (4):**
- `imersao-tools/nexus/v2/lib/db/client.ts` — incremento `version(3)` (4 tabelas novas + re-declaração `transactions` com `[cardId+date]`), 4 propriedades novas na classe `NexusDB`, comentário de header actualizado
- `imersao-tools/nexus/v2/lib/db/schemas.ts` — estendido com `AccountSchema`, `CardSchema`, `TransactionSchema`, `InstallmentSchema`, `CategorySchema` + `AccountTypeSchema`
- `imersao-tools/nexus/v2/tests/unit/db/schemas.test.ts` — estendido com casos negativos do domínio financeiro (AC14)
- `imersao-tools/nexus/v2/tests/unit/db/schema-upgrade.test.ts` — asserções actualizadas para `version(3)` / 19 tabelas (manutenção causada pelo AC1)

Total: **15 ficheiros novos** (5 repos + 1 helper + 2 hooks + 7 ficheiros de teste) + **4 ficheiros modificados**.

---

## Architect Gate de Implementação

**Quality gate:** Aria (`@architect`) — `separation-of-roles.md` A6 (schema/DDL é domínio do `@data-engineer`, gate sobe para `@architect`)
**Data:** 21/05/2026
**Veredicto:** **PASS** — Status `Ready for Review → Done`
**Detalhe completo:** `imersao-tools/nexus/docs/QA-GATE-STORY-3.1.md`

### Quality gates reproduzidos independentemente (5/5)

| Gate | Resultado reproduzido | Confere com relatório Dara? |
|------|-----------------------|------------------------------|
| `npm run lint` | 0 erros (1 warning preexistente `route.ts`, fora de scope) | SIM |
| `npm run typecheck` | exit 0 | SIM |
| `npm run test:unit` | 728/728 PASS, 59 ficheiros | SIM |
| `npm run build` | exit 0 (19 rotas) | SIM |
| `npm run test:coverage` | 100% lines nos 7 ficheiros financeiros (`client.ts`, `schemas.ts`, `repos/{accounts,cards,transactions,installments,categories}.ts`, `formatCurrency.ts`) | SIM |

### Resultados do gate

- **18/18 ACs honrados** — cada um verificado contra o código real (path+linhas).
- **[AUTO-DECISIONS] A1/A2/A3/A4 todas ratificadas.** A4 (índice `[cardId+date]` re-declarando `transactions` em `version(3)`) confirmada como upgrade Dexie aditivo seguro — o conjunto de índices é superset estrito do de `version(1)`, dados preservados, provado por `schema-upgrade-v3.test.ts` (3 cenários). A3 (PK `categories.name`) confirmada arquitecturalmente — respeita o contrato `types/db.ts` sem invenção.
- **3 desvios avaliados e aceites:** (1) `formatCurrency` sem `Intl` — decisão correcta, não débito (`Intl`/`toLocaleString` produzem U+202F como separador, divergindo do formato-alvo `€1.234,56`); (2) `listDefaultCategories` filtro em memória — padrão correcto (IndexedDB não indexa booleanos); (3) manutenção de `schema-upgrade.test.ts` — legítima, causada directamente pelo AC1.
- **Conformidade ADR-1/ADR-2/ADR-4.** Nenhum ADR reaberto. Schema `version(3)` aditivo. Hooks `'use client'` — coerente com Dexie client-side.
- **7/7 quality checks qa-gate AIOX PASS** (Security N/A — sem superfície). **13/13 anti-padrões livres.**
- `mock-protocol-fidelity` conforme (honest mock `NexusDBV2Only` reflecte o schema real anterior). `not-tested-trailer-rules` conforme — o trailer `Not-tested:` do commit refere o upgrade Dexie real em produção (edge case de runtime, waiver válido); nenhum path bloqueador tocado.
- **Zero findings bloqueadores.** Zero findings de severidade Média+. Separação A6 cumprida.

**Próximo passo:** `@devops *push feature/3.1-schema-financas` — push da branch + PR contra `main`.

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 20/05/2026 | 1.0 | Story criada via `*draft 3.1`. Resolução do [GAP-3.1] com evidência directa do código (`types/db.ts:84` — `ownerType: 'transaction'` disponível; `client.ts:77` — tabela `recurrences` em `version(2)` genérica). 3 reconciliações PRD↔arquitectura. 3 [AUTO-DECISIONS] documentadas. 18 ACs. Padrão Story 2.1 replicado. Status: Draft | `@sm` River |
| 21/05/2026 | 1.1 | Validação `*validate-story-draft 3.1`. Veredicto **GO** — score 9,5/10, Implementation Readiness 10/10, Confidence High. 14/14 claims anti-hallucination confirmados em código. [GAP-3.1] resolvido definitivamente (verificação independente — risco R2 EPIC-3 fechado). [AUTO-DECISIONS] A1/A2/A3 ratificadas (A3 com nota de gate confirmatório). F1 corrigido inline: Dev Notes L256 — aritmética contraditória na contagem de tabelas corrigida para 19 firme (15 em `version(2)` + 4 em `version(3)`), com trace ao `schema-upgrade.test.ts:174-179` da Story 2.1. Detalhe completo: `PO-VALIDATION-STORY-3.1.md`. Status: Draft → **Approved**. | `@po` Pax |
| 21/05/2026 | 1.2 | Implementação via `*develop 3.1`. Branch `feature/3.1-schema-financas`. Schema Dexie `version(3)` (4 tabelas novas: `accounts`, `cards`, `installments`, `categories`) + re-declaração de `transactions` com índice `[cardId+date]` ([AUTO-DECISION] A4 — vista cartões Story 3.8). 5 schemas Zod financeiros, 5 repos tipados, helper `formatCurrency`, 2 hooks reactivos, 8 ficheiros de teste (116 testes do domínio financeiro). 18/18 ACs honrados. Quality gates: lint PASS, typecheck exit 0, 728/728 testes PASS, build PASS, coverage 100% lines nos 7 ficheiros financeiros. Decisões D1-D4 documentadas no Dev Agent Record. Status: Approved → **Ready for Review**. | `@data-engineer` Dara |
| 21/05/2026 | 1.3 | Architect Gate de implementação via `*architect-gate 3.1`. Veredicto **PASS**. 5/5 quality gates reproduzidos independentemente (lint 0 erros + 1 warn preexistente, typecheck exit 0, test:unit 728/728, build exit 0, coverage 100% lines nos 7 ficheiros financeiros). 18/18 ACs verificados contra código real (path+linhas). [AUTO-DECISIONS] A1/A2/A3/A4 todas ratificadas — A4 (índice `[cardId+date]`) confirmada como upgrade Dexie aditivo seguro, provada por `schema-upgrade-v3.test.ts`. 3 desvios avaliados e aceites (formatCurrency sem `Intl` = decisão correcta; `listDefaultCategories` filtro em memória = padrão correcto; manutenção `schema-upgrade.test.ts` = legítima). Conformidade ADR-1/ADR-2/ADR-4. 7/7 quality checks qa-gate AIOX PASS, 13/13 anti-padrões livres. `mock-protocol-fidelity` conforme; `not-tested-trailer-rules` conforme (waiver válido — nenhum path bloqueador). Zero findings bloqueadores. Separação A6 cumprida (`@data-engineer` executou, `@architect` validou). Detalhe: `QA-GATE-STORY-3.1.md`. Status: Ready for Review → **Done**. | `@architect` Aria |
