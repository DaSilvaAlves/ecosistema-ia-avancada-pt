# Story 5.7 — Brain Dump AI parser (texto livre → 4 buckets)

**Story ID:** 5.7
**Epic:** 5 — Diário + Brain Dump + Conhecimento
**Status:** Ready for Done
**Bloqueia:** 5.8 (Brain Dump approval flow — consome `parsedOutput` para a aprovação item-a-item)
**Bloqueada por:** Story 5.6 (Brain Dump UI — seam `onStructure(markdown)`) DONE em `main` @ `b3a538e7`; Story 5.1 (Schema — `BrainDump` + repo `brain-dumps.ts`) DONE @ `7171a99f`. Ambas satisfeitas.
**Estimativa:** 5-8h (helper puro de schema/parse + client de inferência + ligação do seam 5.6 + persistência + display read-only dos 4 buckets + overlay + testes de fidelidade de protocolo)
**Tipo:** Frontend + AI/parser — território de risco (protocolo AI + estado)

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [coderabbit, lint, typecheck, vitest]
```

> Trace: `EPIC-5.md` §5 (row 5.7 — executor `@dev`, gate `@architect`) + §5 nota de padrão ("AI/parser/pesquisa web/tools → gate `@architect`"). Respeita `separation-of-roles.md` (A6). **Architect Gate de ENTRADA recomendado** (`EPIC-5.md` §8 nº4 + lição positiva 4.8 §5.6; precedente directo `[D-5.4-ENDPOINT]` da 5.4): Aria (`@architect`) ratifica as decisões abertas (secção "Architect Gate de Entrada" abaixo) **antes** de Dex (`@dev`) implementar, e assina o gate de saída.

---

## Nota do `@sm` River — verificação em código (Constitution Artigo IV)

Verificação 11/06/2026. O parser AI do Brain Dump tem um **precedente directo e recente**: a estruturação AI do diário (Story 5.4), que o `@architect` (Aria) já resolveu via `[D-5.4-ENDPOINT]`. A 5.7 deve **reutilizar esse padrão**, não inventar um novo.

### Evidência verificada

**Seam da 5.6 (a ligar):**
- `BrainDumpLauncher.tsx` passa um `onStructure` placeholder que só fecha o modal (comentário: "a Story 5.7 substitui este callback pelo pipeline AI (parse + createBrainDump + transição status)"). A 5.7 substitui esse placeholder. _[Verificado: `components/brain-dump/BrainDumpLauncher.tsx:47-49`]_
- `BrainDumpModal` é presentational e emite `onStructure(markdown)` — não muda nesta story (salvo o overlay "A estruturar…", ver `[D-5.7-SCOPE]`). _[Verificado: 5.6 `[D-5.6-SEAM]`]_

**Camada de dados (5.1, a usar):**
- `BrainDump { id, createdAt, bodyMarkdown, parsedOutput?, status }`; `status: 'pending' | 'parsed' | 'partially_approved' | 'fully_approved'`; **`parsedOutput?: unknown`** — o tipo exacto dos 4 buckets é definido NESTA story (5.7). _[Verificado: `types/db.ts:261-267`]_
- Repo `brain-dumps.ts`: `createBrainDump`, `updateBrainDump`, `getBrainDump`, `listBrainDumps`, `listBrainDumpsByStatus`, `deleteBrainDump` — prontos. `BrainDumpStatus` em `@/lib/db/schemas`. _[Verificado: `lib/db/repos/brain-dumps.ts`]_

**Padrão do parser AI do diário (5.4 — a reutilizar):**
- `lib/diario/ai-estrutura.ts` — helper PURO: Zod `.strict()` schema do shape de domínio (`StructuredDiarioResponseSchema`), `parseStructuredDiario(value: unknown)` com throw PT-PT, `hasStructuredContent`. Edge/browser-safe (só `zod` + string; sem SDK/key). É a fonte de verdade falsificável que satisfaz `mock-protocol-fidelity.md` (`.strict()` → campo renomeado/extra falha). _[Verificado: `lib/diario/ai-estrutura.ts`]_
- `lib/diario/estruturar-cliente.ts` — client helper: chama **`/api/anthropic/proxy`** (NÃO há endpoint dedicado), JSON síncrono, Sonnet (`DEFAULT_EXECUTOR_MODEL='claude-sonnet-4-6'`), `temperature:0`, system prompt PT-PT, `stripJsonMarkdownFences` (hotfix classifier 31/05), verifica `res.ok` **antes** do body (lição Epic 4 §5.1), lança PT-PT em qualquer falha (`internal-state-contract-gate.md` eixo c), `fetchFn` injectável para testes. _[Verificado: `lib/diario/estruturar-cliente.ts`]_
- `[D-5.4-ENDPOINT]` (Aria, 10/06): Escolha 1 runtime = reutiliza `/api/anthropic/proxy` (Opção B, não cria endpoint); Escolha 2 transporte = JSON síncrono; Escolha 3 shape = Zod strict mapeado 1:1 ao campo db. _[Verificado: `estruturar-cliente.ts:11-18`]_

**Divergência a resolver pelo `@architect`:** a `front-end-spec-v2.md` §1.4 [4] diz "POST `/api/agent/brain-dump` (Edge, SSE)", mas o precedente 5.4 (`[D-5.4-ENDPOINT]`) reutilizou `/api/anthropic/proxy` em **JSON síncrono**. As duas vias divergem — decisão `[D-5.7-MECHANISM]` (abaixo). Não resolvo isto no draft (é decisão de arquitectura): apresento as candidatas para o Architect Gate de entrada.

**Arch §16 (a confirmar):** "Sonnet com tools `criar_tarefa`/`criar_projecto`/`criar_nota` em modo `requiresPreview: true` por chamada". Mas o 5.4 usou JSON estruturado ad-hoc (sem Tool Registry), e as tools registam-se só na **5.13**. Decisão `[D-5.7-TOOLS]` (abaixo).

---

## Architect Gate de Entrada — Decisões a Ratificar (Aria, antes de `@dev`)

> Padrão `[D-5.4-ENDPOINT]`: estas decisões são ratificadas pelo `@architect` no início, registadas aqui como `[D-5.7-*]`, e só então `@dev` implementa. Resolvem `[GAP-5.3]` (`EPIC-5.md` §7) para a parte de parsing.

| # | Decisão | Candidatas (com precedente) | Recomendação `@sm` (não-vinculativa) |
|---|---------|------------------------------|--------------------------------------|
| **D-5.7-MECHANISM** | Runtime/transporte da chamada AI | (A) Reutilizar `/api/anthropic/proxy` JSON síncrono — precedente `[D-5.4-ENDPOINT]`; (B) Novo `/api/agent/brain-dump` Edge SSE — FE spec §1.4 [4] | (A) — consistência com 5.4, menos superfície; o overlay "A estruturar…" não exige streaming. AC2 (<8s/200 palavras) confirma-se com Sonnet JSON síncrono. |
| **D-5.7-SHAPE** | Tipo de `parsedOutput` (4 buckets) | Zod `.strict()` (à imagem de `StructuredDiarioResponseSchema`). 4 buckets: `tarefas`, `projectos`, `ideias`, `decisoes` (nomes de campo ASCII — `external-contract-identifiers.md`). Cada item precisa de `id` (para a selecção item-a-item da 5.8) + texto | Definir o schema + tipo `BrainDumpParsed` exportado, usado por `parsedOutput` e pela 5.8. Itens com `{ id, texto }` (+ campos específicos por bucket se a 5.8 precisar). |
| **D-5.7-TOOLS** | JSON estruturado ad-hoc vs Tool Registry com `requiresPreview` | (A) JSON estruturado ad-hoc (precedente 5.4) — tools registam-se na 5.13; (B) Tool Registry `criar_tarefa/projecto/nota` com `requiresPreview:true` agora (arch §16) | (A) — desacopla parsing (5.7) de registo de tools (5.13) e de aprovação (5.8); evita meio-fluxo. Confirmar que o `requiresPreview` por-tool é assunto da 5.8/5.13. |
| **D-5.7-SCOPE** | Fronteira 5.7 ↔ 5.8 | 5.7 = parse + persist (`status` `pending`→`parsed`) + **display read-only** dos 4 buckets (FE [5], default expandidos); 5.8 = selecção/edição/rejeição item-a-item + persistência como entidades + `status`→`partially/fully_approved` (FE [6]-[8]) | Confirmar. `parsedOutput` (persistido) é o seam para a 5.8. |
| **D-5.7-PERSIST** | Quando persistir o `BrainDump` | Após parse com sucesso: `createBrainDump({ id, createdAt, bodyMarkdown, parsedOutput, status:'parsed' })`. Parse falhado → não persiste, mostra erro (não cria dump `pending` órfão) | Confirmar — evita dumps `pending` sem parser (mesma lógica do `[D-5.6-SEAM]`). |

### Ratificação — Aria (`@architect`), 12/06/2026 (T0 EXECUTADO)

> Evidência re-verificada em código nesta sessão: `lib/diario/ai-estrutura.ts` + `lib/diario/estruturar-cliente.ts` (padrão 5.4 completo, `[D-5.4-ENDPOINT]` no cabeçalho), `types/db.ts:261-267` (`parsedOutput?: unknown`), `lib/db/schemas.ts:457-463` (`BrainDumpSchema` com `z.unknown()`), `lib/db/repos/brain-dumps.ts` (repo pronto), `components/brain-dump/BrainDumpLauncher.tsx:47-49` (seam), `front-end-spec-v2.md:205-249` (FE §1.4 [3]-[8]), `architecture-v2.md:1138` (§16 tools Brain Dump).

**`[D-5.7-MECHANISM]` = RATIFICADA Opção (A)** — reutilizar `/api/anthropic/proxy`, JSON síncrono, Sonnet `DEFAULT_EXECUTOR_MODEL`, `temperature: 0`.
Fundamentos: (1) precedente `[D-5.4-ENDPOINT]` validado em produção para a mesma classe de operação (texto livre → JSON estruturado one-shot); (2) o overlay "A estruturar…" (FE [4]) é um estado binário loading→done — SSE/streaming não melhora a UX de um parse atómico; (3) zero superfície nova (sem rota Edge nova, sem contrato SSE para manter); (4) a FE spec §1.4 [4] ("POST `/api/agent/brain-dump` (Edge, SSE)") é anterior ao `[D-5.4-ENDPOINT]` — fica **reconciliada**: a fonte de verdade de UI são os estados visuais ([4] overlay, [5] secções), o transporte é decisão de arquitectura.
Consequências para `@dev`: (a) actualizar o comentário `[D-5.6-SEAM]` em `BrainDumpLauncher.tsx:18-19` (refere o endpoint SSE que não vai existir); (b) `DEFAULT_MAX_TOKENS = 2048` no `parser-cliente.ts` — os 1024 do diário cobrem 3 buckets curtos; 4 buckets com N itens cada precisam de folga (AC2 <8s mantém-se com Sonnet síncrono).

**`[D-5.7-SHAPE]` = RATIFICADA com separação wire/domínio** — dois schemas no `ai-parser.ts`, ambos `.strict()`:
- **Wire (resposta AI):** `BrainDumpWireSchema` — 4 buckets **obrigatórios** (`tarefas`, `projectos`, `ideias`, `decisoes` — ASCII, `external-contract-identifiers.md`), cada um `z.array(z.string().min(1))`. A AI devolve **textos, não ids** — um LLM não é fonte fiável de unicidade de identificadores.
- **Domínio (persistido — seam 5.8):** `BrainDumpParsedSchema` — mesmos 4 buckets obrigatórios, itens `{ id: string; texto: string }`. Tipo exportado `BrainDumpParsed = z.infer<...>`.
- **Enriquecimento puro:** `enrichWithIds(wire, idFn = () => crypto.randomUUID())` — atribui ids no cliente após parse; `idFn` injectável para testes determinísticos.
Divergência deliberada face ao diário (campos opcionais): aqui os 4 buckets são **sempre presentes** (arrays vazios permitidos) porque a 5.8 itera os 4 e o display mostra contador "(N)" — arrays garantidos eliminam guards no consumo.

**`[D-5.7-TOOLS]` = RATIFICADA Opção (A)** — JSON estruturado ad-hoc (padrão 5.4), **sem** Tool Registry nesta story.
O arch §16 (`architecture-v2.md:1138` — "Sonnet com tools `criar_tarefa`/`criar_projecto`/`criar_nota` em modo `requiresPreview: true`") aplica-se ao fluxo de **persistência aprovada** — ou seja, à 5.8 (aprovação item-a-item) com tools registadas na 5.13. Invocar tools `criar_*` no parse da 5.7 contradiria o próprio `requiresPreview` (não existe aprovação nesta story). Não há divergência real com o arch: o parse é transformação read-only; as tools entram quando os itens se tornam entidades. O `parsedOutput` persistido é o contrato entre as duas fases.

**`[D-5.7-SCOPE]` = CONFIRMADA** — 5.7 entrega FE [4]-[5] **sem controlos de item**: os ☐/✏️/✗ e o botão "Guardar N itens" visíveis no mock do [5] pertencem aos passos [6]-[8] (= Story 5.8). Decisão menor de UI fixada: as 4 secções aparecem sempre; buckets **não-vazios** default expandidos (FE [5]); buckets **vazios** mostram "(0)" e ficam colapsados (feedback de que a AI considerou o bucket, sem roubar espaço).

**`[D-5.7-PERSIST]` = CONFIRMADA** — `createBrainDump({ id, createdAt, bodyMarkdown, parsedOutput, status: 'parsed' })` **apenas** após parse + enrich com sucesso; qualquer falha (rede, `!res.ok`, JSON inválido, shape inválido) → throw PT-PT → estado `error`, **zero writes**. `pending` nunca é persistido pela 5.7. `internal-state-contract-gate.md`: eixo (c) coberto por AC2/AC4; eixos (a)/(b) só ganham superfície multi-camada na 5.8 — o gate completo aplica-se lá.

**Decisão AC8 (`types/db.ts`) = MANTER `unknown`** — `parsedOutput?: unknown` em `types/db.ts` e `z.unknown()` no `BrainDumpSchema` ficam **intocados**. O comentário da 5.1 (`D-BRAINDUMP-STORE`) previa explicitamente "sem coupling nesta camada". O tipo forte (`BrainDumpParsed`) vive em `lib/brain-dump/ai-parser.ts`; a escrita (AC3) só persiste output já validado; a leitura (5.8) revalida com o parse de domínio. Evita import `types/db.ts` → `lib/brain-dump/` e qualquer toque na suite schema-upgrade. `types/db.ts` e `lib/db/schemas.ts` saem da File List.

---

## User Story

Como Eurico (utilizador do Nexus), quero que, ao clicar "Estruturar com AI" no Brain Dump, a AI reorganize o meu texto livre em **4 buckets** — tarefas propostas, projectos propostos, ideias soltas e decisões a tomar — mostrados em secções colapsáveis, para eu ver a estrutura que saiu do meu despejo de ideias (a aprovação item-a-item e a persistência como tarefas/projectos/notas ficam para a Story 5.8).

---

## Contexto

`PRD-NEXUS-V2.md` §6.9 (FR48 — "AI estrutura output em: tarefas propostas, projectos propostos, ideias soltas, decisões a tomar") é o requisito raiz. `EPIC-5.md` §5 (row 5.7) atribui à 5.7 o **parser AI** (texto → 4 buckets), distinto da 5.6 (UI de input) e da 5.8 (aprovação item-a-item). A `front-end-spec-v2.md` §1.4 (Flow 4) passos [4]-[5] são a fonte de verdade de UI: clicar "Estruturar" → overlay "A estruturar…" → 4 secções colapsáveis (default expandidas) com os itens por bucket.

Esta story entrega os passos **[4]-[5]**: ligar o seam `onStructure` da 5.6 ao pipeline AI, parsear a resposta nos 4 buckets, **persistir** o brain dump (`status: 'parsed'`), e **mostrar** os 4 buckets em read-only. Os passos [6]-[8] (desmarcar/editar/rejeitar item-a-item, "Guardar N itens", persistência em `db.tasks`/`db.projects`/`db.knowledge_notes`, toast, mensagem retroactiva) são da **Story 5.8** (`internal-state-contract-gate.md` aplica-se lá — estado distribuído proposta→aceite→persistido).

Reutiliza o padrão validado da estruturação AI do diário (5.4): helper puro com Zod `.strict()` + client `/api/anthropic/proxy` JSON síncrono (`[D-5.4-ENDPOINT]`), sujeito à ratificação do `@architect` (`[D-5.7-MECHANISM]`).

---

## Acceptance Criteria

> Trace: FR48 (PRD §6.9), `EPIC-5.md` §5 (row 5.7), §10 (AC2 — 200 palavras < 8s), `front-end-spec-v2.md` §1.4 [4]-[5] + §1014 (overlay), `mock-protocol-fidelity.md` (A1 Epic 1), `internal-state-contract-gate.md` (eixo c — falhas; o ciclo completo é da 5.8), `external-contract-identifiers.md` (nomes de campo dos buckets ASCII), `design-system-ia-avancada.md`, `react-component-test-criteria.md`, NFR15/NFR17/NFR18. **Os AC abaixo assumem as decisões `[D-5.7-*]` ratificadas pelo `@architect`; se a ratificação divergir, os AC reconciliam-se antes de `@dev` implementar.**

- **AC1 (helper puro de schema/parse — `mock-protocol-fidelity.md`, reconciliado `[D-5.7-SHAPE]`):** `v2/lib/brain-dump/ai-parser.ts` (Edge/browser-safe — só `zod`, sem SDK/key) exporta: (a) `BrainDumpWireSchema` Zod **`.strict()`** — wire shape da resposta AI: 4 buckets **obrigatórios** (`tarefas`, `projectos`, `ideias`, `decisoes` — nomes ASCII), cada um `z.array(z.string().min(1))` (a AI devolve textos, NÃO ids); (b) `BrainDumpParsedSchema` **`.strict()`** — shape de domínio persistido: mesmos 4 buckets com itens `{ id: string; texto: string }`; (c) tipo `BrainDumpParsed` inferido; (d) `parseBrainDumpWire(value: unknown)` com throw PT-PT em shape inválido; (e) `enrichWithIds(wire, idFn?)` puro (default `crypto.randomUUID`, injectável p/ testes); (f) `hasParsedContent(parsed)` (≥1 bucket não-vazio). `.strict()` torna o contrato falsificável (campo renomeado/extra falha). _Trace: FR48; padrão `ai-estrutura.ts` (5.4); `[D-5.7-SHAPE]`._

- **AC2 (client de inferência — reconciliado `[D-5.7-MECHANISM]` RATIFICADA):** `v2/lib/brain-dump/parser-cliente.ts` chama **`/api/anthropic/proxy`** (JSON síncrono, Sonnet `DEFAULT_EXECUTOR_MODEL`, `temperature:0`, **`max_tokens: 2048`**, system prompt PT-PT dos 4 buckets, `stripJsonMarkdownFences`), verifica `res.ok` **antes** do body, lança PT-PT em qualquer falha (rede/`!res.ok`/sem `text`/JSON inválido/shape inválido), `fetchFn` injectável. Devolve o **domínio enriquecido** (`parseBrainDumpWire` → `enrichWithIds`). Espelha `estruturar-cliente.ts`. _Trace: `[D-5.7-MECHANISM]`; `[D-5.4-ENDPOINT]`; `internal-state-contract-gate.md` eixo c._

- **AC3 (ligação do seam 5.6 + persistência — `[D-5.7-PERSIST]`):** o `BrainDumpLauncher` (5.6) passa a um `onStructure(markdown)` real: chama o client (AC2) → em sucesso, `createBrainDump({ id: crypto.randomUUID(), createdAt: Date.now(), bodyMarkdown: markdown, parsedOutput, status: 'parsed' })` e mostra os buckets; em falha, mostra erro PT-PT e **não** persiste (sem dump `pending` órfão). Sem `updateBrainDump` nesta story (transições `partially/fully_approved` são da 5.8). _Trace: `[D-5.6-SEAM]`; FR48._

- **AC4 (estados de processamento — `internal-state-contract-gate.md` eixo c):** máquina de estados de união discriminada (padrão `AiState` da 5.4): `idle | loading | parsed(BrainDumpParsed) | error(message)`. `loading` → overlay "A estruturar…" + spinner Cyan, textarea readonly (FE [4], §1014). `error` → mensagem PT-PT, sem buckets vazios silenciosos. Resposta com 0 buckets úteis (`!hasParsedContent`) → estado informativo ("A AI não encontrou itens…"), não preview vazio. _Trace: FE §1.4 [4], §1014; padrão 5.4._

- **AC5 (display dos 4 buckets — read-only, `[D-5.7-SCOPE]`):** ao chegar `parsed`, o modal mostra 4 secções colapsáveis (default expandidas) com cabeçalho + contador por bucket (`Tarefas propostas (N)`, `Projectos propostos (N)`, `Ideias soltas (N)`, `Decisões a tomar (N)`) e os itens listados. **Read-only nesta story** — sem checkboxes/editar/rejeitar/guardar (isso é a 5.8). Bucket vazio: mostra "(0)" e fica colapsado por default (ratificação `[D-5.7-SCOPE]`, linha 79 — feedback de que a AI considerou o bucket sem roubar espaço). _Trace: FE §1.4 [5]._

- **AC6 (design system + a11y — `design-system-ia-avancada.md`):** fundo `#04040A`, buckets em glassmorphism; tipografia Inter (corpo) + JetBrains Mono (contadores); cores por bucket apenas da paleta (ex: tarefas Cyan, projectos Purple, ideias Gold, decisões — definir dentro da paleta, sem cores arbitrárias); secções colapsáveis acessíveis (`aria-expanded`, botão de toggle com nome acessível); overlay com `aria-live`/`role="status"`. Sem light mode. _Trace: `design-system-ia-avancada.md`; EPIC §8 A3 (a11y)._

- **AC7 (testes — `mock-protocol-fidelity.md` + `react-component-test-criteria.md`):** (a) helper puro `ai-parser.test.ts` ~100% (parse válido; **teste de fidelidade**: shape divergente — bucket renomeado, item sem `texto`, campo extra — **falha** o `.strict()`; `hasParsedContent`); (b) client `parser-cliente.test.ts` com `fetchFn` mock que **reflecte o protocolo real** da Anthropic Messages API (`content[].text` com JSON, fences, `!res.ok`, JSON inválido) — ≥1 teste que falharia se o wire shape divergisse; (c) teste de componente do display dos buckets: contar estados de render (`idle`/`loading`/`parsed`/`error` ⇒ ≥3 ⇒ **obrigatório**) — overlay em loading, 4 buckets em parsed, mensagem em error, contadores correctos. _Trace: `mock-protocol-fidelity.md`; `react-component-test-criteria.md`; AC2 EPIC §10 (latência é validada manualmente/perf, não em unit)._

- **AC8 (quality gates — reconciliado, decisão `@architect` tomada):** lint + typecheck + vitest PASS; cobertura dos helpers `lib/brain-dump/*.ts` ≥60% (NFR17, alvo ~100% no `ai-parser.ts` puro); CodeRabbit sem CRITICAL (NFR18). **`types/db.ts` e `lib/db/schemas.ts` ficam INTOCADOS** (`parsedOutput?: unknown` / `z.unknown()` mantêm-se — ratificação AC8 no gate de entrada): o tipo forte vive em `lib/brain-dump/ai-parser.ts` e o consumo valida com o parse de domínio. Sem version bump Dexie; suite schema-upgrade não é afectada (zero alterações na camada db). _Trace: NFR15/NFR17/NFR18; ratificação Aria 12/06._

---

## Tasks / Subtasks

- [x] **T0 — Architect Gate de Entrada (`@architect`):** EXECUTADO 12/06/2026 (Aria) — 5 decisões `[D-5.7-*]` ratificadas + decisão AC8 (`types/db.ts` intocado); registadas na secção "Ratificação" acima; AC1/AC2/AC8 reconciliados. **Pré-requisito de T1 satisfeito.**
- [x] **T1 — Helper puro de schema/parse (AC1):** `v2/lib/brain-dump/ai-parser.ts` — `BrainDumpWireSchema` + `BrainDumpParsedSchema` (ambos `.strict()`) + `BrainDumpParsed` + `parseBrainDumpWire` (throw PT-PT) + `enrichWithIds` (idFn injectável) + `hasParsedContent` + `BRAIN_DUMP_BUCKETS`. Sem React/Dexie/SDK. Cobertura 100%. (AC1)
- [x] **T2 — Client de inferência (AC2):** `v2/lib/brain-dump/parser-cliente.ts` — `/api/anthropic/proxy` JSON síncrono (`[D-5.7-MECHANISM]`), `max_tokens: 2048`, `temperature: 0`, system prompt PT-PT dos 4 buckets, `res.ok` antes do body, throw PT-PT, `stripJsonMarkdownFences`, `fetchFn`+`idFn` injectáveis, devolve domínio enriquecido. Espelha `estruturar-cliente.ts`. Cobertura 96,61%. (AC2)
- [x] **T3 — Estado + overlay no modal (AC4):** máquina `BrainDumpAiState` (`idle|loading|parsed|error`) — exportada do `BrainDumpModal`, controlada pelo `BrainDumpLauncher`; overlay "A estruturar…" `role="status"` + spinner Cyan + textarea readonly em loading; erro PT-PT `role="alert"`; `!hasParsedContent` → mensagem informativa. (AC4)
- [x] **T4 — Ligação do seam + persistência (AC3):** `BrainDumpLauncher.onStructure` real (async) → `parseBrainDump` → `createBrainDump({id,createdAt,bodyMarkdown,parsedOutput,status:'parsed'})` em sucesso; em falha estado `error`, zero writes. Comentário `[D-5.6-SEAM]` actualizado (endpoint SSE removido). (AC3)
- [x] **T5 — Display read-only dos 4 buckets (AC5, AC6):** 4 secções colapsáveis (não-vazias expandidas, vazias "(0)" colapsadas — `[D-5.7-SCOPE]`) + contadores JetBrains Mono + itens; design system (#04040A, glassmorphism, Inter); cores da paleta canónica (tarefas Cyan, projectos Purple, ideias Gold/Ouro, decisões Magenta); a11y (`aria-expanded`+`aria-controls`, toggle acessível); sem interactividade item-a-item (5.8). (AC5/AC6)
- [x] **T6 — Testes (AC7):** `ai-parser.test.ts` (16 testes, 100%, fidelidade `.strict()`: bucket renomeado/em falta/extra/item vazio/item-objecto FALHAM) + `parser-cliente.test.ts` (11 testes, fetch mock fiel ao protocolo Anthropic Messages, ≥1 falsificável) + `BrainDumpModal.test.tsx` (estados idle/loading/parsed/error + contadores + aria-expanded) + `BrainDumpLauncher.test.tsx` (pipeline AC3: persiste em sucesso, zero writes em falha, overlay pendente). (AC7)
- [x] **T7 — Quality gates (AC8):** lint 0 erros (1 warning pré-existente `logout/route.ts` fora scope) + typecheck 0 erros + vitest 1623/1623 PASS (sem regressões, +60 vs baseline 1563); cobertura helpers `ai-parser.ts` 100% / `parser-cliente.ts` 96,61% (combinada 98,18% ≥ 60%); `types/db.ts`/`schemas.ts` INTOCADOS (decisão AC8 da Ratificação); CodeRabbit pre-commit (uncommitted) **0 findings**. (AC8)

---

## Dev Notes

- **Reutilizar o padrão 5.4, não inventar.** `ai-parser.ts` ≈ `ai-estrutura.ts`; `parser-cliente.ts` ≈ `estruturar-cliente.ts`. Mesma filosofia: helper puro Zod `.strict()` (falsificável) + client edge-safe sem SDK/key + throw PT-PT + `stripJsonMarkdownFences`. [Source: `lib/diario/ai-estrutura.ts`, `lib/diario/estruturar-cliente.ts`]
- **`mock-protocol-fidelity.md` (A1 Epic 1) — obrigatório.** O mock da resposta Sonnet reflecte o protocolo real (`content[].text` com JSON dos 4 buckets); ≥1 teste falha se o shape divergir. O `.strict()` é o mecanismo que o garante. [Source: `.claude/rules/mock-protocol-fidelity.md`; EPIC §8 A1]
- **`external-contract-identifiers.md`:** os nomes de campo dos buckets (`tarefas`/`projectos`/`ideias`/`decisoes`) são identificadores de contrato interno do `parsedOutput` — ASCII, sem acentos/cedilha (validar no schema). As tools `criar_tarefa`/`criar_projecto`/`criar_nota` (se usadas — `[D-5.7-TOOLS]`) cruzam o Tool Registry e são da 5.13 (nomes já ASCII no EPIC row 5.13). [Source: `.claude/rules/external-contract-identifiers.md`]
- **`internal-state-contract-gate.md` — parcial na 5.7, completo na 5.8.** Na 5.7 aplica-se o eixo (c) falhas: rede/`!res.ok`/JSON inválido tratados como erro (nunca sucesso silencioso). O ciclo completo proposta→aceite/rejeitado→persistido (eixos a/b) é da 5.8. [Source: `.claude/rules/internal-state-contract-gate.md`; EPIC §8 A1 Epic 4]
- **`status: 'parsed'` é a única transição da 5.7.** `pending` nunca é persistido (parse falhado não cria dump); `partially/fully_approved` são da 5.8. [Source: `types/db.ts:261`; FR48/FR49]
- **Edge-safety (ADR-1/NFR5):** os helpers não importam `@anthropic-ai/sdk` nem `ANTHROPIC_API_KEY`. A key vive só no proxy server-side. [Source: `estruturar-cliente.ts:20-22`]
- **AC2 latência (<8s/200 palavras):** validação manual/perf, não unit. Sonnet `temperature:0`, `max_tokens` suficiente para 4 buckets (rever vs os 1024 do diário — 4 buckets podem precisar de mais). [Source: EPIC §10 AC2]
- **Display read-only é o seam para a 5.8.** Os checkboxes ☐/✏️/✗ e "Guardar N itens" do FE [6]-[8] são da 5.8; a 5.7 lista os itens sem acções. [Source: FE §1.4 [5]-[8]]
- **A2 Epic 4 (varredura de classe):** se o CR/gate apanhar um bug de protocolo/parse, verificar a mesma classe no parser do diário (5.4) e na pesquisa web (5.11 futura) no mesmo ciclo. [Source: EPIC §8 A2]
- **Sem version bump Dexie.** A tabela `brain_dumps` existe (5.1). Só o **tipo** de `parsedOutput` se concretiza — não toca `client.ts`/schema. Confirmar que nenhum teste schema-upgrade quebra. [Source: `brain-dumps.ts:10`]

### File Locations (esperado — sujeito a `[D-5.7-*]`)

| Ficheiro | Acção |
|----------|-------|
| `v2/lib/brain-dump/ai-parser.ts` | NOVO — helper puro (schema Zod `.strict()` 4 buckets + parse + `hasParsedContent`) |
| `v2/lib/brain-dump/parser-cliente.ts` | NOVO — client de inferência (proxy/SSE per `[D-5.7-MECHANISM]`) |
| `v2/components/brain-dump/BrainDumpModal.tsx` | EDITADO — estado `AiState` + overlay + display dos 4 buckets read-only |
| `v2/components/brain-dump/BrainDumpLauncher.tsx` | EDITADO — `onStructure` real (client + `createBrainDump`) + actualizar comentário `[D-5.6-SEAM]` (refere endpoint SSE que não existe — `[D-5.7-MECHANISM]`) |
| `v2/types/db.ts` | ~~POSSÍVEL~~ **INTOCADO** — ratificação AC8: `parsedOutput?: unknown` mantém-se; tipo forte vive no helper |
| `v2/tests/unit/lib/brain-dump/ai-parser.test.ts` | NOVO — helper puro (~100%, fidelidade) |
| `v2/tests/unit/lib/brain-dump/parser-cliente.test.ts` | NOVO — client (fetch mock fiel ao protocolo) |
| `v2/tests/unit/components/brain-dump/BrainDumpModal.test.tsx` | EDITADO — estados idle/loading/parsed/error + buckets |

## Testing

- Framework: Vitest + Testing Library (padrão Epics 3/4/5).
- **Helper puro (`ai-parser.test.ts`):** parse válido dos 4 buckets; `.strict()` falha com bucket renomeado / item sem `texto` / campo extra (fidelidade `mock-protocol-fidelity.md`); `hasParsedContent` (0 buckets → false). Cobertura ~100%.
- **Client (`parser-cliente.test.ts`):** `fetchFn` mock que devolve o **wire shape real** da Anthropic (`{ content: [{ type:'text', text:'<json>' }] }`); casos: JSON limpo, JSON com fences, `!res.ok` (lança), body sem `text` (lança), JSON inválido (lança). ≥1 teste que falharia se o wire shape divergisse.
- **Componente (`BrainDumpModal.test.tsx`):** estados idle (input) / loading (overlay + readonly) / parsed (4 buckets + contadores) / error (mensagem PT-PT); a11y dos colapsáveis (`aria-expanded`).
- Validação final: `npm run lint` + `npm run typecheck` + `npx vitest run`. Sem version bump → suite sem regressões.

---

## 🤖 CodeRabbit Integration

```yaml
Story Type Analysis:
  Primary Type: AI/Parser (Frontend + inferência)
  Secondary Type(s): Frontend (display dos buckets)
  Complexity: High  # protocolo AI + máquina de estados + persistência + display + fidelidade de mock

Specialized Agent Assignment:
  Primary Agents:
    - "@dev"          # executor
    - "@architect"    # quality gate (entrada + saída) — território de risco AI/estado
  Supporting Agents:
    - "@devops"       # gate de PR (CodeRabbit --base main)

Quality Gate Tasks:
  - "[ ] Architect Gate de ENTRADA (@architect): ratificar [D-5.7-*] antes de @dev implementar"
  - "[ ] Pre-Commit (@dev): coderabbit --prompt-only -t uncommitted antes de Ready for Review"
  - "[ ] Pre-PR (@devops): coderabbit --prompt-only --base main antes do PR"

CodeRabbit Focus Areas:
  Primary Focus:
    - Fidelidade de protocolo (mock-protocol-fidelity.md): mock reflecte a Anthropic Messages API; teste falsificável
    - Contrato de erro (internal-state-contract-gate.md eixo c): res.ok antes do body; falha nunca é sucesso silencioso
    - Edge-safety: helpers sem @anthropic-ai/sdk nem ANTHROPIC_API_KEY
  Secondary Focus:
    - Seam 5.7/5.8 limpo (display read-only; sem persistência item-a-item nem status partially/fully_approved)
    - Design system + a11y dos colapsáveis (aria-expanded, sem cores arbitrárias)
    - Identificadores de bucket ASCII (external-contract-identifiers.md)

Self-Healing Configuration:
  Primary Agent: "@dev" (light)
  Max Iterations: 2
  Timeout: 15 min
  Severity Filter: CRITICAL
  Predicted Behavior:
    - CRITICAL: auto_fix (até 2 iterações)
    - HIGH: document_only (Dev Notes)
```

---

## QA Results

### Architect Gate de SAÍDA — Aria (`@architect`), 12/06/2026

**Veredicto: PASS — `Status: InReview → Ready for Done`.**

**Escolha do gate alternativo (`separation-of-roles.md` regra 3):** a 5.7 é território AI/parser+estado (protocolo de inferência + máquina de estados + persistência). O `EPIC-5.md` §5 (row 5.7) e a nota de padrão ("AI/parser/pesquisa web/tools → gate `@architect`") atribuem o quality gate ao `@architect`, não ao `@qa` — o executor (`@dev` Dex) e o gate (`@architect` Aria) são agentes distintos (regra 1 satisfeita). Eu (Aria) ratifiquei também o Architect Gate de ENTRADA (T0, decisões `[D-5.7-*]`) e não toquei em nenhum ficheiro de implementação — o gate de saída verifica se a implementação honrou as decisões que ratifiquei, sem auto-aprovação de trabalho meu (não executei T1-T7). Precedente directo: Story 5.4 (mesmo par entrada+saída Aria).

**Evidência independente re-corrida por mim em `v2/` (não aceite do relatório do `@dev`):**

| Comando | Resultado |
|---------|-----------|
| `npx tsc --noEmit` | EXIT 0 (zero erros) |
| `npx eslint lib/brain-dump/ components/brain-dump/` | EXIT 0 (zero erros) |
| `npx vitest run tests/unit/lib/brain-dump tests/unit/components/brain-dump` | 60/60 PASS (5 ficheiros: ai-parser 16, parser-cliente 11, input 10, Launcher 4, Modal 19) |
| coverage `ai-parser.ts` | 100% stmts / 100% branch / 100% funcs / 100% lines |
| coverage `parser-cliente.ts` | 96,61% stmts / 100% funcs; linhas 95-96 não cobertas (ramo defensivo `proxyErrorMessage` — corpo de erro do proxy não-JSON, idêntico por construção a `estruturar-cliente.ts` da 5.4; sem lógica de domínio descoberta) |
| `git show 314a6bc8 --stat` | 9 ficheiros, 0 toques em `types/db.ts`/`lib/db/schemas.ts`/`client.ts` (sem version bump Dexie) |

**Tabela dos 9 checks obrigatórios:**

| # | Check | Veredicto | Evidência |
|---|-------|-----------|-----------|
| 1 | Conformidade `[D-5.7-MECHANISM/SHAPE/TOOLS/SCOPE/PERSIST]` + AC8 (db intocado) | PASS | `git show 314a6bc8 --stat`: `types/db.ts`/`schemas.ts` ausentes do diff. Mechanism: `parser-cliente.ts:44` `/api/anthropic/proxy`, `:50` `max_tokens 2048`, `:139` `temperature:0`, sem `stream`. Shape: `ai-parser.ts` wire=arrays-de-string + domínio=`{id,texto}` + `enrichWithIds`. Tools: zero `criar_*`/Tool Registry. Persist: `BrainDumpLauncher.tsx:63-82` write único pós-sucesso |
| 2 | AC1-AC8 vs ficheiros novos/editados | PASS | AC1-AC2 helpers conformes; AC3 seam real (`BrainDumpLauncher.tsx:63`); AC4 união discriminada `idle/loading/parsed/error` (`BrainDumpModal.tsx:44-48`); AC5 display read-only colapsável; AC6 cores paleta `#00F5FF/#9D00FF/#FFB800/#FF006E` + `aria-expanded`/`role=status`/`role=alert`; AC7 testes; AC8 gates verde |
| 3 | `mock-protocol-fidelity.md` (mock reflecte protocolo real + ≥1 falsificável) | PASS | `parser-cliente.test.ts:23-36` `proxyResponse` = wire real Anthropic (`content:[{type:'text',text}]`); `:121-131` LANÇA com bucket renomeado (falha `.strict()` — falsificável); `ai-parser.test.ts:83-93` item-objecto no wire FALHA |
| 4 | `internal-state-contract-gate.md` eixo (c): `res.ok` antes do body; zero writes em falha | PASS | `parser-cliente.ts:146` `if(!res.ok) throw` antes de `res.json()`; `BrainDumpLauncher.test.tsx:85-101` prova `createBrainDump` NÃO chamado em falha do parse |
| 5 | Edge-safety: zero `@anthropic-ai/sdk`/`ANTHROPIC_API_KEY` em `lib/brain-dump/` | PASS | `grep`: apenas menções em comentários (a afirmar a ausência); zero imports/usos |
| 6 | `external-contract-identifiers.md`: buckets ASCII | PASS | `ai-parser.ts:43` `['tarefas','projectos','ideias','decisoes']`; teste ASCII explícito `ai-parser.test.ts:155-161`; schema db não toca buckets (`z.unknown()` intocado) |
| 7 | Gates re-corridos por mim em `v2/` | PASS | typecheck EXIT 0; lint EXIT 0; vitest brain-dump 60/60; coverage 100%/96,61% (números reais acima) |
| 8 | Seam 5.7/5.8 limpo (read-only; sem `updateBrainDump`/transições 5.8/controlos item-a-item) | PASS | `grep updateBrainDump\|partially_approved\|fully_approved` em `components/brain-dump/`+`lib/brain-dump/` = só 1 comentário ("são da 5.8"); zero checkboxes/editar/rejeitar/"Guardar N"; `BrainDumpModal.tsx:399-568` display puramente read-only |
| 9 | PT-PT em UI/erro (amostragem) | PASS | "A estruturar…", "Estruturar com AI", "A AI não encontrou itens…", "Tarefas propostas/Projectos propostos/Ideias soltas/Decisões a tomar", throws "formato inesperado"/"não é JSON válido"/"proxy respondeu N" — todas PT-PT |

**Avaliação do `[AUTO-DECISION]` do `@dev` (onde mora o `BrainDumpAiState`):** arquitecturalmente SÃ e aprovada. O tipo+render do `AiState` exportado do `BrainDumpModal` (presentational, controlado por prop `aiState`, default `idle` → compat 5.6) com o **estado** detido pelo `BrainDumpLauncher` (dono do pipeline fetch+Dexie) separa correctamente render de I/O: o Modal testa-se sem mocks de rede/db; os efeitos concentram-se no Launcher (testado com mocks). Respeita o princípio "render testável / I/O isolado" e mantém o seam 5.6 intacto.

**CONCERNS (não-bloqueantes, severidade Baixa):**
1. `parser-cliente.ts:95-96` não cobertas (ramo defensivo `proxyErrorMessage`). Mesma situação aceite na 5.4 (`estruturar-cliente.ts`); cobertura combinada 98,18% ≥ 60% (NFR17). Sem acção.
2. AC2 latência <8s/200 palavras (`Not-tested:` no commit): validação manual/perf, não-unit — waiver VÁLIDO (`not-tested-trailer-rules.md`: edge de runtime, NÃO toca CI/config/build/segurança). Validar manualmente em produção pós-merge (responsabilidade do Eurico, como os AC de prod das stories anteriores).
3. CodeRabbit `--base main` (gate de PR) ainda não corrido — é do `@devops` no PR, não bloqueia o Architect Gate. O CR pre-commit (uncommitted) deu 0 findings.

**Justificação do PASS:** os 9 checks passam com evidência re-verificada por mim (typecheck/lint/vitest/coverage re-corridos, não aceites de fé). As 5 decisões `[D-5.7-*]` que ratifiquei no T0 estão honradas linha-a-linha no código. O seam 5.7/5.8 está limpo (zero superfície da 5.8). Constitution Art. IV (No Invention) respeitado — todas as afirmações têm âncora em ficheiro:linha. Nenhum finding bloqueador. Story segue para merge (`@devops`) e fecho (`@po *close-story` pós-merge).

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 12/06/2026 | 1.2 | **Fixes CR Iter 1 (PR #66) — `@dev` modo YOLO, Ready for Done mantido.** 8 findings do CodeRabbit cloud resolvidos (2 Major código + 2 Minor testes + 4 Minor docs, todos in-scope desta branch). **Major 1** `ai-parser.ts`: `z.string().min(1)` → `z.string().trim().min(1)` nos 4 buckets do `BrainDumpWireSchema` (linhas 58-61) E no `texto` do `BrainDumpItemSchema` (linha 72) — rejeita itens whitespace-only (`"   "`) que passariam o schema, seriam persistidos, contados por `hasParsedContent` e mostrados como linha vazia no preview. **Major 2** `parser-cliente.ts`: `parseBrainDump` ganha short-circuit `if (!bodyMarkdown.trim()) throw` no topo (antes de qualquer `fetch`) — input vazio/whitespace rejeitado localmente, zero chamadas/tokens ao proxy, resultado determinístico (defensivo: a UI da 5.6 já desactiva o botão com `canStructure`). **Minor testes**: +2 em `ai-parser.test.ts` (item whitespace-only LANÇA; itens válidos são trimmed) + 2 em `parser-cliente.test.ts` (input `''` e `'   \n\t  '` lançam `/vazio/` SEM chamar `fetchFn`, asserção `not.toHaveBeenCalled`). **Minor docs**: `AUDITORIA-ROADMAP` (contagem moderate 12→11 alinhada à tabela; ref handoff → `archive/`), `5.7.story.md` (MD040 ```yaml no bloco Executor Assignment; AC5 desambiguado para "(0) + colapsado" conforme `[D-5.7-SCOPE]` linha 79). Gates FRESCOS em `v2/`: lint 0 erros (1 warning `logout/route.ts` pré-existente fora scope), typecheck EXIT 0, vitest brain-dump 64/64 (era 60, +4), suite completa 1605 passed + suite `schema-upgrade-v3` (timeout de ambiente na corrida full) passa isolada 4/4 = 1609, zero falhas de asserção. Varredura de classe (lição A2 Epic 4): registada como dívida abaixo. SEM push (autoridade `@devops`). | Dex (`@dev`) |
| 12/06/2026 | 1.1 | **Architect Gate de SAÍDA (Aria `@architect`) — PASS, `InReview → Ready for Done`.** 9 checks obrigatórios verificados com evidência independente re-corrida em `v2/` (não aceite do relatório `@dev`): typecheck EXIT 0, lint EXIT 0, vitest brain-dump 60/60, coverage `ai-parser.ts` 100% / `parser-cliente.ts` 96,61% (linhas 95-96 = ramo defensivo idêntico à 5.4). 5 decisões `[D-5.7-*]` + AC8 honradas (db intocado, zero version bump Dexie). `mock-protocol-fidelity.md` (mock=wire Anthropic real + ≥1 falsificável `.strict()`), `internal-state-contract-gate.md` eixo c (`res.ok` antes do body, zero writes em falha), edge-safety (zero SDK/key), buckets ASCII, seam 5.7/5.8 limpo (zero controlos item-a-item/transições 5.8), PT-PT. `[AUTO-DECISION]` do dev (AiState exportado do Modal / estado no Launcher) avaliado e aprovado (render testável / I/O isolado). Gate alternativo `@architect` per `separation-of-roles.md` regra 3 (território AI/parser, executor≠gate). 3 CONCERNS Baixa não-bloqueantes (cobertura 95-96, AC2 latência manual, CR `--base main` é do PR). Próxima: `@devops *push` → PR → merge → `@po *close-story 5.7`. | Aria (`@architect`) |
| 11/06/2026 | 0.1 | Draft inicial a partir de `EPIC-5.md` §5 (row 5.7) + `PRD-NEXUS-V2.md` §6.9 (FR48) + `front-end-spec-v2.md` §1.4 [4]-[5]. Verificação em código: seam `onStructure` da 5.6 (`BrainDumpLauncher:47-49`), `BrainDump.parsedOutput: unknown` (5.7 tipa), repo `brain-dumps.ts` pronto, padrão 5.4 (`ai-estrutura.ts` + `estruturar-cliente.ts` + `[D-5.4-ENDPOINT]`) a reutilizar. **Architect Gate de ENTRADA** com 5 decisões a ratificar (`[D-5.7-MECHANISM]` proxy-vs-SSE, `[D-5.7-SHAPE]`, `[D-5.7-TOOLS]` ad-hoc-vs-Tool-Registry, `[D-5.7-SCOPE]` 5.7↔5.8, `[D-5.7-PERSIST]`). Seam 5.7/5.8: 5.7 = parse+persist(`parsed`)+display read-only; 5.8 = aprovação item-a-item. | River (`@sm`) |
| 12/06/2026 | 0.2 | **Architect Gate de Entrada (T0) EXECUTADO.** 5 decisões ratificadas: `[D-5.7-MECHANISM]`=(A) proxy JSON síncrono (FE spec §1.4 [4] reconciliada — transporte é decisão de arquitectura); `[D-5.7-SHAPE]`=separação wire/domínio (`BrainDumpWireSchema` arrays de strings + `BrainDumpParsedSchema` itens `{id,texto}` + `enrichWithIds` com `idFn` injectável — a AI não gera ids); `[D-5.7-TOOLS]`=(A) JSON ad-hoc (arch §16 `requiresPreview` aplica-se à 5.8/5.13); `[D-5.7-SCOPE]`=confirmada (controlos ☐/✏️/✗ do mock FE [5] são 5.8; buckets vazios "(0)" colapsados); `[D-5.7-PERSIST]`=confirmada (write único pós-sucesso, zero `pending`). Decisão AC8: `types/db.ts`/`schemas.ts` INTOCADOS. AC1/AC2/AC8 + T1/T2 + File Locations reconciliados. `max_tokens: 2048`. Evidência re-verificada em código (Art. IV). | Aria (`@architect`) |
| 12/06/2026 | 1.0 | **Implementação (`@dev`, T1-T7) — Approved→InReview, modo YOLO.** 5 ficheiros novos (`ai-parser.ts`, `parser-cliente.ts`, 3 testes) + 3 editados (`BrainDumpModal.tsx`, `BrainDumpLauncher.tsx`, `BrainDumpModal.test.tsx`). `[D-5.7-SHAPE]` separação wire/domínio (`BrainDumpWireSchema` arrays de strings + `enrichWithIds` ids no cliente + `BrainDumpParsedSchema` itens `{id,texto}`). `[D-5.7-MECHANISM]` `/api/anthropic/proxy` JSON síncrono `max_tokens 2048`; comentário `[D-5.6-SEAM]` actualizado. `[D-5.7-SCOPE]` display read-only colapsável (não-vazio expandido / vazio "(0)" colapsado), zero controlos item-a-item (5.8). `[D-5.7-PERSIST]` write único pós-sucesso. AC8: `types/db.ts`/`schemas.ts` INTOCADOS, sem version bump Dexie. Cores paleta canónica (Cyan/Purple/Gold/Magenta). Gates: typecheck 0 · lint 0 erros (1 warning pré-existente fora scope) · vitest 1623/1623 (+60) · cobertura `ai-parser.ts` 100% / `parser-cliente.ts` 96,61% · CodeRabbit pre-commit 0 findings. Gate de saída: `@architect` (Aria — `separation-of-roles.md`). | Dex (`@dev`) |
| 12/06/2026 | 0.3 | **Validação PO (`*validate-story`) — GO 9/10, Draft→Approved.** Condição READY-COM-CONDIÇÃO (T0 Architect Gate de entrada) satisfeita: T0 `[x]`, secção Ratificação com 5 `[D-5.7-*]` + decisão AC8. Evidência 100% re-verificada em código por amostragem (Art. IV): seam `BrainDumpLauncher.tsx:47-49`+`:18-19`, `types/db.ts:261-267` (`parsedOutput?: unknown`), `schemas.ts:457-463` (`z.unknown()`), repo `brain-dumps.ts` (helpers prontos), padrão 5.4 `lib/diario/ai-estrutura.ts`+`estruturar-cliente.ts` (Zod `.strict()`, `/api/anthropic/proxy`, `res.ok` antes do body, `stripJsonMarkdownFences`, `fetchFn` injectável, `max_tokens 1024`→2048 para 4 buckets), `lib/brain-dump/input.ts` (5.6, directório existe). 2 Should-Fix residuais não-bloqueantes (reconciliação pós-T0): [SF-5.7-1] AC5 ainda diz "ocultar ou (0)" mas D-5.7-SCOPE já fixou "(0) colapsado"; [SF-5.7-2] T7 ainda refere decisão `types/db.ts` "com @architect" como pendente mas já decidida na Ratificação (intocado). Autoridade está na secção Ratificação — @dev segue-a. | Pax (`@po`) |

---

## Dev Agent Record

### Agent Model Used

- **Architect Gate de Entrada (T0):** Aria (`@architect`) — claude-fable-5, 12/06/2026. Ratificação das 5 decisões `[D-5.7-*]` + decisão AC8 com evidência re-verificada em código.
- **Implementação (T1-T7):** Dex (`@dev`) — claude-fable-5, 12/06/2026. Modo YOLO autónomo.

### Debug Log References

- Nenhum bloqueio. node_modules do v2/ verificados completos (`@anthropic-ai/sdk`, `zod`, `@dnd-kit` presentes) antes dos gates — evita falsos negativos (gotcha conhecido do v2/).
- `[DEV-D-5.7-ENRICH]` Decisão de implementação: `enrichWithIds` é puro e não muta o wire (testado). O `idFn` é injectável no helper E propagado pelo `parser-cliente` (`ParseBrainDumpOpts.idFn`) → permite testes determinísticos do enriquecimento ponta-a-ponta sem mockar `crypto.randomUUID`.
- `[AUTO-DECISION]` Onde mora o `AiState`: a story dava margem ("`BrainDumpModal` ou wrapper"). Decisão → `BrainDumpAiState` é **exportado do `BrainDumpModal`** (tipo + render) mas o **estado é dono do `BrainDumpLauncher`** (que tem o pipeline AI/Dexie). Mantém o modal próximo de presentational (recebe `aiState` por prop, default `idle` → compat 5.6) e concentra os efeitos (fetch + write) no Launcher. Razão: separa render (testável sem mocks) de I/O (testado com mocks no Launcher).

### Completion Notes List

- **T1-T7 completas.** 5 ficheiros novos (2 helpers + 3 testes) + 3 editados (Modal, Launcher, Modal.test). `types/db.ts` e `lib/db/schemas.ts` INTOCADOS (decisão AC8). Sem version bump Dexie.
- **IDS protocol:** `ai-parser.ts` e `parser-cliente.ts` são CREATE justificados — ADAPTam o padrão validado da 5.4 (`lib/diario/ai-estrutura.ts` + `estruturar-cliente.ts`) mas o contrato difere (4 buckets wire/domínio separados vs 3 buckets opcionais 1:1). REUSE directo: `stripJsonMarkdownFences`, `DEFAULT_EXECUTOR_MODEL`, `createBrainDump`, paleta `lib/tags/colors.ts`.
- **`[D-5.7-SHAPE]` separação wire/domínio honrada:** a AI devolve textos (`BrainDumpWireSchema` = arrays de strings); ids atribuídos no cliente (`enrichWithIds`); persistido `BrainDumpParsed` (itens `{id,texto}`). Teste de fidelidade prova que um item-objecto `{id,texto}` no wire FALHA (a AI não gera ids).
- **`[D-5.7-MECHANISM]` consequências aplicadas:** (a) comentário `[D-5.6-SEAM]` no Launcher reescrito — já não refere `/api/agent/brain-dump` SSE; (b) `max_tokens: 2048` no `parser-cliente.ts` (vs 1024 do diário).
- **`[D-5.7-SCOPE]` fronteira 5.7↔5.8 limpa:** display read-only; teste explícito garante ZERO checkboxes e ZERO botão "Guardar N itens" (são da 5.8). Buckets vazios "(0)" colapsados; não-vazios expandidos.
- **`[D-5.7-PERSIST]` contrato de erro:** write único pós-sucesso; qualquer falha (rede/`!res.ok`/JSON inválido/shape inválido/Dexie) → estado `error`, zero writes. Teste do Launcher prova `createBrainDump` NÃO é chamado em falha do parse.
- **Cores AC6 documentadas:** tarefas Cyan `#00F5FF`, projectos Purple `#9D00FF`, ideias Gold `#FFB800` (Ouro), decisões Magenta `#FF006E` — todas da paleta canónica `TAG_PALETTE`, sem cores arbitrárias.
- **Gates:** typecheck 0 erros · lint 0 erros (1 warning pré-existente fora scope) · vitest 1623/1623 (98,80s) · cobertura helpers `ai-parser.ts` 100% / `parser-cliente.ts` 96,61% (combinada 98,18%) · CodeRabbit pre-commit 0 findings.
- **Nota para o gate `@architect`:** as linhas 95-96 não cobertas no `parser-cliente.ts` são o ramo de fallback do `proxyErrorMessage` (corpo de erro do proxy que não é JSON — `text.slice(0,200)`). É o mesmo ramo defensivo do `estruturar-cliente.ts` (5.4), idêntico por construção; não há lógica de domínio descoberta.

#### Fixes CR Iter 1 (PR #66) — v1.2

- **Major 1 — whitespace-only nos schemas (`ai-parser.ts`):** `z.string().min(1)` aceitava `"   "`. Corrigido para `z.string().trim().min(1)` nos 4 buckets do `BrainDumpWireSchema` (l. 58-61) E no `texto` do `BrainDumpItemSchema` (l. 72). O `.trim()` do Zod transforma a string ANTES da verificação de comprimento: um item em branco é rejeitado e os itens válidos ficam sem espaços nas pontas (normalização). Cobre wire (resposta crua da AI) e domínio (shape persistido/consumido pela 5.8).
- **Major 2 — short-circuit de input vazio (`parser-cliente.ts`):** `parseBrainDump` chamava o proxy mesmo com `bodyMarkdown` vazio/whitespace. Adicionado `if (!bodyMarkdown.trim()) throw new Error('O brain dump está vazio — escreve algum texto antes de estruturar.')` no topo, ANTES de resolver `fetchFn`. Rejeita localmente (zero chamadas/tokens), resultado determinístico em vez de depender do modelo. **Coerência UI verificada:** o `BrainDumpLauncher.handleStructure` (l. 63-82) envolve `parseBrainDump` num try/catch que mapeia qualquer throw → `setAiState({ kind: 'error', message })`, zero writes — o novo throw cai exactamente aí. O `BrainDumpModal` desactiva o botão "Estruturar" via `canStructure(text)` (≥50 chars, `disabled={!structurable}`) — o short-circuit é defesa em profundidade, não muda o fluxo normal. Nada quebra.
- **Minor testes:** `ai-parser.test.ts` +2 (item `'   '` LANÇA `/formato inesperado/`; `'  comprar tinta  '` é trimmed para `'comprar tinta'`). `parser-cliente.test.ts` +2 (input `''` e `'   \n\t  '` lançam `/vazio/` com `expect(fetchFn).not.toHaveBeenCalled()` — pinam que a rejeição é local). Brain-dump 60→64 testes.
- **Minor docs (todos in-scope desta branch):** `AUDITORIA-20260612-ROADMAP-CONCLUSAO.md` — contagem de moderate vulnerabilities alinhada (l. 18: "12 moderate" → "11 moderate", consistente com a tabela l. 13: 14 = 2 critical + 1 high + 11 moderate); ref do handoff DRAFTED apontada para `archive/` (l. 56, ficheiro já arquivado no INDEX). `5.7.story.md` — MD040 resolvido (bloco Executor Assignment com tag ```yaml); AC5 (l. 115) desambiguado de "ocultar ou (0)" para "(0) + colapsado" conforme a ratificação `[D-5.7-SCOPE]` (l. 79).
- **Varredura de classe (lição A2 Epic 4) — dívida identificada, NÃO corrigida (fora de scope da 5.7):** a mesma classe de bug foi procurada no parser do diário (Story 5.4):
  - `lib/diario/ai-estrutura.ts` — usa `z.string().optional()` (campos de texto livre, não arrays de itens). A verificação de conteúdo `hasStructuredContent` já faz `?.trim().length > 0`, pelo que um campo whitespace-only é tratado correctamente como "sem conteúdo". **NÃO é o mesmo defeito** (não há preview de itens com linhas vazias); risco residual baixo (o campo whitespace-only seria persistido cru, mas nunca contado como conteúdo).
  - `lib/diario/estruturar-cliente.ts` — `estruturarDiario` (l. 113-119) chama `fetchFn` sem short-circuit de input vazio. **A MESMA classe do Major 2 aplica-se.** Mitigante: o caller usa `shouldStructure` (threshold `bodyMarkdown.trim().length > STRUCTURE_THRESHOLD`, l. 47) como gate, mas o helper em si não é defensivo. **Recomendação:** aplicar o mesmo short-circuit numa story técnica de housekeeping do Epic 5 (alinhar com P1.3/P2 do roadmap). Registado aqui para varredura — não tocado nesta branch.

### File List

| Ficheiro | Acção |
|----------|-------|
| `v2/lib/brain-dump/ai-parser.ts` | NOVO — helper puro (schemas wire/domínio `.strict()` + parse + enrich + `hasParsedContent`) |
| `v2/lib/brain-dump/parser-cliente.ts` | NOVO — client de inferência (`/api/anthropic/proxy` JSON síncrono, `max_tokens 2048`) |
| `v2/components/brain-dump/BrainDumpModal.tsx` | EDITADO — `BrainDumpAiState` + overlay + display read-only dos 4 buckets |
| `v2/components/brain-dump/BrainDumpLauncher.tsx` | EDITADO — `onStructure` real (parse + `createBrainDump`) + comentário `[D-5.6-SEAM]` actualizado |
| `v2/tests/unit/lib/brain-dump/ai-parser.test.ts` | NOVO — 18 testes (100%, fidelidade `.strict()` + whitespace/trim — v1.2 +2) |
| `v2/tests/unit/lib/brain-dump/parser-cliente.test.ts` | NOVO — 13 testes (fetch mock fiel ao protocolo Anthropic + short-circuit input vazio — v1.2 +2) |
| `v2/tests/unit/components/brain-dump/BrainDumpLauncher.test.tsx` | NOVO — 4 testes (pipeline AC3: persiste/não-persiste/overlay) |
| `v2/tests/unit/components/brain-dump/BrainDumpModal.test.tsx` | EDITADO — +11 testes (estados idle/loading/parsed/error + aria-expanded) |
| `docs/AUDITORIA-20260612-ROADMAP-CONCLUSAO.md` | EDITADO (v1.2 CR) — contagem moderate 12→11; ref handoff → `archive/` |
| `docs/stories/active/5.7.story.md` | EDITADO (v1.2 CR) — MD040 ```yaml; AC5 desambiguado; Change Log + Completion Notes |
