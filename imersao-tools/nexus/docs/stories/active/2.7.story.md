# Story 2.7 — Motor de recorrência (FR10)

**Story ID:** 2.7
**Epic:** 2 — Tarefas v2 + Projectos
**Status:** Draft
**Data de criação:** 2026-05-20
**SM:** River (`@sm`)
**PO Validator:** Pax (`@po`) — pendente validação
**Executor previsto:** `@dev` (Dex) — motor client-side: `lib/shared/recurrence.ts` + geração de instâncias + hook `useRecurrenceEngine` + integração UI no formulário de tarefa
**Quality gate previsto:** `@architect` (Aria) — EPIC-2.md §5 indica gate `@architect`; separação A6 cumprida: Dex executa, Aria faz gate
**Story points estimados:** 8 (large — motor de lógica de domínio complexo + integração UI + ~20-25 testes Vitest). Nota: o [GAP-1] arquitectural foi resolvido (ADR-2.7-1) — não acrescenta esforço; mantém-se a estimativa pela complexidade do motor.
**Branch sugerida:** `feature/2.7-motor-recorrencia` (criar a partir de `main` actualizado — Story 2.6 MERGED 20/05/2026 via PR #27 squash `401ee594`)
**Desbloqueada por:** Story 2.1 (schema `recurrences` genérica + `RecurrenceSchema` Zod + `repos/recurrences.ts` + `lib/shared/recurrence.ts` skeleton — MERGED em main via PR #18 squash `86ddb6a6`, 15/05/2026)
**Bloqueia:** Epic 4 (Hábitos + Metas + Lembretes — reutiliza o motor de recorrência); Story 2.10 não depende directamente (tools `criar_tarefa` e `completar_tarefa` não exigem motor activo)

> **INFRAESTRUTURA PRONTA (Stories 2.1 + 2.3 + 2.4 + 2.5 + 2.6):**
> - `lib/shared/recurrence.ts` — `buildRRule(config: RecurrenceConfig): RRule` + `occurrencesBetween(rule, from, to): Date[]` — **ESTENDER** (não recriar)
> - `lib/db/repos/recurrences.ts` — `createRecurrence`, `getRecurrence`, `getRecurrenceByOwner(ownerType, ownerId)`, `deleteRecurrence` — **NÃO alterar**
> - `lib/db/schemas.ts` — `RecurrenceSchema` Zod + `RecurrenceOwnerTypeSchema` — **NÃO alterar**
> - `types/db.ts:79-86` — `interface Recurrence { id, rule, startDate, endDate, ownerType, ownerId }` — **NÃO alterar**
> - `types/db.ts:54-70` — `interface Task { ..., recurrenceId: string | null, parentTaskId: string | null, ... }` — os campos já existem; o motor popula-os
> - `lib/db/client.ts:version(2)` — tabela `recurrences: 'id, ownerType, ownerId, [ownerType+ownerId]'` — **NÃO alterar**
> - `lib/db/repos/tasks.ts` — `createTask`, `listTasks`, `updateTask`, `setTaskStatus` — **NÃO alterar** (motor chama `createTask` para instâncias)
> - `app/(app)/tarefas/page.tsx` — página de tarefas existente (Stories 2.3+2.6) — **integrar** formulário de recorrência
>
> **[GAP-1] RESOLVIDO (20/05/2026 — `@architect` Aria):** Mecanismo de activação do motor de recorrência decidido como **`useEffect` one-shot no mount da page `/tarefas`** (cron client-side). A nota `architecture-v2.md` §16 L1123 (ServiceWorker) foi corrigida para alinhar com PRD §10 L446 + EPIC-2 §7. Decisão formal e justificação em **ADR-2.7-1** (secção final desta story). A divergência PRD/EPIC-2 ↔ arquitectura está reconciliada — esta story pode avançar para PO Validation e implementação.

---

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, vitest, build, review-lógica-recorrência]
```

> Trace: `EPIC-2.md` §5 (Story 2.7, executor `@dev`, quality gate `@architect`). Respeita `separation-of-roles.md` (A6) — `executor != quality_gate`. Dex implementa motor + UI; Aria faz gate de arquitectura (lógica de recorrência + mecanismo de activação + cobertura de testes).

---

## Nota do `@sm` River sobre escopo e dependências

A Story 2.7 no `EPIC-2.md` §5 está descrita como **"Geração instâncias recorrentes (cron client-side via `setInterval` ou `requestIdleCallback`) — horizonte 90 dias"** traçando FR10. O PRD §6.2 linha 133 confirma:

> **FR10:** Recorrência configurável: diária, semanal, mensal, dias úteis, fim-de-semana, dia específico do mês. Trace: JARVIS.txt L122-129.

O PRD §10 Epic 2 (linha 446) descreve: `"2.7 Geração instâncias recorrentes (cron client-side via setInterval ou requestIdleCallback)"`.

A arquitectura `architecture-v2.md` §16 L1123 diz: `"Story 2.7 instâncias recorrentes geradas por ServiceWorker (não setInterval do tab) usando Background Sync ou no activate event do SW"`.

Esta divergência é o principal bloqueador desta story (ver [GAP-1]).

**Decisões autónomas do SM:**

[AUTO-DECISION A1] **Escopo da geração de instâncias:** horizonte fixo de **90 dias** a partir da data actual (`Date.now()` + 90 * 24 * 60 * 60 * 1000), conforme EPIC-2.md §5 e AC Epic 2 AC3. O engine gera instâncias (tasks filhas com `parentTaskId` = UUID da task-mãe) para todas as `Recurrence` com `ownerType: 'task'` dentro do horizonte. Razão: PRD §6.2 AC3 diz explicitamente "horizonte de 90 dias". Alternativa "horizonte infinito" descartada — levaria a milhares de tasks em IndexedDB + UI congestionada.

[AUTO-DECISION A2] **Tipos de recorrência suportados** — exactamente os do FR10 (sem invenção):
- Diária (`DAILY`)
- Semanal (`WEEKLY`) — dia(s) da semana especificados
- Mensal (`MONTHLY`) — dia do mês especificado
- Dias úteis (`WEEKLY`, `byweekday: [0,1,2,3,4]`) — Segunda a Sexta
- Fim-de-semana (`WEEKLY`, `byweekday: [5,6]`) — Sábado + Domingo
- Dia específico do mês (`MONTHLY`, `bymonthday: [N]`)

Razão: FR10 é explícito nestes 6 tipos. A estrutura `RecurrenceConfig` já os suporta via `buildRRule`. Alternativa "usar RRULE string livre" descartada — desnecessariamente complexo para o Eurico; a UI deve expor os 6 tipos como opções discretas, não uma string RRULE manual.

[AUTO-DECISION A3] **Idempotência da geração:** antes de criar uma instância para uma data, verificar se já existe uma task filha com `parentTaskId === recurrence.ownerId` para essa data via `db.tasks.where('parentTaskId').equals(ownerId).toArray()` + filtro `dueDate === isoDate`. Razão: o motor pode correr múltiplas vezes (ao reload, ao SW activate); criar duplicados quebraria a UI. Alternativa "apagar e recriar todas as instâncias do horizonte" descartada — destrutivo (apagaria contexto/notas nas tasks filhas já existentes).

[AUTO-DECISION A4] **`lib/shared/recurrence.ts` estendido** com funções adicionais ao skeleton existente:
- `generateTaskInstances(recurrence: Recurrence, horizonDays?: number): Promise<void>` — gera instâncias dentro do horizonte, idempotente
- `runRecurrenceEngine(): Promise<{ created: number; skipped: number }>` — itera todas as `Recurrence` com `ownerType: 'task'`, chama `generateTaskInstances` para cada
- `buildRecurrenceConfig(freq, opts): RecurrenceConfig` — helper que abstrai a construção de `RecurrenceConfig` para os 6 tipos do FR10 (facilita a UI)

[AUTO-DECISION A5] **UI de configuração de recorrência no formulário de tarefa** (`app/(app)/tarefas/page.tsx` ou componente `TaskFormModal.tsx` se existir). Toggle "Tarefa recorrente" → expande para selector do tipo de recorrência (dropdown com os 6 tipos) + selector de data de início + selector de data de fim opcional. Ao criar/editar uma tarefa com recorrência, a UI:
1. Cria a `Recurrence` via `createRecurrence` com `ownerType: 'task'`, `ownerId: task.id`.
2. Actualiza a task com `recurrenceId: recurrence.id` via `updateTask`.
3. Chama `generateTaskInstances(recurrence)` para popular o horizonte imediatamente.

[AUTO-DECISION A6] **Resolução parcial do [GAP-1]:** enquanto aguarda decisão formal de `@architect`, a implementação segue a abordagem mais simples e confirmada pelo PRD + EPIC-2: **`useEffect` com activação no primeiro render da page `/tarefas`** (equivalente ao "primeiro carregamento do dia" mencionado no EPIC-3 §16 para padrão similar). A activação via ServiceWorker fica como `[GAP-1]` explícito — se `@architect` decidir SW, o único ficheiro a alterar é o ponto de activação (o motor em `lib/shared/recurrence.ts` é agnóstico ao mecanismo de activação). Razão: implementar SW Background Sync aumentaria o escopo em 2-3x (VAPID, SW registration, Background Sync API) sem dados que provem que o Eurico precisa de geração offline. O `useEffect` no primeiro render resolve o caso de uso imediato. Alternativa "só setInterval" descartada — setInterval activo em tab abierta desperdiça CPU; `useEffect` no mount do page é equivalente a "on load" sem polling.

[AUTO-DECISION A7] **Hook `useRecurrenceEngine`** exposto para a page `/tarefas` activar o motor:
```ts
// hooks/useRecurrenceEngine.ts
export function useRecurrenceEngine(): void {
  useEffect(() => {
    runRecurrenceEngine().catch(console.error);
  }, []); // runs once on mount — equivalent to "first load"
}
```
A page `/tarefas` chama `useRecurrenceEngine()` no topo. Zero parâmetros — o motor lê as `Recurrence` directamente da DB.

[AUTO-DECISION A8] **Formulário de recorrência** no modal/UI de criação de tarefa. Verificar se `TaskFormModal` existe no codebase; se não, a Story 2.7 adiciona um componente `RecurrenceFieldset.tsx` injectável no formulário de tarefa existente. Decisão final durante T1.2 — leitura do formulário de tarefa actual. **Não criar um modal novo se o formulário já existir.** Sem modificar a estrutura de validação Zod de `TaskSchema` (já tem `recurrenceId` como nullable — não alterar).

[AUTO-DECISION A9] **Instâncias recorrentes na UI** devem ser distinguíveis visualmente (badge "Recorrente" ou ícone de ciclo). A task-mãe (`parentTaskId: null`, `recurrenceId: non-null`) exibe o tipo de recorrência. As tasks filhas (`parentTaskId: non-null`) exibem badge "Instância" e link para a task-mãe. Implementação cirúrgica em `TaskRow.tsx` ou `KanbanCard.tsx` — apenas badge adicional, sem refactor estrutural.

[AUTO-DECISION A10] **Completar uma instância NÃO cancela recorrência futura.** Completar a task filha de hoje (`status: 'done'`) não afecta as instâncias futuras já criadas nem a `Recurrence`. Para cancelar a recorrência, o Eurico elimina a `Recurrence` via a UI (botão "Cancelar recorrência" na task-mãe — confirmar com `window.confirm` PT-PT).

**O que esta story entrega (resumo):**

1. **`lib/shared/recurrence.ts` estendido**: `buildRecurrenceConfig(freq, opts)` + `generateTaskInstances(recurrence, horizonDays?)` (idempotente) + `runRecurrenceEngine()`.
2. **`hooks/useRecurrenceEngine.ts`**: hook de activação one-shot no mount.
3. **`components/tarefas/RecurrenceFieldset.tsx`**: campo de configuração de recorrência (toggle + 6 tipos + data início + data fim opcional) — injectável no formulário de tarefa.
4. **Integração na page `/tarefas`**: chamada a `useRecurrenceEngine()` no mount + lógica de criação/edição de task com recorrência.
5. **Distinção visual de instâncias**: badge "Recorrente" na task-mãe + badge "Instância" nas filhas em `TaskRow.tsx`/`KanbanCard.tsx`.
6. **Testes Vitest** — ~20-25 cenários cobrindo lógica de recorrência, idempotência, 6 tipos FR10, geração dentro/fora do horizonte, cancelamento.

---

## User Story

**As a** Eurico,
**I want** poder marcar qualquer tarefa como recorrente (diária, semanal, mensal, dias úteis, fim-de-semana, ou dia específico do mês) e ter as instâncias futuras geradas automaticamente para um horizonte de 90 dias —,
**so that** consigo gerir tarefas que se repetem (exercício, revisão semanal, pagamentos mensais) sem criar manualmente cada ocorrência, cumprindo FR10 (Recorrência configurável) e mantendo o design system inegociável.

---

## Contexto

| Pilar | Detalhe |
|-------|---------|
| **PRD** | FR10: "Recorrência configurável: diária, semanal, mensal, dias úteis, fim-de-semana, dia específico do mês. Trace: JARVIS.txt L122-129." + §8.2 "Datas/Recorrência: date-fns + rrule". Trace: `PRD-NEXUS-V2.md` §6.2 linha 133 + §8.2 linha 336 + §10 Epic 2 linha 446. |
| **Arquitectura** | `architecture-v2.md` §16 L1123: "Story 2.7 instâncias recorrentes geradas por ServiceWorker". ADR-2 (Dexie 4) — tabela `recurrences` genérica. `lib/shared/recurrence.ts` skeleton criado na Story 2.1 com `buildRRule` + `occurrencesBetween`. EPIC-2 §7 reconcilia como "cron client-side via `setInterval` ou `requestIdleCallback`". |
| **[GAP-1]** | Divergência entre PRD/EPIC-2 (setInterval/requestIdleCallback) e arquitectura §16 L1123 (ServiceWorker). Esta story implementa `useEffect` on-mount como solução pragmática pendente de decisão formal de `@architect`. Ver secção [GAP] no final. |
| **Schema** | `Recurrence { id, rule: string (RRULE), startDate, endDate, ownerType: 'task', ownerId: taskId }`. `Task { ..., recurrenceId: string|null, parentTaskId: string|null }`. Ambos já em `types/db.ts` e tabela `recurrences` em `version(2)`. |
| **Library** | `rrule` v2.8.0 já em `package.json` (`architecture-v2.md §16 L1180`). `date-fns` v4.1.0 idem. Disponíveis sem instalação adicional. |
| **Design system** | `.claude/rules/design-system-ia-avancada.md` — badges: `background: rgba(0,245,255,0.08); border: 1px solid rgba(0,245,255,0.2); border-radius: 20px; font-family: JetBrains Mono; font-size: 0.65rem`. Badge "Recorrente" usa Cyan; badge "Instância" usa Grey. |
| **Lições Epic 1** | A1 (mock-protocol-fidelity) — N/A (sem mocks de protocolos externos). A2 (Not-Tested Evidence Gate) — relevante: se algum commit tocar `vitest.config.ts` ou `package.json:scripts`, requerer evidência local. A6 (separation-of-roles) — Dex executa, Aria faz gate. |
| **PT-PT** | `language-standards.md` — zero "você"/"sua"/"em um", tutear, "utilizar" não "usar". Textos UI: "Recorrente", "Instância", "Tipo de recorrência", "Data de início", "Data de fim (opcional)", "Cancelar recorrência". |
| **Epic 4 partilha** | O motor criado aqui (`lib/shared/recurrence.ts`) será reutilizado pelos Epics 3 (Finanças recorrentes) e 4 (Hábitos + Lembretes). Manter a função `runRecurrenceEngine()` genérica e parametrizada por `ownerType` para facilitar extensão futura. |

### Interfaces relevantes (verificadas directamente em código)

```typescript
// types/db.ts (Story 0.3 + 2.1) — confirmadas estáveis
interface Task {
  id: string;
  // ...
  recurrenceId: string | null;   // FK Recurrence (null se não-recorrente)
  parentTaskId: string | null;   // UUID da task-mãe (null se task-mãe ou não-recorrente)
  createdAt: number;
  updatedAt: number;
}

interface Recurrence {
  id: string;
  rule: string;                  // RRULE string — gerado por buildRRule()
  startDate: string;             // ISO YYYY-MM-DD
  endDate: string | null;        // null = sem fim
  ownerType: 'task' | 'transaction' | 'habit' | 'reminder';
  ownerId: string;               // taskId da task-mãe
}

// lib/shared/recurrence.ts (Story 2.1 skeleton) — ESTENDER
interface RecurrenceConfig {
  freq: 'DAILY' | 'WEEKLY' | 'MONTHLY' | 'YEARLY';
  interval?: number;
  byweekday?: number[];  // 0=Seg..6=Dom (rrule constants)
  bymonthday?: number[];
  startDate: string;     // ISO YYYY-MM-DD
  endDate?: string | null;
  count?: number;
}
// existentes: buildRRule(config): RRule, occurrencesBetween(rule, from, to): Date[]
// NOVOS na 2.7:
// buildRecurrenceConfig(freq, opts): RecurrenceConfig  (ver A2/A4)
// generateTaskInstances(recurrence, horizonDays?): Promise<void>  (idempotente, A3)
// runRecurrenceEngine(): Promise<{ created: number; skipped: number }>  (A4)
```

### Mapeamento FR10 → RecurrenceConfig

| Tipo UI (PT-PT) | freq | byweekday | bymonthday | notes |
|-----------------|------|-----------|------------|-------|
| Diária | `DAILY` | — | — | interval = 1 |
| Semanal | `WEEKLY` | [dia da semana seleccionado] | — | ex: [1] = Terça |
| Mensal | `MONTHLY` | — | [dia do mês seleccionado] | ex: [15] = dia 15 |
| Dias úteis | `WEEKLY` | [0,1,2,3,4] | — | Seg-Sex |
| Fim-de-semana | `WEEKLY` | [5,6] | — | Sáb+Dom |
| Dia específico do mês | `MONTHLY` | — | [N] | equivalente a "Mensal" — pode ser unificado; SM deixa decisão de UI ao Dex |

> **Nota SM:** "Mensal" e "Dia específico do mês" são funcionalmente idênticos via `MONTHLY + bymonthday: [N]`. A UI pode expô-los como uma opção única "Mensal — dia N" com um picker. Decisão final de UX ao Dex (executor) — não é restrição do SM.

---

## Acceptance Criteria

- **AC1:** **`lib/shared/recurrence.ts` estendido** com `buildRecurrenceConfig`:
  - Assinatura: `export function buildRecurrenceConfig(type: RecurrenceType, opts: RecurrenceTypeOpts): RecurrenceConfig`
  - `RecurrenceType` é um string literal union dos 6 tipos do FR10: `'daily' | 'weekly' | 'monthly' | 'weekdays' | 'weekends' | 'monthly-specific-day'`.
  - Para cada tipo, `buildRecurrenceConfig` retorna a `RecurrenceConfig` correcta (ver tabela de mapeamento acima).
  - Lança `Error('Tipo de recorrência inválido: {type}')` PT-PT em PT para tipos desconhecidos.
  - Trace: FR10 + `architecture-v2.md` §6.2 `RecurrenceConfig`.

- **AC2:** **`lib/shared/recurrence.ts` estendido** com `generateTaskInstances`:
  - Assinatura: `export async function generateTaskInstances(recurrence: Recurrence, horizonDays?: number): Promise<{ created: number; skipped: number }>`
  - `horizonDays` defaul 90 (conforme EPIC-2 §5 + AC Epic 2 AC3).
  - Reconstrói a `RRule` a partir de `recurrence.rule` via `RRule.fromString(recurrence.rule)`.
  - Calcula ocorrências entre `today` e `today + horizonDays` via `occurrencesBetween`.
  - Para cada ocorrência:
    1. Verifica se já existe task filha com `parentTaskId === recurrence.ownerId` e `dueDate === isoDate` (idempotência — A3).
    2. Se já existe: incrementa `skipped`.
    3. Se não existe: cria task filha via `createTask({ title: motherTask.title, ..., recurrenceId: recurrence.id, parentTaskId: recurrence.ownerId, dueDate: isoDate })` + incrementa `created`.
  - A task filha herda: `title`, `description`, `priority`, `projectId`, `tags` da task-mãe. `status` começa em `'todo'`. `context` e `lastWorkedAt` começam `null`.
  - `Recurrence.endDate !== null` → não gera instâncias após `endDate`.
  - Trace: FR10 + `architecture-v2.md` §6.2 L497 (`parentTaskId`).

- **AC3:** **`lib/shared/recurrence.ts` estendido** com `runRecurrenceEngine`:
  - Assinatura: `export async function runRecurrenceEngine(): Promise<{ created: number; skipped: number; errors: number }>`
  - Lê **todas** as `Recurrence` com `ownerType: 'task'` via `db.recurrences.where('ownerType').equals('task').toArray()`.
  - Para cada recorrência, chama `generateTaskInstances(recurrence)`.
  - Captura erros por recorrência em try/catch — um erro numa recorrência não interrompe as restantes; incrementa `errors`.
  - Retorna contadores agregados `{ created, skipped, errors }`.
  - Trace: EPIC-2 §5 Story 2.7 + AC Epic 2 AC3.

- **AC4:** **`hooks/useRecurrenceEngine.ts`** criado:
  - Assinatura: `export function useRecurrenceEngine(): void`
  - `useEffect(() => { runRecurrenceEngine().catch(console.error); }, [])` — activa o motor **exactamente uma vez** no mount da page `/tarefas` (cron client-side).
  - **Mecanismo de activação decidido por ADR-2.7-1** (`@architect`, 20/05/2026): `useEffect` one-shot on-mount. Não há activação por `setInterval`, `requestIdleCallback` ou ServiceWorker no Epic 2. O motor em `lib/shared/recurrence.ts` permanece agnóstico ao mecanismo — uma futura migração para ServiceWorker (Epic 4, se justificada) substitui apenas este hook, sem tocar no motor.
  - Trace: ADR-2.7-1 + EPIC-2 §7 + PRD §10 linha 446 + `architecture-v2.md` §16 (corrigido 20/05/2026).

- **AC5:** **`app/(app)/tarefas/page.tsx`** chama `useRecurrenceEngine()` no topo do componente. O motor corre uma vez no mount da página, após o qual `useTasks()` e `useLiveQuery` reactivos reflectem as novas instâncias criadas.
  - Trace: A5 + A7.

- **AC6:** **`components/tarefas/RecurrenceFieldset.tsx`** criado:
  - Props: `value: RecurrenceFieldValue | null; onChange: (v: RecurrenceFieldValue | null) => void` onde `RecurrenceFieldValue = { type: RecurrenceType; startDate: string; endDate?: string | null; weekday?: number; monthday?: number }`.
  - Toggle `<input type="checkbox" aria-label="Tarefa recorrente">` — se desmarcado, `onChange(null)`.
  - Se marcado, expande para:
    - `<select aria-label="Tipo de recorrência">` com 6 opções PT-PT (Diária, Semanal, Mensal, Dias úteis, Fim-de-semana, Dia específico do mês).
    - Se "Semanal": `<select aria-label="Dia da semana">` (Segunda, Terça, Quarta, Quinta, Sexta, Sábado, Domingo).
    - Se "Mensal" ou "Dia específico do mês": `<input type="number" min="1" max="31" aria-label="Dia do mês">`.
    - `<input type="date" aria-label="Data de início da recorrência">` — obrigatório.
    - `<input type="date" aria-label="Data de fim da recorrência (opcional)">` — opcional.
  - Validação: `startDate` obrigatório; se `endDate` preenchido, deve ser `>= startDate`.
  - Trace: FR10 + A5 + A8.

- **AC7:** **Integração no formulário de criação/edição de tarefa.** A story verifica (T1.2) se existe `TaskFormModal.tsx` ou equivalente. Se existir, injecta `RecurrenceFieldset` como campo adicional. Se não existir, a integração é directa na lógica da `tarefas/page.tsx` no handler `onSubmit`. Em qualquer caso:
  - Se `RecurrenceFieldset` retorna valor não-null ao submeter:
    1. Constrói `RecurrenceConfig` via `buildRecurrenceConfig(value.type, opts)`.
    2. Constrói `RRULE string` via `buildRRule(config).toString()`.
    3. Persiste `Recurrence` via `createRecurrence({ id: crypto.randomUUID(), rule: rruleStr, startDate, endDate: endDate ?? null, ownerType: 'task', ownerId: task.id })`.
    4. Actualiza a task com `updateTask(task.id, { recurrenceId: recurrence.id })`.
    5. Chama `generateTaskInstances(recurrence)` para popular o horizonte imediatamente.
  - Se tarefa editada já tem `recurrenceId` e user remove a recorrência (toggle desmarcado): chama `deleteRecurrence(task.recurrenceId)` + `updateTask(task.id, { recurrenceId: null })`.
  - Trace: A5 + AC2 + AC3.

- **AC8:** **Distinção visual de tasks recorrentes e instâncias.** Em `components/tarefas/TaskRow.tsx` (e/ou `KanbanCard.tsx`):
  - Task-mãe (`recurrenceId !== null && parentTaskId === null`): badge "Recorrente" JetBrains Mono 0.65rem, `background: rgba(0,245,255,0.08); border: 1px solid rgba(0,245,255,0.2); border-radius: 20px; color: #00F5FF`.
  - Task filha/instância (`parentTaskId !== null`): badge "Instância" com style Cyan-muted (Grey: `background: rgba(136,146,164,0.08); border: 1px solid rgba(136,146,164,0.2); color: #8892A4`).
  - Zero modificação estrutural dos componentes — apenas um badge adicional condicional.
  - Trace: A9 + design-system-ia-avancada.md.

- **AC9:** **Cancelar recorrência** (na task-mãe, se `recurrenceId !== null && parentTaskId === null`): botão "Cancelar recorrência" (secondary, estilo Magenta destructive `#FF006E`) na task-mãe. `window.confirm` PT-PT: `"Cancelar a recorrência desta tarefa? As instâncias futuras não serão eliminadas automaticamente."`. Se confirmado: `deleteRecurrence(task.recurrenceId)` + `updateTask(task.id, { recurrenceId: null })`. As instâncias filhas já criadas **ficam** como tasks normais (não são eliminadas — ver A10). Trace: A10 + FR10.

- **AC10:** **Completar uma instância NÃO cancela recorrência.** `setTaskStatus(instanceId, 'done')` não afecta a `Recurrence` nem as outras instâncias. Verificado por T12. Trace: A10.

- **AC11:** **PT-PT consistente** em todos os textos UI:
  - Fieldset: "Tarefa recorrente", "Tipo de recorrência", "Diária", "Semanal", "Mensal", "Dias úteis", "Fim-de-semana", "Dia específico do mês", "Dia da semana", "Dia do mês", "Data de início da recorrência", "Data de fim da recorrência (opcional)".
  - Badges: "Recorrente", "Instância".
  - Confirmação cancelamento: "Cancelar a recorrência desta tarefa? As instâncias futuras não serão eliminadas automaticamente."
  - Erro data: "Data de início é obrigatória", "Data de fim deve ser posterior à data de início".
  - Zero PT-BR. Zero emojis nas mensagens de erro.
  - Trace: `language-standards.md`.

- **AC12:** **Acessibilidade WAI-ARIA**:
  - Toggle recorrência: `<input type="checkbox" aria-label="Tarefa recorrente">`.
  - Selectores: `aria-label` em cada `<select>`.
  - Inputs date: `aria-label` + `aria-describedby` para mensagens de erro.
  - Badges: `aria-label="Tarefa recorrente"` e `aria-label="Instância recorrente"`.
  - Sem aria warnings em axe-core spot-check manual durante QA gate.

- **AC13:** **Testes Vitest + Testing Library** cobrem (~20-25 cenários):
  - **T1 — `buildRecurrenceConfig` todos os 6 tipos**: cada tipo retorna `RecurrenceConfig` correcta (freq, byweekday, bymonthday).
  - **T2 — `buildRecurrenceConfig` tipo inválido**: lança `Error` PT-PT.
  - **T3 — `generateTaskInstances` diária horizonte 90 dias**: 1 recorrência diária starting today → gera 90 instâncias.
  - **T4 — `generateTaskInstances` idempotência**: correr 2x com mesma recorrência → `skipped === N` na 2ª corrida, sem duplicados.
  - **T5 — `generateTaskInstances` com endDate**: recorrência semanal com `endDate` daqui a 10 dias → gera apenas instâncias ≤ endDate.
  - **T6 — `generateTaskInstances` dias úteis**: `byweekday [0,1,2,3,4]` → instâncias apenas em dias Seg-Sex dentro do horizonte.
  - **T7 — `generateTaskInstances` fim-de-semana**: `byweekday [5,6]` → instâncias apenas em Sáb+Dom.
  - **T8 — `generateTaskInstances` mensal dia 15**: recorrência `MONTHLY bymonthday [15]` → instâncias apenas no dia 15 de cada mês dentro do horizonte.
  - **T9 — `generateTaskInstances` herança de campos**: instância filha herda `title`, `description`, `priority`, `projectId`, `tags` da mãe; `status = 'todo'`; `context = null`; `parentTaskId === ownerId`.
  - **T10 — `runRecurrenceEngine` conta correcta**: 2 recorrências activas → `created` correcta; correr 2x → `skipped` correcta.
  - **T11 — `runRecurrenceEngine` tolerância a erros**: se 1 recorrência lança erro interno → `errors === 1`, outras processadas.
  - **T12 — completar instância não cancela recorrência**: `setTaskStatus(instanceId, 'done')` → `Recurrence` continua intacta; próxima instância futura não afectada.
  - **T13 — cancelar recorrência**: `deleteRecurrence` + `updateTask({ recurrenceId: null })` → task-mãe com `recurrenceId === null`; instâncias filhas existentes ficam com `parentTaskId` original (não eliminadas).
  - **T14 — `RecurrenceFieldset` render base**: toggle desmarcado → campos ocultos.
  - **T15 — `RecurrenceFieldset` toggle activa campos**: toggle marcado → campos de tipo, data início visíveis.
  - **T16 — `RecurrenceFieldset` tipo Semanal**: select "Semanal" → picker de dia da semana aparece.
  - **T17 — `RecurrenceFieldset` tipo Mensal**: select "Mensal" → input de dia do mês aparece.
  - **T18 — `RecurrenceFieldset` validação startDate obrigatório**: submit sem startDate → mensagem PT-PT visível.
  - **T19 — `RecurrenceFieldset` endDate < startDate**: `endDate < startDate` → mensagem PT-PT "Data de fim deve ser posterior à data de início".
  - **T20 — badge Recorrente visível**: task com `recurrenceId !== null && parentTaskId === null` → badge "Recorrente" no `TaskRow`.
  - **T21 — badge Instância visível**: task com `parentTaskId !== null` → badge "Instância" no `TaskRow`.
  - **T22 — `useRecurrenceEngine` chama `runRecurrenceEngine` uma vez no mount**: mock de `runRecurrenceEngine` → chamado exactamente 1x após render.
  - **T23 — confirmação cancelamento**: click "Cancelar recorrência" → `window.confirm` chamado 1x com mensagem PT-PT; se confirmar → `deleteRecurrence` e `updateTask` chamados.
  - **T24 — cancelamento abortado**: `window.confirm` retorna false → `deleteRecurrence` NÃO chamado.

- **AC14:** **Quality gates locais** passam em `imersao-tools/nexus/v2/`:
  - `npm run lint` — exit 0 (warnings pré-existentes permitidos — não introduzir novos).
  - `npm run typecheck` — exit 0.
  - `npm run test:unit` — todos os existentes (após Story 2.6, ≥529 testes) + os ~20-25 novos passam.
  - `npm run build` — exit 0.

- **AC15:** **Coverage** medida via `npm run test:coverage`:
  - `lib/shared/recurrence.ts` >= **80% lines** (motor principal).
  - `hooks/useRecurrenceEngine.ts` — opcional no coverage (hooks/ fora da coverage include por convenção).
  - `components/tarefas/RecurrenceFieldset.tsx` >= **70% lines**.
  - `lib/db/repos/recurrences.ts` — já coberto (Story 2.1 ≥80%); a Story 2.7 não altera este repo, apenas consome.
  - Agregado all-files mantém >= **60%** (NFR17).
  - **NÃO alterar `vitest.config.ts:coverage.thresholds`** — é caminho bloqueador (Not-Tested Evidence Gate).

---

## ADR-2.7-1 — Mecanismo de activação do motor de recorrência (GAP-1 RESOLVIDO)

> **Status:** Aceite · **Decisor:** Aria (`@architect`) · **Data:** 20/05/2026 · **Contexto:** architect-gate Story 2.7, Epic 2 Nexus v2.
> Esta ADR é local à Story 2.7 — **não reabre nenhum dos 5 ADRs base** da `architecture-v2.md` (`project_nexus_v2_architecture.md`). Resolve uma divergência de detalhe de implementação não coberta por ADR.

### Problema (GAP-1)

Divergência de três fontes sobre como activar o motor que gera as instâncias recorrentes:

| Dimensão | PRD §10 L446 / EPIC-2 §7 L84 | architecture-v2.md §16 L1123 (nota — não ADR) |
|----------|------------------------------|------------------------------------------------|
| Mecanismo | `setInterval` ou `requestIdleCallback` (cron client-side) | ServiceWorker — Background Sync ou `activate` event |
| Escopo | cron no tab activo | SW — independente do tab |
| Complexidade | Baixa | Alta (SW registration, Background Sync API, VAPID) |
| Geração offline | Não (tab tem de estar aberto) | Sim (SW corre em background) |

A nota §16 era a única fonte a apontar ServiceWorker e **não é um ADR** — EPIC-2 §7 já registava "Sem ADR específico — segue padrão client-side". A nota §16 era, portanto, uma sugestão de optimização sem fundamento formal e em conflito com as duas fontes canónicas (PRD + EPIC-2).

### Opções consideradas

| Opção | Mecanismo | Trade-offs |
|-------|-----------|------------|
| **A** | `useEffect` one-shot no mount da page `/tarefas` | + KISS, ~3 ficheiros novos · + agnóstico — motor puro · + zero polling/CPU desperdiçado · + alinhado PRD+EPIC-2 · − sem geração offline (tab tem de abrir) |
| B | `setInterval` no tab activo | − polling contínuo desperdiça CPU sem ganho real · − exige cleanup de interval · = sem geração offline na mesma |
| C | ServiceWorker + Background Sync | + geração offline real · − escopo +2-3x (SW registration, `service-worker.ts`, Background Sync handler) · − complexidade não justificada por requisito documentado |

### Decisão

**Opção A — `useEffect` one-shot no mount da page `/tarefas`.** O motor (`lib/shared/recurrence.ts`) é uma função pura sobre Dexie, agnóstica ao mecanismo de activação; o hook `useRecurrenceEngine` é o único ponto que conhece o mecanismo.

### Justificação

| Princípio | Como a decisão o cumpre |
|-----------|--------------------------|
| **CLI First / fonte da verdade** | O PRD §10 e o EPIC-2 §7 são as fontes canónicas e ambos dizem cron client-side. A nota §16 (ServiceWorker) não é ADR e estava em conflito — foi corrigida, não promovida. A camada de arquitectura prevalece, mas aqui "arquitectura" formal são os 5 ADRs, não uma nota de §16 sem fundamento. |
| **KISS / simplicidade** | A opção A entrega o caso de uso real com ~3 ficheiros novos. A opção C triplicaria o escopo (SW registration, Background Sync API) sem nenhum requisito (FR ou NFR) que exija geração offline de instâncias. |
| **Contexto de uso pessoal** | O Nexus v2 é uso pessoal do Eurico, com o tab tipicamente sempre aberto. A "geração offline" da opção C resolve um problema que, neste contexto, não existe — não há cenário documentado em que o motor precise de correr com o tab fechado. `useEffect` no mount cobre 100% do caso de uso real ("ao abrir as tarefas, o horizonte de 90 dias está populado"). |
| **No Invention (Constitution Art. IV)** | Nenhum FR/NFR pede geração offline. Escolher SW seria inventar um requisito. A opção A rastreia directamente a FR10 + EPIC-2 §5 (horizonte 90 dias) sem adicionar nada. |
| **Living Architecture** | O motor é agnóstico — se o Epic 4 (lembretes via Web Push) trouxer um requisito real de Background Sync, a migração para SW substitui só `useRecurrenceEngine.ts`. A decisão de hoje não fecha portas; apenas não paga complexidade adiantada. |

### Consequências

1. **`architecture-v2.md` §16 L1123 corrigido** (20/05/2026) — passa a indicar motor client-side + `useEffect`, com referência cruzada a esta ADR. A divergência das três fontes deixa de existir.
2. **AC4 da Story 2.7 fixado** — `useEffect` one-shot é a especificação definitiva, sem linguagem de "pendente".
3. **`setInterval`/`requestIdleCallback` também não são usados** — o termo do PRD ("via `setInterval` ou `requestIdleCallback`") era ilustrativo de "cron client-side"; `useEffect` on-mount é a concretização mais simples e sem polling. Não há violação do PRD — `useEffect` é cron client-side.
4. **Migração futura para ServiceWorker** — reavaliada no Epic 4 **se e só se** o Background Sync de lembretes (FR33/Web Push) a justificar. Não é dívida técnica desta story; é uma opção em aberto deliberada.
5. **Sem impacto na estimativa** — a story mantém 8 pontos; a resolução do GAP elimina incerteza, não adiciona trabalho.

---

## Architect Gate — `@architect` Aria (20/05/2026)

> Gate executado em fase de draft (pré-PO Validation), conforme `EPIC-2.md` §5 (Story 2.7 quality gate `@architect`) e `separation-of-roles.md` A6 (Dex executará, Aria faz o gate). Este gate avalia **viabilidade técnica do draft** — o gate de implementação (lint/typecheck/vitest/build sobre código real) ocorre após `@dev` entregar.

### Veredicto: **APPROVED**

### 1. Resolução do GAP-1

GAP-1 resolvido por **ADR-2.7-1** (secção acima): mecanismo de activação = `useEffect` one-shot on-mount. `architecture-v2.md` §16 L1123 corrigido para alinhar com PRD §10 + EPIC-2 §7. Divergência das três fontes eliminada. Zero GAPs críticos remanescentes.

### 2. Viabilidade técnica (verificada em código)

| Item | Verificação | Estado |
|------|-------------|--------|
| `lib/shared/recurrence.ts` skeleton | `buildRRule(config)` + `occurrencesBetween(rule, from, to)` + `interface RecurrenceConfig` confirmados (L12/L33/L63) | OK — estender é viável |
| `Task.dueDate` | `types/db.ts:58` → `dueDate: string \| null` (ISO) — comparação `dueDate === isoDate` válida sem conversão de tipo | OK — Dev Note T1.5 confirmada |
| `Task.recurrenceId` / `parentTaskId` | `types/db.ts:63-64` ambos `string \| null` | OK |
| Repos `tasks.ts` | `createTask`, `listTasks`, `updateTask`, `setTaskStatus`, `getTask`, `deleteTask` exportados | OK |
| `rrule` v2.8.0 + `date-fns` v4.1.0 | `architecture-v2.md` §17 L1179-1180 confirma em `package.json` | OK — sem instalação |

### 3. Complexidade e dependências

- 8 pontos (large) — justificada: motor de domínio com 6 tipos FR10, idempotência, herança de campos, ~20-25 testes. Estimativa adequada.
- Desbloqueada por Story 2.1 (MERGED). Bloqueia Epic 4. Independente da 2.10. Cadeia de dependências correcta.
- A2 (Not-Tested Evidence Gate): AC15 proíbe alterar `vitest.config.ts:coverage.thresholds` — correcto. Se algum commit tocar `vitest.config.ts`/`package.json:scripts`, a secção de evidência é obrigatória.

### 4. Rastreabilidade dos 15 AC

Todos os 15 AC rastreiam a FR10, a EPIC-2 §5, ou a ADR-2.7-1. Sem invenção (Constitution Art. IV). AC1-3 → motor; AC4-5 → activação (ADR-2.7-1); AC6-7 → UI fieldset + integração; AC8-10 → distinção visual + ciclo de vida; AC11-12 → PT-PT + a11y; AC13 → 24 cenários de teste; AC14-15 → quality gates + coverage. Cobertura completa.

### 5. Pontos de atenção (não-bloqueantes — para o gate de implementação)

| # | Ponto | Acção no gate de implementação |
|---|-------|--------------------------------|
| PA-1 | **`parentTaskId` não indexado em `version(2)`** — Dev Notes L421 reconhece. `where('parentTaskId')` faz full-table-scan. Para uso pessoal (volume baixo) é aceitável. | Aria valida no gate pós-implementação se o volume real (90 instâncias × N recorrências) justifica índice em `version(3)` no Epic 3. **NÃO alterar `version(2)`** — retroactivo é destrutivo. Aceite como dívida documentada. |
| PA-2 | **Motor agnóstico ao mecanismo** — Dev Notes L417 exige que `recurrence.ts` não importe `useEffect`/`setInterval`/SW. | Gate de implementação verifica que `recurrence.ts` é função pura sobre Dexie. Critério de aceitação reforçado por ADR-2.7-1. |
| PA-3 | **Idempotência (A3 + AC2 + T4)** — é o risco central do motor (correr 2x não duplica). | T4 cobre; gate verifica que `generateTaskInstances` faz o check `parentTaskId + dueDate` antes de criar. |
| PA-4 | Edge cases de calendário (31 Fev, DST) — delegados a `rrule`. | Aceite — `rrule` é a biblioteca canónica; T8 (mensal dia 15) cobre o caminho normal. |

### 6. Conclusão

Story 2.7 **APPROVED** para PO Validation. GAP-1 resolvido formalmente. Viabilidade técnica confirmada contra código real. 15 AC rastreáveis. Pontos de atenção PA-1 a PA-4 não bloqueiam o draft — são checklist do gate de implementação. Sem NEEDS_REVISION.

---

## CodeRabbit Integration

| Item | Configuração |
|------|--------------|
| Story Type | **Business Logic** (motor de recorrência, geração de instâncias, idempotência) + secundário **Frontend** (fieldset de recorrência + badges) + terciário **Database** (leitura/escrita via repos existentes) |
| Complexity | Large — lógica de domínio de recorrência complexa (6 tipos, rrule, idempotência, horizonte, herança de campos). Surface area ~8-10 ficheiros novos + 2-3 modificados. ~20-25 testes Vitest. |
| Specialized Agents | `@dev` (Dex — executor), `@architect` (Aria — quality gate) |
| Quality Gate Tasks | Pre-Commit (Dex): `coderabbit --prompt-only -t uncommitted` antes de Ready for Review · Pre-PR (Dex/`@devops`): `coderabbit --prompt-only --base main` antes do PR |
| Self-Healing Configuration | mode: `light`, max_iterations: **2** (hard-stop — `EPIC-2.md` §8), severity_filter: `[CRITICAL, HIGH]`, behavior: `CRITICAL: auto_fix`, `HIGH: auto_fix (iter<2) else document_as_debt` |
| Focus Areas | **Idempotência**: `generateTaskInstances` não cria duplicados se correr múltiplas vezes. **6 tipos FR10**: cada tipo mapeado correctamente para `RecurrenceConfig` (byweekday/bymonthday). **Herança de campos**: task filha herda campos correctos da mãe. **endDate respeitado**: sem instâncias após endDate. **Agnóstico ao mecanismo de activação**: motor em `lib/shared/recurrence.ts` não referencia `useEffect`/`setInterval`/SW. **PT-PT enforcement**. **NÃO tocar `vitest.config.ts`**. |
| Risco | Alto — motor de recorrência é lógica de domínio complexa com edge cases (DST, dias inexistentes como 31 Fev, byweekday vazio). Mitigação: T1-T11 cobrem os 6 tipos + idempotência + tolerância a erros. `rrule` lida com DST/calendário internamente. |
| **Local CLI skip** | CodeRabbit corre via **integração GitHub no PR** (server-side, automático). Convenção Nexus v2 consolidada. |

---

## Tasks / Subtasks

- [ ] **T1.** Preparação e leitura de contexto (pré-implementação)
  - [ ] T1.1 [GAP-1 RESOLVIDO — ADR-2.7-1] Mecanismo de activação fixado: `useEffect` one-shot on-mount. Sem `setInterval`/`requestIdleCallback`/ServiceWorker. Implementar AC4 conforme decidido — sem reabrir a decisão.
  - [ ] T1.2 Ler `app/(app)/tarefas/page.tsx` + qualquer `TaskFormModal.tsx` existente — confirmar onde injectar `RecurrenceFieldset`
  - [ ] T1.3 Ler `components/tarefas/TaskRow.tsx` + `components/tarefas/KanbanCard.tsx` — confirmar onde adicionar badges "Recorrente"/"Instância"
  - [ ] T1.4 Ler `lib/shared/recurrence.ts` actual — confirmar assinatura exacta de `buildRRule` + `occurrencesBetween`
  - [ ] T1.5 Ler `lib/db/repos/tasks.ts` — confirmar `createTask` aceita `{ recurrenceId, parentTaskId, dueDate }`

- [ ] **T2.** Estender `lib/shared/recurrence.ts` (AC1, AC2, AC3)
  - [ ] T2.1 Definir `RecurrenceType` string literal union (6 tipos FR10)
  - [ ] T2.2 Implementar `buildRecurrenceConfig(type, opts): RecurrenceConfig` (AC1)
  - [ ] T2.3 Implementar `generateTaskInstances(recurrence, horizonDays?)` com idempotência (AC2)
  - [ ] T2.4 Implementar `runRecurrenceEngine()` com tolerância a erros (AC3)
  - [ ] T2.5 Adicionar comentários trace inline (Story 2.7 + AC# correspondente + FR10)

- [ ] **T3.** `hooks/useRecurrenceEngine.ts` (AC4)
  - [ ] T3.1 Criar hook com `useEffect` one-shot on-mount
  - [ ] T3.2 Comentário inline: "Activação via useEffect on-mount — decisão ADR-2.7-1 (architect-gate 20/05/2026). Motor agnóstico ao mecanismo."

- [ ] **T4.** `components/tarefas/RecurrenceFieldset.tsx` (AC6, AC11, AC12)
  - [ ] T4.1 Toggle "Tarefa recorrente" (checkbox)
  - [ ] T4.2 Select de tipo com 6 opções PT-PT
  - [ ] T4.3 Pickers condicionais (dia da semana / dia do mês) conforme tipo
  - [ ] T4.4 Inputs de data início + data fim opcional
  - [ ] T4.5 Validação (startDate obrigatório, endDate >= startDate)
  - [ ] T4.6 `aria-label` em todos os controlos interactivos

- [ ] **T5.** Integração no formulário de tarefa (AC7)
  - [ ] T5.1 Injectar `RecurrenceFieldset` no formulário (resultado de T1.2)
  - [ ] T5.2 Lógica de criação: se recorrência preenchida → `createRecurrence` + `updateTask({ recurrenceId })` + `generateTaskInstances`
  - [ ] T5.3 Lógica de edição: se recorrência removida (toggle → null) → `deleteRecurrence` + `updateTask({ recurrenceId: null })`
  - [ ] T5.4 Botão "Cancelar recorrência" na task-mãe (AC9) com `window.confirm` PT-PT

- [ ] **T6.** Badges visuais (AC8)
  - [ ] T6.1 Badge "Recorrente" em `TaskRow.tsx` para tasks com `recurrenceId !== null && parentTaskId === null`
  - [ ] T6.2 Badge "Instância" em `TaskRow.tsx` para tasks com `parentTaskId !== null`
  - [ ] T6.3 Replicar badges em `KanbanCard.tsx` (se aplicável — verificar durante T1.3)

- [ ] **T7.** Activação na page `/tarefas` (AC5)
  - [ ] T7.1 Adicionar `useRecurrenceEngine()` ao topo de `app/(app)/tarefas/page.tsx`
  - [ ] T7.2 Confirmar que `useTasks()` + `useLiveQuery` já reagem às novas instâncias sem reload

- [ ] **T8.** Testes Vitest (AC13)
  - [ ] T8.1 `tests/unit/shared/recurrence.test.ts` — T1 a T11 (lógica de motor: 6 tipos, idempotência, herança, tolerância a erros)
  - [ ] T8.2 `tests/unit/hooks/useRecurrenceEngine.test.ts` — T22 (mock `runRecurrenceEngine`, confirmar 1 chamada no mount)
  - [ ] T8.3 `tests/unit/components/tarefas/RecurrenceFieldset.test.tsx` — T14 a T19 (render, toggle, validação)
  - [ ] T8.4 `tests/unit/components/tarefas/TaskRow-badges.test.tsx` — T20 a T21 (badges)
  - [ ] T8.5 `tests/unit/shared/recurrence-cancel.test.ts` — T12 a T13 (completar instância, cancelar recorrência)
  - [ ] T8.6 `tests/unit/components/tarefas/recurrence-confirm.test.tsx` — T23 a T24 (confirmação cancelamento)

- [ ] **T9.** Quality gates locais (AC14, AC15)
  - [ ] T9.1 `npm run lint` — exit 0
  - [ ] T9.2 `npm run typecheck` — exit 0
  - [ ] T9.3 `npm run test:unit` — todos PASS
  - [ ] T9.4 `npm run build` — exit 0
  - [ ] T9.5 `npm run test:coverage` — verificar thresholds (não alterar `vitest.config.ts`)

- [ ] **T10.** Story file maintenance
  - [ ] T10.1 Actualizar checkboxes conforme tasks completadas
  - [ ] T10.2 Actualizar File List com ficheiros criados/modificados
  - [ ] T10.3 Actualizar Change Log

---

## Dev Notes

- **Motor agnóstico ao mecanismo de activação:** `lib/shared/recurrence.ts` não deve importar nem referenciar `useEffect`, `setInterval`, `requestIdleCallback`, ou APIs de ServiceWorker. O motor é uma função pura (com acesso a Dexie) que pode ser chamada de qualquer contexto — React hook, SW, ou teste. [Source: A4 + A6 + [GAP-1]]

- **`rrule.fromString` para reconstruir a RRule:** a `Recurrence.rule` guarda a RRULE string (ex: `"FREQ=WEEKLY;BYDAY=MO"`). Para reconstruir o objecto `RRule` dentro de `generateTaskInstances`, usar `RRule.fromString(recurrence.rule)` — não reconstruir via `buildRRule` novamente (evitar double-parse). [Source: `lib/shared/recurrence.ts` pattern já existente]

- **Idempotência via `parentTaskId + dueDate`:** o check de duplicados usa `db.tasks.where('parentTaskId').equals(ownerId).toArray()` + filter por `dueDate`. Atenção: `parentTaskId` **não está indexado** em `version(2)` (`client.ts:version(2)` só adiciona `recurrences` e `tags`). Se o volume de tasks filhas for grande (horizonte 90 dias × recorrência diária = 90 tasks), o `where('parentTaskId')` vai fazer full-table-scan em `tasks`. Para uso pessoal do Eurico (volume baixo), aceitável. Se `@architect` identificar risco de performance no gate, adicionar índice `parentTaskId` em `version(3)` (Epic 3 incremento) — **NÃO alterar `version(2)` aqui** (retroactivo é destrutivo). [GAP potencial de performance — documentar no AC14 se identificado]

- **`Task.dueDate`**: verificar o tipo exacto de `dueDate` em `types/db.ts` e `lib/db/repos/tasks.ts` — pode ser `string` (ISO date `YYYY-MM-DD`) ou `number` (epoch). A `architecture-v2.md` §6.2 L489 mostra `dueDate` na interface `Task` sem tipo explícito na grep. Confirmar durante T1.5 antes de comparar `dueDate === isoDate`. [Source: verificação durante T1.5 obrigatória]

- **Task-mãe tem `dueDate`:** quando o Eurico cria uma tarefa recorrente, a task-mãe pode ou não ter `dueDate` (corresponde ao `startDate` da recorrência ou à primeira ocorrência). Decisão final ao executor (Dex) durante T5.2. Sugestão: a task-mãe recebe `dueDate` = primeira ocorrência da recorrência.

- **Lições Epic 1 A2 (Not-Tested Evidence Gate):** se algum commit tocar `vitest.config.ts`, `playwright.config.ts`, ou `package.json:scripts`, é **obrigatório** incluir evidência local (output de dry-run ou `--list`) no Change Log da story antes do commit. [Source: `not-tested-trailer-rules.md`]

- **Epic 4 reutilização:** o `runRecurrenceEngine()` é genérico por `ownerType`. O Epic 4 (Hábitos + Lembretes) vai chamar o mesmo motor com `ownerType: 'habit'` ou `ownerType: 'reminder'`. Para facilitar esta extensão futura, `runRecurrenceEngine()` pode aceitar um parâmetro opcional `ownerType?: RecurrenceOwnerType` para filtrar — ou mantê-lo sem parâmetro e iterar todos os ownerTypes num único loop. Decisão ao executor (Dex) — documentar a opção escolhida no Change Log.

---

## Test Notes

- Usar `fake-indexeddb` (já em `tests/setup.ts` — Story 2.1 estabeleceu este padrão) para todos os testes de motor que escrevem em Dexie.
- Para testes de horizonte temporal (T3-T8), controlar a data actual via `jest.useFakeTimers()` ou equivalente Vitest para garantir resultados determinísticos independentes da data real.
- `rrule` calcula ocorrências correctamente em UTC internamente — atenção à conversão de fuso horário ao comparar `Date` com string ISO `YYYY-MM-DD`. Usar `date-fns/startOfDay` ou equivalente para normalizar.
- Teste T3 (90 ocorrências diárias) pode ser lento em ambientes lentos — considerar `horizonDays = 7` nos testes de unidade e `horizonDays = 90` apenas no teste de integração. Documentar se o test correr em >5s.

---

## Security Notes

- N/A — motor client-side com dados locais Dexie (IndexedDB do browser do Eurico). Sem API calls, sem dados sensíveis expostos.

---

## Change Log

| Versão | Data | Autor | Descrição |
|--------|------|-------|-----------|
| v0.1 | 2026-05-20 | River (`@sm`) | Draft inicial — aguarda PO Validation |
| v0.2 | 2026-05-20 | Aria (`@architect`) | GAP-1 resolvido — ADR-2.7-1 (`useEffect` on-mount). AC4 fixado, nota inicial/header/T1.1/T3.2 actualizados, `architecture-v2.md` §16 corrigido. Architect-gate executado — veredicto na secção Architect Gate. |

---

## File List

| Ficheiro | Estado | Notas |
|----------|--------|-------|
| `lib/shared/recurrence.ts` | Modificar | Estender skeleton com `buildRecurrenceConfig`, `generateTaskInstances`, `runRecurrenceEngine` |
| `hooks/useRecurrenceEngine.ts` | Criar | Hook de activação on-mount |
| `components/tarefas/RecurrenceFieldset.tsx` | Criar | Fieldset de configuração de recorrência |
| `components/tarefas/TaskRow.tsx` | Modificar | Adicionar badges "Recorrente"/"Instância" |
| `components/tarefas/KanbanCard.tsx` | Modificar (verificar T1.3) | Adicionar badges se aplicável |
| `app/(app)/tarefas/page.tsx` | Modificar | Adicionar `useRecurrenceEngine()` + lógica de criação/edição com recorrência |
| `tests/unit/shared/recurrence.test.ts` | Criar | T1-T11 |
| `tests/unit/shared/recurrence-cancel.test.ts` | Criar | T12-T13 |
| `tests/unit/hooks/useRecurrenceEngine.test.ts` | Criar | T22 |
| `tests/unit/components/tarefas/RecurrenceFieldset.test.tsx` | Criar | T14-T19 |
| `tests/unit/components/tarefas/TaskRow-badges.test.tsx` | Criar | T20-T21 |
| `tests/unit/components/tarefas/recurrence-confirm.test.tsx` | Criar | T23-T24 |
