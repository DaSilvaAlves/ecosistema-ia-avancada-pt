# RETOMA — Epic 6 arrancado; Story 6.1 (OAuth Google Calendar) pronta para DEV; retrospetiva Epic 5 + regra A1 + Epic 6 já em `main`

> ATENÇÃO — ESTE HANDOFF SEGUE A REGRA OBRIGATÓRIA `handoff-location.md`.
> ANTES DE CRIAR OU MOVER QUALQUER HANDOFF, CONSULTAR `.claude/rules/handoff-location.md`.
> HANDOFFS SÓ VIVEM DENTRO DA PASTA DO PROJECTO A QUE SE REFEREM.

```yaml
from_agent: "orquestrador /sdc 6.1 — sessão que fez retrospetiva Epic 5, regra A1, criou Epic 6 e levou a Story 6.1 até ao gate de entrada"
to_agent: "any — próximo terminal: retomar a Fase 3 (DEV) da Story 6.1 com `/sdc 6.1 --from dev`"
created: "2026-06-16T00:00:00Z"
status: pending
consumed: false
project: nexus-v2
next_action: "/sdc 6.1 --from dev  (implementar lógica + unit tests; AC de produção ficam deferidos ao pré-req humano de credenciais Google)"
```

## Summary

Sessão pós-fecho do Epic 5. Fez-se, por esta ordem, e **tudo já está em `main`**:

1. **Retrospetiva do Epic 5** (`@po`) — `imersao-tools/nexus/docs/retrospectives/EPIC-5-retrospective.md`. Commit `dd3caeb9`.
2. **Acção A1 da retrospetiva** — emenda à regra `.claude/rules/coderabbit-integration.md`: o gate de saída e o pre-commit passam a exigir `coderabbit --base main` (diff completo do branch), não só `-t uncommitted`. Fecha o ponto cego que deixou escapar o SSRF da Story 5.11 no PR #72. Commit `249bb7d3`.
3. **Epic 6 criado** (`@pm`) — `imersao-tools/nexus/docs/EPIC-6.md` (OAuth: Calendar + Gmail + Telegram, FR58-76, **17 stories** 6.1-6.17). Commit `cd5290ce`.
4. **`/sdc 6.1` arrancado** — Story 6.1 (OAuth flow Google, Calendar scope) passou SM draft → PO GO 9/10 → Architect Gate de Entrada PASS-COM-CONDIÇÕES. **A Fase 3 (DEV) foi interrompida pelo Eurico ANTES de tocar em código.**

`main` em **`cd5290ce`**. `main`/`origin/main` sincronizados. Nenhuma branch de feature criada ainda (sem `--push` no /sdc).

## Estado da Story 6.1

Ficheiro: `imersao-tools/nexus/docs/stories/active/6.1.story.md` — **Status `Draft`**, T0 (Architect Gate) marcada `[x]`, 8 AC, 7 tasks (T0-T7).

| Fase /sdc | Agente | Veredicto |
|-----------|--------|-----------|
| 1 — SM draft | `@sm` (River) | PASS — 8 AC, 7 tasks, checklist READY 6/6 |
| 2 — PO validar | `@po` (Pax) | **GO 9/10** (único PARCIAL: ponto 2 source-tree, já reconciliado pela Aria) |
| Architect Gate Entrada | `@architect` (Aria) | **PASS-COM-CONDIÇÕES** — 5 `[D-6.1-*]` ratificadas |
| 3 — DEV | `@dev` (Dex) | **NÃO INICIADO** (interrompido por ti; zero código tocado) |
| 4 — QA | `@qa` | pendente |

### Decisões ratificadas pela Aria (NÃO reabrir — implementar à risca)

- **`[D-6.1-PKCE]` = A** — state **HMAC-SHA256** com `SESSION_SECRET` + KV TTL 600s single-use. **Sem PKCE** (confidential server-side client; arch §16 já prescreve state assinado).
- **`[D-6.1-CALLBACK]` = A** — route literal **`app/api/google/oauth/callback/route.ts`**. Divergência da arch resolvida: §9.2:790 (`/callback`) prevalece sobre §3:102 (`[provider]`) porque o redirect URI é contrato externo (bate ao caracter no Google Cloud Console — ver regra `external-contract-identifiers.md`).
- **`[D-6.1-ERROR]`** — erro tipado fechado → redirect `?error=<tipo>`; UI PT-PT + CTA; **ZERO token/code em URL ou logs**.
- **`[D-6.1-TESTMODE]` = B** — documentar test mode permanente single-user em `v2/docs/setup-oauth.md` (arch §18 AR5 pede README de setup).
- **`[D-6.1-SCOPE]` = A** — KV **sem encriptação** na 6.1; criar seam **`lib/google/token-store.ts`** que a Story 6.2 reimplementa por dentro (encriptação at-rest = GAP-6.2, fora de scope da 6.1).

### Reconciliações já aplicadas na story (verificadas contra código real)

- `(app)/settings/page.tsx` é **NOVO (criar)** — a pasta `settings/` NÃO existe (as 9 pastas reais: diario/financas/habitos/knowledge/lembretes/metas/projectos/tags/tarefas).
- `v2/lib/shared/env.ts` **NÃO se altera** — `GOOGLE_OAUTH_*` (L19-21) e `SESSION_SECRET` (L16) já existem; ler via `getServerEnv()`.

---

**LEMBRETE — REGRA HANDOFF-LOCATION:** ESTE FICHEIRO ESTÁ EM `imersao-tools/nexus/docs/handoffs/`. PROJECTO = NEXUS V2 (`imersao-tools/nexus/`). CAMINHO CORRECTO. CONSULTAR `.claude/rules/handoff-location.md`.

---

## PRÉ-REQUISITO HUMANO (bloqueia só os AC de PRODUÇÃO, NÃO o código)

A Aria confirmou: **toda a lógica + unit tests + integração corre SEM credenciais Google reais.** O `@dev` está **desbloqueado** para arrancar no T1.

Só os **AC de produção** ficam deferidos (padrão AC13 da Story 4.9, VAPID da 4.7):
- **AC1** (OAuth flow < 60s) e **AC5** (estado `ligado` real) — só verificáveis manualmente pós-deploy.

O Eurico (com `@devops`) tem de provisionar, antes da verificação final:
- **P1** — Google Cloud Console: criar OAuth client (Web), scope Calendar, redirect URI **exacto** = `https://imersao.ia.expressia.pt/api/google/oauth/callback` (tem de bater ao caracter — ver `[D-6.1-CALLBACK]`).
- **P2** — Vercel env: `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `SESSION_SECRET` (confirmar se já existem em produção).
- **P3** — adicionar o email do Eurico como test user no consent screen (test mode permanente single-user).

Ao retomar o DEV, **NÃO** marcar AC1/AC5 como completos — marcar "pendente verificação de produção / pré-req humano".

## Próximo passo (retoma)

```
cd "C:\Users\XPS\Documents\ecosistema-ia-avancada-pt"
git checkout main && git pull --ff-only origin main   # HEAD esperado = cd5290ce (ou mais recente)
/sdc 6.1 --from dev
```

O `--from dev` salta SM/PO/gate-entrada (já feitos, story Draft com T0 `[x]`) e arranca a implementação. Fase 3 (DEV, `@dev`) → Gate 3 (checkboxes + File List + lint/typecheck/vitest verdes, evidência exigida) → Fase 4 (QA, `@qa`). **Sem `--push`** o ciclo NÃO faz push; termina sugerindo o comando `@devops`.

### Regras críticas para o DEV desta story (OAuth = estado multi-camada)

- **`internal-state-contract-gate.md`** é **especialmente relevante aqui** (a própria regra nomeia o Epic 6 como alvo principal): o estado distribui-se por **callback + sessão + refresh token**. O quality gate TEM de fazer a análise de ciclo de vida nos 3 eixos (classes de estado, transição-já-ocorrida, caminhos de falha) — não basta o caminho feliz.
- **`mock-must-reflect-real-protocol.md`**: MSW espelha o protocolo OAuth real do Google (state assinado/TTL/single-use, troca code→token, persistência) com fake-KV. NÃO fazer mocks que só fazem o teste passar.
- **`external-contract-identifiers.md`**: o redirect URI e os nomes de evento/scope cruzam contrato externo — validar contra o Google, não contra a grafia do PRD.
- Imports absolutos, error handling tipado, zero `any`. **NUNCA `git add -A`** (raiz com submódulos sujos + 150+ untracked fora-scope). `gh` SEMPRE `--repo DaSilvaAlves/ecosistema-ia-avancada-pt`.

## Contexto do Epic 6 (para planeamento futuro)

`EPIC-6.md`: 17 stories. **6.1 OAuth Calendar** (esta) é a fundação — Calendar e Gmail partilham o cliente OAuth. 6 GAPs deferidos ao `@architect` (GAP-6.1 OAuth flow e GAP-6.2 token storage já resolvidos no gate de entrada da 6.1). Sub-blocos: Calendar (6.1-6.6), Gmail (6.7-6.10), Telegram (6.11-6.17). **GAP-6.4** lembra: as 2 vulnerabilidades critical `request`/`node-telegram-bot-api` foram adiadas para cá — decidir SDK Telegram (Edge favorece Bot API por `fetch` directo).

## Débitos herdados (não-bloqueadores — candidatos a P2)

- **REC-SSRF-2** (5.11): eliminar fetch HTTP interno do endpoint web-search ao proxy Edge (invocar a lógica directamente, sem reenvio de cookie).
- **FLAG env Vercel** (5.11): confirmar `VERCEL_PROJECT_PRODUCTION_URL` exposta ao runtime Node em produção.
- **OBS-5.10-A2**: `.then()` sem `.catch()` no useEffect de pesquisa em `app/(app)/diario/page.tsx`.
- A2 da retrospetiva Epic 5 (verificação de existência de ficheiro por ≥2 ferramentas no gate) e A3/A4/A5/A6/A7 — ver `EPIC-5-retrospective.md` §9.

## Git

- `main` em `cd5290ce`. Commits desta sessão: `dd3caeb9` (retro), `249bb7d3` (regra A1), `cd5290ce` (Epic 6). Todos docs-only directos em `main` (convenção Nexus v2, sem PR).
- Working tree: por tocar e fora de scope ficam `docs/HANDOFF-INDEX.md` (modified), submódulos `comunidade`/`starter-builder`, 150+ untracked. A Story 6.1 está untracked em `active/` (entra no push do `@devops` quando o ciclo correr com `--push`).
- Fonte de verdade viva: `imersao-tools/nexus/docs/AUDITORIA-20260612-ROADMAP-CONCLUSAO.md`.

---

## REGRA HANDOFF-LOCATION — CONFIRMAÇÃO FINAL

ESTE HANDOFF FOI CRIADO SEGUINDO A REGRA OBRIGATÓRIA `.claude/rules/handoff-location.md`.

- PROJECTO A QUE SE REFERE: `nexus-v2` (`imersao-tools/nexus/`)
- LOCALIZAÇÃO CORRECTA: `imersao-tools/nexus/docs/handoffs/`
- LOCALIZAÇÃO ACTUAL: `imersao-tools/nexus/docs/handoffs/RETOMA-20260616-sdc-6.1-DEV-pendente-retro5-A1-epic6-feitos.md`
- COINCIDEM? `SIM`

AGENTE RESPONSÁVEL: `orquestrador /sdc 6.1 (Claude Code)`
DATA: `16/06/2026`
