/**
 * Nexus v2 — PostCSS config (Tailwind 4)
 * Tailwind 4 usa o plugin @tailwindcss/postcss em vez do plugin antigo.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
