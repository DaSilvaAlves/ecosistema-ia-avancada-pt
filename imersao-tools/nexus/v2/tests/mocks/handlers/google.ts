/**
 * Nexus v2 — MSW handlers Google (stub)
 *
 * Stub vazio — handlers reais (Calendar + Gmail) implementados em Epic 6.
 * Ficheiro existe para que `tests/mocks/handlers/index.ts` possa importar agora.
 */
import type { HttpHandler } from 'msw';

export const googleHandlers: HttpHandler[] = [];
